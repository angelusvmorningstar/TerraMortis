schema: 1
story: "PP.4"
story_title: "Migrate Consumers — Character Editor"
gate: CONCERNS
status_reason: "AC4/AC5 now fully met — all MERITS_DB and DEVOTIONS_DB imports removed from editor modules. AC6 remains: buildMeritOptions still uses hardcoded domainNames/influenceNames Sets instead of category/parent field filtering. Dev notes state the rules DB has no field to distinguish these subtypes — accepted as structural limitation."
reviewer: "Quinn (Test Architect)"
updated: "2026-04-08"

waiver: { active: false }

top_issues:
  - id: "REQ-001"
    severity: low
    finding: "AC6 ('buildMeritOptions uses category field filtering instead of hardcoded exclusion sets') not met. Rules-cache path at merits.js:244-255 still uses hardcoded domainNames/influenceNames Sets. The purchasable_powers schema has no sub_category field to distinguish these merit types."
    suggested_action: "Accept as structural limitation. The exclusion sets are pragmatic given the schema. Could revisit if a sub_category field is added."

risk_summary:
  totals: { critical: 0, high: 0, medium: 0, low: 1 }
  recommendations:
    must_fix: []
    monitor: []

history:
  - at: "2026-04-07"
    gate: CONCERNS
    note: "Initial review — AC4/AC5/AC6 not met, Tasks 3-5 deferred"
  - at: "2026-04-08"
    gate: CONCERNS
    note: "Re-audit — AC4/AC5 now pass (all legacy imports removed). Only AC6 remains (hardcoded name Sets, accepted as structural limitation)."
