---
id: npcr.3
epic: npcr
status: ready-for-dev
priority: high
depends_on: [npcr.2]
---

# Story NPCR-3: NPC flags collection and admin flag queue

As an ST,
I want an `npc_flags` collection and a Flagged queue in the admin NPC Register,
So that players can signal concerns about NPCs (via NPCR.11) and I can resolve them.

---

## Context

Establishes the flag model end-to-end on the server and the ST-side resolution UI. The player-facing flag creation UI lands in NPCR.11; the POST endpoint is implemented here with correct auth so NPCR.11 is pure client work.

---

## Acceptance Criteria

**Given** `server/schemas/npc_flag.schema.js` exists **Then** it defines fields `npc_id, flagged_by{player_id, character_id}, reason, status (open|resolved), resolved_by, resolved_at, resolution_note, created_at` with `additionalProperties: false`.

**Given** indexes are required **Then** `server/scripts/create-npc-flag-indexes.js` produces indexes on `status` and `npc_id` when run.

**Given** the ST-only route `GET /api/npc-flags?status=open` **Then** it returns open flags sorted by created_at desc.

**Given** ST-only `PUT /api/npc-flags/:id/resolve` accepts `{resolution_note}` **Then** status='resolved', resolved_by, resolved_at are set.

**Given** `POST /api/npc-flags` exists **Then** player must be authenticated AND have an active relationship edge to the flagged NPC. **And** reason is required. **And** it returns 403 if unauthorised, 400 if missing reason.

**Given** an NPC has open flags **Then** the NPC detail pane shows a red "Flagged · N" chip and a Flags section listing each flag with reason, flagged_by display name, and a Resolve button.

**Given** I click Resolve **Then** a modal opens for `resolution_note`. **And** saving calls the resolve endpoint. **And** the row shows muted.

**Given** the "Flagged" filter chip on the admin list is clicked **Then** the list filters to NPCs with at least one open flag. **And** the chip shows total open flag count.

---

## Implementation Notes

- Relationship check in POST: query `relationships` for any edge involving flagger's PC AND this NPC with status='active'. If none, return 403.
- One open flag per (player, npc) pair: enforced by composite uniqueness check on insert (client shouldn't allow, but server enforces too).
- Resolution note is optional; if blank, the resolution still records the resolver and timestamp.
- User runs the index script manually.

---

## Files Expected to Change

- `server/schemas/npc_flag.schema.js` (new)
- `server/routes/npc-flags.js` (new)
- `server/index.js` (mount router)
- `server/scripts/create-npc-flag-indexes.js` (new, user runs manually)
- `public/js/admin/npc-register.js` (add Flags section + chip count + Flagged filter behaviour)
- `server/tests/api-npc-flags.test.js` (new, against tm_suite_test)

---

## Definition of Done

- Schema round-trip verified
- Indexes verified via MongoDB MCP
- API tests pass; 403 on unauthorised POST verified
- In-browser: flagged chip appears when a test-seeded flag is inserted via MCP; resolve flow works end-to-end with resolution note
- Quinn verification pass
- `bmad-code-review` required (schema + auth-boundary change)

---

## Review Findings (2026-04-24)

Generated by `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor). Auditor confirmed all 8 AC **pass**; the items below are hardening and two real bugs.

### Decisions resolved (3)

- [x] [Review][Decision] `pending_confirmation` relationship edges and the flag-gate — **keep narrow** (`status: 'active'` only). Rationale: PC-NPC edges are always ST-created as `active`; `pending_confirmation` exists for PC-PC mutual confirm (NPCR.10) which is not what the flag-gate scopes to. Revisit if pending PC-NPC edges ever emerge.
- [x] [Review][Decision] Resolve UX — **build a themed modal** matching the admin convention; retire `prompt()`. Promoted to **P12** below.
- [x] [Review][Decision] Resolved row behaviour — **keep row visible but muted** after resolve. Promoted to **P13** below.

### Patches applied (13)

All 13 review patches landed in a single hardening commit. Key changes:

- [x] [Review][Patch] **P1** — DB-level uniqueness: `open_flag_uniqueness` index is now `{ unique: true, partialFilterExpression: { status: 'open' } }`; POST catches duplicate-key (11000) and returns 409. Test uses a concurrent `Promise.all` race to exercise the path end-to-end.
- [x] [Review][Patch] **P2** — Server-side maxLength enforcement on `reason` (2000) and `resolution_note` (2000); tests added for both.
- [x] [Review][Patch] **P3** — Relationship-gate excludes `st_hidden: true` edges. Player cannot confirm hidden edges exist via the flag signal.
- [x] [Review][Patch] **P4** — Resolve-409 (both "already resolved" and "raced") now returns `body.flag` with the resolved/current doc.
- [x] [Review][Patch] **P5** — AJV-compiled `validateFlagDoc` runs against every constructed flag before insert; returns 500 on construction drift.
- [x] [Review][Patch] **P6** — `charNameFor` fallback is now `"(unknown character)"`, no raw hex leak.
- [x] [Review][Patch] **P7** — Resolve flow uses `apiRaw`, inspects status codes. On 409/404 the flag is dropped from `_openFlags`, promoted to `_sessionResolved` if the server returned one, and the UI shows a specific error.
- [x] [Review][Patch] **P8** — `renderHeader` auto-clears `_activeChip === 'flagged'` when `_openFlags.length === 0`.
- [x] [Review][Patch] **P9** — `_flagCountByNpc: Map` is rebuilt once after every mutation of `_openFlags`; `openFlagCount` is now O(1).
- [x] [Review][Patch] **P10** — Schema round-trip test added: read the stored doc back from Mongo and validate via AJV (`Hardening — review findings` block).
- [x] [Review][Patch] **P11** — `st_hidden` regression test + a bonus `retired`-edge test confirming both are blocked.
- [x] [Review][Patch] **P12** (Decision 2) — `openResolveModal()` builds a themed overlay in `public/css/admin-layout.css` (`.npcr-modal-*` classes) with Cancel / Resolve / Esc / Ctrl+Enter handling. `prompt()` retired.
- [x] [Review][Patch] **P13** (Decision 3) — Resolved flags migrate from `_openFlags` into a `_sessionResolved` `Map<flagId, flag>` and render with `.resolved` class (line-through reason, muted opacity, "Resolved · timestamp" badge, resolution-note rendered inline). On next full page reload the `status=open` fetch naturally excludes them.

### Deferred (8)

- [x] [Review][Defer] `createTestApp` mounts `mockAuth` with no `NODE_ENV` guard — pre-existing pattern across the test harness, not introduced by NPCR.3.
- [x] [Review][Defer] Index-creation scripts default to `tm_suite` when `MONGODB_DB` is unset — pre-existing convention; addressed by the vitest setup file for tests but not for manual script runs.
- [x] [Review][Defer] Timestamps stored as ISO strings not BSON Date — project-wide pattern consistent with NPCR.2 relationships; change requires a cross-collection migration decision.
- [x] [Review][Defer] `apiPost`/`apiPut` do not expose HTTP status codes to callers — app-wide concern affecting every client route, not NPCR.3-specific.
- [x] [Review][Defer] No rate limit on POST — infrastructure-level concern. Player can spam-POST up to one-per-active-edge (uniqueness then blocks), bounded by edge count.
- [x] [Review][Defer] Retired characters can still flag — product decision (do we silence retired PCs across all player surfaces, or just this one?).
- [x] [Review][Defer] Test fixtures share `CREATED_FLAG_IDS[0]`/`[1]` by ordinal index; vitest serial order keeps this working. Refactor to per-test fixtures is a style cleanup.
- [x] [Review][Defer] `getTestCharacterIds` auto-seeds `_test_seeded: true` characters in `tm_suite_test` with no cleanup path — pre-existing helper concern.

### Dismissed as noise

Defensive pedantry that doesn't fire in practice: `parseId` swallowing non-string inputs, `isStRole` called before `req.user` existence check, `Promise.all` partial-state on re-runnable idempotent index script, POST `insertOne`+`findOne` theoretical race, `F{n}` badge visual for large counts, `flaggedCls` space-joiner fragility, `renderHeader` `null > 0` accidentally evaluating false, `toLocaleString` on server-generated ISO strings, `_openFlags` reassignment mutation, "no test for `dev` role POST" (covered by ST path via `isStRole`), GET `npc_id` ObjectId-check asymmetry (POST doesn't enforce but stored values are always ObjectId-shaped in practice).
