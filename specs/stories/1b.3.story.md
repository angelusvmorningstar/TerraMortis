---
epic: 1
subphase: 1b
story: 1b.3
title: Verify shared theme across both apps
status: done
---

# Story 1b.3: Verify shared theme across both apps

**As a** developer,
**I want** to verify the shared theme works correctly in both Editor and Suite,
**So that** sub-phase 1b is complete.

## Acceptance Criteria

**Given** all colours are defined in `theme.css` and referenced via variables
**When** both apps are loaded via Live Server
**Then** both apps use identical colour values for shared elements (backgrounds, text, gold accents, crimson highlights)
**And** changing `--gold2` in `theme.css` visibly updates gold accents in both apps
**And** no console errors
**And** the regression checklist from 1a.6 still passes
