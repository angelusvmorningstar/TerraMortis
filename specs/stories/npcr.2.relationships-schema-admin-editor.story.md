---
id: npcr.2
epic: npcr
status: ready-for-dev
priority: high
depends_on: [npcr.1]
---

# Story NPCR-2: Relationships schema and admin edge editor

As an ST,
I want a Relationships section on the NPC detail pane where I can create, edit, and retire typed edges,
So that I can model each NPC's connections to PCs and other NPCs.

---

## Context

Introduces the `relationships` collection, the closed-enum kind taxonomy module, and the admin-side edge editor surface. All edges are ST-created in this story; player-writable endpoints land in NPCR.6 and NPCR.7.

See `specs/epic-npcr.md` Context section for the full graph model.

---

## Acceptance Criteria

**Given** `server/schemas/relationship.schema.js` exists **Then** it defines fields `a{type,id}, b{type,id}, kind, direction, disposition, state, st_hidden, status, created_by, history[], created_at, updated_at` with `additionalProperties: false`.

**Given** multikey indexes are required **Then** `server/scripts/create-relationship-indexes.js` exists and produces indexes on `a.id` and `b.id` when run.

**Given** a starting kind taxonomy is needed **Then** `public/js/data/relationship-kinds.js` exists with the ~15 starting kinds, each carrying `{code, label, family, direction, typicalEndpoints, custom_label_allowed}`.

**Given** I open an NPC detail pane **Then** a "Relationships" section lists all edges involving this NPC, grouped by kind family (Lineage, Political, Mortal, Other).

**Given** I click "Add Relationship" **Then** a form opens: endpoint picker (PC or NPC), kind dropdown, optional disposition chip, optional freeform state, optional st_hidden toggle.

**Given** kind='other' **When** I save **Then** a `custom_label` field is required.

**Given** I save a new edge **Then** POST `/api/relationships` creates it with status='active', created_by={type:'st', id: me}, and initial history row `{at, by, change: 'created'}`.

**Given** I edit disposition, state, kind, or st_hidden and save **Then** PUT appends a history row recording before/after values.

**Given** I click Retire on an edge **Then** status='retired' and it renders muted.

**Given** the API receives identical endpoints (same type and id on both sides) **Then** it returns 400 VALIDATION_ERROR.

**Given** I am not ST **Then** POST, PUT, and DELETE on `/api/relationships` return 403.

---

## Implementation Notes

- `server/routes/relationships.js` is ST-only in this story. Player-readable `GET /for-character/:id` lands in NPCR.6.
- History writes are server-side on every mutation; never user-editable. Shape: `{at, by: {type, id}, change: string, fields?: [{name, before, after}]}` — `fields` is an array of per-field deltas so the UI can render each change as a separate line (chosen 2026-04-24 during NPCR.2 design).
- Kind taxonomy starting list (from epic):
  - Lineage: sire, childe, grand-sire, clan-mate
  - Political: coterie, ally, rival, enemy, mentor, debt-holder, debt-bearer
  - Mortal: touchstone, family, contact, retainer, correspondent, romantic
  - Other: other (with custom_label)
- Endpoint picker for admin: PC list from `characters`, NPC list from `npcs` (both sourced via existing admin data)
- User runs `create-relationship-indexes.js` manually per project convention

---

## Files Expected to Change

- `server/schemas/relationship.schema.js` (new)
- `server/routes/relationships.js` (new)
- `server/index.js` (mount router)
- `server/scripts/create-relationship-indexes.js` (new, user runs manually)
- `public/js/data/relationship-kinds.js` (new)
- `public/js/admin/relationship-editor.js` (new)
- `public/js/admin/npc-register.js` (integrate editor into detail pane)
- `server/tests/api-relationships.test.js` (new, against tm_suite_test)

---

## Definition of Done

- Schema round-trip verified (write → read → edit → re-save with no additionalProperties errors)
- Indexes verified via MongoDB MCP `collection-indexes` tool
- API tests pass in `tm_suite_test`
- In-browser: create, edit, retire edge from NPC detail pane; verify history log grows on each change
- Save-failure errors surface visibly (no silent failure)
- Quinn verification pass
- `bmad-code-review` required (schema + auth-boundary change)

---

## Review Findings (2026-04-24)

Generated by `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor).

### Decisions resolved (5)

- [x] [Review][Decision] Disposition enum — **keep 3-point** (`positive/neutral/negative`). User approved during design. Epic aligned to schema (2026-04-24).
- [x] [Review][Decision] Status enum missing `rejected` — **add now** for NPCR.10 forward-compat. `STATUS_ENUM` extended to `[active, retired, pending_confirmation, rejected]` (2026-04-24).
- [x] [Review][Decision] Direction vocabulary — **no mismatch in practice**. Schema `'a_to_b'`/`'mutual'` describes the edge instance; kinds-module `'directed'`/`'mutual'` describes the kind category (drives picker defaults). `defaultDirectionFor()` translates between them. Epic updated from `'symmetric'` → `'mutual'` and the two-level model is now documented (2026-04-24). Actor-type `'st'`/`'player'` is canonical; epic updated from `'pc'` → `'player'`.
- [x] [Review][Decision] Sire/childe and debt-holder/debt-bearer inverse pairs — **keep both**. Users pick the code matching their entry-time mental frame; graph aggregation uses `family` not individual codes; duplicate-inverse detection is a client-side concern for player-creation flows (NPCR.6). Documented in epic (2026-04-24).
- [x] [Review][Decision] History row shape — **keep array shape** (`fields: [{name, before, after}]`) as shipped. Story line 54 updated to match schema and tests (2026-04-24).

### Patches resolved (13)

All 13 patches addressed across commits `739491e` (5 bugs + 2 symmetric fixes) and the follow-up hardening commit.

Bugs (from `739491e`):
- [x] [Review][Patch] Disposition cannot be cleared — schema allows `null`; route $unsets on null and records delta.
- [x] [Review][Patch] Concurrent PUT race — optimistic concurrency via `{_id, updated_at}` filter; 409 on mismatch.
- [x] [Review][Patch] PUT can resurrect retired — route returns 409 when existing.status === 'retired' and body doesn't echo it.
- [x] [Review][Patch] Client `loadEdges` stale-fetch — captures npcId at call start; drops mismatched response.
- [x] [Review][Patch] Client strip `custom_label` on kind change — client sends empty string when `kind !== 'other'`.
- [x] [Review][Patch] Server strip `custom_label` when `kind !== 'other'` — POST skips the field; PUT $unsets it.
- [x] [Review][Patch] Trim `custom_label` in PUT — PUT refactor calls `.trim()` before delta.

Hardening (from follow-up commit):
- [x] [Review][Patch] Validate GET query params — malformed ObjectIds on endpoint/a_id/b_id → 400; unknown kind/status → 400.
- [x] [Review][Patch] Schema `maxLength` on state (4000) and custom_label (200).
- [x] [Review][Patch] `actorSchema.id` `minLength: 1`.
- [x] [Review][Patch] Client-side `sameEndpoint` check before POST for instant feedback.
- [x] [Review][Patch] Warn on unsaved draft when switching NPCs — confirm() before discarding an open edit form.
- [x] [Review][Patch] Hide Edit button on retired edges — Edit + Retire hidden when `status === 'retired'`; History button preserved.

### Deferred (12)

- [x] [Review][Defer] Module-level state in relationship-editor.js — architectural, not a blocker; revisit in NPCR.6 when player-side editor lands
- [x] [Review][Defer] PUT schema should explicitly reject `history`/`created_by`/`created_at` instead of accepting-and-ignoring (defence-in-depth)
- [x] [Review][Defer] Cascade: retiring an NPC does not retire its edges — proper scope is NPCR.5 (migration / lifecycle)
- [x] [Review][Defer] No unique index on `(a, b, kind)` — may be intentional (multiple mentor edges possible); decide during NPCR.6
- [x] [Review][Defer] Compound indexes not created (`{'a.id':1, status:1}`, `{'b.id':1, status:1}`, `updated_at`) — optimisation after volume is known
- [x] [Review][Defer] Stale `_chars` / `_npcs` cache in editor — requires full character list to be reactive; bigger refactor
- [x] [Review][Defer] Deleted PC / NPC references render raw hex fallback — UX enhancement; affects NPCR.5 migration UX
- [x] [Review][Defer] `apiGet` doesn't differentiate 403 vs 500 — app-wide concern, not NPCR.2 scope
- [x] [Review][Defer] Endpoint-existence validation against `characters`/`npcs` collections — adds DB round-trips per POST/PUT; defer unless dangling edges become a real problem
- [x] [Review][Defer] Self-reference across endpoint types (same id, type a=pc/b=npc) not blocked — astronomically rare; documented in findings
- [x] [Review][Defer] Unicode normalization / null-byte stripping — defence-in-depth, not caused by this change
- [x] [Review][Defer] DELETE read-then-write race on history row — extreme edge; single-op atomic retire is a refactor
