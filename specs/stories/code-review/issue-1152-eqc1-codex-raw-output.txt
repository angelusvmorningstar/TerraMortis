2026-08-13T02:38:50.217844Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 94 column 5
OpenAI Codex v0.146.0
--------
workdir: D:\Terra Mortis\TM Suite
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: none
session id: 019ff8fc-9447-7002-8eb9-d1a8e21fd50d
--------
user
# Adversarial review - EQC-1 (bucket re-partition, containment schema, live-data migration,
issue #1152), TerraMortis (TM Suite)

You are reviewing a completed change in a repo you have full access to. You have NONE of the
conversation in which it was written, which is the point: you are here to catch what the author
could not catch about their own work.

## How to run this - read this section before anything else

This is **three passes in one session, in a fixed order**, and the order is load-bearing. Each pass
is allowed to see strictly more than the one before it. You cannot un-read a spec, so the pass that
must judge the code cold goes first.

1. Work the passes **in the order written**. Do not read ahead. Do not open a file a later pass
   grants you until you reach that pass. In particular: **the story spec is deliberately NOT in the
   diff.** Do not go looking for it during the earlier passes. The final pass will hand you the path.
2. **Freeze each pass before advancing.** Write that pass's findings out in full, to
   `specs/stories/code-review/issue-1152-eqc1-codex-findings.md`, before you open anything the next
   pass allows. Do not revise an earlier pass's findings in light of what a later pass taught you - if
   a later pass contradicts an earlier one, say so as a new finding and leave the original standing.
3. At the very end, **attest** to what you actually did: which files you opened in each pass, which
   commands you ran, and anything you could not run. Do not paper over a gap - see "Honesty" below.

## Ground rules

- Repo root: `D:\Terra Mortis\TM Suite`. **Quote this path in every shell command you run** - it
  contains a space. The diff is at `specs/stories/code-review/issue-1152-eqc1-diff.txt` (relative to
  that root), taken between base commit `ddf059f8` and the committed state at `c7e6771b` on branch
  `ms/issue-1152-eqc1-bucket-container-schema` (current HEAD - the change is already committed, not
  a working-tree diff).
- The diff is **deliberately scoped to source and tooling only**. The story spec
  (`specs/stories/feature.1152.eqc1-bucket-container-schema.story.md`) is excluded from it on purpose,
  so the earlier passes stay genuinely blind to the author's own account. Do not treat its absence as
  an omission or go hunting for it before Pass 3.
- **Read and run freely** to verify a claim. Running the code beats reasoning about it every time.
- **Do NOT modify, commit, or push anything.**
- **This is an umbrella workspace.** `D:\Terra Mortis\TM Suite` sits alongside three sibling repos:
  `D:\Terra Mortis\TM Wiki`, `D:\Terra Mortis\TM Cockpit`, `D:\Terra Mortis\TM Herald`. **Never modify,
  commit, or push in any of them, under any circumstance**, and you should not need to read them either
  - this change is entirely self-contained within TM Suite.
- Temporarily editing a file to prove something (revert one line, confirm the check now fails the way
  you expect, restore it) **is allowed and encouraged** - you MUST restore it exactly, confirm the
  restore with `git diff`, and say so in your output. Given the change is already committed, "restore"
  here means `git checkout -- <path>` back to the committed state, then confirm `git status --short`
  is clean of your own edits.
- **Environment hazards**: `npm test` (vitest, `server/` directory) needs a local `mongod` for several
  suites - they SKIP rather than fail without one (a documented quirk of this repo, per its own
  CLAUDE.md). If you don't have one, disclose that plainly rather than treating a skip as a pass. The
  equipment-specific suites you'll run (`equipment.test.js`, `equipment-client-fixes.test.js`,
  `issue-868-ecm-1-equipment-catalogue-api.test.js`, `issue-871-876-ecm-4-9-bundle.test.js`,
  `issue-872-ecm-5-editor-cache.test.js`, `issue-896-availability-filter.test.js`,
  `issue-879-defence-penalty-wirein.test.js`, `issue-1152-eqc1-bucket-migration.test.js`,
  `issue-873-ecm-6-admin-sidebar.test.js`) DO need `mongod` for their integration slices - if those
  skip in your environment, say so and report what DID run (the pure-function / static-analysis
  portions still exercise without a DB).
- **Blast radius**: `equipment_catalogue.bucket` is read directly by armour-penalty derivation
  (`equipment-derivation.js`), both roll calculators' weapon/skill-bonus chip filters (`roll.js`,
  `roll-v2.js`), the character sheet renderer, the DT form equipment dropdown, and the admin CRUD UI.
  A mistake in the bucket re-partition or the migration script would silently mis-derive combat
  mechanics (armour bonuses, weapon/skill roll chips) for EVERY character with equipment - live game
  data, not a corner case. This is exactly the kind of shared-gate, real-data change that warrants a
  full pass rather than a light one.

## Honesty requirements (these outrank completeness)

- If you could not run something, **say so plainly and name what you could not run**. A disclosed gap
  is far more useful than a confident static read presented as a verified one.
- If you found nothing in a pass or at a severity, **say that explicitly** rather than omitting the
  section or padding with style opinions.
- Report the **exact current gate numbers** you observe: the equipment-related vitest suites named
  above, run individually (`npx vitest run tests/<name>.test.js` from `server/`). Report the real
  numbers even if they disagree with anything the story claims - especially then.

---

## PASS 1 - BLIND HUNTER (the diff, and nothing else)

You get the diff at `specs/stories/code-review/issue-1152-eqc1-diff.txt` and **nothing else**. No
spec, no story file, no project context. Do not explore the repository. Do not go looking for the
spec. Read other files only to resolve an import path the diff itself leaves ambiguous.

The blinding is the point. You are here to catch what a competent reviewer with zero project memory
would catch, uncontaminated by the author's framing of what the change was supposed to do.

### What this diff claims to be

A re-partition of an equipment catalogue's `bucket` taxonomy from four old values
(`weapon|armour|equipment|asset`) to five new ones (`combat_gear|skill_gear|tool_utility|narrative|
container`), where `combat_gear` merges the old weapon and armour buckets (distinguished at read time
by which stat fields are populated: `weapon_type`/`damage_mod` for weapon-shaped, `armour_value`/
`defence_penalty` for armour-shaped - NOT by a new subtype field), and `container` replaces `asset`
(items can now be placed "inside" a container via a new `container_id` field on character equipment
entries, single-level only, no recursion). The diff includes: two JSON-schema files, a new
dry-run-by-default migration script (`migrate-eqc1-bucket-taxonomy.mjs`) with its own pure planning
function `planBucketMigration`, six production consumer files updated to the new bucket vocabulary,
and five pre-existing test files updated for the new fixtures/assertions plus one new test file for
the migration script itself.

**That is the shape it claims. Do not trust the shape - verify it.**

### What to hunt for

1. **The armour/weapon disambiguation within `combat_gear` is the single highest-risk change in this
   diff.** Before this change, a weapon item and an armour item could never be confused (different
   buckets). After it, they share a bucket and are distinguished ONLY by which stat fields happen to
   be populated. Find every predicate that reads `bucket === 'combat_gear'` and check it ALSO checks
   the right stat field (`armour_value != null` for armour-only logic, `weapon_type != null` for
   weapon-only logic) - not just the bucket. A predicate that checks bucket alone would now silently
   apply armour-penalty math to a weapon, or list a piece of armour in the weapon-reference panel.
2. **`planBucketMigration`'s mapping table**: `weapon→combat_gear`, `armour→combat_gear`,
   `equipment→skill_gear`, `asset→container`. Is every one of the four old values actually mapped?
   Is there any old value silently left unmapped, or any NEW value accidentally present as a map KEY
   (which would make an already-migrated document look unrecognised)? What happens to a document whose
   `bucket` field is missing, null, or an empty string - does the code path handle it, or throw?
3. **Migration idempotency claim**: the code should recognise a document already on a NEW bucket value
   and leave it untouched (`touched: false`) rather than re-processing or erroring. Verify the actual
   branch logic achieves this, don't take the intent on faith.
4. **The `container_id` field** (`character.schema.js`): is it validated as EITHER null/absent OR a
   24-hex string, with no way to sneak in some other type? Does the schema's `additionalProperties:
   false` on the equipment sub-document actually still hold with the new field added, or could there be
   a syntax slip that accidentally widens/narrows the allowed key set?
5. **Trivially-satisfiable assertions**: scan the new/changed test assertions for anything that would
   pass even if the underlying fix were wrong - e.g. a `toEqual` between two computed values that could
   coincidentally match, rather than being pinned against a hard-coded expected literal.
6. **Self-contradiction within the diff**: does any updated comment now claim behaviour the code
   doesn't actually implement (e.g. claiming recursive containment is blocked, when nothing in the
   diff actually prevents it programmatically - is the "single-level only" claim enforced in CODE, or
   only asserted in a comment)?
7. **Dead code / unused branches**: does the new `BUCKET_FIELDS`/`BUCKET_LABELS`-style mapping in
   `equipment-catalogue-admin.js` and `sheet.js` cover every one of the five new bucket values with no
   gaps that would silently render an empty options list for a real bucket?
8. **Error paths**: `migrate-eqc1-bucket-taxonomy.mjs`'s `main()` and `migrate()` - what happens on a
   Mongo connection failure mid-loop? Does a failure on document N leave documents 1..N-1 written but
   silently abandon the rest without reporting which ones were missed?

**STOP. Write your Pass 1 findings to `specs/stories/code-review/issue-1152-eqc1-codex-findings.md`
now, before reading further.**

---

## PASS 2 - EDGE CASE HUNTER (the diff, plus the repository)

You now have full read access to `D:\Terra Mortis\TM Suite`. Read whatever surrounding code you need
to understand what this change is actually plugging into. You still do **not** have the story spec or
any account of the author's intent - work from the code itself.

Your remit is boundaries and branches: walk every path, not just the one the author had in mind.

### Orientation (not ground truth - verify against the code)

Same summary as Pass 1: a five-value bucket re-partition (combat_gear/skill_gear/tool_utility/
narrative/container) replacing the old four (weapon/armour/equipment/asset), with combat_gear merging
weapon+armour (disambiguated by stat-field presence) and container replacing asset (adding a new
`container_id` field for single-level containment). A migration script backfills existing catalogue
documents; six consumer files were updated to read the new vocabulary.

### What to hunt for

1. **Hand-trace `armourDefencePenalty` and `wornArmourCount`** in `public/js/data/equipment-derivation.js`
   for a character wearing THREE items: one weapon-shaped combat_gear item (`weapon_type: 'melee'`,
   `armour_value: null`), one armour-shaped combat_gear item (`armour_value: 2`, `weapon_type: null`),
   and one combat_gear item with NEITHER field populated (an ST created it, never filled in stats).
   Confirm by tracing the actual code which of the three contribute to the penalty sum and which don't
   - does the third (neither-shaped) item silently count as armour, silently count as nothing, or
   throw?
2. **Hand-trace `roll.js`'s and `roll-v2.js`'s weapon-reference filter and skill-bonus chip filter**
   for the SAME three-item scenario. Confirm the weapon-shaped item appears in the weapon reference
   panel, the armour-shaped item does NOT, and walk what happens to the neither-shaped item in both
   filters.
3. **`editor/sheet.js`'s new "Other Combat Gear" fallback section** - find it, and trace: is it
   genuinely reachable (i.e., does SOME code path actually classify an item into it), or is the
   filtering logic upstream (`isWeaponShaped`/`isArmourShaped`) written such that every combat_gear
   item necessarily matches one of the two shaped filters, making the fallback dead code? Read the
   actual filter predicates, don't assume from the section's existence that it's reachable.
4. **Route/consumer completeness**: grep the WHOLE repo (not just the files the diff touched) for any
   OTHER place that compares against the literal strings `'weapon'`, `'armour'`, `bucket === 'equipment'`,
   or `bucket === 'asset'` in a bucket-comparison context that this diff did NOT update. The story's own
   claim is that all direct consumers were updated - verify that by searching independently rather than
   trusting the claim. Distinguish a genuine miss (something that reads bucket values and will now
   silently misbehave) from an incidental/comment-only/cosmetically-stale reference (lower severity, if
   any).
5. **Malformed/absent input at the migration script's entry points**: what does `planBucketMigration`
   return for `null`, `undefined`, `{}`, and a document whose `bucket` is a number or an array instead
   of a string? Trace the actual code, not the tests' coverage of it.
6. **`character.schema.js`'s `container_id`**: is there ANY write-site code path (not shown in this
   diff, but reachable from it) that would accept a `container_id` value referencing an item that does
   NOT exist in the same character's equipment array, or that references itself (an item claiming
   itself as its own container)? The story claims this is "display-inert" rather than write-rejected -
   confirm no downstream consumer added in THIS diff actually crashes on such a dangling/self reference
   (search `editor/sheet.js` and any other touched file for a `container_id` read site, since the
   diff's own scope doesn't appear to include a container-assignment UI yet - confirm that's really
   true, i.e. nothing reads `container_id` at all yet, or if something does, that it fails soft).
7. **CSS bucket-tag classes** (`admin-layout.css`): confirm every one of the five new bucket values has
   a matching `.ec-bucket-<value>` rule, and that no OLD `.ec-bucket-weapon` etc. rule was left
   dangling (harmless but worth noting as a Low if so).

**STOP. Write your Pass 2 findings to `specs/stories/code-review/issue-1152-eqc1-codex-findings.md`
now, before reading further.**

---

## PASS 3 - ACCEPTANCE AUDITOR (the diff, plus the spec)

Two sub-passes, in this order. **The order is the highest-value instruction in this whole document.**

### Pass 3a - form findings BEFORE reading the author's own account

1. Read `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md` - the **Story**,
   **Background**, **The new taxonomy** table, and **Acceptance Criteria (1-7)** sections.
2. **Do NOT read the "Dev Agent Record" section yet** (Agent Model Used / Debug Log References /
   Completion Notes List / Change Log). Skip past it entirely for now.
3. Against the seven acceptance criteria, check the diff and the real code it touches for:
   - Violations of an AC's **literal wording**. Read the words, not the surrounding narrative.
   - AC #2's "single-level containment only" claim - is this enforced anywhere in code, or purely
     descriptive? If purely descriptive, is that a gap against the AC's own wording, or is the AC
     itself only claiming a schema-level absence of recursion support (which would be true simply by
     there being no code that walks a container chain)? Reason carefully about what the AC actually
     commits to versus what would be nice-to-have.
   - AC #4's list of "every direct reader" - cross-check against your own Pass 2 grep for completeness.
   - AC #5's "no behavioural regression for correctly-migrated data" - is this actually demonstrated by
     a test that proves OLD-shape-derived output equals NEW-shape-derived output for equivalent data,
     or merely asserted?
4. **Write your Pass 3a findings down now, before moving on.**

Explicitly settled/out of scope for this story, per its own account - do not flag these as gaps if
genuinely absent, but DO flag if quietly done anyway: on-me vs owned-elsewhere display; a
container-assignment UI (something letting an ST actually set `container_id`); programmatic purchase
flow; skill-based acquisition removal; recursive containment; running the live migration against
production `tm_suite` (explicitly disclosed as NOT done, a deploy-time action).

### Pass 3b - now read the author's record and check it against reality

5. Now read the **Dev Agent Record** in full. It makes several specific, checkable claims:
   - The consumer blast-radius was originally scoped as 2 files (schema-only) and expanded to include
     14 files found via grep, with the final diff touching a named list of files.
   - `npm test`: 2463 total, 2458 pass, 5 pre-existing failures across 10 named test files - and a
     claim that these were **independently verified as pre-existing** via `git stash push` on the 13
     tracked EQC-1 files, re-running the same 10 previously-failing files against the clean base, and
     observing an **identical failure signature** (same file count, same test count, same test names),
     before `git stash pop` restored the changes.
   - "None of the 5 pre-existing failures reference equipment/bucket/armour/weapon/container in any
     way."
   - The live migration was deliberately NOT run against production `tm_suite`.
6. **Verify each claim by running it, not by reading it.** Run the equipment-related vitest suites
   yourself, right now (see Ground Rules for the list and the `mongod` caveat). If you have `mongod`
   available, additionally try to REPRODUCE the pre-existing-failure claim yourself: run the 10 named
   files (`epic.708.3-cycle-phase-controls`, `issue-1013-indomitable-rules-text`,
   `issue-1021-failed-breakpoint-merit`, `issue-811-sumchannels-rootcause`,
   `issue-826-cleanup-script-integration`, `issue-836-legacy-tracker-cache-removed`,
   `issue-837-xp-totals-deprecation`, `n7-n9-allocator-readers`, `n8-mandragora-prereq`,
   `oath-a-pledge-helpers`) against the CURRENT committed state (no stash needed - they should already
   fail identically, since the story's own claim is that these failures are unrelated and pre-existing)
   and confirm the failure count/names match what the story claims. If you don't have time/environment
   to fully reproduce the stash comparison, at minimum confirm these 10 files still show the same
   failure signature against current HEAD, and say plainly whether you attempted the deeper
   verification.
7. Flag anything **FALSE, OVERSTATED, or UNVERIFIABLE-AS-STATED**. This is the single highest-value
   thing this pass can find. A record's own "confirmed", "verified" or "resolved" label can itself be
   wrong - re-examine each one rather than inheriting it.
8. State plainly whether you believe this change is ready to ship as-is, needs patches, or has a
   blocking problem. Given the live-game stakes (Game 7 is Saturday), be explicit about whether you'd
   trust this to run correctly against real character data TODAY if the migration script were applied.

---

## Output

Write everything to `specs/stories/code-review/issue-1152-eqc1-codex-findings.md`, grouped `## High` /
`## Medium` / `## Low`, each finding tagged with the pass that produced it (`[Pass 1]`, `[Pass 2]`,
`[Pass 3a]`, `[Pass 3b]`). Write `- None found.` under any empty heading rather than dropping it.

For each finding:

- **One-line title**
- **Severity**: High / Medium / Low
- **File:line**
- **The triggering input or sequence** - be concrete about what reaches it
- **The observable consequence** - what actually goes wrong, for whom
- **Confidence**: how sure you are this is real and not a misread

Close with a **Validation notes** section stating:

- Which files you opened in each pass, and confirmation you did not read ahead.
- Every command you ran, with its real result, including the named vitest suites.
- **Anything you could not run, and why.** Name it specifically (e.g. no local mongod).
- Confirmation that you modified nothing, or that anything you touched was restored and verified
  (`git status --short` clean of unintended change).

2026-08-13T02:38:53.266476Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
I’ll preserve the review’s information barriers exactly: Pass 1 from the supplied diff only, freeze it to the findings file, then inspect repository context for Pass 2 and freeze again, then read the story in the required 3a/3b order and run the named gates. I’ll also keep an exact command/file-open log for the final attestation.
  → Pass 1: inspect only the supplied diff and freeze blind findings
  • Pass 2: inspect repository context, trace edge cases, and freeze findings
  • Pass 3a: read only pre-record story sections and freeze AC findings
  • Pass 3b: read Dev Agent Record, run verification gates, and audit claims
  • Finalize severity-grouped report and validation attestation
2026-08-13T02:39:01.161155Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw -LiteralPath 'D:\\Terra Mortis\\TM Suite\\specs\\stories\\code-review\\issue-1152-eqc1-diff.txt'" in D:\Terra Mortis\TM Suite
 succeeded in 2630ms:
diff --git a/public/css/admin-layout.css b/public/css/admin-layout.css
index ae0d0ab7..1de83822 100644
--- a/public/css/admin-layout.css
+++ b/public/css/admin-layout.css
@@ -10144,10 +10144,13 @@ html:not([data-theme="dark"]) .rules-tbl tbody tr:hover            { background:
   background: var(--surf2);
   color: var(--txt2);
 }
-.ec-bucket-weapon    { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
-.ec-bucket-armour    { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
-.ec-bucket-equipment { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
-.ec-bucket-asset     { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
+/* EQC-1 (#1152): re-tagged for the new five-value taxonomy. combat_gear takes
+   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
+.ec-bucket-combat_gear  { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
+.ec-bucket-skill_gear   { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
+.ec-bucket-tool_utility { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
+.ec-bucket-narrative    { background: var(--surf2); color: var(--txt3); }
+.ec-bucket-container    { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
 
 /* ── Forms ────────────────────────────────────────────────────────────────── */
 
diff --git a/public/js/admin/equipment-catalogue-admin.js b/public/js/admin/equipment-catalogue-admin.js
index be64c0dc..1a3cbb24 100644
--- a/public/js/admin/equipment-catalogue-admin.js
+++ b/public/js/admin/equipment-catalogue-admin.js
@@ -29,15 +29,22 @@
 import { apiGet, apiPost, apiPatch, apiRaw } from '../data/api.js';
 import { esc } from '../data/helpers.js';
 
-const BUCKETS = ['weapon', 'armour', 'equipment', 'asset'];
+// EQC-1 (issue #1152, epic #1038, 2026-08-13): re-partitioned from the old
+// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
+// absorbs both weapon and armour — an item fills whichever stat-field subset
+// applies (weapon fields, armour fields, or in principle both) and leaves
+// the rest null; see BUCKET_FIELDS below and equipment-derivation.js's own
+// armour_value-based discriminator.
+const BUCKETS = ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'];
 
 // Bucket → list of optional fields shown in create/edit forms. Mirrors
 // the EQ-1 null-template pattern (EQ_NULLS / WP_NULLS / AR_NULLS / AS_NULLS).
 const BUCKET_FIELDS = {
-  weapon:    ['damage_mod', 'damage_type', 'weapon_type'],
-  armour:    ['armour_value', 'defence_penalty'],
-  equipment: ['skill_domain', 'bonus_dice'],
-  asset:     ['mechanical_effect'],
+  combat_gear:  ['damage_mod', 'damage_type', 'weapon_type', 'armour_value', 'defence_penalty'],
+  skill_gear:   ['skill_domain', 'bonus_dice'],
+  tool_utility: ['mechanical_effect'],
+  narrative:    [],
+  container:    ['mechanical_effect'],
 };
 
 // Module-level state — re-rendered on every mutation. Single-view module
@@ -201,7 +208,7 @@ function wireListEvents() {
 
 function openCreateForm() {
   _editingId = null;
-  renderForm({ bucket: 'equipment', tags: [] });
+  renderForm({ bucket: 'skill_gear', tags: [] });
 }
 
 async function openEditForm(id) {
@@ -223,7 +230,7 @@ function renderForm(initial, impact = null) {
   const host = document.getElementById('ec-form-host');
   if (!host) return;
   const isEdit = !!_editingId;
-  const bucket = initial.bucket || 'equipment';
+  const bucket = initial.bucket || 'skill_gear';
   const bucketOpts = BUCKETS.map(b =>
     `<option value="${b}"${bucket === b ? ' selected' : ''}>${b}</option>`
   ).join('');
@@ -326,7 +333,7 @@ function wireFormEvents(initial) {
  */
 function collectFormValues(coerce = true) {
   const $ = id => document.getElementById(id);
-  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'equipment');
+  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'skill_gear');
   const name = ($('ec-f-name')?.value || '').trim();
   const description = ($('ec-f-description')?.value || '').trim() || null;
   const availRaw = $('ec-f-availability')?.value;
diff --git a/public/js/data/equipment-derivation.js b/public/js/data/equipment-derivation.js
index 219b2c77..a2f86b6a 100644
--- a/public/js/data/equipment-derivation.js
+++ b/public/js/data/equipment-derivation.js
@@ -5,8 +5,15 @@
  * array without an ADR" gate. ADR-006 specifies:
  *
  *   D1: armourDefencePenalty(c, catalogueLookup?) reads c.equipment[] filtered
- *       by state === 'worn' + bucket === 'armour'; injectable catalogue
- *       lookup (default: the ECM-5 cache reader).
+ *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
+ *       injectable catalogue lookup (default: the ECM-5 cache reader).
+ *
+ *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
+ *       merged into `combat_gear` (which also covers weapons). "Armour-shaped"
+ *       is identified by `entry.armour_value != null` rather than by bucket
+ *       alone — the epic's own "distinct stat fields" distinction within the
+ *       single combat_gear bucket. A weapon-shaped combat_gear entry has
+ *       `armour_value: null` and is correctly excluded.
  *   D2: worst-case math — Math.max(...penalties). The editor surfaces a soft
  *       hint when >1 armour is worn (wording is concern #8 below).
  *   D2-FLOOR: floor at 0 lives ONLY at the helper composition site. STM overlay
@@ -58,7 +65,7 @@ export function armourDefencePenalty(c, catalogueLookup = getCatalogueEntry) {
   for (const item of c.equipment) {
     if (!item || item.state !== 'worn') continue;
     const entry = catalogueLookup(item.catalogue_id);
-    if (!entry || entry.bucket !== 'armour') continue;
+    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
     const p = Number.isInteger(entry.defence_penalty) ? entry.defence_penalty : 0;
     if (p > 0) penalties.push(p);
   }
@@ -81,7 +88,7 @@ export function wornArmourCount(c, catalogueLookup = getCatalogueEntry) {
   for (const item of c.equipment) {
     if (!item || item.state !== 'worn') continue;
     const entry = catalogueLookup(item.catalogue_id);
-    if (entry?.bucket === 'armour') n++;
+    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
   }
   return n;
 }
diff --git a/public/js/editor/sheet.js b/public/js/editor/sheet.js
index b3e1ac35..b55a2165 100644
--- a/public/js/editor/sheet.js
+++ b/public/js/editor/sheet.js
@@ -2565,9 +2565,19 @@ export function shRenderManoeuvres(c, editMode) {
 }
 
 // ── Equipment renderer (EQ-2, issue #656) ────────────────────────────────────
-// Renders catalogue-ref equipment[]. All four buckets (weapon/armour/equipment/asset)
-// flow through the same array as of 2026-06-19 (character.assets[] retired).
+// Renders catalogue-ref equipment[]. All buckets flow through the same array
+// as of 2026-06-19 (character.assets[] retired).
 // Edit mode shows the same view -- equipment is managed via the ST CRUD API (EQ-1).
+//
+// EQC-1 (issue #1152, epic #1038, 2026-08-13): re-partitioned from the old
+// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
+// skill_gear/tool_utility/narrative/container). combat_gear merges the old
+// weapon+armour buckets -- the Weapons/Armour section split below is now
+// driven by which stat fields are POPULATED on the item (weapon_type/
+// damage_mod = weapon-shaped; armour_value/defence_penalty = armour-shaped),
+// not by a bucket comparison, since both shapes now share one bucket value.
+// An item with neither shape populated (an ST entry with no stats filled in
+// yet) falls to "Other Combat Gear" rather than being silently dropped.
 export function shRenderEquipment(c, editMode) {
   const equip  = c.equipment || [];
   if (!editMode && !equip.length) return '';
@@ -2581,18 +2591,27 @@ export function shRenderEquipment(c, editMode) {
   let h = '<div class="sh-sec"><div class="sh-sec-title">Equipment</div><div class="merit-list">';
 
   // Group equipment items by bucket, preserving flat-array index for remove buttons
-  const byBucket = { weapon: [], armour: [], equipment: [], asset: [] };
+  const byBucket = { combat_gear: [], skill_gear: [], tool_utility: [], narrative: [], container: [] };
   for (let i = 0; i < equip.length; i++) {
     const item   = equip[i];
     const entry  = getCatalogueEntry(item.catalogue_id) || {};
-    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
+    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'skill_gear';
     byBucket[bucket].push({ item, entry, idx: i });
   }
 
+  // combat_gear sub-split by populated stat fields, not bucket (both shapes
+  // now share the combat_gear bucket). An item matching neither shape (no
+  // stats filled in) still renders, under "Other Combat Gear".
+  const isWeaponShaped = e => e.weapon_type != null || e.damage_mod != null || e.damage_type != null;
+  const isArmourShaped = e => e.armour_value != null || e.defence_penalty != null;
+  const combatWeapons = byBucket.combat_gear.filter(x => isWeaponShaped(x.entry));
+  const combatArmour  = byBucket.combat_gear.filter(x => !isWeaponShaped(x.entry) && isArmourShaped(x.entry));
+  const combatOther   = byBucket.combat_gear.filter(x => !isWeaponShaped(x.entry) && !isArmourShaped(x.entry));
+
   // ── Weapons ──
-  if (byBucket.weapon.length) {
+  if (combatWeapons.length) {
     h += '<div class="sh-sub-title">Weapons</div>';
-    for (const { item, entry, idx } of byBucket.weapon) {
+    for (const { item, entry, idx } of combatWeapons) {
       const name  = entry.name || item.catalogue_id;
       // #896: per-character effective availability (raw - Fixer reduction).
       const eff = entry.availability != null ? effectiveAvailability(entry, c) : null;
@@ -2612,7 +2631,7 @@ export function shRenderEquipment(c, editMode) {
   }
 
   // ── Armour ──
-  if (byBucket.armour.length) {
+  if (combatArmour.length) {
     h += '<div class="sh-sub-title">Armour</div>';
     // Issue #879 (ADR-006 D2 + Concern #8): soft non-blocking hint when
     // multiple armour items are in state==='worn'. Stacking rule is
@@ -2623,7 +2642,7 @@ export function shRenderEquipment(c, editMode) {
       h += '<div class="sh-armour-hint" style="font-size:0.85em;opacity:0.75;margin-bottom:6px;">Only one armour applies; highest defence_penalty wins.</div>';
     }
     const baseDefence = calcDefence(c);
-    for (const { item, entry, idx } of byBucket.armour) {
+    for (const { item, entry, idx } of combatArmour) {
       const name  = entry.name || item.catalogue_id;
       // #896: per-character effective availability (raw - Fixer reduction).
       const eff = entry.availability != null ? effectiveAvailability(entry, c) : null;
@@ -2641,10 +2660,26 @@ export function shRenderEquipment(c, editMode) {
     }
   }
 
-  // ── Equipment (tools / tech) ──
-  if (byBucket.equipment.length) {
-    h += '<div class="sh-sub-title">Equipment</div>';
-    for (const { item, entry, idx } of byBucket.equipment) {
+  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
+  // — e.g. an ST entry with no stat fields filled in yet) ──
+  if (combatOther.length) {
+    h += '<div class="sh-sub-title">Other Combat Gear</div>';
+    for (const { item, entry, idx } of combatOther) {
+      const name  = entry.name || item.catalogue_id;
+      const eff   = entry.availability != null ? effectiveAvailability(entry, c) : null;
+      const qual  = eff != null ? `avail ${eff}` : '';
+      const rmBtn = editMode ? `<button class="sk-spec-rm" style="float:right;margin-top:2px" onclick="shRemoveEquip(${idx})" title="Remove">× Remove</button>` : '';
+      h += `<div class="merit-plain"><div class="trait-row">` +
+        `<div class="trait-main"><span class="trait-name">${esc(name)}</span><div class="trait-right">${stateChip(item.state)}${rmBtn}</div></div>` +
+        (qual || item.notes ? `<div class="trait-sub">${qual ? `<span class="trait-qual">${esc(qual)}</span>` : ''}${item.notes ? `<span class="trait-qual dim">${esc(item.notes)}</span>` : ''}</div>` : '') +
+        `</div></div>`;
+    }
+  }
+
+  // ── Skill Gear (old "Equipment" bucket, unchanged meaning) ──
+  if (byBucket.skill_gear.length) {
+    h += '<div class="sh-sub-title">Skill Gear</div>';
+    for (const { item, entry, idx } of byBucket.skill_gear) {
       const name  = entry.name || item.catalogue_id;
       const pool  = (entry.skill_domain && entry.bonus_dice != null)
         ? `${entry.skill_domain} +${entry.bonus_dice} dice` : '';
@@ -2663,10 +2698,39 @@ export function shRenderEquipment(c, editMode) {
     }
   }
 
-  // ── Assets (catalogue-backed since 2026-06-19) ──
-  if (byBucket.asset.length) {
-    h += '<div class="sh-sub-title">Assets</div>';
-    for (const { item, entry, idx } of byBucket.asset) {
+  // ── Tools / Utility (NEW bucket — "does a thing, no bonus") ──
+  if (byBucket.tool_utility.length) {
+    h += '<div class="sh-sub-title">Tools / Utility</div>';
+    for (const { item, entry, idx } of byBucket.tool_utility) {
+      const name  = entry.name || item.catalogue_id;
+      const eff   = entry.availability != null ? effectiveAvailability(entry, c) : null;
+      const parts = [entry.mechanical_effect || null, eff != null ? `avail ${eff}` : null].filter(Boolean);
+      const qual  = parts.join(' · ');
+      const rmBtn = editMode ? `<button class="sk-spec-rm" style="float:right;margin-top:2px" onclick="shRemoveEquip(${idx})" title="Remove">× Remove</button>` : '';
+      h += `<div class="merit-plain"><div class="trait-row">` +
+        `<div class="trait-main"><span class="trait-name">${esc(name)}</span><div class="trait-right">${stateChip(item.state)}${rmBtn}</div></div>` +
+        (qual || item.notes ? `<div class="trait-sub">${qual ? `<span class="trait-qual">${esc(qual)}</span>` : ''}${item.notes ? `<span class="trait-qual dim">${esc(item.notes)}</span>` : ''}</div>` : '') +
+        `</div></div>`;
+    }
+  }
+
+  // ── Narrative (NEW bucket — purely descriptive, no stat fields) ──
+  if (byBucket.narrative.length) {
+    h += '<div class="sh-sub-title">Narrative</div>';
+    for (const { item, entry, idx } of byBucket.narrative) {
+      const name  = entry.name || item.catalogue_id;
+      const rmBtn = editMode ? `<button class="sk-spec-rm" style="float:right;margin-top:2px" onclick="shRemoveEquip(${idx})" title="Remove">× Remove</button>` : '';
+      h += `<div class="merit-plain"><div class="trait-row">` +
+        `<div class="trait-main"><span class="trait-name">${esc(name)}</span><div class="trait-right">${stateChip(item.state)}${rmBtn}</div></div>` +
+        (entry.description || item.notes ? `<div class="trait-sub">${entry.description ? `<span class="trait-qual">${esc(entry.description)}</span>` : ''}${item.notes ? `<span class="trait-qual dim">${esc(item.notes)}</span>` : ''}</div>` : '') +
+        `</div></div>`;
+    }
+  }
+
+  // ── Containers (old "Assets" bucket — catalogue-backed since 2026-06-19) ──
+  if (byBucket.container.length) {
+    h += '<div class="sh-sub-title">Containers</div>';
+    for (const { item, entry, idx } of byBucket.container) {
       const name  = entry.name || item.catalogue_id;
       const eff   = entry.availability != null ? effectiveAvailability(entry, c) : null;
       const parts = [
@@ -2685,19 +2749,26 @@ export function shRenderEquipment(c, editMode) {
   }
 
   // ── Edit-mode add form ──
-  // 2026-06-19: single add form covers all four buckets (weapon/armour/equipment/asset).
-  // Asset is now in the bucket dropdown; the separate free-text "Add Asset" form is gone
-  // along with character.assets[] — catalogue-ref is the single canonical storage shape.
+  // 2026-06-19: single add form covers all buckets. Asset (now "container") is
+  // in the bucket dropdown; the separate free-text "Add Asset" form is gone
+  // along with character.assets[] — catalogue-ref is the single canonical
+  // storage shape. EQC-1 (#1152, 2026-08-13): dropdown values updated to the
+  // new five-bucket taxonomy; labels use BUCKET_LABELS rather than a bare
+  // capitalise (combat_gear/skill_gear/tool_utility need real spacing).
   if (editMode) {
     const STATES   = ['carried', 'worn', 'stashed', 'active', 'lost'];
-    const BUCKETS  = ['weapon', 'armour', 'equipment', 'asset'];
+    const BUCKETS  = ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'];
+    const BUCKET_LABELS = {
+      combat_gear: 'Combat Gear', skill_gear: 'Skill Gear', tool_utility: 'Tools / Utility',
+      narrative: 'Narrative', container: 'Container',
+    };
     const defCycle = state.activeCycleNum ?? 0;
 
     h += '<div class="sh-sub-title" style="margin-top:10px">Add Equipment Item</div>';
     h += '<div class="dev-add-row" style="display:flex;flex-wrap:wrap;gap:4px;align-items:center;padding:4px 0">'
       + '<select id="eq-add-bucket" class="dev-add-btn" onchange="shEquipBucketFilter()">'
       + '<option value="">Bucket…</option>'
-      + BUCKETS.map(b => `<option value="${b}">${b.charAt(0).toUpperCase() + b.slice(1)}</option>`).join('')
+      + BUCKETS.map(b => `<option value="${b}">${BUCKET_LABELS[b]}</option>`).join('')
       + '</select>'
       + '<select id="eq-add-item" class="dev-add-btn"><option value="">-- select bucket first --</option></select>'
       + '<select id="eq-add-state" class="dev-add-btn">'
diff --git a/public/js/suite/roll-v2.js b/public/js/suite/roll-v2.js
index bf2ae760..badc1e10 100644
--- a/public/js/suite/roll-v2.js
+++ b/public/js/suite/roll-v2.js
@@ -217,7 +217,8 @@ export function updPool() {
       // #752: 'active' is the strongest in-use state — treat it identically
       // to carried/worn for chip eligibility (Khepri's option (a) — preserves
       // 'active' as a semantically-stronger marker an ST may have already used).
-      return entry && entry.bucket === 'equipment' &&
+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
+      return entry && entry.bucket === 'skill_gear' &&
              entry.bonus_dice > 0 &&
              entry.skill_domain === pi.skill &&
              (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
@@ -307,7 +308,9 @@ export function updWeaponRef() {
     // #752: 'active' is the strongest in-use state — include it in weapon-ref
     // eligibility (matches the chip predicate above).
     const entry = getCatalogueEntry(item.catalogue_id);
-    return entry && entry.bucket === 'weapon' &&
+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
+    // is identified by weapon_type presence (armour-shaped entries leave it null).
+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
            (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
   });
   if (!weapons.length) {
diff --git a/public/js/suite/roll.js b/public/js/suite/roll.js
index 095857e0..8ec5f622 100644
--- a/public/js/suite/roll.js
+++ b/public/js/suite/roll.js
@@ -141,7 +141,8 @@ export function updPool() {
       // #752: 'active' is the strongest in-use state — treat it identically
       // to carried/worn for chip eligibility (Khepri's option (a) — preserves
       // 'active' as a semantically-stronger marker an ST may have already used).
-      return entry && entry.bucket === 'equipment' &&
+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
+      return entry && entry.bucket === 'skill_gear' &&
              entry.bonus_dice > 0 &&
              entry.skill_domain === pi.skill &&
              (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
@@ -231,7 +232,9 @@ export function updWeaponRef() {
     // #752: 'active' is the strongest in-use state — include it in weapon-ref
     // eligibility (matches the chip predicate above).
     const entry = getCatalogueEntry(item.catalogue_id);
-    return entry && entry.bucket === 'weapon' &&
+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
+    // is identified by weapon_type presence (armour-shaped entries leave it null).
+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
            (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
   });
   if (!weapons.length) {
diff --git a/public/js/tabs/downtime-form.js b/public/js/tabs/downtime-form.js
index eea3744e..d33f65d1 100644
--- a/public/js/tabs/downtime-form.js
+++ b/public/js/tabs/downtime-form.js
@@ -5541,10 +5541,18 @@ function renderEquipmentRow(n, saved) {
   if (legacyName && !selectedId) {
     h += `<option value="" disabled>(legacy: ${esc(legacyName)} \u2014 please reselect)</option>`;
   }
-  for (const bucket of ['weapon', 'armour', 'equipment', 'asset']) {
+  // EQC-1 (#1152, epic #1038, 2026-08-13): re-partitioned from the old four
+  // buckets (weapon/armour/equipment/asset) to five; labels use
+  // EQUIPMENT_BUCKET_LABELS rather than the bare bucket value (combat_gear/
+  // skill_gear/tool_utility need real spacing, not a raw enum string).
+  const EQUIPMENT_BUCKET_LABELS = {
+    combat_gear: 'Combat Gear', skill_gear: 'Skill Gear', tool_utility: 'Tools / Utility',
+    narrative: 'Narrative', container: 'Container',
+  };
+  for (const bucket of ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']) {
     const arr = byBucket.get(bucket);
     if (!arr || !arr.length) continue;
-    h += `<optgroup label="${esc(bucket)}">`;
+    h += `<optgroup label="${esc(EQUIPMENT_BUCKET_LABELS[bucket] || bucket)}">`;
     for (const it of arr) {
       const sel       = String(it._id) === selectedId ? ' selected' : '';
       const eff       = effectiveAvailability(it, currentChar);
diff --git a/server/schemas/character.schema.js b/server/schemas/character.schema.js
index 2860ed0e..75247163 100644
--- a/server/schemas/character.schema.js
+++ b/server/schemas/character.schema.js
@@ -315,6 +315,23 @@ export const characterSchema = {
     // specs/epic-ecm-equipment-catalogue-migration.md and Khepri's ECM-3
     // dispatch for the rationale (the coercion is canonical Express+Mongo
     // hygiene, not transitional kludge — it stays after ECM-4/5 ship).
+    //
+    // `container_id` (EQC-1, issue #1152, epic #1038) — null/absent means
+    // this item is loose (carried on the character, not stored inside
+    // anything). When set, it is the `catalogue_id` of ANOTHER entry in this
+    // SAME character's `equipment[]` array whose catalogue bucket is
+    // `container` (a haven, vehicle, safe, etc.) — "a property contains a
+    // security system... never a bonus on the asset". Single-level only for
+    // v1 (a container's own contents are never themselves container-eligible
+    // targets) — the epic's own examples (a safe inside a haven) are all
+    // single-level, and recursive containment is real added complexity with
+    // no stated requirement yet; a future story can lift this if actually
+    // needed. Not validated as a REFERENCE here (schema has no cross-item
+    // lookup) — the write route is responsible for confirming the referenced
+    // entry exists in the same array and resolves to a container-bucket
+    // catalogue item; an unresolvable/stale container_id is display-inert
+    // (renders as loose) rather than a write-time hard failure, matching
+    // this schema's existing catalogue_id-is-a-lean-ref philosophy.
     equipment: {
       type: 'array',
       default: [],
@@ -326,6 +343,7 @@ export const characterSchema = {
           state:           { type: 'string', enum: ['carried', 'worn', 'stashed', 'lost', 'active'] },
           acquired_cycle:  { type: 'integer', minimum: 0 },
           notes:           { type: ['string', 'null'] },
+          container_id:    { type: ['string', 'null'], pattern: '^[a-f0-9]{24}$' },
         },
         additionalProperties: false,
       },
diff --git a/server/schemas/equipment_catalogue.schema.js b/server/schemas/equipment_catalogue.schema.js
index a2405ecf..28ce2fdf 100644
--- a/server/schemas/equipment_catalogue.schema.js
+++ b/server/schemas/equipment_catalogue.schema.js
@@ -12,6 +12,32 @@
  * — that's a separate bugfix. Bucket-specific fields are loosely typed (nullable
  * primitives) so the schema accepts every existing seed entry without further
  * coercion in ECM-2.
+ *
+ * EQC-1 (issue #1152, epic #1038) — bucket re-partition, 2026-08-13. The old
+ * four buckets (weapon/armour/equipment/asset) become FIVE new values:
+ *
+ *   combat_gear  — weapons AND armour, distinguished by which stat fields are
+ *                  populated (damage_mod/damage_type/weapon_type = weapon-shaped;
+ *                  armour_value/defence_penalty = armour-shaped). Both sets of
+ *                  fields stay on the schema for this bucket; a given item
+ *                  populates whichever subset applies and leaves the rest null.
+ *   skill_gear   — old `equipment` bucket, unchanged meaning (skill_domain +
+ *                  bonus_dice). Renamed only.
+ *   tool_utility — NEW. "Does a thing, no bonus" per the epic description —
+ *                  mechanical_effect (free text), no numeric bonus field.
+ *   narrative    — NEW. Purely descriptive; no bucket-specific stat fields.
+ *   container    — old `asset` bucket. "Assets are containers... no
+ *                  stat-stacking on the asset itself" (epic #1038) — holds
+ *                  other equipment (see character.schema.js's `container_id`)
+ *                  rather than granting a bonus of its own. mechanical_effect
+ *                  stays available for descriptive/narrative notes (e.g. what
+ *                  a haven confers), not a combat bonus.
+ *
+ * Migration: server/scripts/migrate-eqc1-bucket-taxonomy.mjs maps every live
+ * equipment_catalogue document from the old enum to the new one
+ * (weapon→combat_gear, armour→combat_gear, equipment→skill_gear,
+ * asset→container). Must run before/alongside this schema landing — see that
+ * script's own header for why the two cannot ship independently.
  */
 
 export const equipmentCatalogueSchema = {
@@ -26,7 +52,7 @@ export const equipmentCatalogueSchema = {
     _id: {},
 
     // ── Identity / core ──
-    bucket:       { type: 'string', enum: ['weapon', 'armour', 'equipment', 'asset'] },
+    bucket:       { type: 'string', enum: ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'] },
     name:         { type: 'string', minLength: 1 },
     description:  { type: ['string', 'null'] },
     availability: { type: ['integer', 'null'], minimum: 0, maximum: 5 },
diff --git a/server/scripts/migrate-eqc1-bucket-taxonomy.mjs b/server/scripts/migrate-eqc1-bucket-taxonomy.mjs
new file mode 100644
index 00000000..25692ee8
--- /dev/null
+++ b/server/scripts/migrate-eqc1-bucket-taxonomy.mjs
@@ -0,0 +1,154 @@
+/**
+ * EQC-1 (issue #1152, epic #1038) — equipment_catalogue bucket re-partition.
+ *
+ * Maps every live `equipment_catalogue` document from the OLD four-bucket
+ * taxonomy to the NEW five-value one:
+ *
+ *   weapon    → combat_gear   (weapon-shaped: damage_mod/damage_type/weapon_type)
+ *   armour    → combat_gear   (armour-shaped: armour_value/defence_penalty)
+ *   equipment → skill_gear    (unchanged meaning — skill_domain + bonus_dice)
+ *   asset     → container     ("assets are containers... no stat-stacking
+ *                              on the asset itself" — epic #1038)
+ *
+ * `combat_gear` absorbs both old weapon and armour buckets, distinguished at
+ * read time by WHICH stat fields are populated on the item — not by a new
+ * subtype field. This mirrors the epic's own wording ("combat gear
+ * (weapons/armour, distinct stat fields)") and needs no new schema field.
+ *
+ * WHY THIS CANNOT SHIP INDEPENDENTLY OF THE SCHEMA CHANGE. Unlike
+ * backfill-free-grants.js's union-sum runtime guard, nothing in this codebase
+ * reads BOTH the old and new bucket vocabulary simultaneously (a deliberate
+ * choice — see the EQC-1 story's "Migration shape" decision, atomic not
+ * dual-support). So an un-migrated document is invisible to every consumer
+ * (armourDefencePenalty, roll calculator weapon/skill chips, sheet renderer,
+ * DT form dropdown) the moment the new-taxonomy-aware code lands. This script
+ * and the schema/consumer changes in the same story MUST land together on
+ * this branch before merge — there is no safe "schema first, migrate later"
+ * window here, unlike the free-grants case.
+ *
+ * Idempotent: re-running matches zero docs the second time (every document's
+ * `bucket` is already one of the new five values after the first apply).
+ * Dry-run default, mirrors backfill-free-grants.js's own CLI shape.
+ *
+ * Usage:
+ *   # preview (default — no writes):
+ *   node server/scripts/migrate-eqc1-bucket-taxonomy.mjs
+ *
+ *   # apply:
+ *   node server/scripts/migrate-eqc1-bucket-taxonomy.mjs --apply
+ *
+ *   # target the test DB:
+ *   MONGODB_DB=tm_suite_test node server/scripts/migrate-eqc1-bucket-taxonomy.mjs --apply
+ *
+ * Locally, run from `server/` so cwd-relative `dotenv/config` picks up
+ * `server/.env`. On Render env vars are pre-set (no-op).
+ */
+
+import 'dotenv/config';
+import { pathToFileURL } from 'url';
+import { connectDb, getCollection, closeDb } from '../db.js';
+
+/** Old bucket → new bucket. The one-to-one mapping this whole script exists to apply. */
+export const BUCKET_MAP = Object.freeze({
+  weapon: 'combat_gear',
+  armour: 'combat_gear',
+  equipment: 'skill_gear',
+  asset: 'container',
+});
+
+const NEW_BUCKETS = new Set(['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']);
+
+/**
+ * Compute the migration plan for a single catalogue document. Pure — no DB
+ * access, no module-level state.
+ *
+ * @param {object} doc - an equipment_catalogue document
+ * @returns {{ touched: boolean, fromBucket: string|undefined, toBucket: string|undefined, reason: string|null }}
+ */
+export function planBucketMigration(doc) {
+  if (!doc || typeof doc !== 'object') {
+    return { touched: false, fromBucket: undefined, toBucket: undefined, reason: 'malformed document' };
+  }
+  const from = doc.bucket;
+  if (NEW_BUCKETS.has(from)) {
+    return { touched: false, fromBucket: from, toBucket: from, reason: null };
+  }
+  const to = BUCKET_MAP[from];
+  if (!to) {
+    // Neither a known old value nor a known new one — flag rather than guess.
+    return { touched: false, fromBucket: from, toBucket: undefined, reason: `unrecognised bucket value "${from}"` };
+  }
+  return { touched: true, fromBucket: from, toBucket: to, reason: null };
+}
+
+/**
+ * Drive the migration against MongoDB. Returns the aggregate summary.
+ *
+ * @param {object} [opts]
+ * @param {boolean} [opts.dryRun]
+ * @param {boolean} [opts.log]
+ */
+export async function migrate(opts = {}) {
+  const { dryRun = true, log = true } = opts;
+  const col = getCollection('equipment_catalogue');
+  const docs = await col.find({}).toArray();
+
+  const totals = {
+    scanned: docs.length,
+    touched: 0,
+    perMapping: Object.create(null),
+    unrecognised: [],
+  };
+
+  for (const doc of docs) {
+    const plan = planBucketMigration(doc);
+    if (plan.reason && !plan.touched) {
+      totals.unrecognised.push({ id: String(doc._id), name: doc.name || '(unnamed)', bucket: plan.fromBucket });
+      continue;
+    }
+    if (!plan.touched) continue;
+
+    const key = `${plan.fromBucket} → ${plan.toBucket}`;
+    totals.perMapping[key] = (totals.perMapping[key] || 0) + 1;
+    totals.touched += 1;
+
+    if (!dryRun) {
+      await col.updateOne({ _id: doc._id }, { $set: { bucket: plan.toBucket, updated_at: new Date().toISOString() } });
+    }
+  }
+
+  if (log) {
+    const verb = dryRun ? '[DRY RUN] would touch' : 'touched';
+    console.log(`Scanned ${totals.scanned} catalogue item(s); ${verb} ${totals.touched}.`);
+    if (Object.keys(totals.perMapping).length) {
+      console.log('Per-mapping:');
+      for (const [k, n] of Object.entries(totals.perMapping)) console.log(`  ${k.padEnd(24)} ${n}`);
+    }
+    if (totals.unrecognised.length) {
+      console.log(`\nUNRECOGNISED bucket values (${totals.unrecognised.length}) — SKIPPED, need human review:`);
+      for (const u of totals.unrecognised) console.log(`  ${u.name} (${u.id}): bucket="${u.bucket}"`);
+    }
+  }
+  return totals;
+}
+
+export async function main(argv = process.argv) {
+  const dryRun = !argv.includes('--apply');
+  console.log(`Mode: ${dryRun ? 'DRY RUN (read only; pass --apply to write)' : 'APPLY (will write)'}`);
+  console.log(`Target DB: ${process.env.MONGODB_DB || 'tm_suite'}`);
+  console.log('');
+
+  await connectDb();
+  try {
+    await migrate({ dryRun });
+  } finally {
+    await closeDb();
+  }
+
+  console.log('\nIdempotency check (after --apply): re-run with no flag and confirm "touched 0".');
+}
+
+// Auto-run only when invoked directly (not when imported by a test).
+if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href) {
+  main().catch(err => { console.error(err); process.exit(1); });
+}
diff --git a/server/tests/equipment-client-fixes.test.js b/server/tests/equipment-client-fixes.test.js
index 2929996a..09283579 100644
--- a/server/tests/equipment-client-fixes.test.js
+++ b/server/tests/equipment-client-fixes.test.js
@@ -64,15 +64,18 @@ describe('#752 — roll.js predicates include the active state', () => {
   it('equipment-chip filter accepts state === active', () => {
     const src = read('public/js/suite/roll.js');
     // The predicate block: any line with `item.state === 'active'` near a
-    // `bucket === 'equipment'` filter clause counts. We confirm BOTH the
-    // 'equipment' filter and the 'active' check coexist within a small
+    // `bucket === 'skill_gear'` filter clause counts. We confirm BOTH the
+    // skill_gear filter and the 'active' check coexist within a small
     // window so a future predicate rewrite doesn't silently drop 'active'.
-    expect(src).toMatch(/bucket === 'equipment'[\s\S]{0,400}item\.state === 'active'/);
+    // EQC-1 (#1152): the old 'equipment' bucket is now 'skill_gear'.
+    expect(src).toMatch(/bucket === 'skill_gear'[\s\S]{0,400}item\.state === 'active'/);
   });
 
   it('weapon-reference filter accepts state === active', () => {
     const src = read('public/js/suite/roll.js');
-    expect(src).toMatch(/bucket === 'weapon'[\s\S]{0,400}item\.state === 'active'/);
+    // EQC-1 (#1152): the old 'weapon' bucket merged into 'combat_gear',
+    // weapon-shaped items distinguished by weapon_type != null.
+    expect(src).toMatch(/bucket === 'combat_gear' && entry\.weapon_type != null[\s\S]{0,400}item\.state === 'active'/);
   });
 
   it('the legacy carried + worn states still appear in the same predicates (no accidental swap)', () => {
diff --git a/server/tests/issue-1152-eqc1-bucket-migration.test.js b/server/tests/issue-1152-eqc1-bucket-migration.test.js
new file mode 100644
index 00000000..16fc9640
--- /dev/null
+++ b/server/tests/issue-1152-eqc1-bucket-migration.test.js
@@ -0,0 +1,130 @@
+/**
+ * EQC-1 (issue #1152, epic #1038) — bucket taxonomy migration script.
+ *
+ * Pure-function tests for planBucketMigration (no DB) plus an integration
+ * test driving migrate() against tm_suite_test, mirroring the pattern
+ * backfill-free-grants.js's own test suite would use (dry-run default,
+ * idempotent, per-mapping summary).
+ */
+
+import { describe, it, expect, beforeAll, afterAll, afterEach } from 'vitest';
+import 'dotenv/config';
+import { setupDb, teardownDb } from './helpers/db-setup.js';
+import { getCollection } from '../db.js';
+import { BUCKET_MAP, planBucketMigration, migrate } from '../scripts/migrate-eqc1-bucket-taxonomy.mjs';
+
+describe('planBucketMigration (pure)', () => {
+  it('maps weapon -> combat_gear', () => {
+    const r = planBucketMigration({ bucket: 'weapon' });
+    expect(r).toEqual({ touched: true, fromBucket: 'weapon', toBucket: 'combat_gear', reason: null });
+  });
+
+  it('maps armour -> combat_gear', () => {
+    const r = planBucketMigration({ bucket: 'armour' });
+    expect(r.touched).toBe(true);
+    expect(r.toBucket).toBe('combat_gear');
+  });
+
+  it('maps equipment -> skill_gear', () => {
+    const r = planBucketMigration({ bucket: 'equipment' });
+    expect(r.touched).toBe(true);
+    expect(r.toBucket).toBe('skill_gear');
+  });
+
+  it('maps asset -> container', () => {
+    const r = planBucketMigration({ bucket: 'asset' });
+    expect(r.touched).toBe(true);
+    expect(r.toBucket).toBe('container');
+  });
+
+  it('leaves an already-migrated document untouched (idempotency)', () => {
+    for (const bucket of ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']) {
+      const r = planBucketMigration({ bucket });
+      expect(r.touched).toBe(false);
+      expect(r.toBucket).toBe(bucket);
+    }
+  });
+
+  it('flags an unrecognised bucket value rather than guessing', () => {
+    const r = planBucketMigration({ bucket: 'wandwood' });
+    expect(r.touched).toBe(false);
+    expect(r.toBucket).toBeUndefined();
+    expect(r.reason).toMatch(/unrecognised/);
+  });
+
+  it('handles a malformed/missing document without throwing', () => {
+    expect(planBucketMigration(null).touched).toBe(false);
+    expect(planBucketMigration(undefined).touched).toBe(false);
+    expect(planBucketMigration({}).touched).toBe(false);
+  });
+
+  it('BUCKET_MAP covers exactly the four old bucket values, nothing else', () => {
+    expect(Object.keys(BUCKET_MAP).sort()).toEqual(['armour', 'asset', 'equipment', 'weapon']);
+  });
+});
+
+describe('migrate() — integration against tm_suite_test', () => {
+  const seededIds = [];
+
+  beforeAll(async () => { await setupDb(); });
+  afterAll(async () => {
+    if (seededIds.length) await getCollection('equipment_catalogue').deleteMany({ _id: { $in: seededIds } });
+    await teardownDb();
+  });
+  afterEach(async () => {
+    if (seededIds.length) {
+      await getCollection('equipment_catalogue').deleteMany({ _id: { $in: seededIds } });
+      seededIds.length = 0;
+    }
+  });
+
+  async function seed(bucket, name) {
+    const now = new Date().toISOString();
+    const result = await getCollection('equipment_catalogue').insertOne({
+      bucket, name, description: 'fixture', availability: 1, tags: [],
+      damage_mod: null, damage_type: null, weapon_type: null,
+      armour_value: null, defence_penalty: null, skill_domain: null, bonus_dice: null,
+      mechanical_effect: null, created_at: now, updated_at: now,
+    });
+    seededIds.push(result.insertedId);
+    return result.insertedId;
+  }
+
+  it('dry run makes no writes', async () => {
+    const id = await seed('weapon', 'Dry Run Knife');
+    const totals = await migrate({ dryRun: true, log: false });
+    expect(totals.touched).toBeGreaterThanOrEqual(1);
+    const doc = await getCollection('equipment_catalogue').findOne({ _id: id });
+    expect(doc.bucket).toBe('weapon');
+  });
+
+  it('apply migrates every old-taxonomy document and is idempotent on re-run', async () => {
+    const wId = await seed('weapon', 'Apply Knife');
+    const aId = await seed('armour', 'Apply Vest');
+    const eId = await seed('equipment', 'Apply Lockpick');
+    const asId = await seed('asset', 'Apply Haven');
+
+    const first = await migrate({ dryRun: false, log: false });
+    expect(first.touched).toBeGreaterThanOrEqual(4);
+
+    const col = getCollection('equipment_catalogue');
+    expect((await col.findOne({ _id: wId })).bucket).toBe('combat_gear');
+    expect((await col.findOne({ _id: aId })).bucket).toBe('combat_gear');
+    expect((await col.findOne({ _id: eId })).bucket).toBe('skill_gear');
+    expect((await col.findOne({ _id: asId })).bucket).toBe('container');
+
+    // Idempotency: re-run touches nothing further among these four.
+    const second = await migrate({ dryRun: false, log: false });
+    const stillOld = await col.find({ _id: { $in: [wId, aId, eId, asId] }, bucket: { $nin: ['combat_gear', 'skill_gear', 'container'] } }).toArray();
+    expect(stillOld.length).toBe(0);
+    expect(second.touched).toBe(0);
+  });
+
+  it('an unrecognised bucket value is reported, not silently dropped or guessed', async () => {
+    const id = await seed('wandwood', 'Mystery Item');
+    const totals = await migrate({ dryRun: true, log: false });
+    const hit = totals.unrecognised.find(u => u.id === String(id));
+    expect(hit).toBeDefined();
+    expect(hit.bucket).toBe('wandwood');
+  });
+});
diff --git a/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js b/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
index 3dd47f34..261610a0 100644
--- a/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
+++ b/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
@@ -139,7 +139,7 @@ describe('POST /api/equipment_catalogue', () => {
     const res = await request(app)
       .post('/api/equipment_catalogue')
       .set('X-Test-User', stUser())
-      .send({ bucket: 'weapon', name: 'Glock 17', description: 'Pistol', availability: 2, damage_mod: 2 });
+      .send({ bucket: 'combat_gear', name: 'Glock 17', description: 'Pistol', availability: 2, damage_mod: 2, weapon_type: 'ranged' });
     expect(res.status).toBe(201);
     expect(res.body._id).toBeDefined();
     expect(res.body.name).toBe('Glock 17');
diff --git a/server/tests/issue-871-876-ecm-4-9-bundle.test.js b/server/tests/issue-871-876-ecm-4-9-bundle.test.js
index c4e7d06c..58aec5dc 100644
--- a/server/tests/issue-871-876-ecm-4-9-bundle.test.js
+++ b/server/tests/issue-871-876-ecm-4-9-bundle.test.js
@@ -58,10 +58,10 @@ describe('#871 — downtime-form.js wires the catalogue dropdown', () => {
     expect(src).toMatch(/<select id="dt-equipment_\$\{n\}_catalogue_id"/);
   });
 
-  it('emits optgroups per bucket (weapon / armour / equipment / asset)', () => {
-    // The render iterates ['weapon', 'armour', 'equipment', 'asset'] and emits <optgroup label="...">
-    expect(src).toMatch(/for\s*\(\s*const\s+bucket\s+of\s+\[\s*['"]weapon['"]\s*,\s*['"]armour['"]\s*,\s*['"]equipment['"]\s*,\s*['"]asset['"]\s*\]/);
-    expect(src).toMatch(/<optgroup label="\$\{esc\(bucket\)\}">/);
+  it('emits optgroups per bucket (combat_gear / skill_gear / tool_utility / narrative / container — EQC-1 #1152)', () => {
+    // The render iterates the new five-value taxonomy and emits <optgroup label="...">
+    expect(src).toMatch(/for\s*\(\s*const\s+bucket\s+of\s+\[\s*['"]combat_gear['"]\s*,\s*['"]skill_gear['"]\s*,\s*['"]tool_utility['"]\s*,\s*['"]narrative['"]\s*,\s*['"]container['"]\s*\]/);
+    expect(src).toMatch(/<optgroup label="\$\{esc\(EQUIPMENT_BUCKET_LABELS\[bucket\] \|\| bucket\)\}">/);
   });
 
   it('option value is the 24-hex `_id` (not the legacy slug)', () => {
diff --git a/server/tests/issue-879-defence-penalty-wirein.test.js b/server/tests/issue-879-defence-penalty-wirein.test.js
index e670ffbd..93f3806b 100644
--- a/server/tests/issue-879-defence-penalty-wirein.test.js
+++ b/server/tests/issue-879-defence-penalty-wirein.test.js
@@ -91,8 +91,9 @@ describe('#879 — equipment-derivation.js module shape', () => {
     expect(src).not.toMatch(/item\.state\s*===\s*['"]stashed['"]/);
   });
 
-  it('filters by bucket === \'armour\'', () => {
-    expect(src).toMatch(/entry\.bucket\s*!==\s*['"]armour['"]/);
+  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
+    expect(src).toMatch(/entry\.bucket\s*!==\s*['"]combat_gear['"]/);
+    expect(src).toMatch(/entry\.armour_value\s*==\s*null/);
   });
 });
 
@@ -243,8 +244,8 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'aaa1', bucket: 'armour', defence_penalty: 2 },
-      { _id: 'aaa2', bucket: 'armour', defence_penalty: 3 },
+      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
+      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'aaa1', state: 'carried' },
@@ -253,12 +254,12 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(0);
   });
 
-  it('D1: filters by bucket === \'armour\' — ignores worn non-armour items', async () => {
+  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'wep1', bucket: 'weapon', defence_penalty: 5 },   // not armour, ignored
-      { _id: 'arm1', bucket: 'armour', defence_penalty: 2 },
+      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'wep1', state: 'worn' },
@@ -267,13 +268,23 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(2);
   });
 
+  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
+    if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
+    const mod = await import('../../public/js/data/equipment-derivation.js');
+    const items = [
+      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
+    ];
+    const c = mkChar({ equipment: [{ catalogue_id: 'x1', state: 'worn' }] });
+    expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(0);
+  });
+
   it('D2: returns the MAX defence_penalty across multiple worn armour items (worst-case stacking)', async () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'arm1', bucket: 'armour', defence_penalty: 1 },
-      { _id: 'arm2', bucket: 'armour', defence_penalty: 3 },
-      { _id: 'arm3', bucket: 'armour', defence_penalty: 2 },
+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
+      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
+      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'arm1', state: 'worn' },
@@ -287,9 +298,9 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'a1', bucket: 'armour', defence_penalty: null },
-      { _id: 'a2', bucket: 'armour', defence_penalty: undefined },
-      { _id: 'a3', bucket: 'armour', defence_penalty: 'bad' },
+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
+      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
     ];
     const c = mkChar({ equipment: items.map(it => ({ catalogue_id: it._id, state: 'worn' })) });
     expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(0);
@@ -308,8 +319,8 @@ describe('#879 — wornArmourCount (drives the editor hint)', () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'a1', bucket: 'armour' }, { _id: 'a2', bucket: 'armour' },
-      { _id: 'a3', bucket: 'armour' }, { _id: 'w1', bucket: 'weapon' },
+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'a1', state: 'worn' },
@@ -328,7 +339,7 @@ describe('#879 — materialiseDerivedDefence (D3 + D4)', () => {
     const accessors = await import('../../public/js/data/accessors.js');
     // calcDefence on the mkChar fixture: min(Dex=3, Wits=3) + Athletics=2 + discBonus=0 = 5.
     const c = mkChar({ equipment: [{ catalogue_id: 'arm1', state: 'worn' }] });
-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
     const result = mod.materialiseDerivedDefence(c, mkLookup(items));
     expect(result).toBe(accessors.calcDefence(c) - 2);
     expect(c.derived.defence).toBe(result);
@@ -343,7 +354,7 @@ describe('#879 — materialiseDerivedDefence (D3 + D4)', () => {
       skills: {},
       equipment: [{ catalogue_id: 'arm1', state: 'worn' }],
     });
-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 5 }];
+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
     expect(mod.materialiseDerivedDefence(tinyChar, mkLookup(items))).toBe(0);
   });
 });
@@ -394,7 +405,7 @@ describe('#879 — defenceMechanicalBase (export-site helper)', () => {
       equipment: [{ catalogue_id: 'arm1', state: 'worn' }],
       derived: { defence: 99 },   // overlay-modded; should be ignored
     });
-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
     const result = mod.defenceMechanicalBase(c, mkLookup(items));
     expect(result).toBe(Math.max(0, accessors.calcDefence(c) - 2));
     expect(result).not.toBe(99);


2026-08-13T02:39:11.391091Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -LiteralPath 'D:\\Terra Mortis\\TM Suite\\specs\\stories\\code-review\\issue-1152-eqc1-diff.txt' -Pattern '"'^diff --git '"'" in D:\Terra Mortis\TM Suite
 succeeded in 2275ms:

specs\stories\code-review\issue-1152-eqc1-diff.txt:1:[7mdiff [0m[7m--git [0m[7m[0ma/public/css/admin-layout.css [0m
[7m[0mb/public/css/admin-layout.css[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:23:[7mdiff [0m[7m--git [0m[7m[0ma/public/js/admin/equipment-catalogue-admin.js [0m
[7m[0mb/public/js/admin/equipment-catalogue-admin.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:82:[7mdiff [0m[7m--git [0m[7m[0ma/public/js/data/equipment-derivation.js [0m
[7m[0mb/public/js/data/equipment-derivation.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:122:[7mdiff [0m[7m--git [0m[7m[0ma/public/js/editor/sheet.js [0m
[7m[0mb/public/js/editor/sheet.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:305:[7mdiff [0m[7m--git [0m[7m[0ma/public/js/suite/roll-v2.js [0m
[7m[0mb/public/js/suite/roll-v2.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:330:[7mdiff --git [0ma/public/js/suite/roll.js b/public/js/suite/roll.js
specs\stories\code-review\issue-1152-eqc1-diff.txt:355:[7mdiff [0m[7m--git [0m[7m[0ma/public/js/tabs/downtime-form.js [0m
[7m[0mb/public/js/tabs/downtime-form.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:380:[7mdiff [0m[7m--git [0m[7m[0ma/server/schemas/character.schema.js [0m
[7m[0mb/server/schemas/character.schema.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:416:[7mdiff [0m[7m--git [0m[7m[0ma/server/schemas/equipment_catalogue.schema.js [0m
[7m[0mb/server/schemas/equipment_catalogue.schema.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:462:[7mdiff [0m[7m--git [0m[7m[0ma/server/scripts/migrate-eqc1-bucket-taxonomy.mjs [0m
[7m[0mb/server/scripts/migrate-eqc1-bucket-taxonomy.mjs[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:622:[7mdiff [0m[7m--git [0m[7m[0ma/server/tests/equipment-client-fixes.test.js [0m
[7m[0mb/server/tests/equipment-client-fixes.test.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:649:[7mdiff [0m[7m--git [0m
[7m[0ma/server/tests/issue-1152-eqc1-bucket-migration.test.js [0m[7m[0mb/server/tests/issue-1152-eqc1-bucket-migration.test.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:785:[7mdiff [0m[7m--git [0m
[7m[0ma/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js [0m
[7m[0mb/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:798:[7mdiff [0m[7m--git [0m[7m[0ma/server/tests/issue-871-876-ecm-4-9-bundle.test.js [0m
[7m[0mb/server/tests/issue-871-876-ecm-4-9-bundle.test.js[0m
specs\stories\code-review\issue-1152-eqc1-diff.txt:817:[7mdiff [0m[7m--git [0m
[7m[0ma/server/tests/issue-879-defence-penalty-wirein.test.js [0m[7m[0mb/server/tests/issue-879-defence-penalty-wirein.test.js[0m


2026-08-13T02:39:17.409450Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewDiff = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\specs\\stories\\code-review\\issue-1152-eqc1-diff.txt'; "'$reviewDiff[304..621]' in D:\Terra Mortis\TM Suite
 succeeded in 2193ms:
diff --git a/public/js/suite/roll-v2.js b/public/js/suite/roll-v2.js
index bf2ae760..badc1e10 100644
--- a/public/js/suite/roll-v2.js
+++ b/public/js/suite/roll-v2.js
@@ -217,7 +217,8 @@ export function updPool() {
       // #752: 'active' is the strongest in-use state — treat it identically
       // to carried/worn for chip eligibility (Khepri's option (a) — preserves
       // 'active' as a semantically-stronger marker an ST may have already used).
-      return entry && entry.bucket === 'equipment' &&
+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
+      return entry && entry.bucket === 'skill_gear' &&
              entry.bonus_dice > 0 &&
              entry.skill_domain === pi.skill &&
              (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
@@ -307,7 +308,9 @@ export function updWeaponRef() {
     // #752: 'active' is the strongest in-use state — include it in weapon-ref
     // eligibility (matches the chip predicate above).
     const entry = getCatalogueEntry(item.catalogue_id);
-    return entry && entry.bucket === 'weapon' &&
+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
+    // is identified by weapon_type presence (armour-shaped entries leave it null).
+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
            (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
   });
   if (!weapons.length) {
diff --git a/public/js/suite/roll.js b/public/js/suite/roll.js
index 095857e0..8ec5f622 100644
--- a/public/js/suite/roll.js
+++ b/public/js/suite/roll.js
@@ -141,7 +141,8 @@ export function updPool() {
       // #752: 'active' is the strongest in-use state — treat it identically
       // to carried/worn for chip eligibility (Khepri's option (a) — preserves
       // 'active' as a semantically-stronger marker an ST may have already used).
-      return entry && entry.bucket === 'equipment' &&
+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
+      return entry && entry.bucket === 'skill_gear' &&
              entry.bonus_dice > 0 &&
              entry.skill_domain === pi.skill &&
              (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
@@ -231,7 +232,9 @@ export function updWeaponRef() {
     // #752: 'active' is the strongest in-use state — include it in weapon-ref
     // eligibility (matches the chip predicate above).
     const entry = getCatalogueEntry(item.catalogue_id);
-    return entry && entry.bucket === 'weapon' &&
+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
+    // is identified by weapon_type presence (armour-shaped entries leave it null).
+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
            (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
   });
   if (!weapons.length) {
diff --git a/public/js/tabs/downtime-form.js b/public/js/tabs/downtime-form.js
index eea3744e..d33f65d1 100644
--- a/public/js/tabs/downtime-form.js
+++ b/public/js/tabs/downtime-form.js
@@ -5541,10 +5541,18 @@ function renderEquipmentRow(n, saved) {
   if (legacyName && !selectedId) {
     h += `<option value="" disabled>(legacy: ${esc(legacyName)} \u2014 please reselect)</option>`;
   }
-  for (const bucket of ['weapon', 'armour', 'equipment', 'asset']) {
+  // EQC-1 (#1152, epic #1038, 2026-08-13): re-partitioned from the old four
+  // buckets (weapon/armour/equipment/asset) to five; labels use
+  // EQUIPMENT_BUCKET_LABELS rather than the bare bucket value (combat_gear/
+  // skill_gear/tool_utility need real spacing, not a raw enum string).
+  const EQUIPMENT_BUCKET_LABELS = {
+    combat_gear: 'Combat Gear', skill_gear: 'Skill Gear', tool_utility: 'Tools / Utility',
+    narrative: 'Narrative', container: 'Container',
+  };
+  for (const bucket of ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']) {
     const arr = byBucket.get(bucket);
     if (!arr || !arr.length) continue;
-    h += `<optgroup label="${esc(bucket)}">`;
+    h += `<optgroup label="${esc(EQUIPMENT_BUCKET_LABELS[bucket] || bucket)}">`;
     for (const it of arr) {
       const sel       = String(it._id) === selectedId ? ' selected' : '';
       const eff       = effectiveAvailability(it, currentChar);
diff --git a/server/schemas/character.schema.js b/server/schemas/character.schema.js
index 2860ed0e..75247163 100644
--- a/server/schemas/character.schema.js
+++ b/server/schemas/character.schema.js
@@ -315,6 +315,23 @@ export const characterSchema = {
     // specs/epic-ecm-equipment-catalogue-migration.md and Khepri's ECM-3
     // dispatch for the rationale (the coercion is canonical Express+Mongo
     // hygiene, not transitional kludge — it stays after ECM-4/5 ship).
+    //
+    // `container_id` (EQC-1, issue #1152, epic #1038) — null/absent means
+    // this item is loose (carried on the character, not stored inside
+    // anything). When set, it is the `catalogue_id` of ANOTHER entry in this
+    // SAME character's `equipment[]` array whose catalogue bucket is
+    // `container` (a haven, vehicle, safe, etc.) — "a property contains a
+    // security system... never a bonus on the asset". Single-level only for
+    // v1 (a container's own contents are never themselves container-eligible
+    // targets) — the epic's own examples (a safe inside a haven) are all
+    // single-level, and recursive containment is real added complexity with
+    // no stated requirement yet; a future story can lift this if actually
+    // needed. Not validated as a REFERENCE here (schema has no cross-item
+    // lookup) — the write route is responsible for confirming the referenced
+    // entry exists in the same array and resolves to a container-bucket
+    // catalogue item; an unresolvable/stale container_id is display-inert
+    // (renders as loose) rather than a write-time hard failure, matching
+    // this schema's existing catalogue_id-is-a-lean-ref philosophy.
     equipment: {
       type: 'array',
       default: [],
@@ -326,6 +343,7 @@ export const characterSchema = {
           state:           { type: 'string', enum: ['carried', 'worn', 'stashed', 'lost', 'active'] },
           acquired_cycle:  { type: 'integer', minimum: 0 },
           notes:           { type: ['string', 'null'] },
+          container_id:    { type: ['string', 'null'], pattern: '^[a-f0-9]{24}$' },
         },
         additionalProperties: false,
       },
diff --git a/server/schemas/equipment_catalogue.schema.js b/server/schemas/equipment_catalogue.schema.js
index a2405ecf..28ce2fdf 100644
--- a/server/schemas/equipment_catalogue.schema.js
+++ b/server/schemas/equipment_catalogue.schema.js
@@ -12,6 +12,32 @@
  * — that's a separate bugfix. Bucket-specific fields are loosely typed (nullable
  * primitives) so the schema accepts every existing seed entry without further
  * coercion in ECM-2.
+ *
+ * EQC-1 (issue #1152, epic #1038) — bucket re-partition, 2026-08-13. The old
+ * four buckets (weapon/armour/equipment/asset) become FIVE new values:
+ *
+ *   combat_gear  — weapons AND armour, distinguished by which stat fields are
+ *                  populated (damage_mod/damage_type/weapon_type = weapon-shaped;
+ *                  armour_value/defence_penalty = armour-shaped). Both sets of
+ *                  fields stay on the schema for this bucket; a given item
+ *                  populates whichever subset applies and leaves the rest null.
+ *   skill_gear   — old `equipment` bucket, unchanged meaning (skill_domain +
+ *                  bonus_dice). Renamed only.
+ *   tool_utility — NEW. "Does a thing, no bonus" per the epic description —
+ *                  mechanical_effect (free text), no numeric bonus field.
+ *   narrative    — NEW. Purely descriptive; no bucket-specific stat fields.
+ *   container    — old `asset` bucket. "Assets are containers... no
+ *                  stat-stacking on the asset itself" (epic #1038) — holds
+ *                  other equipment (see character.schema.js's `container_id`)
+ *                  rather than granting a bonus of its own. mechanical_effect
+ *                  stays available for descriptive/narrative notes (e.g. what
+ *                  a haven confers), not a combat bonus.
+ *
+ * Migration: server/scripts/migrate-eqc1-bucket-taxonomy.mjs maps every live
+ * equipment_catalogue document from the old enum to the new one
+ * (weapon→combat_gear, armour→combat_gear, equipment→skill_gear,
+ * asset→container). Must run before/alongside this schema landing — see that
+ * script's own header for why the two cannot ship independently.
  */
 
 export const equipmentCatalogueSchema = {
@@ -26,7 +52,7 @@ export const equipmentCatalogueSchema = {
     _id: {},
 
     // ── Identity / core ──
-    bucket:       { type: 'string', enum: ['weapon', 'armour', 'equipment', 'asset'] },
+    bucket:       { type: 'string', enum: ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'] },
     name:         { type: 'string', minLength: 1 },
     description:  { type: ['string', 'null'] },
     availability: { type: ['integer', 'null'], minimum: 0, maximum: 5 },
diff --git a/server/scripts/migrate-eqc1-bucket-taxonomy.mjs b/server/scripts/migrate-eqc1-bucket-taxonomy.mjs
new file mode 100644
index 00000000..25692ee8
--- /dev/null
+++ b/server/scripts/migrate-eqc1-bucket-taxonomy.mjs
@@ -0,0 +1,154 @@
+/**
+ * EQC-1 (issue #1152, epic #1038) — equipment_catalogue bucket re-partition.
+ *
+ * Maps every live `equipment_catalogue` document from the OLD four-bucket
+ * taxonomy to the NEW five-value one:
+ *
+ *   weapon    → combat_gear   (weapon-shaped: damage_mod/damage_type/weapon_type)
+ *   armour    → combat_gear   (armour-shaped: armour_value/defence_penalty)
+ *   equipment → skill_gear    (unchanged meaning — skill_domain + bonus_dice)
+ *   asset     → container     ("assets are containers... no stat-stacking
+ *                              on the asset itself" — epic #1038)
+ *
+ * `combat_gear` absorbs both old weapon and armour buckets, distinguished at
+ * read time by WHICH stat fields are populated on the item — not by a new
+ * subtype field. This mirrors the epic's own wording ("combat gear
+ * (weapons/armour, distinct stat fields)") and needs no new schema field.
+ *
+ * WHY THIS CANNOT SHIP INDEPENDENTLY OF THE SCHEMA CHANGE. Unlike
+ * backfill-free-grants.js's union-sum runtime guard, nothing in this codebase
+ * reads BOTH the old and new bucket vocabulary simultaneously (a deliberate
+ * choice — see the EQC-1 story's "Migration shape" decision, atomic not
+ * dual-support). So an un-migrated document is invisible to every consumer
+ * (armourDefencePenalty, roll calculator weapon/skill chips, sheet renderer,
+ * DT form dropdown) the moment the new-taxonomy-aware code lands. This script
+ * and the schema/consumer changes in the same story MUST land together on
+ * this branch before merge — there is no safe "schema first, migrate later"
+ * window here, unlike the free-grants case.
+ *
+ * Idempotent: re-running matches zero docs the second time (every document's
+ * `bucket` is already one of the new five values after the first apply).
+ * Dry-run default, mirrors backfill-free-grants.js's own CLI shape.
+ *
+ * Usage:
+ *   # preview (default — no writes):
+ *   node server/scripts/migrate-eqc1-bucket-taxonomy.mjs
+ *
+ *   # apply:
+ *   node server/scripts/migrate-eqc1-bucket-taxonomy.mjs --apply
+ *
+ *   # target the test DB:
+ *   MONGODB_DB=tm_suite_test node server/scripts/migrate-eqc1-bucket-taxonomy.mjs --apply
+ *
+ * Locally, run from `server/` so cwd-relative `dotenv/config` picks up
+ * `server/.env`. On Render env vars are pre-set (no-op).
+ */
+
+import 'dotenv/config';
+import { pathToFileURL } from 'url';
+import { connectDb, getCollection, closeDb } from '../db.js';
+
+/** Old bucket → new bucket. The one-to-one mapping this whole script exists to apply. */
+export const BUCKET_MAP = Object.freeze({
+  weapon: 'combat_gear',
+  armour: 'combat_gear',
+  equipment: 'skill_gear',
+  asset: 'container',
+});
+
+const NEW_BUCKETS = new Set(['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']);
+
+/**
+ * Compute the migration plan for a single catalogue document. Pure — no DB
+ * access, no module-level state.
+ *
+ * @param {object} doc - an equipment_catalogue document
+ * @returns {{ touched: boolean, fromBucket: string|undefined, toBucket: string|undefined, reason: string|null }}
+ */
+export function planBucketMigration(doc) {
+  if (!doc || typeof doc !== 'object') {
+    return { touched: false, fromBucket: undefined, toBucket: undefined, reason: 'malformed document' };
+  }
+  const from = doc.bucket;
+  if (NEW_BUCKETS.has(from)) {
+    return { touched: false, fromBucket: from, toBucket: from, reason: null };
+  }
+  const to = BUCKET_MAP[from];
+  if (!to) {
+    // Neither a known old value nor a known new one — flag rather than guess.
+    return { touched: false, fromBucket: from, toBucket: undefined, reason: `unrecognised bucket value "${from}"` };
+  }
+  return { touched: true, fromBucket: from, toBucket: to, reason: null };
+}
+
+/**
+ * Drive the migration against MongoDB. Returns the aggregate summary.
+ *
+ * @param {object} [opts]
+ * @param {boolean} [opts.dryRun]
+ * @param {boolean} [opts.log]
+ */
+export async function migrate(opts = {}) {
+  const { dryRun = true, log = true } = opts;
+  const col = getCollection('equipment_catalogue');
+  const docs = await col.find({}).toArray();
+
+  const totals = {
+    scanned: docs.length,
+    touched: 0,
+    perMapping: Object.create(null),
+    unrecognised: [],
+  };
+
+  for (const doc of docs) {
+    const plan = planBucketMigration(doc);
+    if (plan.reason && !plan.touched) {
+      totals.unrecognised.push({ id: String(doc._id), name: doc.name || '(unnamed)', bucket: plan.fromBucket });
+      continue;
+    }
+    if (!plan.touched) continue;
+
+    const key = `${plan.fromBucket} → ${plan.toBucket}`;
+    totals.perMapping[key] = (totals.perMapping[key] || 0) + 1;
+    totals.touched += 1;
+
+    if (!dryRun) {
+      await col.updateOne({ _id: doc._id }, { $set: { bucket: plan.toBucket, updated_at: new Date().toISOString() } });
+    }
+  }
+
+  if (log) {
+    const verb = dryRun ? '[DRY RUN] would touch' : 'touched';
+    console.log(`Scanned ${totals.scanned} catalogue item(s); ${verb} ${totals.touched}.`);
+    if (Object.keys(totals.perMapping).length) {
+      console.log('Per-mapping:');
+      for (const [k, n] of Object.entries(totals.perMapping)) console.log(`  ${k.padEnd(24)} ${n}`);
+    }
+    if (totals.unrecognised.length) {
+      console.log(`\nUNRECOGNISED bucket values (${totals.unrecognised.length}) — SKIPPED, need human review:`);
+      for (const u of totals.unrecognised) console.log(`  ${u.name} (${u.id}): bucket="${u.bucket}"`);
+    }
+  }
+  return totals;
+}
+
+export async function main(argv = process.argv) {
+  const dryRun = !argv.includes('--apply');
+  console.log(`Mode: ${dryRun ? 'DRY RUN (read only; pass --apply to write)' : 'APPLY (will write)'}`);
+  console.log(`Target DB: ${process.env.MONGODB_DB || 'tm_suite'}`);
+  console.log('');
+
+  await connectDb();
+  try {
+    await migrate({ dryRun });
+  } finally {
+    await closeDb();
+  }
+
+  console.log('\nIdempotency check (after --apply): re-run with no flag and confirm "touched 0".');
+}
+
+// Auto-run only when invoked directly (not when imported by a test).
+if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href) {
+  main().catch(err => { console.error(err); process.exit(1); });
+}
diff --git a/server/tests/equipment-client-fixes.test.js b/server/tests/equipment-client-fixes.test.js

2026-08-13T02:39:23.381391Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewDiff = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\specs\\stories\\code-review\\issue-1152-eqc1-diff.txt'; "'$reviewDiff[621..937]' in D:\Terra Mortis\TM Suite
 succeeded in 2148ms:
diff --git a/server/tests/equipment-client-fixes.test.js b/server/tests/equipment-client-fixes.test.js
index 2929996a..09283579 100644
--- a/server/tests/equipment-client-fixes.test.js
+++ b/server/tests/equipment-client-fixes.test.js
@@ -64,15 +64,18 @@ describe('#752 — roll.js predicates include the active state', () => {
   it('equipment-chip filter accepts state === active', () => {
     const src = read('public/js/suite/roll.js');
     // The predicate block: any line with `item.state === 'active'` near a
-    // `bucket === 'equipment'` filter clause counts. We confirm BOTH the
-    // 'equipment' filter and the 'active' check coexist within a small
+    // `bucket === 'skill_gear'` filter clause counts. We confirm BOTH the
+    // skill_gear filter and the 'active' check coexist within a small
     // window so a future predicate rewrite doesn't silently drop 'active'.
-    expect(src).toMatch(/bucket === 'equipment'[\s\S]{0,400}item\.state === 'active'/);
+    // EQC-1 (#1152): the old 'equipment' bucket is now 'skill_gear'.
+    expect(src).toMatch(/bucket === 'skill_gear'[\s\S]{0,400}item\.state === 'active'/);
   });
 
   it('weapon-reference filter accepts state === active', () => {
     const src = read('public/js/suite/roll.js');
-    expect(src).toMatch(/bucket === 'weapon'[\s\S]{0,400}item\.state === 'active'/);
+    // EQC-1 (#1152): the old 'weapon' bucket merged into 'combat_gear',
+    // weapon-shaped items distinguished by weapon_type != null.
+    expect(src).toMatch(/bucket === 'combat_gear' && entry\.weapon_type != null[\s\S]{0,400}item\.state === 'active'/);
   });
 
   it('the legacy carried + worn states still appear in the same predicates (no accidental swap)', () => {
diff --git a/server/tests/issue-1152-eqc1-bucket-migration.test.js b/server/tests/issue-1152-eqc1-bucket-migration.test.js
new file mode 100644
index 00000000..16fc9640
--- /dev/null
+++ b/server/tests/issue-1152-eqc1-bucket-migration.test.js
@@ -0,0 +1,130 @@
+/**
+ * EQC-1 (issue #1152, epic #1038) — bucket taxonomy migration script.
+ *
+ * Pure-function tests for planBucketMigration (no DB) plus an integration
+ * test driving migrate() against tm_suite_test, mirroring the pattern
+ * backfill-free-grants.js's own test suite would use (dry-run default,
+ * idempotent, per-mapping summary).
+ */
+
+import { describe, it, expect, beforeAll, afterAll, afterEach } from 'vitest';
+import 'dotenv/config';
+import { setupDb, teardownDb } from './helpers/db-setup.js';
+import { getCollection } from '../db.js';
+import { BUCKET_MAP, planBucketMigration, migrate } from '../scripts/migrate-eqc1-bucket-taxonomy.mjs';
+
+describe('planBucketMigration (pure)', () => {
+  it('maps weapon -> combat_gear', () => {
+    const r = planBucketMigration({ bucket: 'weapon' });
+    expect(r).toEqual({ touched: true, fromBucket: 'weapon', toBucket: 'combat_gear', reason: null });
+  });
+
+  it('maps armour -> combat_gear', () => {
+    const r = planBucketMigration({ bucket: 'armour' });
+    expect(r.touched).toBe(true);
+    expect(r.toBucket).toBe('combat_gear');
+  });
+
+  it('maps equipment -> skill_gear', () => {
+    const r = planBucketMigration({ bucket: 'equipment' });
+    expect(r.touched).toBe(true);
+    expect(r.toBucket).toBe('skill_gear');
+  });
+
+  it('maps asset -> container', () => {
+    const r = planBucketMigration({ bucket: 'asset' });
+    expect(r.touched).toBe(true);
+    expect(r.toBucket).toBe('container');
+  });
+
+  it('leaves an already-migrated document untouched (idempotency)', () => {
+    for (const bucket of ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']) {
+      const r = planBucketMigration({ bucket });
+      expect(r.touched).toBe(false);
+      expect(r.toBucket).toBe(bucket);
+    }
+  });
+
+  it('flags an unrecognised bucket value rather than guessing', () => {
+    const r = planBucketMigration({ bucket: 'wandwood' });
+    expect(r.touched).toBe(false);
+    expect(r.toBucket).toBeUndefined();
+    expect(r.reason).toMatch(/unrecognised/);
+  });
+
+  it('handles a malformed/missing document without throwing', () => {
+    expect(planBucketMigration(null).touched).toBe(false);
+    expect(planBucketMigration(undefined).touched).toBe(false);
+    expect(planBucketMigration({}).touched).toBe(false);
+  });
+
+  it('BUCKET_MAP covers exactly the four old bucket values, nothing else', () => {
+    expect(Object.keys(BUCKET_MAP).sort()).toEqual(['armour', 'asset', 'equipment', 'weapon']);
+  });
+});
+
+describe('migrate() — integration against tm_suite_test', () => {
+  const seededIds = [];
+
+  beforeAll(async () => { await setupDb(); });
+  afterAll(async () => {
+    if (seededIds.length) await getCollection('equipment_catalogue').deleteMany({ _id: { $in: seededIds } });
+    await teardownDb();
+  });
+  afterEach(async () => {
+    if (seededIds.length) {
+      await getCollection('equipment_catalogue').deleteMany({ _id: { $in: seededIds } });
+      seededIds.length = 0;
+    }
+  });
+
+  async function seed(bucket, name) {
+    const now = new Date().toISOString();
+    const result = await getCollection('equipment_catalogue').insertOne({
+      bucket, name, description: 'fixture', availability: 1, tags: [],
+      damage_mod: null, damage_type: null, weapon_type: null,
+      armour_value: null, defence_penalty: null, skill_domain: null, bonus_dice: null,
+      mechanical_effect: null, created_at: now, updated_at: now,
+    });
+    seededIds.push(result.insertedId);
+    return result.insertedId;
+  }
+
+  it('dry run makes no writes', async () => {
+    const id = await seed('weapon', 'Dry Run Knife');
+    const totals = await migrate({ dryRun: true, log: false });
+    expect(totals.touched).toBeGreaterThanOrEqual(1);
+    const doc = await getCollection('equipment_catalogue').findOne({ _id: id });
+    expect(doc.bucket).toBe('weapon');
+  });
+
+  it('apply migrates every old-taxonomy document and is idempotent on re-run', async () => {
+    const wId = await seed('weapon', 'Apply Knife');
+    const aId = await seed('armour', 'Apply Vest');
+    const eId = await seed('equipment', 'Apply Lockpick');
+    const asId = await seed('asset', 'Apply Haven');
+
+    const first = await migrate({ dryRun: false, log: false });
+    expect(first.touched).toBeGreaterThanOrEqual(4);
+
+    const col = getCollection('equipment_catalogue');
+    expect((await col.findOne({ _id: wId })).bucket).toBe('combat_gear');
+    expect((await col.findOne({ _id: aId })).bucket).toBe('combat_gear');
+    expect((await col.findOne({ _id: eId })).bucket).toBe('skill_gear');
+    expect((await col.findOne({ _id: asId })).bucket).toBe('container');
+
+    // Idempotency: re-run touches nothing further among these four.
+    const second = await migrate({ dryRun: false, log: false });
+    const stillOld = await col.find({ _id: { $in: [wId, aId, eId, asId] }, bucket: { $nin: ['combat_gear', 'skill_gear', 'container'] } }).toArray();
+    expect(stillOld.length).toBe(0);
+    expect(second.touched).toBe(0);
+  });
+
+  it('an unrecognised bucket value is reported, not silently dropped or guessed', async () => {
+    const id = await seed('wandwood', 'Mystery Item');
+    const totals = await migrate({ dryRun: true, log: false });
+    const hit = totals.unrecognised.find(u => u.id === String(id));
+    expect(hit).toBeDefined();
+    expect(hit.bucket).toBe('wandwood');
+  });
+});
diff --git a/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js b/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
index 3dd47f34..261610a0 100644
--- a/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
+++ b/server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
@@ -139,7 +139,7 @@ describe('POST /api/equipment_catalogue', () => {
     const res = await request(app)
       .post('/api/equipment_catalogue')
       .set('X-Test-User', stUser())
-      .send({ bucket: 'weapon', name: 'Glock 17', description: 'Pistol', availability: 2, damage_mod: 2 });
+      .send({ bucket: 'combat_gear', name: 'Glock 17', description: 'Pistol', availability: 2, damage_mod: 2, weapon_type: 'ranged' });
     expect(res.status).toBe(201);
     expect(res.body._id).toBeDefined();
     expect(res.body.name).toBe('Glock 17');
diff --git a/server/tests/issue-871-876-ecm-4-9-bundle.test.js b/server/tests/issue-871-876-ecm-4-9-bundle.test.js
index c4e7d06c..58aec5dc 100644
--- a/server/tests/issue-871-876-ecm-4-9-bundle.test.js
+++ b/server/tests/issue-871-876-ecm-4-9-bundle.test.js
@@ -58,10 +58,10 @@ describe('#871 — downtime-form.js wires the catalogue dropdown', () => {
     expect(src).toMatch(/<select id="dt-equipment_\$\{n\}_catalogue_id"/);
   });
 
-  it('emits optgroups per bucket (weapon / armour / equipment / asset)', () => {
-    // The render iterates ['weapon', 'armour', 'equipment', 'asset'] and emits <optgroup label="...">
-    expect(src).toMatch(/for\s*\(\s*const\s+bucket\s+of\s+\[\s*['"]weapon['"]\s*,\s*['"]armour['"]\s*,\s*['"]equipment['"]\s*,\s*['"]asset['"]\s*\]/);
-    expect(src).toMatch(/<optgroup label="\$\{esc\(bucket\)\}">/);
+  it('emits optgroups per bucket (combat_gear / skill_gear / tool_utility / narrative / container — EQC-1 #1152)', () => {
+    // The render iterates the new five-value taxonomy and emits <optgroup label="...">
+    expect(src).toMatch(/for\s*\(\s*const\s+bucket\s+of\s+\[\s*['"]combat_gear['"]\s*,\s*['"]skill_gear['"]\s*,\s*['"]tool_utility['"]\s*,\s*['"]narrative['"]\s*,\s*['"]container['"]\s*\]/);
+    expect(src).toMatch(/<optgroup label="\$\{esc\(EQUIPMENT_BUCKET_LABELS\[bucket\] \|\| bucket\)\}">/);
   });
 
   it('option value is the 24-hex `_id` (not the legacy slug)', () => {
diff --git a/server/tests/issue-879-defence-penalty-wirein.test.js b/server/tests/issue-879-defence-penalty-wirein.test.js
index e670ffbd..93f3806b 100644
--- a/server/tests/issue-879-defence-penalty-wirein.test.js
+++ b/server/tests/issue-879-defence-penalty-wirein.test.js
@@ -91,8 +91,9 @@ describe('#879 — equipment-derivation.js module shape', () => {
     expect(src).not.toMatch(/item\.state\s*===\s*['"]stashed['"]/);
   });
 
-  it('filters by bucket === \'armour\'', () => {
-    expect(src).toMatch(/entry\.bucket\s*!==\s*['"]armour['"]/);
+  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
+    expect(src).toMatch(/entry\.bucket\s*!==\s*['"]combat_gear['"]/);
+    expect(src).toMatch(/entry\.armour_value\s*==\s*null/);
   });
 });
 
@@ -243,8 +244,8 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'aaa1', bucket: 'armour', defence_penalty: 2 },
-      { _id: 'aaa2', bucket: 'armour', defence_penalty: 3 },
+      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
+      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'aaa1', state: 'carried' },
@@ -253,12 +254,12 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(0);
   });
 
-  it('D1: filters by bucket === \'armour\' — ignores worn non-armour items', async () => {
+  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'wep1', bucket: 'weapon', defence_penalty: 5 },   // not armour, ignored
-      { _id: 'arm1', bucket: 'armour', defence_penalty: 2 },
+      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'wep1', state: 'worn' },
@@ -267,13 +268,23 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(2);
   });
 
+  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
+    if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
+    const mod = await import('../../public/js/data/equipment-derivation.js');
+    const items = [
+      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
+    ];
+    const c = mkChar({ equipment: [{ catalogue_id: 'x1', state: 'worn' }] });
+    expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(0);
+  });
+
   it('D2: returns the MAX defence_penalty across multiple worn armour items (worst-case stacking)', async () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'arm1', bucket: 'armour', defence_penalty: 1 },
-      { _id: 'arm2', bucket: 'armour', defence_penalty: 3 },
-      { _id: 'arm3', bucket: 'armour', defence_penalty: 2 },
+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
+      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
+      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'arm1', state: 'worn' },
@@ -287,9 +298,9 @@ describe('#879 — armourDefencePenalty behaviour (D1 + D2)', () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'a1', bucket: 'armour', defence_penalty: null },
-      { _id: 'a2', bucket: 'armour', defence_penalty: undefined },
-      { _id: 'a3', bucket: 'armour', defence_penalty: 'bad' },
+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
+      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
     ];
     const c = mkChar({ equipment: items.map(it => ({ catalogue_id: it._id, state: 'worn' })) });
     expect(mod.armourDefencePenalty(c, mkLookup(items))).toBe(0);
@@ -308,8 +319,8 @@ describe('#879 — wornArmourCount (drives the editor hint)', () => {
     if (typeof globalThis.location === 'undefined') globalThis.location = { hostname: '' };
     const mod = await import('../../public/js/data/equipment-derivation.js');
     const items = [
-      { _id: 'a1', bucket: 'armour' }, { _id: 'a2', bucket: 'armour' },
-      { _id: 'a3', bucket: 'armour' }, { _id: 'w1', bucket: 'weapon' },
+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
     ];
     const c = mkChar({ equipment: [
       { catalogue_id: 'a1', state: 'worn' },
@@ -328,7 +339,7 @@ describe('#879 — materialiseDerivedDefence (D3 + D4)', () => {
     const accessors = await import('../../public/js/data/accessors.js');
     // calcDefence on the mkChar fixture: min(Dex=3, Wits=3) + Athletics=2 + discBonus=0 = 5.
     const c = mkChar({ equipment: [{ catalogue_id: 'arm1', state: 'worn' }] });
-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
     const result = mod.materialiseDerivedDefence(c, mkLookup(items));
     expect(result).toBe(accessors.calcDefence(c) - 2);
     expect(c.derived.defence).toBe(result);
@@ -343,7 +354,7 @@ describe('#879 — materialiseDerivedDefence (D3 + D4)', () => {
       skills: {},
       equipment: [{ catalogue_id: 'arm1', state: 'worn' }],
     });
-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 5 }];
+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
     expect(mod.materialiseDerivedDefence(tinyChar, mkLookup(items))).toBe(0);
   });
 });
@@ -394,7 +405,7 @@ describe('#879 — defenceMechanicalBase (export-site helper)', () => {
       equipment: [{ catalogue_id: 'arm1', state: 'worn' }],
       derived: { defence: 99 },   // overlay-modded; should be ignored
     });
-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
     const result = mod.defenceMechanicalBase(c, mkLookup(items));
     expect(result).toBe(Math.max(0, accessors.calcDefence(c) - 2));
     expect(result).not.toBe(99);

2026-08-13T02:39:30.129972Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewDiff = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\specs\\stories\\code-review\\issue-1152-eqc1-diff.txt'; "'$reviewDiff[0..304]' in D:\Terra Mortis\TM Suite
 succeeded in 2139ms:
diff --git a/public/css/admin-layout.css b/public/css/admin-layout.css
index ae0d0ab7..1de83822 100644
--- a/public/css/admin-layout.css
+++ b/public/css/admin-layout.css
@@ -10144,10 +10144,13 @@ html:not([data-theme="dark"]) .rules-tbl tbody tr:hover            { background:
   background: var(--surf2);
   color: var(--txt2);
 }
-.ec-bucket-weapon    { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
-.ec-bucket-armour    { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
-.ec-bucket-equipment { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
-.ec-bucket-asset     { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
+/* EQC-1 (#1152): re-tagged for the new five-value taxonomy. combat_gear takes
+   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
+.ec-bucket-combat_gear  { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
+.ec-bucket-skill_gear   { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
+.ec-bucket-tool_utility { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
+.ec-bucket-narrative    { background: var(--surf2); color: var(--txt3); }
+.ec-bucket-container    { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
 
 /* ── Forms ────────────────────────────────────────────────────────────────── */
 
diff --git a/public/js/admin/equipment-catalogue-admin.js b/public/js/admin/equipment-catalogue-admin.js
index be64c0dc..1a3cbb24 100644
--- a/public/js/admin/equipment-catalogue-admin.js
+++ b/public/js/admin/equipment-catalogue-admin.js
@@ -29,15 +29,22 @@
 import { apiGet, apiPost, apiPatch, apiRaw } from '../data/api.js';
 import { esc } from '../data/helpers.js';
 
-const BUCKETS = ['weapon', 'armour', 'equipment', 'asset'];
+// EQC-1 (issue #1152, epic #1038, 2026-08-13): re-partitioned from the old
+// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
+// absorbs both weapon and armour — an item fills whichever stat-field subset
+// applies (weapon fields, armour fields, or in principle both) and leaves
+// the rest null; see BUCKET_FIELDS below and equipment-derivation.js's own
+// armour_value-based discriminator.
+const BUCKETS = ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'];
 
 // Bucket → list of optional fields shown in create/edit forms. Mirrors
 // the EQ-1 null-template pattern (EQ_NULLS / WP_NULLS / AR_NULLS / AS_NULLS).
 const BUCKET_FIELDS = {
-  weapon:    ['damage_mod', 'damage_type', 'weapon_type'],
-  armour:    ['armour_value', 'defence_penalty'],
-  equipment: ['skill_domain', 'bonus_dice'],
-  asset:     ['mechanical_effect'],
+  combat_gear:  ['damage_mod', 'damage_type', 'weapon_type', 'armour_value', 'defence_penalty'],
+  skill_gear:   ['skill_domain', 'bonus_dice'],
+  tool_utility: ['mechanical_effect'],
+  narrative:    [],
+  container:    ['mechanical_effect'],
 };
 
 // Module-level state — re-rendered on every mutation. Single-view module
@@ -201,7 +208,7 @@ function wireListEvents() {
 
 function openCreateForm() {
   _editingId = null;
-  renderForm({ bucket: 'equipment', tags: [] });
+  renderForm({ bucket: 'skill_gear', tags: [] });
 }
 
 async function openEditForm(id) {
@@ -223,7 +230,7 @@ function renderForm(initial, impact = null) {
   const host = document.getElementById('ec-form-host');
   if (!host) return;
   const isEdit = !!_editingId;
-  const bucket = initial.bucket || 'equipment';
+  const bucket = initial.bucket || 'skill_gear';
   const bucketOpts = BUCKETS.map(b =>
     `<option value="${b}"${bucket === b ? ' selected' : ''}>${b}</option>`
   ).join('');
@@ -326,7 +333,7 @@ function wireFormEvents(initial) {
  */
 function collectFormValues(coerce = true) {
   const $ = id => document.getElementById(id);
-  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'equipment');
+  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'skill_gear');
   const name = ($('ec-f-name')?.value || '').trim();
   const description = ($('ec-f-description')?.value || '').trim() || null;
   const availRaw = $('ec-f-availability')?.value;
diff --git a/public/js/data/equipment-derivation.js b/public/js/data/equipment-derivation.js
index 219b2c77..a2f86b6a 100644
--- a/public/js/data/equipment-derivation.js
+++ b/public/js/data/equipment-derivation.js
@@ -5,8 +5,15 @@
  * array without an ADR" gate. ADR-006 specifies:
  *
  *   D1: armourDefencePenalty(c, catalogueLookup?) reads c.equipment[] filtered
- *       by state === 'worn' + bucket === 'armour'; injectable catalogue
- *       lookup (default: the ECM-5 cache reader).
+ *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
+ *       injectable catalogue lookup (default: the ECM-5 cache reader).
+ *
+ *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
+ *       merged into `combat_gear` (which also covers weapons). "Armour-shaped"
+ *       is identified by `entry.armour_value != null` rather than by bucket
+ *       alone — the epic's own "distinct stat fields" distinction within the
+ *       single combat_gear bucket. A weapon-shaped combat_gear entry has
+ *       `armour_value: null` and is correctly excluded.
  *   D2: worst-case math — Math.max(...penalties). The editor surfaces a soft
  *       hint when >1 armour is worn (wording is concern #8 below).
  *   D2-FLOOR: floor at 0 lives ONLY at the helper composition site. STM overlay
@@ -58,7 +65,7 @@ export function armourDefencePenalty(c, catalogueLookup = getCatalogueEntry) {
   for (const item of c.equipment) {
     if (!item || item.state !== 'worn') continue;
     const entry = catalogueLookup(item.catalogue_id);
-    if (!entry || entry.bucket !== 'armour') continue;
+    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
     const p = Number.isInteger(entry.defence_penalty) ? entry.defence_penalty : 0;
     if (p > 0) penalties.push(p);
   }
@@ -81,7 +88,7 @@ export function wornArmourCount(c, catalogueLookup = getCatalogueEntry) {
   for (const item of c.equipment) {
     if (!item || item.state !== 'worn') continue;
     const entry = catalogueLookup(item.catalogue_id);
-    if (entry?.bucket === 'armour') n++;
+    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
   }
   return n;
 }
diff --git a/public/js/editor/sheet.js b/public/js/editor/sheet.js
index b3e1ac35..b55a2165 100644
--- a/public/js/editor/sheet.js
+++ b/public/js/editor/sheet.js
@@ -2565,9 +2565,19 @@ export function shRenderManoeuvres(c, editMode) {
 }
 
 // ── Equipment renderer (EQ-2, issue #656) ────────────────────────────────────
-// Renders catalogue-ref equipment[]. All four buckets (weapon/armour/equipment/asset)
-// flow through the same array as of 2026-06-19 (character.assets[] retired).
+// Renders catalogue-ref equipment[]. All buckets flow through the same array
+// as of 2026-06-19 (character.assets[] retired).
 // Edit mode shows the same view -- equipment is managed via the ST CRUD API (EQ-1).
+//
+// EQC-1 (issue #1152, epic #1038, 2026-08-13): re-partitioned from the old
+// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
+// skill_gear/tool_utility/narrative/container). combat_gear merges the old
+// weapon+armour buckets -- the Weapons/Armour section split below is now
+// driven by which stat fields are POPULATED on the item (weapon_type/
+// damage_mod = weapon-shaped; armour_value/defence_penalty = armour-shaped),
+// not by a bucket comparison, since both shapes now share one bucket value.
+// An item with neither shape populated (an ST entry with no stats filled in
+// yet) falls to "Other Combat Gear" rather than being silently dropped.
 export function shRenderEquipment(c, editMode) {
   const equip  = c.equipment || [];
   if (!editMode && !equip.length) return '';
@@ -2581,18 +2591,27 @@ export function shRenderEquipment(c, editMode) {
   let h = '<div class="sh-sec"><div class="sh-sec-title">Equipment</div><div class="merit-list">';
 
   // Group equipment items by bucket, preserving flat-array index for remove buttons
-  const byBucket = { weapon: [], armour: [], equipment: [], asset: [] };
+  const byBucket = { combat_gear: [], skill_gear: [], tool_utility: [], narrative: [], container: [] };
   for (let i = 0; i < equip.length; i++) {
     const item   = equip[i];
     const entry  = getCatalogueEntry(item.catalogue_id) || {};
-    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
+    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'skill_gear';
     byBucket[bucket].push({ item, entry, idx: i });
   }
 
+  // combat_gear sub-split by populated stat fields, not bucket (both shapes
+  // now share the combat_gear bucket). An item matching neither shape (no
+  // stats filled in) still renders, under "Other Combat Gear".
+  const isWeaponShaped = e => e.weapon_type != null || e.damage_mod != null || e.damage_type != null;
+  const isArmourShaped = e => e.armour_value != null || e.defence_penalty != null;
+  const combatWeapons = byBucket.combat_gear.filter(x => isWeaponShaped(x.entry));
+  const combatArmour  = byBucket.combat_gear.filter(x => !isWeaponShaped(x.entry) && isArmourShaped(x.entry));
+  const combatOther   = byBucket.combat_gear.filter(x => !isWeaponShaped(x.entry) && !isArmourShaped(x.entry));
+
   // ── Weapons ──
-  if (byBucket.weapon.length) {
+  if (combatWeapons.length) {
     h += '<div class="sh-sub-title">Weapons</div>';
-    for (const { item, entry, idx } of byBucket.weapon) {
+    for (const { item, entry, idx } of combatWeapons) {
       const name  = entry.name || item.catalogue_id;
       // #896: per-character effective availability (raw - Fixer reduction).
       const eff = entry.availability != null ? effectiveAvailability(entry, c) : null;
@@ -2612,7 +2631,7 @@ export function shRenderEquipment(c, editMode) {
   }
 
   // ── Armour ──
-  if (byBucket.armour.length) {
+  if (combatArmour.length) {
     h += '<div class="sh-sub-title">Armour</div>';
     // Issue #879 (ADR-006 D2 + Concern #8): soft non-blocking hint when
     // multiple armour items are in state==='worn'. Stacking rule is
@@ -2623,7 +2642,7 @@ export function shRenderEquipment(c, editMode) {
       h += '<div class="sh-armour-hint" style="font-size:0.85em;opacity:0.75;margin-bottom:6px;">Only one armour applies; highest defence_penalty wins.</div>';
     }
     const baseDefence = calcDefence(c);
-    for (const { item, entry, idx } of byBucket.armour) {
+    for (const { item, entry, idx } of combatArmour) {
       const name  = entry.name || item.catalogue_id;
       // #896: per-character effective availability (raw - Fixer reduction).
       const eff = entry.availability != null ? effectiveAvailability(entry, c) : null;
@@ -2641,10 +2660,26 @@ export function shRenderEquipment(c, editMode) {
     }
   }
 
-  // ── Equipment (tools / tech) ──
-  if (byBucket.equipment.length) {
-    h += '<div class="sh-sub-title">Equipment</div>';
-    for (const { item, entry, idx } of byBucket.equipment) {
+  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
+  // — e.g. an ST entry with no stat fields filled in yet) ──
+  if (combatOther.length) {
+    h += '<div class="sh-sub-title">Other Combat Gear</div>';
+    for (const { item, entry, idx } of combatOther) {
+      const name  = entry.name || item.catalogue_id;
+      const eff   = entry.availability != null ? effectiveAvailability(entry, c) : null;
+      const qual  = eff != null ? `avail ${eff}` : '';
+      const rmBtn = editMode ? `<button class="sk-spec-rm" style="float:right;margin-top:2px" onclick="shRemoveEquip(${idx})" title="Remove">× Remove</button>` : '';
+      h += `<div class="merit-plain"><div class="trait-row">` +
+        `<div class="trait-main"><span class="trait-name">${esc(name)}</span><div class="trait-right">${stateChip(item.state)}${rmBtn}</div></div>` +
+        (qual || item.notes ? `<div class="trait-sub">${qual ? `<span class="trait-qual">${esc(qual)}</span>` : ''}${item.notes ? `<span class="trait-qual dim">${esc(item.notes)}</span>` : ''}</div>` : '') +
+        `</div></div>`;
+    }
+  }
+
+  // ── Skill Gear (old "Equipment" bucket, unchanged meaning) ──
+  if (byBucket.skill_gear.length) {
+    h += '<div class="sh-sub-title">Skill Gear</div>';
+    for (const { item, entry, idx } of byBucket.skill_gear) {
       const name  = entry.name || item.catalogue_id;
       const pool  = (entry.skill_domain && entry.bonus_dice != null)
         ? `${entry.skill_domain} +${entry.bonus_dice} dice` : '';
@@ -2663,10 +2698,39 @@ export function shRenderEquipment(c, editMode) {
     }
   }
 
-  // ── Assets (catalogue-backed since 2026-06-19) ──
-  if (byBucket.asset.length) {
-    h += '<div class="sh-sub-title">Assets</div>';
-    for (const { item, entry, idx } of byBucket.asset) {
+  // ── Tools / Utility (NEW bucket — "does a thing, no bonus") ──
+  if (byBucket.tool_utility.length) {
+    h += '<div class="sh-sub-title">Tools / Utility</div>';
+    for (const { item, entry, idx } of byBucket.tool_utility) {
+      const name  = entry.name || item.catalogue_id;
+      const eff   = entry.availability != null ? effectiveAvailability(entry, c) : null;
+      const parts = [entry.mechanical_effect || null, eff != null ? `avail ${eff}` : null].filter(Boolean);
+      const qual  = parts.join(' · ');
+      const rmBtn = editMode ? `<button class="sk-spec-rm" style="float:right;margin-top:2px" onclick="shRemoveEquip(${idx})" title="Remove">× Remove</button>` : '';
+      h += `<div class="merit-plain"><div class="trait-row">` +
+        `<div class="trait-main"><span class="trait-name">${esc(name)}</span><div class="trait-right">${stateChip(item.state)}${rmBtn}</div></div>` +
+        (qual || item.notes ? `<div class="trait-sub">${qual ? `<span class="trait-qual">${esc(qual)}</span>` : ''}${item.notes ? `<span class="trait-qual dim">${esc(item.notes)}</span>` : ''}</div>` : '') +
+        `</div></div>`;
+    }
+  }
+
+  // ── Narrative (NEW bucket — purely descriptive, no stat fields) ──
+  if (byBucket.narrative.length) {
+    h += '<div class="sh-sub-title">Narrative</div>';
+    for (const { item, entry, idx } of byBucket.narrative) {
+      const name  = entry.name || item.catalogue_id;
+      const rmBtn = editMode ? `<button class="sk-spec-rm" style="float:right;margin-top:2px" onclick="shRemoveEquip(${idx})" title="Remove">× Remove</button>` : '';
+      h += `<div class="merit-plain"><div class="trait-row">` +
+        `<div class="trait-main"><span class="trait-name">${esc(name)}</span><div class="trait-right">${stateChip(item.state)}${rmBtn}</div></div>` +
+        (entry.description || item.notes ? `<div class="trait-sub">${entry.description ? `<span class="trait-qual">${esc(entry.description)}</span>` : ''}${item.notes ? `<span class="trait-qual dim">${esc(item.notes)}</span>` : ''}</div>` : '') +
+        `</div></div>`;
+    }
+  }
+
+  // ── Containers (old "Assets" bucket — catalogue-backed since 2026-06-19) ──
+  if (byBucket.container.length) {
+    h += '<div class="sh-sub-title">Containers</div>';
+    for (const { item, entry, idx } of byBucket.container) {
       const name  = entry.name || item.catalogue_id;
       const eff   = entry.availability != null ? effectiveAvailability(entry, c) : null;
       const parts = [
@@ -2685,19 +2749,26 @@ export function shRenderEquipment(c, editMode) {
   }
 
   // ── Edit-mode add form ──
-  // 2026-06-19: single add form covers all four buckets (weapon/armour/equipment/asset).
-  // Asset is now in the bucket dropdown; the separate free-text "Add Asset" form is gone
-  // along with character.assets[] — catalogue-ref is the single canonical storage shape.
+  // 2026-06-19: single add form covers all buckets. Asset (now "container") is
+  // in the bucket dropdown; the separate free-text "Add Asset" form is gone
+  // along with character.assets[] — catalogue-ref is the single canonical
+  // storage shape. EQC-1 (#1152, 2026-08-13): dropdown values updated to the
+  // new five-bucket taxonomy; labels use BUCKET_LABELS rather than a bare
+  // capitalise (combat_gear/skill_gear/tool_utility need real spacing).
   if (editMode) {
     const STATES   = ['carried', 'worn', 'stashed', 'active', 'lost'];
-    const BUCKETS  = ['weapon', 'armour', 'equipment', 'asset'];
+    const BUCKETS  = ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'];
+    const BUCKET_LABELS = {
+      combat_gear: 'Combat Gear', skill_gear: 'Skill Gear', tool_utility: 'Tools / Utility',
+      narrative: 'Narrative', container: 'Container',
+    };
     const defCycle = state.activeCycleNum ?? 0;
 
     h += '<div class="sh-sub-title" style="margin-top:10px">Add Equipment Item</div>';
     h += '<div class="dev-add-row" style="display:flex;flex-wrap:wrap;gap:4px;align-items:center;padding:4px 0">'
       + '<select id="eq-add-bucket" class="dev-add-btn" onchange="shEquipBucketFilter()">'
       + '<option value="">Bucket…</option>'
-      + BUCKETS.map(b => `<option value="${b}">${b.charAt(0).toUpperCase() + b.slice(1)}</option>`).join('')
+      + BUCKETS.map(b => `<option value="${b}">${BUCKET_LABELS[b]}</option>`).join('')
       + '</select>'
       + '<select id="eq-add-item" class="dev-add-btn"><option value="">-- select bucket first --</option></select>'
       + '<select id="eq-add-state" class="dev-add-btn">'
diff --git a/public/js/suite/roll-v2.js b/public/js/suite/roll-v2.js

2026-08-13T02:39:37.649167Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Pass 1 is complete and still genuinely blind: I opened only the supplied diff. The core predicates and mapping table are correct, but the diff exposes two substantive risks—a containment contract that the schema/comments claim without enforcing, and an apply-mode migration that can leave a mixed taxonomy without emitting its accumulated progress if a write fails mid-loop. I’m freezing those findings now before opening repository files.
apply patch
patch: completed
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-findings.md
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:40:21.903146Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
  ✓ Pass 1: inspect only the supplied diff and freeze blind findings
  → Pass 2: inspect repository context, trace edge cases, and freeze findings
  • Pass 3a: read only pre-record story sections and freeze AC findings
  • Pass 3b: read Dev Agent Record, run verification gates, and audit claims
  • Finalize severity-grouped report and validation attestation
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:40:29.695209Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!.git/**'"' --glob '"'!specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-diff.txt'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-codex-findings.md'"' \"bucket\\s*(===|"'!==|==|!=)'"\\s*['\\\"](weapon|armour|equipment|asset)['\\\"]|['\\\"](weapon|armour|equipment|asset)['\\\"]\\s*(===|"'!==|==|!=)'"\\s*["'^'"\\r\\n]*bucket\" 'D:\\Terra Mortis\\TM Suite'" in D:\Terra Mortis\TM Suite
2026-08-13T02:40:40.486878Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.4 seconds
Output:
[31;1mParserError: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m . ===|!==|==|!=)\s*['\"](weapon|armour|equipment|asset)['\"]|['[36;1m\[0m"](weap .[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m                                                                ~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mMissing ']' after array index expression.[0m

 exited 1 in 2440ms:
[31;1mParserError: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m . ===|!==|==|!=)\s*['\"](weapon|armour|equipment|asset)['\"]|['[36;1m\[0m"](weap .[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m                                                                ~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mMissing ']' after array index expression.[0m

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:40:40.520425Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!.git/**'"' --glob '"'!specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-diff.txt'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-codex-findings.md'"' \"bucket.{0,40}(weapon|armour|equipment|asset)|(weapon|armour|equipment|asset).{0,40}bucket\" 'D:\\Terra Mortis\\TM Suite'" in D:\Terra Mortis\TM Suite
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:40:58.755706Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
2026-08-13T02:41:09.787483Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 22.1 seconds
Total output lines: 333
Output:
command timed out after 22055 milliseconds
D:\Terra Mortis\TM Suite\public\css\admin-layout.css:10148:   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:144:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:235:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:237:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:220:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:311:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:313:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\public\js\admin.js:1297:  // the editor's equipment-bucket dropdown renders synchronously when the
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2568:// Renders catalogue-ref equipment[]. All buckets flow through the same array
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2573:// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2575:// weapon+armour buckets -- the Weapons/Armour section split below is now
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2593:  // Group equipment items by bucket, preserving flat-array index for remove buttons
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2663:  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:5545:  // buckets (weapon/armour/equipment/asset) to five; labels use
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:33:// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:8: *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:11: *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:13: *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:15: *       single combat_gear bucket. A weapon-shaped combat_gear entry has
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:68:    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:91:    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\public\js\editor\edit.js:1230:// into equipment[]. Asset-bucket catalogue items now flow through shAddEquip.
D:\Terra Mortis\TM Suite\specs\epic-ecm-equipment-catalogue-migration.md:50:  bucket: 'weapon',             // enum: 'weapon' | 'armour' | 'equipment' | 'asset'
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:52:        { id: 'knife',      bucket: 'weapon',    name: 'Knife',       availability: 1, tags: ['melee'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:53:        { id: 'flashlight', bucket: 'equipment', name: 'Flashlight',  availability: 1, tags: ['mental', 'tools'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:54:        { id: 'ballistic-vest', bucket: 'armour', name: 'Ballistic Vest', availability: 2, tags: ['armour'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:55:        { id: 'haven-apartment', bucket: 'asset', name: 'Haven (Apartment)', availability: 2, tags: ['haven'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:120:    expect(buckets).toContain('weapon');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:121:    expect(buckets).toContain('armour');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:122:    expect(buckets).toContain('equipment');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:123:    expect(buckets).toContain('asset');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:136:      const eqEntry  = cat.find(e => e.bucket === 'equipment');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:137:      const wpEntry  = cat.find(e => e.bucket === 'weapon');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:138:      const arEntry  = cat.find(e => e.bucket === 'armour');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:177:      if (entry.bucket !== 'weapon') return { ok: false, error: `bucket should be weapon, got ${entry.bucket}` };
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:196:      if (weapons.some(e => e.bucket !== 'weapon'))        return { ok: false, error: 'non-weapon in weapon bucket results' };
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:12: *   AC-4   Add equipment form present; bucket filter populates item select
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:151:  // ── AC-4: Add equipment form + bucket filter ────────────────────────────────
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:153:  test('AC-4: add equipment form has bucket select, item select, state select, Add button', async ({ page }) => {
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:167:        bucketOptions: ['weapon', 'armour', 'equipment'].every(b => html.includes(b)),
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:180:  test('AC-4: shEquipBucketFilter populates item select with weapons when bucket=weapon', async ({ page }) => {
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:196:      bucketSel.value = 'weapon';
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:55:4. Filters to entries with `bucket === 'armour'` (catalogue refs may point at non-armour buckets in malformed data; ignoring them is the safe fail-soft).
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:66:- `active` = currently used for non-armour buckets (e.g. a powered equipment item being run); armour with `state: 'active'` is malformed data — fail-soft, exclude.
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:72:**Math:** `armourDefencePenalty(c)` returns the **maximum** `defence_penalty` among items satisfying the D1 filter (state==='worn' + bucket==='armour'). Other worn armour items are silently ignored by the formula. Returns 0 when no qualifying armour is present.
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:178:    .filter(entry => entry?.bucket === 'armour')
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:191:| Read path | D1 | #879 (impl, blocked) | New helper in `public/js/data/equipment-derivation.js` (or accessors.js adjacent to calcDefence). State filter `=== 'worn'`. Catalogue lookup via injected reader (post-ECM-5: cache; pre-ECM-5 fallback OK). bucket === 'armour' guard. |
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:202:- **Equipment bucket: weapon / equipment defence interactions.** Some weapons grant Defence bonuses (parrying weapons); some equipment items (a shield) might too. This ADR is armour-specific. Future helpers (`weaponDefenceBonus`, etc.) extend the composition site additively.
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:232:| D1 | resolved | new helper `armourDefencePenalty(c, catalogueLookup?)`; reads `c.equipment[]` filtered by `state === 'worn'` AND `entry.bucket === 'armour'`; catalogue lookup via injected reader (default: ECM-5 cache); returns non-negative integer |
D:\Terra Mortis\TM Suite\tests\feature-662-eq3-roll-calc-equipment-chips.spec.js:13: *   AC-5  Only bucket === 'equipment' eligible (weapon/armour/asset excluded)
D:\Terra Mortis\TM Suite\tests\feature-662-eq3-roll-calc-equipment-chips.spec.js:114:  // crime-scene-kit: bucket=equipment, skill_domain=Investigation, bonus_dice=2
D:\Terra Mortis\TM Suite\tests\feature-662-eq3-roll-calc-equipment-chips.spec.js:218:    // knife = weapon, armoured-vest = armour — neither is bucket=equipment
D:\Terra Mortis\TM Suite\server\routes\characters.js:829:// via the catalogue's bucket: 'asset' items). Response shape is now just
D:\Terra Mortis\TM Suite\server\routes\characters.js:926:// consolidated into equipment[] via the catalogue's bucket: 'asset' items.
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:15: *   into equipment[] via catalogue bucket: 'asset' items.
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:51:    bucket: 'weapon', name: `Test Cat Item ${Math.random().toString(36).slice(2, 8)}`,
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:73:  catKnife      = await seedCatalogueItem({ name: 'Knife (test)',      bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:74:  catFlashlight = await seedCatalogueItem({ name: 'Flashlight (test)', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:75:  catRope       = await seedCatalogueItem({ name: 'Rope (test)',       bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:70:    // EQC-1 (#1152): the old 'equipment' bucket is now 'skill_gear'.
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:76:    // EQC-1 (#1152): the old 'weapon' bucket merged into 'combat_gear',
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:78:    expect(src).toMatch(/bucket === 'combat_gear' && entry\.weapon_type != null[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:2: * EQC-1 (issue #1152, epic #1038) — equipment_catalogue bucket re-partition.
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:4: * Maps every live `equipment_catalogue` document from the OLD four-bucket
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:13: * `combat_gear` absorbs both old weapon and armour buckets, distinguished at
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:56:- **Blast radius**: `equipment_catalogue.bucket` is read directly by armour-penalty derivation
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:87:A re-partition of an equipment catalogue's `bucket` taxonomy from four old values
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:89:container`), where `combat_gear` merges the old weapon and armour buckets (distinguished at read time
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:108:   weapon-only logic) - not just the bucket. A predicate that checks bucket alone would now silently
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:176:   OTHER place that compares against the literal strings `'weapon'`, `'armour'`, `bucket === 'equipment'`,
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:177:   or `bucket === 'asset'` in a bucket-comparison context that this diff did NOT update. The story's own
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:194:   a matching `.ec-bucket-<value>` rule, and that no OLD `.ec-bucket-weapon` etc. rule was left
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:241:   - "None of the 5 pre-existing failures reference equipment/bucket/armour/weapon/container in any
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:69:- **Blast radius**: `equipment_catalogue.bucket` is read directly by armour-penalty derivation
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:100:A re-partition of an equipment catalogue's `bucket` taxonomy from four old values
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:102:container`), where `combat_gear` merges the old weapon and armour buckets (distinguished at read time
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:121:   weapon-only logic) - not just the bucket. A predicate that checks bucket alone would now silently
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:189:   OTHER place that compares against the literal strings `'weapon'`, `'armour'`, `bucket === 'equipment'`,
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:190:   or `bucket === 'asset'` in a bucket-comparison context that this diff did NOT update. The story's own
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:207:   a matching `.ec-bucket-<value>` rule, and that no OLD `.ec-bucket-weapon` etc. rule was left
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:254:   - "None of the 5 pre-existing failures reference equipment/bucket/armour/weapon/container in any
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:322:-.ec-bucket-weapon    { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:323:-.ec-bucket-armour    { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:324:-.ec-bucket-equipment { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:325:-.ec-bucket-asset     { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:327:+   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:346:+// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:372:-  renderForm({ bucket: 'equipment', tags: [] });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:381:-  const bucket = initial.bucket || 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:390:-  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'equipment');
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:403:- *       by state === 'worn' + bucket === 'armour'; injectable catalogue
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:405:+ *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:408:+ *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:410:+ *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:412:+ *       single combat_gear bucket. A weapon-shaped combat_gear entry has
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:421:-    if (!entry || entry.bucket !== 'armour') continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:422:+    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:430:-    if (entry?.bucket === 'armour') n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:431:+    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:443:-// Renders catalogue-ref equipment[]. All four buckets (weapon/armour/equipment/asset)
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:445:+// Renders catalogue-ref equipment[]. All buckets flow through the same array
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:450:+// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:452:+// weapon+armour buckets -- the Weapons/Armour section split below is now
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:464:   // Group equipment items by bucket, preserving flat-array index for remove buttons
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:470:-    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:519:+  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:590:-  // 2026-06-19: single add form covers all four buckets (weapon/armour/equipment/asset).
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:626:-      return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:627:+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:636:-    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:637:+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:639:+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:651:-      return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:652:+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:661:-    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:662:+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:664:+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:676:-  for (const bucket of ['weapon', 'armour', 'equipment', 'asset']) {
D:\Terr…4830 tokens truncated…review\issue-1152-eqc1-codex-raw-output.txt:1873:-      { _id: 'arm1', bucket: 'armour', defence_penalty: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1874:-      { _id: 'arm2', bucket: 'armour', defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1875:-      { _id: 'arm3', bucket: 'armour', defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1876:+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1877:+      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1878:+      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1886:-      { _id: 'a1', bucket: 'armour', defence_penalty: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1887:-      { _id: 'a2', bucket: 'armour', defence_penalty: undefined },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1888:-      { _id: 'a3', bucket: 'armour', defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1889:+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1890:+      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1891:+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1899:-      { _id: 'a1', bucket: 'armour' }, { _id: 'a2', bucket: 'armour' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1900:-      { _id: 'a3', bucket: 'armour' }, { _id: 'w1', bucket: 'weapon' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1901:+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1902:+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1910:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1911:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1919:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1920:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1928:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1929:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1946:-.ec-bucket-weapon    { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1947:-.ec-bucket-armour    { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1948:-.ec-bucket-equipment { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1949:-.ec-bucket-asset     { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1951:+   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1970:+// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1996:-  renderForm({ bucket: 'equipment', tags: [] });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2005:-  const bucket = initial.bucket || 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2014:-  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'equipment');
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2027:- *       by state === 'worn' + bucket === 'armour'; injectable catalogue
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2029:+ *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2032:+ *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2034:+ *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2036:+ *       single combat_gear bucket. A weapon-shaped combat_gear entry has
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2045:-    if (!entry || entry.bucket !== 'armour') continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2046:+    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2054:-    if (entry?.bucket === 'armour') n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2055:+    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2067:-// Renders catalogue-ref equipment[]. All four buckets (weapon/armour/equipment/asset)
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2069:+// Renders catalogue-ref equipment[]. All buckets flow through the same array
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2074:+// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2076:+// weapon+armour buckets -- the Weapons/Armour section split below is now
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2088:   // Group equipment items by bucket, preserving flat-array index for remove buttons
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2094:-    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2143:+  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2214:-  // 2026-06-19: single add form covers all four buckets (weapon/armour/equipment/asset).
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2414:"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!.git/**'"' --glob '"'!specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-diff.txt'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-codex-findings.md'"' \"bucket\\s*(===|"'!==|==|!=)'"\\s*['\\\"](weapon|armour|equipment|asset)['\\\"]|['\\\"](weapon|armour|equipment|asset)['\\\"]\\s*(===|"'!==|==|!=)'"\\s*["'^'"\\r\\n]*bucket\" 'D:\\Terra Mortis\\TM Suite'" in D:\Terra Mortis\TM Suite
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2485:"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!.git/**'"' --glob '"'!specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-diff.txt'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-codex-findings.md'"' \"bucket.{0,40}(weapon|armour|equipment|asset)|(weapon|armour|equipment|asset).{0,40}bucket\" 'D:\\Terra Mortis\\TM Suite'" in D:\Terra Mortis\TM Suite
D:\Terra Mortis\TM Suite\server\tests\issue-896-availability-filter.test.js:43:  return { _id: 'item-' + availability, bucket: 'equipment', name: 'Item ' + availability, availability };
D:\Terra Mortis\TM Suite\server\tests\issue-896-availability-filter.test.js:209:    // One per bucket-render path: weapons, armour, equipment (asset bucket has
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:94:  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:247:      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:248:      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:257:  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:261:      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:262:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:271:  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:275:      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:285:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:286:      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:287:      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:301:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:302:      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:303:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:322:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:323:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:342:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:357:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:408:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\tests\issue-873-ecm-6-admin-sidebar.test.js:4: * Server-side: the schema-widen for `mechanical_effect` (the asset-bucket
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:54:    bucket: 'equipment',
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:86:    await seedItem({ name: 'Alpha', bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:87:    await seedItem({ name: 'Bravo', bucket: 'armour' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:97:    await seedItem({ name: 'Zebra', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:98:    await seedItem({ name: 'Aardvark', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:100:    const equip = res.body.filter(d => d.bucket === 'equipment');
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:154:      .send({ bucket: 'equipment', name: 'X' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:162:      .send({ bucket: 'equipment', name: 'X' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:170:      .send({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:198:      .send({ _id: sentinel, bucket: 'equipment', name: 'Try-injection' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:230:    const seed = await seedItem({ bucket: 'equipment', name: 'Stable' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:234:      .send({ bucket: 'weapon', _id: new ObjectId(), created_at: '1999-01-01T00:00:00.000Z', name: 'OK' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:236:    expect(res.body.bucket).toBe('equipment');
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:246:      .send({ bucket: 'weapon' });   // bucket is non-updatable
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:254:  // field across all buckets; the admin UI scopes it to assets.
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:256:    const seed = await seedItem({ bucket: 'asset', name: 'Warehouse', mechanical_effect: null });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:18:    const r = planBucketMigration({ bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:23:    const r = planBucketMigration({ bucket: 'armour' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:29:    const r = planBucketMigration({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:35:    const r = planBucketMigration({ bucket: 'asset' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:98:    expect(doc.bucket).toBe('weapon');
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:26:EQ-3 wires `bucket === 'equipment'` items into the roll calculator as toggleable dice-bonus chips (same visual pattern as specialties), and adds a weapon selector reference panel for combat pools. Neither feature changes base pool size — equipment chips toggle `MOD`; the weapon panel is informational only.
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:40:5. Only `bucket === 'equipment'` items are chip-eligible. Weapons, armour, assets are excluded.
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:133:    return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:205:    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:27:- [ ] Add equipment: inline form with bucket select → item select (filtered by bucket) → state select → notes → Add; writes via `POST /api/characters/:id/equipment`
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:259:The `equip` array is flat. The three rendered bucket sub-sections (weapon, armour, equipment) display items from that same flat array. When building the HTML, you must track the original flat-array index for each item so the Remove button passes the correct index to `shRemoveEquip(idx)`.
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:267:  const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:357:| 4 | AC-3 | Add equipment: select bucket → item populates → select item + state → Add → row appears |
D:\Terra Mortis\TM Suite\specs\stories\feature.656.eq2-equipment-assets-sheet-display.story.md:19:EQ-1 (#654) established the four-bucket equipment taxonomy (`weapon` | `armour` | `equipment` | `asset`), the static `EQUIPMENT_CATALOGUE` module at `public/js/data/equipment-data.js`, and the character schema extension (`character.equipment[]` + `character.assets[]`).
D:\Terra Mortis\TM Suite\specs\stories\feature.656.eq2-equipment-assets-sheet-display.story.md:65:  - [x] Group equipment items by bucket (weapon, armour, equipment) -- use `getCatalogueEntry(item.catalogue_id) || {}` for fallback safety
D:\Terra Mortis\TM Suite\specs\stories\feature.654.equipment-schema-crud-api.story.md:32:1. `public/js/data/equipment-data.js` exists and exports a named constant `EQUIPMENT_CATALOGUE` — an array of catalogue entry objects with at least one representative entry per bucket (`weapon`, `armour`, `equipment`, `asset`). All item `id` values are stable lower-kebab-case slugs, treated as immutable from this commit onward.
D:\Terra Mortis\TM Suite\specs\stories\feature.654.equipment-schema-crud-api.story.md:68:  - [x] Include the 38 core book items (Mental/Physical/Social from `Equipment from Core Book.docx`) as `bucket: "equipment"` entries — each with `id`, `name`, `skill_domain`, `bonus_dice`, `availability`, `description`

 exited 124 in 22055ms:
D:\Terra Mortis\TM Suite\public\css\admin-layout.css:10148:   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:144:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:235:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:237:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:220:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:311:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:313:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\public\js\admin.js:1297:  // the editor's equipment-bucket dropdown renders synchronously when the
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2568:// Renders catalogue-ref equipment[]. All buckets flow through the same array
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2573:// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2575:// weapon+armour buckets -- the Weapons/Armour section split below is now
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2593:  // Group equipment items by bucket, preserving flat-array index for remove buttons
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2663:  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:5545:  // buckets (weapon/armour/equipment/asset) to five; labels use
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:33:// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:8: *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:11: *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:13: *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:15: *       single combat_gear bucket. A weapon-shaped combat_gear entry has
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:68:    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:91:    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\public\js\editor\edit.js:1230:// into equipment[]. Asset-bucket catalogue items now flow through shAddEquip.
D:\Terra Mortis\TM Suite\specs\epic-ecm-equipment-catalogue-migration.md:50:  bucket: 'weapon',             // enum: 'weapon' | 'armour' | 'equipment' | 'asset'
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:52:        { id: 'knife',      bucket: 'weapon',    name: 'Knife',       availability: 1, tags: ['melee'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:53:        { id: 'flashlight', bucket: 'equipment', name: 'Flashlight',  availability: 1, tags: ['mental', 'tools'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:54:        { id: 'ballistic-vest', bucket: 'armour', name: 'Ballistic Vest', availability: 2, tags: ['armour'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:55:        { id: 'haven-apartment', bucket: 'asset', name: 'Haven (Apartment)', availability: 2, tags: ['haven'] },
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:120:    expect(buckets).toContain('weapon');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:121:    expect(buckets).toContain('armour');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:122:    expect(buckets).toContain('equipment');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:123:    expect(buckets).toContain('asset');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:136:      const eqEntry  = cat.find(e => e.bucket === 'equipment');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:137:      const wpEntry  = cat.find(e => e.bucket === 'weapon');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:138:      const arEntry  = cat.find(e => e.bucket === 'armour');
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:177:      if (entry.bucket !== 'weapon') return { ok: false, error: `bucket should be weapon, got ${entry.bucket}` };
D:\Terra Mortis\TM Suite\tests\feature-654-equipment-schema.spec.js:196:      if (weapons.some(e => e.bucket !== 'weapon'))        return { ok: false, error: 'non-weapon in weapon bucket results' };
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:12: *   AC-4   Add equipment form present; bucket filter populates item select
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:151:  // ── AC-4: Add equipment form + bucket filter ────────────────────────────────
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:153:  test('AC-4: add equipment form has bucket select, item select, state select, Add button', async ({ page }) => {
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:167:        bucketOptions: ['weapon', 'armour', 'equipment'].every(b => html.includes(b)),
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:180:  test('AC-4: shEquipBucketFilter populates item select with weapons when bucket=weapon', async ({ page }) => {
D:\Terra Mortis\TM Suite\tests\feature-665-eq4-st-admin-equipment.spec.js:196:      bucketSel.value = 'weapon';
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:55:4. Filters to entries with `bucket === 'armour'` (catalogue refs may point at non-armour buckets in malformed data; ignoring them is the safe fail-soft).
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:66:- `active` = currently used for non-armour buckets (e.g. a powered equipment item being run); armour with `state: 'active'` is malformed data — fail-soft, exclude.
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:72:**Math:** `armourDefencePenalty(c)` returns the **maximum** `defence_penalty` among items satisfying the D1 filter (state==='worn' + bucket==='armour'). Other worn armour items are silently ignored by the formula. Returns 0 when no qualifying armour is present.
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:178:    .filter(entry => entry?.bucket === 'armour')
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:191:| Read path | D1 | #879 (impl, blocked) | New helper in `public/js/data/equipment-derivation.js` (or accessors.js adjacent to calcDefence). State filter `=== 'worn'`. Catalogue lookup via injected reader (post-ECM-5: cache; pre-ECM-5 fallback OK). bucket === 'armour' guard. |
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:202:- **Equipment bucket: weapon / equipment defence interactions.** Some weapons grant Defence bonuses (parrying weapons); some equipment items (a shield) might too. This ADR is armour-specific. Future helpers (`weaponDefenceBonus`, etc.) extend the composition site additively.
D:\Terra Mortis\TM Suite\specs\architecture\adr-006-defence-penalty-readpath.md:232:| D1 | resolved | new helper `armourDefencePenalty(c, catalogueLookup?)`; reads `c.equipment[]` filtered by `state === 'worn'` AND `entry.bucket === 'armour'`; catalogue lookup via injected reader (default: ECM-5 cache); returns non-negative integer |
D:\Terra Mortis\TM Suite\tests\feature-662-eq3-roll-calc-equipment-chips.spec.js:13: *   AC-5  Only bucket === 'equipment' eligible (weapon/armour/asset excluded)
D:\Terra Mortis\TM Suite\tests\feature-662-eq3-roll-calc-equipment-chips.spec.js:114:  // crime-scene-kit: bucket=equipment, skill_domain=Investigation, bonus_dice=2
D:\Terra Mortis\TM Suite\tests\feature-662-eq3-roll-calc-equipment-chips.spec.js:218:    // knife = weapon, armoured-vest = armour — neither is bucket=equipment
D:\Terra Mortis\TM Suite\server\routes\characters.js:829:// via the catalogue's bucket: 'asset' items). Response shape is now just
D:\Terra Mortis\TM Suite\server\routes\characters.js:926:// consolidated into equipment[] via the catalogue's bucket: 'asset' items.
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:15: *   into equipment[] via catalogue bucket: 'asset' items.
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:51:    bucket: 'weapon', name: `Test Cat Item ${Math.random().toString(36).slice(2, 8)}`,
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:73:  catKnife      = await seedCatalogueItem({ name: 'Knife (test)',      bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:74:  catFlashlight = await seedCatalogueItem({ name: 'Flashlight (test)', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:75:  catRope       = await seedCatalogueItem({ name: 'Rope (test)',       bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:70:    // EQC-1 (#1152): the old 'equipment' bucket is now 'skill_gear'.
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:76:    // EQC-1 (#1152): the old 'weapon' bucket merged into 'combat_gear',
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:78:    expect(src).toMatch(/bucket === 'combat_gear' && entry\.weapon_type != null[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:2: * EQC-1 (issue #1152, epic #1038) — equipment_catalogue bucket re-partition.
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:4: * Maps every live `equipment_catalogue` document from the OLD four-bucket
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:13: * `combat_gear` absorbs both old weapon and armour buckets, distinguished at
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:56:- **Blast radius**: `equipment_catalogue.bucket` is read directly by armour-penalty derivation
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:87:A re-partition of an equipment catalogue's `bucket` taxonomy from four old values
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:89:container`), where `combat_gear` merges the old weapon and armour buckets (distinguished at read time
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:108:   weapon-only logic) - not just the bucket. A predicate that checks bucket alone would now silently
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:176:   OTHER place that compares against the literal strings `'weapon'`, `'armour'`, `bucket === 'equipment'`,
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:177:   or `bucket === 'asset'` in a bucket-comparison context that this diff did NOT update. The story's own
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:194:   a matching `.ec-bucket-<value>` rule, and that no OLD `.ec-bucket-weapon` etc. rule was left
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-review.md:241:   - "None of the 5 pre-existing failures reference equipment/bucket/armour/weapon/container in any
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:69:- **Blast radius**: `equipment_catalogue.bucket` is read directly by armour-penalty derivation
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:100:A re-partition of an equipment catalogue's `bucket` taxonomy from four old values
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:102:container`), where `combat_gear` merges the old weapon and armour buckets (distinguished at read time
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:121:   weapon-only logic) - not just the bucket. A predicate that checks bucket alone would now silently
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:189:   OTHER place that compares against the literal strings `'weapon'`, `'armour'`, `bucket === 'equipment'`,
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:190:   or `bucket === 'asset'` in a bucket-comparison context that this diff did NOT update. The story's own
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:207:   a matching `.ec-bucket-<value>` rule, and that no OLD `.ec-bucket-weapon` etc. rule was left
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:254:   - "None of the 5 pre-existing failures reference equipment/bucket/armour/weapon/container in any
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:322:-.ec-bucket-weapon    { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:323:-.ec-bucket-armour    { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:324:-.ec-bucket-equipment { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:325:-.ec-bucket-asset     { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:327:+   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:346:+// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:372:-  renderForm({ bucket: 'equipment', tags: [] });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:381:-  const bucket = initial.bucket || 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:390:-  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'equipment');
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:403:- *       by state === 'worn' + bucket === 'armour'; injectable catalogue
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:405:+ *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:408:+ *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:410:+ *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:412:+ *       single combat_gear bucket. A weapon-shaped combat_gear entry has
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:421:-    if (!entry || entry.bucket !== 'armour') continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:422:+    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:430:-    if (entry?.bucket === 'armour') n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:431:+    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:443:-// Renders catalogue-ref equipment[]. All four buckets (weapon/armour/equipment/asset)
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:445:+// Renders catalogue-ref equipment[]. All buckets flow through the same array
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:450:+// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:452:+// weapon+armour buckets -- the Weapons/Armour section split below is now
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:464:   // Group equipment items by bucket, preserving flat-array index for remove buttons
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:470:-    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:519:+  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:590:-  // 2026-06-19: single add form covers all four buckets (weapon/armour/equipment/asset).
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:626:-      return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:627:+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:636:-    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:637:+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:639:+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:651:-      return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:652:+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:661:-    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:662:+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:664:+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:676:-  for (const bucket of ['weapon', 'armour', 'equipment', 'asset']) {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:678:+  // buckets (weapon/armour/equipment/asset) to five; labels use
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:705:+    // SAME character's `equipment[]` array whose catalogue bucket is
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:739:+ * four buckets (weapon/armour/equipment/asset) become FIVE new values:
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:746:+ *   skill_gear   — old `equipment` bucket, unchanged meaning (skill_domain +
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:751:+ *   container    — old `asset` bucket. "Assets are containers... no
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:770:-    bucket:       { type: 'string', enum: ['weapon', 'armour', 'equipment', 'asset'] },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:782:+ * EQC-1 (issue #1152, epic #1038) — equipment_catalogue bucket re-partition.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:784:+ * Maps every live `equipment_catalogue` document from the OLD four-bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:793:+ * `combat_gear` absorbs both old weapon and armour buckets, distinguished at
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:943:-    // `bucket === 'equipment'` filter clause counts. We confirm BOTH the
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:948:-    expect(src).toMatch(/bucket === 'equipment'[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:949:+    // EQC-1 (#1152): the old 'equipment' bucket is now 'skill_gear'.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:955:-    expect(src).toMatch(/bucket === 'weapon'[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:956:+    // EQC-1 (#1152): the old 'weapon' bucket merged into 'combat_gear',
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:958:+    expect(src).toMatch(/bucket === 'combat_gear' && entry\.weapon_type != null[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:985:+    const r = planBucketMigration({ bucket: 'weapon' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:990:+    const r = planBucketMigration({ bucket: 'armour' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:996:+    const r = planBucketMigration({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1002:+    const r = planBucketMigration({ bucket: 'asset' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1065:+    expect(doc.bucket).toBe('weapon');
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1106:-      .send({ bucket: 'weapon', name: 'Glock 17', description: 'Pistol', availability: 2, damage_mod: 2 });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1119:-  it('emits optgroups per bucket (weapon / armour / equipment / asset)', () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1121:-    expect(src).toMatch(/for\s*\(\s*const\s+bucket\s+of\s+\[\s*['"]weapon['"]\s*,\s*['"]armour['"]\s*,\s*['"]equipment['"]\s*,\s*['"]asset['"]\s*\]/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1138:-  it('filters by bucket === \'armour\'', () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1139:-    expect(src).toMatch(/entry\.bucket\s*!==\s*['"]armour['"]/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1140:+  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1150:-      { _id: 'aaa1', bucket: 'armour', defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1151:-      { _id: 'aaa2', bucket: 'armour', defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1152:+      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1153:+      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1161:-  it('D1: filters by bucket === \'armour\' — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1162:+  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1166:-      { _id: 'wep1', bucket: 'weapon', defence_penalty: 5 },   // not armour, ignored
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1167:-      { _id: 'arm1', bucket: 'armour', defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1168:+      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1169:+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1177:+  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1181:+      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1191:-      { _id: 'arm1', bucket: 'armour', defence_penalty: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1192:-      { _id: 'arm2', bucket: 'armour', defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1193:-      { _id: 'arm3', bucket: 'armour', defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1194:+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1195:+      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1196:+      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1204:-      { _id: 'a1', bucket: 'armour', defence_penalty: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1205:-      { _id: 'a2', bucket: 'armour', defence_penalty: undefined },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1206:-      { _id: 'a3', bucket: 'armour', defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1207:+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1208:+      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1209:+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1217:-      { _id: 'a1', bucket: 'armour' }, { _id: 'a2', bucket: 'armour' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1218:-      { _id: 'a3', bucket: 'armour' }, { _id: 'w1', bucket: 'weapon' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1219:+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1220:+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1228:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1229:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1237:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1238:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1246:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1247:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1302:-      return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1303:+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1312:-    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1313:+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1315:+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1327:-      return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1328:+      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1337:-    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1338:+    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1340:+    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1352:-  for (const bucket of ['weapon', 'armour', 'equipment', 'asset']) {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1354:+  // buckets (weapon/armour/equipment/asset) to five; labels use
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1381:+    // SAME character's `equipment[]` array whose catalogue bucket is
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1415:+ * four buckets (weapon/armour/equipment/asset) become FIVE new values:
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1422:+ *   skill_gear   — old `equipment` bucket, unchanged meaning (skill_domain +
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1427:+ *   container    — old `asset` bucket. "Assets are containers... no
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1446:-    bucket:       { type: 'string', enum: ['weapon', 'armour', 'equipment', 'asset'] },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1458:+ * EQC-1 (issue #1152, epic #1038) — equipment_catalogue bucket re-partition.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1460:+ * Maps every live `equipment_catalogue` document from the OLD four-bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1469:+ * `combat_gear` absorbs both old weapon and armour buckets, distinguished at
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1625:-    // `bucket === 'equipment'` filter clause counts. We confirm BOTH the
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1630:-    expect(src).toMatch(/bucket === 'equipment'[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1631:+    // EQC-1 (#1152): the old 'equipment' bucket is now 'skill_gear'.
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1637:-    expect(src).toMatch(/bucket === 'weapon'[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1638:+    // EQC-1 (#1152): the old 'weapon' bucket merged into 'combat_gear',
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1640:+    expect(src).toMatch(/bucket === 'combat_gear' && entry\.weapon_type != null[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1667:+    const r = planBucketMigration({ bucket: 'weapon' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1672:+    const r = planBucketMigration({ bucket: 'armour' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1678:+    const r = planBucketMigration({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1684:+    const r = planBucketMigration({ bucket: 'asset' });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1747:+    expect(doc.bucket).toBe('weapon');
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1788:-      .send({ bucket: 'weapon', name: 'Glock 17', description: 'Pistol', availability: 2, damage_mod: 2 });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1801:-  it('emits optgroups per bucket (weapon / armour / equipment / asset)', () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1803:-    expect(src).toMatch(/for\s*\(\s*const\s+bucket\s+of\s+\[\s*['"]weapon['"]\s*,\s*['"]armour['"]\s*,\s*['"]equipment['"]\s*,\s*['"]asset['"]\s*\]/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1820:-  it('filters by bucket === \'armour\'', () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1821:-    expect(src).toMatch(/entry\.bucket\s*!==\s*['"]armour['"]/);
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1822:+  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1832:-      { _id: 'aaa1', bucket: 'armour', defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1833:-      { _id: 'aaa2', bucket: 'armour', defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1834:+      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1835:+      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1843:-  it('D1: filters by bucket === \'armour\' — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1844:+  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1848:-      { _id: 'wep1', bucket: 'weapon', defence_penalty: 5 },   // not armour, ignored
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1849:-      { _id: 'arm1', bucket: 'armour', defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1850:+      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1851:+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1859:+  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1863:+      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1873:-      { _id: 'arm1', bucket: 'armour', defence_penalty: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1874:-      { _id: 'arm2', bucket: 'armour', defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1875:-      { _id: 'arm3', bucket: 'armour', defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1876:+      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1877:+      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1878:+      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1886:-      { _id: 'a1', bucket: 'armour', defence_penalty: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1887:-      { _id: 'a2', bucket: 'armour', defence_penalty: undefined },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1888:-      { _id: 'a3', bucket: 'armour', defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1889:+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1890:+      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1891:+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1899:-      { _id: 'a1', bucket: 'armour' }, { _id: 'a2', bucket: 'armour' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1900:-      { _id: 'a3', bucket: 'armour' }, { _id: 'w1', bucket: 'weapon' },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1901:+      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1902:+      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1910:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1911:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1919:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1920:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1928:-    const items = [{ _id: 'arm1', bucket: 'armour', defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1929:+    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1946:-.ec-bucket-weapon    { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1947:-.ec-bucket-armour    { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1948:-.ec-bucket-equipment { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1949:-.ec-bucket-asset     { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1951:+   the old weapon tone (it's the bucket an ST reaches for most in a fight). */
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1970:+// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:1996:-  renderForm({ bucket: 'equipment', tags: [] });
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2005:-  const bucket = initial.bucket || 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2014:-  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'equipment');
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2027:- *       by state === 'worn' + bucket === 'armour'; injectable catalogue
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2029:+ *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2032:+ *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2034:+ *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2036:+ *       single combat_gear bucket. A weapon-shaped combat_gear entry has
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2045:-    if (!entry || entry.bucket !== 'armour') continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2046:+    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2054:-    if (entry?.bucket === 'armour') n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2055:+    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2067:-// Renders catalogue-ref equipment[]. All four buckets (weapon/armour/equipment/asset)
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2069:+// Renders catalogue-ref equipment[]. All buckets flow through the same array
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2074:+// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2076:+// weapon+armour buckets -- the Weapons/Armour section split below is now
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2088:   // Group equipment items by bucket, preserving flat-array index for remove buttons
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2094:-    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2143:+  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2214:-  // 2026-06-19: single add form covers all four buckets (weapon/armour/equipment/asset).
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2414:"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!.git/**'"' --glob '"'!specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-diff.txt'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-codex-findings.md'"' \"bucket\\s*(===|"'!==|==|!=)'"\\s*['\\\"](weapon|armour|equipment|asset)['\\\"]|['\\\"](weapon|armour|equipment|asset)['\\\"]\\s*(===|"'!==|==|!=)'"\\s*["'^'"\\r\\n]*bucket\" 'D:\\Terra Mortis\\TM Suite'" in D:\Terra Mortis\TM Suite
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-raw-output.txt:2485:"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!.git/**'"' --glob '"'!specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-diff.txt'"' --glob '"'!specs/stories/code-review/issue-1152-eqc1-codex-findings.md'"' \"bucket.{0,40}(weapon|armour|equipment|asset)|(weapon|armour|equipment|asset).{0,40}bucket\" 'D:\\Terra Mortis\\TM Suite'" in D:\Terra Mortis\TM Suite
D:\Terra Mortis\TM Suite\server\tests\issue-896-availability-filter.test.js:43:  return { _id: 'item-' + availability, bucket: 'equipment', name: 'Item ' + availability, availability };
D:\Terra Mortis\TM Suite\server\tests\issue-896-availability-filter.test.js:209:    // One per bucket-render path: weapons, armour, equipment (asset bucket has
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:94:  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:247:      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:248:      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:257:  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:261:      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:262:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:271:  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:275:      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:285:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:286:      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:287:      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:301:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:302:      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:303:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:322:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:323:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:342:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:357:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:408:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\tests\issue-873-ecm-6-admin-sidebar.test.js:4: * Server-side: the schema-widen for `mechanical_effect` (the asset-bucket
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:54:    bucket: 'equipment',
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:86:    await seedItem({ name: 'Alpha', bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:87:    await seedItem({ name: 'Bravo', bucket: 'armour' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:97:    await seedItem({ name: 'Zebra', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:98:    await seedItem({ name: 'Aardvark', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:100:    const equip = res.body.filter(d => d.bucket === 'equipment');
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:154:      .send({ bucket: 'equipment', name: 'X' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:162:      .send({ bucket: 'equipment', name: 'X' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:170:      .send({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:198:      .send({ _id: sentinel, bucket: 'equipment', name: 'Try-injection' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:230:    const seed = await seedItem({ bucket: 'equipment', name: 'Stable' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:234:      .send({ bucket: 'weapon', _id: new ObjectId(), created_at: '1999-01-01T00:00:00.000Z', name: 'OK' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:236:    expect(res.body.bucket).toBe('equipment');
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:246:      .send({ bucket: 'weapon' });   // bucket is non-updatable
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:254:  // field across all buckets; the admin UI scopes it to assets.
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:256:    const seed = await seedItem({ bucket: 'asset', name: 'Warehouse', mechanical_effect: null });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:18:    const r = planBucketMigration({ bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:23:    const r = planBucketMigration({ bucket: 'armour' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:29:    const r = planBucketMigration({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:35:    const r = planBucketMigration({ bucket: 'asset' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:98:    expect(doc.bucket).toBe('weapon');
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:26:EQ-3 wires `bucket === 'equipment'` items into the roll calculator as toggleable dice-bonus chips (same visual pattern as specialties), and adds a weapon selector reference panel for combat pools. Neither feature changes base pool size — equipment chips toggle `MOD`; the weapon panel is informational only.
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:40:5. Only `bucket === 'equipment'` items are chip-eligible. Weapons, armour, assets are excluded.
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:133:    return entry && entry.bucket === 'equipment' &&
D:\Terra Mortis\TM Suite\specs\stories\feature.662.eq3-roll-calc-equipment-chips.story.md:205:    return entry && entry.bucket === 'weapon' &&
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:27:- [ ] Add equipment: inline form with bucket select → item select (filtered by bucket) → state select → notes → Add; writes via `POST /api/characters/:id/equipment`
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:259:The `equip` array is flat. The three rendered bucket sub-sections (weapon, armour, equipment) display items from that same flat array. When building the HTML, you must track the original flat-array index for each item so the Remove button passes the correct index to `shRemoveEquip(idx)`.
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:267:  const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'equipment';
D:\Terra Mortis\TM Suite\specs\stories\feature.665.eq4-st-admin-equipment.story.md:357:| 4 | AC-3 | Add equipment: select bucket → item populates → select item + state → Add → row appears |
D:\Terra Mortis\TM Suite\specs\stories\feature.656.eq2-equipment-assets-sheet-display.story.md:19:EQ-1 (#654) established the four-bucket equipment taxonomy (`weapon` | `armour` | `equipment` | `asset`), the static `EQUIPMENT_CATALOGUE` module at `public/js/data/equipment-data.js`, and the character schema extension (`character.equipment[]` + `character.assets[]`).
D:\Terra Mortis\TM Suite\specs\stories\feature.656.eq2-equipment-assets-sheet-display.story.md:65:  - [x] Group equipment items by bucket (weapon, armour, equipment) -- use `getCatalogueEntry(item.catalogue_id) || {}` for fallback safety
D:\Terra Mortis\TM Suite\specs\stories\feature.654.equipment-schema-crud-api.story.md:32:1. `public/js/data/equipment-data.js` exists and exports a named constant `EQUIPMENT_CATALOGUE` — an array of catalogue entry objects with at least one representative entry per bucket (`weapon`, `armour`, `equipment`, `asset`). All item `id` values are stable lower-kebab-case slugs, treated as immutable from this commit onward.
D:\Terra Mortis\TM Suite\specs\stories\feature.654.equipment-schema-crud-api.story.md:68:  - [x] Include the 38 core book items (Mental/Physical/Social from `Equipment from Core Book.docx`) as `bucket: "equipment"` entries — each with `id`, `name`, `skill_domain`, `bonus_dice`, `availability`, `description`

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:41:09.858098Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --glob '*.js' --glob '*.mjs' --glob '"'!server/tests/**'"' --glob '"'!tests/**'"' --glob '"'!specs/**'"' \"bucket.{0,50}(weapon|armour|equipment|asset)|(weapon|armour|equipment|asset).{0,50}bucket\" 'D:\\Terra Mortis\\TM Suite\\public' 'D:\\Terra Mortis\\TM Suite\\server'" in D:\Terra Mortis\TM Suite
 succeeded in 4741ms:
D:\Terra Mortis\TM Suite\public\js\admin.js:1297:  // the editor's equipment-bucket dropdown renders synchronously when the
D:\Terra Mortis\TM Suite\server\routes\characters.js:829:// via the catalogue's bucket: 'asset' items). Response shape is now just
D:\Terra Mortis\TM Suite\server\routes\characters.js:926:// consolidated into equipment[] via the catalogue's bucket: 'asset' items.
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:322:    // SAME character's `equipment[]` array whose catalogue bucket is
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:354:    // bucket: 'asset' items (Vehicle (Luxury), Safe House, etc.). The previous
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:356:    // bucket name and split asset-class items across two arrays. Equipment[]
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:17: * four buckets (weapon/armour/equipment/asset) become FIVE new values:
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:24: *   skill_gear   — old `equipment` bucket, unchanged meaning (skill_domain +
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:29: *   container    — old `asset` bucket. "Assets are containers... no
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:69:    // mechanical_effect — asset-bucket-only in practice (free-text note on
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:76:    // bucket=asset; the field is nullable across all buckets.
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:8: *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:11: *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:13: *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:15: *       single combat_gear bucket. A weapon-shaped combat_gear entry has
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:68:    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:91:    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2568:// Renders catalogue-ref equipment[]. All buckets flow through the same array
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2573:// four buckets (weapon/armour/equipment/asset) to five (combat_gear/
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2575:// weapon+armour buckets -- the Weapons/Armour section split below is now
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2593:  // Group equipment items by bucket, preserving flat-array index for remove buttons
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2663:  // ── Other Combat Gear (combat_gear bucket, neither weapon- nor armour-shaped
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:33:// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
D:\Terra Mortis\TM Suite\public\js\editor\edit.js:1230:// into equipment[]. Asset-bucket catalogue items now flow through shAddEquip.
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:2: * EQC-1 (issue #1152, epic #1038) — equipment_catalogue bucket re-partition.
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:4: * Maps every live `equipment_catalogue` document from the OLD four-bucket
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:13: * `combat_gear` absorbs both old weapon and armour buckets, distinguished at
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:5545:  // buckets (weapon/armour/equipment/asset) to five; labels use
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:144:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:235:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:237:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:220:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:311:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:313:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:41:19.057566Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --glob '*.js' --glob '*.mjs' --glob '"'!server/tests/**'"' --glob '"'!tests/**'"' --glob '"'!specs/**'"' -e \"'weapon'\" -e '\"weapon\"' -e \"'armour'\" -e '\"armour\"' -e \"'equipment'\" -e '\"equipment\"' -e \"'asset'\" -e '\"asset\"' 'D:\\Terra Mortis\\TM Suite\\public' 'D:\\Terra Mortis\\TM Suite\\server'" in D:\Terra Mortis\TM Suite
 succeeded in 2352ms:
D:\Terra Mortis\TM Suite\server\routes\characters.js:829:// via the catalogue's bucket: 'asset' items). Response shape is now just
D:\Terra Mortis\TM Suite\server\routes\characters.js:926:// consolidated into equipment[] via the catalogue's bucket: 'asset' items.
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-data.js:382:    key: 'equipment',
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:1107:  if (!DOWNTIME_SECTIONS.find(s => s.key === 'equipment')?.hidden) {
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:2169:    if (section.key === 'equipment') continue;
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:5467:  const section = DOWNTIME_SECTIONS.find(s => s.key === 'equipment');
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:5473:  let h = '<div class="qf-section collapsed" data-section-key="equipment">';
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:144:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:235:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:220:      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:311:    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:354:    // bucket: 'asset' items (Vehicle (Luxury), Safe House, etc.). The previous
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:355:    // free-text assets[] storage shape collided with the catalogue's 'asset'

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:41:27.855399Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!.git/**'"' --glob '"'!specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'"' --glob '"'!specs/stories/code-review/**'"' \"container_id\" 'D:\\Terra Mortis\\TM Suite'" in D:\Terra Mortis\TM Suite
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:41:44.568924Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
 succeeded in 18927ms:
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:319:    // `container_id` (EQC-1, issue #1152, epic #1038) — null/absent means
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:332:    // catalogue item; an unresolvable/stale container_id is display-inert
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:346:          container_id:    { type: ['string', 'null'], pattern: '^[a-f0-9]{24}$' },
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:31: *                  other equipment (see character.schema.js's `container_id`)

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$route = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\server\\routes\\characters.js'; "'$route[760..970]; $route[1320..1460]' in D:\Terra Mortis\TM Suite
 succeeded in 2576ms:
    // Cascade deletes first; character delete is last as the completion marker
    await getCollection('downtime_submissions').deleteMany({ character_id: oid });
    await getCollection('ordeal_submissions').deleteMany({ character_id: oid }).catch(() => {});
    await getCollection('histories').deleteMany({ character_id: oid }).catch(() => {});
    await getCollection('questionnaire_responses').deleteMany({ character_id: oid }).catch(() => {});
    await getCollection('tracker_state').deleteMany({ character_id: oid }).catch(() => {});
    await getCollection('game_sessions').updateMany({}, { $pull: { attendance: { character_id: oid } } });
    await getCollection('players').updateMany({}, { $pull: { character_ids: oid } });
    await getCollection('npcs').updateMany({}, { $pull: { linked_character_ids: oid } }).catch(() => {});

    const result = await col().deleteOne({ _id: oid });
    if (result.deletedCount === 0) return res.status(404).json({ error: 'NOT_FOUND', message: 'Character not found' });
    res.status(204).end();
  } catch (err) {
    console.error('Hard-delete cascade failed:', err);
    res.status(500).json({ error: 'CASCADE_FAILED', message: err.message });
  }
});

// PATCH /api/characters/:id/player_prefs — player (own char) or ST.
// #542: persist player preference ratings. Narrowly scoped: only the
// `player_prefs` subdocument is touched. Ownership mirrors GET /:id.
router.patch('/:id/player_prefs', async (req, res) => {
  const oid = parseId(req.params.id);
  if (!oid) return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Invalid character ID format' });

  if (!isStRole(req.user)) {
    const owns = (req.user.character_ids || []).some(id => id.toString() === oid.toString());
    if (!owns) return res.status(403).json({ error: 'FORBIDDEN', message: 'Not your character' });
  }

  const { player_prefs } = req.body || {};
  if (!player_prefs || typeof player_prefs !== 'object') {
    return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'player_prefs must be an object' });
  }

  const VALID_KEYS = [
    'combat_action', 'horror_dread', 'institutional_corruption',
    'mysticism_mystery', 'personal_story', 'political_intrigue',
  ];
  const prefs = {};
  for (const key of VALID_KEYS) {
    const v = player_prefs[key];
    if (v === undefined) continue;
    const rating = (v && typeof v === 'object') ? v.rating : v;
    if (rating !== null && rating !== undefined &&
        (typeof rating !== 'number' || !Number.isInteger(rating) || rating < 1 || rating > 5)) {
      return res.status(400).json({ error: 'VALIDATION_ERROR', message: `${key}.rating must be 1–5 integer or null` });
    }
    prefs[key] = { rating: rating ?? null };
  }
  prefs.updated_at = new Date().toISOString();

  const result = await col().findOneAndUpdate(
    { _id: oid },
    { $set: { player_prefs: prefs } },
    { returnDocument: 'after' },
  );
  if (!result) return res.status(404).json({ error: 'NOT_FOUND', message: 'Character not found' });
  res.json(result);
});

// ── Equipment routes (EQ-1, issue #654) ─────────────────────────────────────
//
// All three routes require ST auth.
// DELETE routes: client must refresh after delete to avoid stale indices.
//
// 2026-06-19: character.assets[] removed (consolidated into equipment[]
// via the catalogue's bucket: 'asset' items). Response shape is now just
// { equipment } — no more { equipment, assets }.

// GET /api/characters/:id/equipment — returns { equipment } for the character.
router.get('/:id/equipment', requireRole('st'), async (req, res) => {
  const oid = parseId(req.params.id);
  if (!oid) return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Invalid character ID' });
  const char = await col().findOne({ _id: oid }, { projection: { equipment: 1 } });
  if (!char) return res.status(404).json({ error: 'NOT_FOUND', message: 'Character not found' });
  res.json({ equipment: char.equipment || [] });
});

// POST /api/characters/:id/equipment — append a single equipment item.
//
// ECM-3 (#870): `catalogue_id` is a 24-hex ObjectId string on the wire,
// matching the new equipment_catalogue collection's _id shape. Existence
// is checked via a Mongo lookup against equipment_catalogue, not against
// the static EQUIPMENT_CATALOGUE slug set — the slug set is dead post-ECM-3
// (ECM-7 deletes the static module entirely). The catalogue_id is hydrated
// to an ObjectId before $push so storage stays ObjectId-typed across the
// full lifecycle; see specs/epic-ecm-equipment-catalogue-migration.md.
router.post('/:id/equipment', requireRole('st'), async (req, res) => {
  const oid = parseId(req.params.id);
  if (!oid) return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Invalid character ID' });

  const item = req.body;
  const VALID_STATES = ['carried', 'worn', 'stashed', 'lost', 'active'];
  if (!item || typeof item !== 'object') {
    return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Request body must be an equipment item object' });
  }
  if (!item.catalogue_id || typeof item.catalogue_id !== 'string') {
    return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'catalogue_id is required' });
  }
  if (!ObjectId.isValid(item.catalogue_id) || String(new ObjectId(item.catalogue_id)) !== item.catalogue_id) {
    return res.status(400).json({
      error: 'VALIDATION_ERROR',
      message: `catalogue_id must be a 24-hex ObjectId string; got '${item.catalogue_id}'`,
    });
  }
  if (!VALID_STATES.includes(item.state)) {
    return res.status(400).json({ error: 'VALIDATION_ERROR', message: `state must be one of: ${VALID_STATES.join(', ')}` });
  }
  if (!Number.isInteger(item.acquired_cycle) || item.acquired_cycle < 0) {
    return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'acquired_cycle must be a non-negative integer' });
  }

  const catalogueOid = new ObjectId(item.catalogue_id);
  const catalogueDoc = await getCollection('equipment_catalogue').findOne(
    { _id: catalogueOid },
    { projection: { _id: 1 } }
  );
  if (!catalogueDoc) {
    return res.status(404).json({ error: 'NOT_FOUND', message: `Unknown catalogue item: ${item.catalogue_id}` });
  }

  const char = await col().findOne({ _id: oid }, { projection: { _id: 1 } });
  if (!char) return res.status(404).json({ error: 'NOT_FOUND', message: 'Character not found' });

  const cleanItem = {
    catalogue_id:   catalogueOid,
    state:          item.state,
    acquired_cycle: item.acquired_cycle,
    notes:          item.notes ?? null,
  };
  const result = await col().findOneAndUpdate(
    { _id: oid },
    { $push: { equipment: cleanItem } },
    { returnDocument: 'after', projection: { equipment: 1 } },
  );
  res.json({ equipment: result.equipment || [] });
});

// DELETE /api/characters/:id/equipment/:itemIndex — remove by zero-based index.
// Client must refresh after delete to avoid stale indices.
router.delete('/:id/equipment/:itemIndex', requireRole('st'), async (req, res) => {
  const oid = parseId(req.params.id);
  if (!oid) return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Invalid character ID' });

  const char = await col().findOne({ _id: oid }, { projection: { equipment: 1 } });
  if (!char) return res.status(404).json({ error: 'NOT_FOUND', message: 'Character not found' });

  const idx = parseInt(req.params.itemIndex, 10);
  const arr = char.equipment || [];
  if (!Number.isInteger(idx) || idx < 0 || idx >= arr.length) {
    return res.status(404).json({ error: 'NOT_FOUND', message: 'Equipment index out of range' });
  }

  arr.splice(idx, 1);
  const result = await col().findOneAndUpdate(
    { _id: oid },
    { $set: { equipment: arr } },
    { returnDocument: 'after', projection: { equipment: 1 } },
  );
  res.json({ equipment: result.equipment || [] });
});

// /api/characters/:id/assets routes REMOVED 2026-06-19 — character.assets[]
// consolidated into equipment[] via the catalogue's bucket: 'asset' items.
// All asset-class items now flow through the equipment routes above.

export default router;

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:41:59.123984Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"characterSchema|validate.*character|equipment\\s*:|req\\.body\" 'D:\\Terra Mortis\\TM Suite\\server\\routes\\characters.js' 'D:\\Terra Mortis\\TM Suite\\server'" in D:\Terra Mortis\TM Suite
 succeeded in 3220ms:
D:\Terra Mortis\TM Suite\server\routes\characters.js:6:import { normalizeMeritsMiddleware, normalizeCharacterMerits, validateWhiteAntsTerritoriesMiddleware, validateTrapDoorAnchorMiddleware } from '../lib/normalize-character.js';
D:\Terra Mortis\TM Suite\server\routes\characters.js:26:/** Strip ephemeral underscore-prefixed fields from req.body before validation. */
D:\Terra Mortis\TM Suite\server\routes\characters.js:28:  if (req.body && typeof req.body === 'object') {
D:\Terra Mortis\TM Suite\server\routes\characters.js:29:    for (const key of Object.keys(req.body)) {
D:\Terra Mortis\TM Suite\server\routes\characters.js:30:      if (key.startsWith('_')) delete req.body[key];
D:\Terra Mortis\TM Suite\server\routes\characters.js:66:async function validateTouchstones(touchstones, characterId, currentCharDoc) {
D:\Terra Mortis\TM Suite\server\routes\characters.js:420:  const doc = { ...req.body };
D:\Terra Mortis\TM Suite\server\routes\characters.js:440:  const doc = req.body;
D:\Terra Mortis\TM Suite\server\routes\characters.js:455:  const { _id, willpower, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\characters.js:525:  const { st_mods_suppressed } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:559:  const { locations } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:621:  const body = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:792:  const { player_prefs } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:836:  const char = await col().findOne({ _id: oid }, { projection: { equipment: 1 } });
D:\Terra Mortis\TM Suite\server\routes\characters.js:838:  res.json({ equipment: char.equipment || [] });
D:\Terra Mortis\TM Suite\server\routes\characters.js:854:  const item = req.body;
D:\Terra Mortis\TM Suite\server\routes\characters.js:895:    { $push: { equipment: cleanItem } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:896:    { returnDocument: 'after', projection: { equipment: 1 } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:898:  res.json({ equipment: result.equipment || [] });
D:\Terra Mortis\TM Suite\server\routes\characters.js:907:  const char = await col().findOne({ _id: oid }, { projection: { equipment: 1 } });
D:\Terra Mortis\TM Suite\server\routes\characters.js:919:    { $set: { equipment: arr } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:920:    { returnDocument: 'after', projection: { equipment: 1 } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:922:  res.json({ equipment: result.equipment || [] });
D:\Terra Mortis\TM Suite\server\middleware\validate.js:28:    const valid = compiledValidate(req.body);
D:\Terra Mortis\TM Suite\server\lib\normalize-character.js:173:  if (req.body && typeof req.body === 'object') {
D:\Terra Mortis\TM Suite\server\lib\normalize-character.js:174:    normalizeCharacterMerits(req.body);
D:\Terra Mortis\TM Suite\server\lib\normalize-character.js:194: * Partial-save tolerance: if `req.body.merits` is absent, this middleware is a
D:\Terra Mortis\TM Suite\server\lib\normalize-character.js:198:  const merits = req.body && Array.isArray(req.body.merits) ? req.body.merits : null;
D:\Terra Mortis\TM Suite\server\lib\normalize-character.js:243: * Partial-save tolerance: if `req.body.merits` is absent, this is a no-op
D:\Terra Mortis\TM Suite\server\lib\normalize-character.js:250:  const merits = req.body && Array.isArray(req.body.merits) ? req.body.merits : null;
D:\Terra Mortis\TM Suite\server\middleware\validateCharacter.js:2: * Express middleware: validates req.body against the character JSON Schema.
D:\Terra Mortis\TM Suite\server\middleware\validateCharacter.js:7:import { characterSchema, characterPartialSchema } from '../schemas/character.schema.js';
D:\Terra Mortis\TM Suite\server\middleware\validateCharacter.js:10:const validateFull = ajv.compile(characterSchema);
D:\Terra Mortis\TM Suite\server\middleware\validateCharacter.js:11:const validatePartial = ajv.compile(characterPartialSchema);
D:\Terra Mortis\TM Suite\server\middleware\validateCharacter.js:14:  if (validator(req.body)) return next();
D:\Terra Mortis\TM Suite\server\routes\app-settings.js:72:  const body = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\archive-documents.js:69:  if (typeof req.body?.content_html === 'string') updates.content_html = req.body.content_html;
D:\Terra Mortis\TM Suite\server\routes\archive-documents.js:70:  if (typeof req.body?.title === 'string' && req.body.title.trim()) updates.title = req.body.title.trim();
D:\Terra Mortis\TM Suite\server\routes\archive-documents.js:85:  const { character_id, type, title, content_html, visible_to_player } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\attendance.js:110:  const downtime = req.body?.downtime;
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:36:export const characterSchema = {
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:335:    equipment: {
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:705:export const characterPartialSchema = derivePartialSchema(characterSchema);
D:\Terra Mortis\TM Suite\server\routes\auth.js:23:  const { code, redirect_uri } = req.body;
D:\Terra Mortis\TM Suite\server\routes\chapters.js:32:  const { number, label } = req.body;
D:\Terra Mortis\TM Suite\server\routes\chapters.js:51:  if (req.body.number !== undefined) {
D:\Terra Mortis\TM Suite\server\routes\chapters.js:52:    const n = req.body.number;
D:\Terra Mortis\TM Suite\server\routes\chapters.js:58:  if (req.body.label !== undefined) {
D:\Terra Mortis\TM Suite\server\routes\chapters.js:59:    if (typeof req.body.label !== 'string' || !req.body.label.trim()) {
D:\Terra Mortis\TM Suite\server\routes\chapters.js:62:    updates.label = req.body.label.trim();
D:\Terra Mortis\TM Suite\server\routes\characters.js:6:import { normalizeMeritsMiddleware, normalizeCharacterMerits, validateWhiteAntsTerritoriesMiddleware, validateTrapDoorAnchorMiddleware } from '../lib/normalize-character.js';
D:\Terra Mortis\TM Suite\server\routes\characters.js:26:/** Strip ephemeral underscore-prefixed fields from req.body before validation. */
D:\Terra Mortis\TM Suite\server\routes\characters.js:28:  if (req.body && typeof req.body === 'object') {
D:\Terra Mortis\TM Suite\server\routes\characters.js:29:    for (const key of Object.keys(req.body)) {
D:\Terra Mortis\TM Suite\server\routes\characters.js:30:      if (key.startsWith('_')) delete req.body[key];
D:\Terra Mortis\TM Suite\server\routes\characters.js:66:async function validateTouchstones(touchstones, characterId, currentCharDoc) {
D:\Terra Mortis\TM Suite\server\routes\characters.js:420:  const doc = { ...req.body };
D:\Terra Mortis\TM Suite\server\routes\characters.js:440:  const doc = req.body;
D:\Terra Mortis\TM Suite\server\routes\characters.js:455:  const { _id, willpower, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\characters.js:525:  const { st_mods_suppressed } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:559:  const { locations } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:621:  const body = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:792:  const { player_prefs } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\characters.js:836:  const char = await col().findOne({ _id: oid }, { projection: { equipment: 1 } });
D:\Terra Mortis\TM Suite\server\routes\characters.js:838:  res.json({ equipment: char.equipment || [] });
D:\Terra Mortis\TM Suite\server\routes\characters.js:854:  const item = req.body;
D:\Terra Mortis\TM Suite\server\routes\characters.js:895:    { $push: { equipment: cleanItem } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:896:    { returnDocument: 'after', projection: { equipment: 1 } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:898:  res.json({ equipment: result.equipment || [] });
D:\Terra Mortis\TM Suite\server\routes\characters.js:907:  const char = await col().findOne({ _id: oid }, { projection: { equipment: 1 } });
D:\Terra Mortis\TM Suite\server\routes\characters.js:919:    { $set: { equipment: arr } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:920:    { returnDocument: 'after', projection: { equipment: 1 } },
D:\Terra Mortis\TM Suite\server\routes\characters.js:922:  res.json({ equipment: result.equipment || [] });
D:\Terra Mortis\TM Suite\server\routes\contested-rolls.js:14:  const { challenger_character_id } = req.body;
D:\Terra Mortis\TM Suite\server\routes\contested-rolls.js:23:    ...req.body,
D:\Terra Mortis\TM Suite\server\routes\cyoa.js:25:  const { story_id, version, outcome, character, code } = req.body ?? {};
D:\Terra Mortis\TM Suite\server\routes\devlog.js:18:  const doc    = { ...req.body, created_at: now, updated_at: now };
D:\Terra Mortis\TM Suite\server\routes\devlog.js:26:  const update = { ...req.body, updated_at: new Date().toISOString() };
D:\Terra Mortis\TM Suite\server\routes\downtime.js:68:    bodyKeys: Object.keys(req.body || {}),
D:\Terra Mortis\TM Suite\server\routes\downtime.js:95:  const doc = req.body;
D:\Terra Mortis\TM Suite\server\routes\downtime.js:111:  const { territory_id, rights } = req.body;
D:\Terra Mortis\TM Suite\server\routes\downtime.js:208:  } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\downtime.js:331:  const inviteeIdsRaw = req.body?.invitee_character_ids;
D:\Terra Mortis\TM Suite\server\routes\downtime.js:387:  const stOverride = !!(isST && req.body?.st_override);
D:\Terra Mortis\TM Suite\server\routes\downtime.js:512:    if (Object.prototype.hasOwnProperty.call(req.body || {}, k)) {
D:\Terra Mortis\TM Suite\server\routes\downtime.js:513:      setOps[`joint_projects.$.${k}`] = req.body[k];
D:\Terra Mortis\TM Suite\server\routes\downtime.js:573:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\downtime.js:692:  const snapErrors = _validatePoolSnapshots(req.body);
D:\Terra Mortis\TM Suite\server\routes\downtime.js:701:  const doc = { ...req.body };
D:\Terra Mortis\TM Suite\server\routes\downtime.js:833:    const allFieldsFeeding = Object.keys(req.body).every(k => FEEDING_ONLY_FIELDS.includes(k));
D:\Terra Mortis\TM Suite\server\routes\downtime.js:845:    delete req.body.st_review;
D:\Terra Mortis\TM Suite\server\routes\downtime.js:846:    for (const key of Object.keys(req.body)) {
D:\Terra Mortis\TM Suite\server\routes\downtime.js:847:      if (key.startsWith('st_review.')) delete req.body[key];
D:\Terra Mortis\TM Suite\server\routes\downtime.js:853:    req.body['st_review.outcome_visibility'] === 'published' &&
D:\Terra Mortis\TM Suite\server\routes\downtime.js:861:  const snapErrors = _validatePoolSnapshots(req.body);
D:\Terra Mortis\TM Suite\server\routes\downtime.js:870:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\downtime.js:1076:  const { section_key, section_idx, category, reason } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\downtime.js:1118:  const newStatus = req.body?.status;
D:\Terra Mortis\TM Suite\server\routes\downtime.js:1133:    flag.resolution_note = (req.body?.resolution_note || '').toString().trim() || null;
D:\Terra Mortis\TM Suite\server\routes\equipment-catalogue.js:100:      const doc = { ...req.body, created_at: now, updated_at: now };
D:\Terra Mortis\TM Suite\server\routes\equipment-catalogue.js:115:      for (const [field, value] of Object.entries(req.body)) {
D:\Terra Mortis\TM Suite\server\routes\game-sessions.js:67:  const doc = req.body;
D:\Terra Mortis\TM Suite\server\routes\game-sessions.js:82:  const { _id, ...body } = req.body;
D:\Terra Mortis\TM Suite\server\routes\history.js:70:  const { character_id, responses } = req.body;
D:\Terra Mortis\TM Suite\server\routes\history.js:117:  if (req.body.responses !== undefined) updates.responses = req.body.responses;
D:\Terra Mortis\TM Suite\server\routes\history.js:119:  if (req.body.status === 'submitted') {
D:\Terra Mortis\TM Suite\server\routes\history.js:122:  } else if (req.body.status === 'approved' && isStRole(req.user)) {
D:\Terra Mortis\TM Suite\server\routes\history.js:125:  } else if (req.body.status === 'draft') {
D:\Terra Mortis\TM Suite\server\routes\investigations.js:34:  const { target_description, threshold_type, custom_threshold, investigating_character_id, cycle_id, notes } = req.body;
D:\Terra Mortis\TM Suite\server\routes\investigations.js:75:          cycle_id, successes_accumulated } = req.body;
D:\Terra Mortis\TM Suite\server\routes\npc-flags.js:61:  const { npc_id, character_id, reason } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\npc-flags.js:170:  const note = typeof req.body?.resolution_note === 'string'
D:\Terra Mortis\TM Suite\server\routes\npc-flags.js:171:    ? req.body.resolution_note.trim()
D:\Terra Mortis\TM Suite\server\routes\npcs.js:28:  const { name, relationship_note, general_note, character_id } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\npcs.js:157:  const { name, description, status, linked_character_ids, linked_cycle_id, notes } = req.body;
D:\Terra Mortis\TM Suite\server\routes\npcs.js:181:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\office-actions.js:150:  const { actor_id, target_id, action_type } = req.body;
D:\Terra Mortis\TM Suite\server\routes\office-merit-dots.js:37:  const { merit, dots } = req.body;
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:71:  const { type, responses } = req.body;
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:127:  if (req.body.responses !== undefined) updates.responses = req.body.responses;
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:129:  if (req.body.marking !== undefined && isStRole(req.user)) {
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:130:    updates.marking = req.body.marking;
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:131:    if (req.body.marking?.status === 'complete') {
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:137:  if (req.body.status === 'submitted') {
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:140:  } else if (req.body.status === 'approved' && isStRole(req.user)) {
D:\Terra Mortis\TM Suite\server\routes\ordeal-responses.js:143:  } else if (req.body.status === 'draft') {
D:\Terra Mortis\TM Suite\server\routes\ordeal-rubrics.js:27:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\ordeal-submissions.js:136:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\players.js:33:  for (const [k, v] of Object.entries(req.body || {})) {
D:\Terra Mortis\TM Suite\server\routes\players.js:91:  const doc = req.body;
D:\Terra Mortis\TM Suite\server\routes\players.js:134:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\questionnaire.js:61:  const { character_id, responses } = req.body;
D:\Terra Mortis\TM Suite\server\routes\questionnaire.js:113:  if (req.body.responses !== undefined) updates.responses = req.body.responses;
D:\Terra Mortis\TM Suite\server\routes\questionnaire.js:115:  if (req.body.status === 'submitted') {
D:\Terra Mortis\TM Suite\server\routes\questionnaire.js:118:  } else if (req.body.status === 'approved' && isStRole(req.user)) {
D:\Terra Mortis\TM Suite\server\routes\questionnaire.js:121:  } else if (req.body.status === 'draft') {
D:\Terra Mortis\TM Suite\server\routes\ranking_ballots.js:117:  const cycleId = String(req.body.cycle_id);
D:\Terra Mortis\TM Suite\server\routes\ranking_ballots.js:118:  const voter   = String(req.body.voter_character_id);
D:\Terra Mortis\TM Suite\server\routes\ranking_ballots.js:121:  const clanRanking = req.body.clan_ranking || {};
D:\Terra Mortis\TM Suite\server\routes\ranking_ballots.js:122:  const covRanking  = req.body.covenant_ranking || {};
D:\Terra Mortis\TM Suite\server\routes\relationships.js:175:  const body = req.body;
D:\Terra Mortis\TM Suite\server\routes\relationships.js:379:    for (const key of Object.keys(req.body || {})) {
D:\Terra Mortis\TM Suite\server\routes\relationships.js:381:        filtered[key] = req.body[key];
D:\Terra Mortis\TM Suite\server\routes\relationships.js:386:      } else if (key === 'kind' && req.body.kind !== existing.kind) {
D:\Terra Mortis\TM Suite\server\routes\relationships.js:392:    req.body = {
D:\Terra Mortis\TM Suite\server\routes\relationships.js:401:    if (typeof req.body.state === 'string' && req.body.state.length > 2000) {
D:\Terra Mortis\TM Suite\server\routes\relationships.js:409:  const body = req.body;
D:\Terra Mortis\TM Suite\server\routes\rules-engine.js:33:      const checkErr = postCheck(req.body);
D:\Terra Mortis\TM Suite\server\routes\rules-engine.js:36:    const doc = { ...req.body, created_at: new Date().toISOString(), updated_at: new Date().toISOString() };
D:\Terra Mortis\TM Suite\server\routes\rules-engine.js:46:      const checkErr = postCheck(req.body);
D:\Terra Mortis\TM Suite\server\routes\rules-engine.js:49:    const { _id, ...body } = req.body;
D:\Terra Mortis\TM Suite\server\routes\rules.js:60:  const doc = req.body;
D:\Terra Mortis\TM Suite\server\routes\rules.js:89:  for (const [field, value] of Object.entries(req.body)) {
D:\Terra Mortis\TM Suite\server\routes\sessions.js:22:  const doc = req.body;
D:\Terra Mortis\TM Suite\server\routes\st_mods.js:114:  const { character_id, stat_path, delta, reason, show_reason_to_player } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\st_mods.js:271:  const { active } = req.body || {};
D:\Terra Mortis\TM Suite\server\routes\st_mods.js:281:  const overrideReason = typeof req.body?.reason === 'string' && req.body.reason.trim()
D:\Terra Mortis\TM Suite\server\routes\st_mods.js:282:    ? req.body.reason.trim()
D:\Terra Mortis\TM Suite\server\routes\territories.js:33:  const { _id, ...fields } = req.body;
D:\Terra Mortis\TM Suite\server\routes\territories.js:61:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\routes\territories.js:81:  const { feeding_rights } = req.body;
D:\Terra Mortis\TM Suite\server\routes\territories.js:170:  const { lieutenant_id } = req.body;
D:\Terra Mortis\TM Suite\server\routes\tracker.js:33:  const { _id, ...updates } = req.body;
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:55:  equipment: 'skill_gear',
D:\Terra Mortis\TM Suite\server\scripts\archive\ingest-excel.js:11: *   --json      Write validated v3 characters to data/chars_v3.json
D:\Terra Mortis\TM Suite\server\scripts\archive\ingest-excel.js:25:import { characterSchema } from '../schemas/character.schema.js';
D:\Terra Mortis\TM Suite\server\scripts\archive\ingest-excel.js:558:  const schemaForValidation = { ...characterSchema };
D:\Terra Mortis\TM Suite\server\scripts\archive\ingest-excel.js:604:    const schemaKeys = new Set(Object.keys(characterSchema.properties || {}));
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:211:    const fresh = await getCollection('characters').findOne({ _id: char4._id }, { projection: { equipment: 1 } });
D:\Terra Mortis\TM Suite\server\scripts\archive\migrate-schema-v3.js:21:import { characterSchema } from '../schemas/character.schema.js';
D:\Terra Mortis\TM Suite\server\scripts\archive\migrate-schema-v3.js:236:    const schemaForValidation = { ...characterSchema };
D:\Terra Mortis\TM Suite\server\tests\issue-826-cleanup-script-integration.test.js:104:    equipment: [],
D:\Terra Mortis\TM Suite\server\tests\issue-837-xp-totals-deprecation.test.js:28:import { characterSchema } from '../schemas/character.schema.js';
D:\Terra Mortis\TM Suite\server\tests\issue-837-xp-totals-deprecation.test.js:53:  it('characterSchema.properties does not include xp_total', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-837-xp-totals-deprecation.test.js:54:    expect(characterSchema.properties.xp_total).toBeUndefined();
D:\Terra Mortis\TM Suite\server\tests\issue-837-xp-totals-deprecation.test.js:56:  it('characterSchema.properties does not include xp_spent', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-837-xp-totals-deprecation.test.js:57:    expect(characterSchema.properties.xp_spent).toBeUndefined();
D:\Terra Mortis\TM Suite\server\tests\issue-837-xp-totals-deprecation.test.js:62:    expect(characterSchema.additionalProperties).toBe(false);
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:74:    equipment: [{ catalogue_id: catalogueId, state: 'owned', acquired_cycle: 0 }],
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:54:    disciplines: {}, merits: [], equipment: [],
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:239:    const c = mkChar({ equipment: [] });
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:250:    const c = mkChar({ equipment: [
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:264:    const c = mkChar({ equipment: [
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:277:    const c = mkChar({ equipment: [{ catalogue_id: 'x1', state: 'worn' }] });
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:289:    const c = mkChar({ equipment: [
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:305:    const c = mkChar({ equipment: items.map(it => ({ catalogue_id: it._id, state: 'worn' })) });
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:312:    const c = mkChar({ equipment: [{ catalogue_id: 'ghost', state: 'worn' }] });
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:325:    const c = mkChar({ equipment: [
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:341:    const c = mkChar({ equipment: [{ catalogue_id: 'arm1', state: 'worn' }] });
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:355:      equipment: [{ catalogue_id: 'arm1', state: 'worn' }],
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:375:    const c = mkChar({ equipment: [] });
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:405:      equipment: [{ catalogue_id: 'arm1', state: 'worn' }],
D:\Terra Mortis\TM Suite\server\tests\issue-896-availability-filter.test.js:38:    attributes: {}, skills: {}, disciplines: {}, equipment: [],
D:\Terra Mortis\TM Suite\server\tests\oath-a-d8-api-roundtrip.test.js:151:    // characterSchema is additionalProperties:false, so without the

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:42:07.529620Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$route = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\server\\routes\\characters.js'; "'$route[0..120]; $route[390..510]' in D:\Terra Mortis\TM Suite
 succeeded in 3180ms:
import { Router } from 'express';
import { ObjectId } from 'mongodb';
import { getCollection } from '../db.js';
import { requireRole, isStRole } from '../middleware/auth.js';
import { validateCharacter, validateCharacterPartial } from '../middleware/validateCharacter.js';
import { normalizeMeritsMiddleware, normalizeCharacterMerits, validateWhiteAntsTerritoriesMiddleware, validateTrapDoorAnchorMiddleware } from '../lib/normalize-character.js';
// N-1 (ADR-005 Rev 2): map-fallback shape for per-slug reads. Used in the
// partner-dots enrichment below so the server's hardcoded subset survives
// the N-2 backfill from `m.free_<slug>` to `m.free_grants.<slug>`. The
// SUBSET ITSELF (mci + bloodline + retainer) is preserved verbatim per
// Concern #1 Rev 2 — divergence with the client's mci-only subset stays.
import { freeOf, resolveSharingScope } from '../../public/js/data/rules-helpers.js';
// ECM-7 (#874): the EQUIPMENT_CATALOGUE static-module import + the dead
// _CATALOGUE_IDS slug set were removed alongside the static module deletion.
// POST /api/characters/:id/equipment validates catalogue existence via the
// Mongo equipment_catalogue collection lookup (post-ECM-3 #870 #885).

const router = Router();
const col = () => getCollection('characters');

// #510: canonical influence spheres (mirrors public/js/data/constants.js:123).
// Carthian Pull allocations to Allies/Contacts must pick from this fixed set so
// downstream systems recognise the qualifier.
const INFLUENCE_SPHERES = ['Bureaucracy', 'Church', 'Finance', 'Health', 'High Society', 'Industry', 'Legal', 'Media', 'Military', 'Occult', 'Police', 'Politics', 'Street', 'Transportation', 'University', 'Underworld'];

/** Strip ephemeral underscore-prefixed fields from req.body before validation. */
function stripEphemeral(req, res, next) {
  if (req.body && typeof req.body === 'object') {
    for (const key of Object.keys(req.body)) {
      if (key.startsWith('_')) delete req.body[key];
    }
  }
  next();
}

function parseId(id) {
  try {
    return new ObjectId(id);
  } catch {
    return null;
  }
}

/**
 * NPCR.4: validate that every id in touchstone_edge_ids[] points to an
 * active relationship edge with kind='touchstone' and the character on
 * one endpoint. Returns null on success, or an error message string.
 */
/**
 * NPCR.4 helpers — touchstones live on character.touchstones[], capped at 6.
 * Slot rating descends from the clan anchor (Ventrue=7, else=6).
 * Each entry may carry an optional edge_id linking to a relationships doc
 * (kind='touchstone') when the touchstone is a character; omitted for objects.
 */

function anchorFor(character) {
  return character?.clan === 'Ventrue' ? 7 : 6;
}

/**
 * Validate a touchstones[] array on a save body: cap, humanity-in-anchor-range,
 * and each edge_id (when present) points to an active relationships edge of
 * kind='touchstone' with this character on one endpoint.
 * Returns null on success or an error message string.
 */
async function validateTouchstones(touchstones, characterId, currentCharDoc) {
  if (!Array.isArray(touchstones)) return null;
  if (touchstones.length > 6) {
    return `touchstones cap is 6 (received ${touchstones.length})`;
  }

  const anchor = anchorFor(currentCharDoc);
  const minRating = Math.max(1, anchor - 5);
  for (const t of touchstones) {
    if (!Number.isInteger(t?.humanity) || t.humanity < minRating || t.humanity > anchor) {
      return `touchstone humanity ${t?.humanity} out of range (anchor=${anchor}, min=${minRating})`;
    }
  }

  const edgeIds = touchstones
    .map(t => t?.edge_id)
    .filter(id => typeof id === 'string' && id.length > 0);
  if (edgeIds.length === 0) return null;

  const oids = [];
  for (const id of edgeIds) {
    try { oids.push(new ObjectId(id)); }
    catch { return `touchstone edge_id '${id}' is not a valid id`; }
  }
  const rels = getCollection('relationships');
  const edges = await rels.find({ _id: { $in: oids } }).toArray();
  const foundById = new Map(edges.map(e => [String(e._id), e]));
  const charIdStr = String(characterId);
  for (const id of edgeIds) {
    const edge = foundById.get(String(id));
    if (!edge) return `touchstone edge '${id}' not found`;
    if (edge.kind !== 'touchstone') return `edge '${id}' is kind='${edge.kind}', expected 'touchstone'`;
    if (edge.status === 'retired') return `edge '${id}' is retired`;
    const onEdge =
      (edge.a?.type === 'pc' && String(edge.a.id) === charIdStr) ||
      (edge.b?.type === 'pc' && String(edge.b.id) === charIdStr);
    if (!onEdge) return `edge '${id}' does not have this character as an endpoint`;
  }
  return null;
}

/**
 * Attach _npc_name on each touchstones[] entry that carries an edge_id,
 * so the client can render the linked NPC's name without a separate fetch.
 * Edges that are retired or (for player callers) st_hidden are treated as
 * missing — no name attached; the client still renders the entry via its
 * inline name field.
 */
async function enrichTouchstoneNpcNames(chars, { forPlayer } = {}) {
  const allEdgeOids = [];
  for (const c of chars) {
    for (const t of (c.touchstones || [])) {
      if (typeof t?.edge_id === 'string' && t.edge_id.length > 0) {
        try { allEdgeOids.push(new ObjectId(t.edge_id)); } catch { /* skip */ }
      }
    }

// GET /api/characters/:id — ST gets any, player gets only their own
router.get('/:id', async (req, res) => {
  const oid = parseId(req.params.id);
  if (!oid) return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Invalid character ID format' });

  // Player: check they own this character
  if (req.user.role === 'player') {
    const owns = (req.user.character_ids || []).some(id => id.toString() === oid.toString());
    if (!owns) return res.status(403).json({ error: 'FORBIDDEN', message: 'Not your character' });
  }

  const char = await col().findOne({ _id: oid });
  if (!char) return res.status(404).json({ error: 'NOT_FOUND', message: 'Character not found' });

  const forPlayer = req.user.role === 'player';
  await enrichTouchstoneNpcNames([char], { forPlayer });
  res.json(char);
});

// POST /api/characters/wizard — player creates their own character
router.post('/wizard', requireRole('player'), stripEphemeral, validateCharacter, normalizeMeritsMiddleware, validateWhiteAntsTerritoriesMiddleware, validateTrapDoorAnchorMiddleware, async (req, res) => {
  const players = getCollection('players');
  const player = await players.findOne({ _id: req.user.player_id });
  const existingIds = player?.character_ids || [];

  // First character is auto-approved; subsequent characters await ST sign-off
  const isFirst = existingIds.length === 0;

  const doc = { ...req.body };
  delete doc._id;
  doc.pending_approval = !isFirst;
  doc.retired = false;
  doc.created_at = new Date().toISOString();

  const result = await col().insertOne(doc);
  const created = await col().findOne({ _id: result.insertedId });

  // Link to player record
  await players.updateOne(
    { _id: req.user.player_id },
    { $push: { character_ids: result.insertedId } }
  );

  res.status(201).json(created);
});

// POST /api/characters — ST only
router.post('/', requireRole('st'), stripEphemeral, validateCharacter, normalizeMeritsMiddleware, validateWhiteAntsTerritoriesMiddleware, validateTrapDoorAnchorMiddleware, async (req, res) => {
  const doc = req.body;
  if (!doc || !doc.name) return res.status(400).json({ error: 'VALIDATION_ERROR', message: "Field 'name' is required" });

  const result = await col().insertOne(doc);
  const created = await col().findOne({ _id: result.insertedId });
  res.status(201).json(created);
});

// PUT /api/characters/:id — ST only
// Uses partial schema validation: types/shapes checked but no field is required,
// so both full document saves and partial updates (e.g. regent assignment) are valid.
router.put('/:id', requireRole('st'), stripEphemeral, validateCharacterPartial, normalizeMeritsMiddleware, validateWhiteAntsTerritoriesMiddleware, validateTrapDoorAnchorMiddleware, async (req, res) => {
  const oid = parseId(req.params.id);
  if (!oid) return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Invalid character ID format' });

  const { _id, willpower, ...updates } = req.body;

  // ECM-3 (#870): hydrate equipment[].catalogue_id 24-hex strings back to
  // ObjectId before $set. The schema validation upstream already enforces
  // the 24-hex shape; the ObjectId.isValid guard is defensive — if a stray
  // bad value slips through, return 400 rather than throwing in $set.
  if (Array.isArray(updates.equipment)) {
    const hydrated = [];
    for (const item of updates.equipment) {
      if (!item || typeof item !== 'object') {
        return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'equipment[] items must be objects' });
      }
      const cid = item.catalogue_id;
      if (typeof cid === 'string' && ObjectId.isValid(cid) && String(new ObjectId(cid)) === cid) {
        hydrated.push({ ...item, catalogue_id: new ObjectId(cid) });
      } else if (cid instanceof ObjectId) {
        hydrated.push(item);
      } else {
        return res.status(400).json({
          error: 'VALIDATION_ERROR',
          message: `equipment[].catalogue_id must be a 24-hex ObjectId string; got ${typeof cid === 'string' ? `'${cid}'` : typeof cid}`,
        });
      }
    }
    updates.equipment = hydrated;
  }

  // NPCR.4: if the save includes touchstones[], validate cap, humanity-in-range,
  // and each embedded edge_id points to a valid touchstone relationship for this char.
  if (Object.prototype.hasOwnProperty.call(updates, 'touchstones')) {
    const existingChar = await col().findOne({ _id: oid }, { projection: { clan: 1 } });
    const effectiveChar = { ...existingChar, ...updates }; // updates may also change clan
    const err = await validateTouchstones(updates.touchstones, oid, effectiveChar);
    if (err) return res.status(400).json({ error: 'VALIDATION_ERROR', message: err });
  }

  const result = await col().findOneAndUpdate(
    { _id: oid },
    { $set: updates },
    { returnDocument: 'after' }
  );

  if (!result) return res.status(404).json({ error: 'NOT_FOUND', message: 'Character not found' });
  res.json(result);
});

// GET /api/characters/:id/cascade-preview — ST only
router.get('/:id/cascade-preview', requireRole('st'), async (req, res) => {
  const oid = parseId(req.params.id);
  if (!oid) return res.status(400).json({ error: 'VALIDATION_ERROR', message: 'Invalid character ID format' });
  try {
    const [submissions, sessionsAffected, players] = await Promise.all([
      getCollection('downtime_submissions').countDocuments({ character_id: oid }),
      getCollection('game_sessions').countDocuments({ 'attendance.character_id': oid }),
      getCollection('players').countDocuments({ character_ids: oid }),
    ]);
    res.json({ submissions, sessionsAffected, players });

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:42:15.633242Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$schema = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\server\\schemas\\character.schema.js'; "'$schema[300..370]; $schema[680..715]' in D:\Terra Mortis\TM Suite
 succeeded in 2873ms:
        updated_at:               { type: ['string', 'null'] },
      },
      additionalProperties: false,
    },

    // ── Influence balance (monthly income accumulator) ────────
    influence_balance: { type: 'number', minimum: 0 },

    // ── Equipment (EQ-1, issue #654; ECM-3 #870 — catalogue_id ObjectId) ──
    // Lean refs into the equipment_catalogue collection — full stats resolved
    // at render time. `catalogue_id` is an ObjectId on disk and a 24-hex
    // string on the wire (the standard JSON serialisation). Server-side
    // coercion at write sites (PUT /:id, POST /:id/equipment) hydrates the
    // wire string back to an ObjectId before $set — see
    // specs/epic-ecm-equipment-catalogue-migration.md and Khepri's ECM-3
    // dispatch for the rationale (the coercion is canonical Express+Mongo
    // hygiene, not transitional kludge — it stays after ECM-4/5 ship).
    //
    // `container_id` (EQC-1, issue #1152, epic #1038) — null/absent means
    // this item is loose (carried on the character, not stored inside
    // anything). When set, it is the `catalogue_id` of ANOTHER entry in this
    // SAME character's `equipment[]` array whose catalogue bucket is
    // `container` (a haven, vehicle, safe, etc.) — "a property contains a
    // security system... never a bonus on the asset". Single-level only for
    // v1 (a container's own contents are never themselves container-eligible
    // targets) — the epic's own examples (a safe inside a haven) are all
    // single-level, and recursive containment is real added complexity with
    // no stated requirement yet; a future story can lift this if actually
    // needed. Not validated as a REFERENCE here (schema has no cross-item
    // lookup) — the write route is responsible for confirming the referenced
    // entry exists in the same array and resolves to a container-bucket
    // catalogue item; an unresolvable/stale container_id is display-inert
    // (renders as loose) rather than a write-time hard failure, matching
    // this schema's existing catalogue_id-is-a-lean-ref philosophy.
    equipment: {
      type: 'array',
      default: [],
      items: {
        type: 'object',
        required: ['catalogue_id', 'state', 'acquired_cycle'],
        properties: {
          catalogue_id:    { type: 'string', pattern: '^[a-f0-9]{24}$' },
          state:           { type: 'string', enum: ['carried', 'worn', 'stashed', 'lost', 'active'] },
          acquired_cycle:  { type: 'integer', minimum: 0 },
          notes:           { type: ['string', 'null'] },
          container_id:    { type: ['string', 'null'], pattern: '^[a-f0-9]{24}$' },
        },
        additionalProperties: false,
      },
    },

    // ── Assets ────────────────────────────────────────────────
    // REMOVED 2026-06-19 — consolidated into equipment[] via the catalogue's
    // bucket: 'asset' items (Vehicle (Luxury), Safe House, etc.). The previous
    // free-text assets[] storage shape collided with the catalogue's 'asset'
    // bucket name and split asset-class items across two arrays. Equipment[]
    // is the single home for all bucket types now. See chat 2026-06-19.

    // ── XP log ────────────────────────────────────────────────
    xp_log: {
      type: 'object',
      properties: {
        earned: { type: 'object', additionalProperties: { type: 'number' } },
        spent:  { type: 'object', additionalProperties: { type: 'number' } }
      },
      additionalProperties: false
    }
  },

  // ── Definitions ─────────────────────────────────────────────────────────────

    fightingStyle: {
      type: 'object',
      required: ['name'],
      properties: {
        name:      { type: 'string', minLength: 1 },
        type:      { type: 'string', enum: ['style', 'merit'] },
        cp:        { type: 'integer', minimum: 0 },
        xp:        { type: 'integer', minimum: 0 },
        free:      { type: 'integer', minimum: 0 },
        free_mci:  { type: 'integer', minimum: 0 },
        free_ots:  { type: 'integer', minimum: 0 },
        // Legacy field from Excel import — tolerated
        up:        { type: 'integer', minimum: 0 },
        // Legacy per-style picks — tolerated during migration to fighting_picks
        picks:     { type: 'array', items: { type: 'string' } },
        // v3: reference to purchasable_powers key
        rule_key:  { type: ['string', 'null'] }
      },
      additionalProperties: false
    }
  }
};

/** Partial schema for PUT — validates types/shapes but no field is required. */
export const characterPartialSchema = derivePartialSchema(characterSchema);

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:42:24.386092Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$files = @('"'D:\\Terra Mortis\\TM Suite\\public\\js\\data\\equipment-derivation.js','D:\\Terra Mortis\\TM Suite\\public\\js\\suite\\roll.js','D:\\Terra Mortis\\TM Suite\\public\\js\\suite\\roll-v2.js'); foreach ("'$file in $files) { Write-Output "FILE $file"; Get-Content -LiteralPath $file | Select-Object -First 380 }' in D:\Terra Mortis\TM Suite
 succeeded in 2861ms:
FILE D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js
/**
 * equipment-derivation.js — armour penalty + derived-defence materialisation.
 *
 * Issue #879 (ADR-006). Lifts EQ-1's "calcDefence does not read the equipment
 * array without an ADR" gate. ADR-006 specifies:
 *
 *   D1: armourDefencePenalty(c, catalogueLookup?) reads c.equipment[] filtered
 *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
 *       injectable catalogue lookup (default: the ECM-5 cache reader).
 *
 *       EQC-1 (issue #1152, epic #1038, 2026-08-13): the old `armour` bucket
 *       merged into `combat_gear` (which also covers weapons). "Armour-shaped"
 *       is identified by `entry.armour_value != null` rather than by bucket
 *       alone — the epic's own "distinct stat fields" distinction within the
 *       single combat_gear bucket. A weapon-shaped combat_gear entry has
 *       `armour_value: null` and is correctly excluded.
 *   D2: worst-case math — Math.max(...penalties). The editor surfaces a soft
 *       hint when >1 armour is worn (wording is concern #8 below).
 *   D2-FLOOR: floor at 0 lives ONLY at the helper composition site. STM overlay
 *       composes additively on top per ADR-004's no-bounds contract; the
 *       sheet renderer adds NO further clamp.
 *   D3: composition order — calcDefence(c) → subtract helper → floor → applyStMods.
 *   D4: STM overlay composes on top of c.derived.defence (the armour-adjusted
 *       base materialised by this module's materialiseDerivedDefence).
 *   D5: pure helper, symmetric with discAttrBonus.
 *
 * Concern #2 from ADR-006: the per-item armour annotation in editor/sheet.js:2240
 * must KEEP calling raw calcDefence(c). Its display intent is "if you wore only
 * this item, defence would be X" — a hypothetical pre-armour baseline, NOT the
 * live armour-adjusted value. Don't migrate that read site.
 *
 * Concern #9 from ADR-006: NO redundant floor clamps anywhere else (not in
 * applyStMods, not in the sheet renderer). The clamp lives in exactly one
 * place — the materialisation call below.
 */

import { calcDefence } from './accessors.js';
import { getCatalogueEntry } from './equipment-catalogue-cache.js';
// #896: meritEffectiveRating respects free dots / cp / xp / bonus channels.
// Used to detect Resources cap and Fixer presence without rewriting the
// accessor chain.
import { meritEffectiveRating } from '../editor/domain.js';

/**
 * Sum-by-worst-case of defence penalties from currently-worn armour.
 * Returns a non-negative integer (the magnitude — composition site subtracts).
 *
 * Per ADR-006 D2: worst-case stacking. Math.max of all qualifying penalties;
 * 0 when no qualifying armour is worn. Negative defence_penalty values are
 * non-sensical for armour and are filtered out before combination.
 *
 * Per ADR-006 D1: state === 'worn' is the only state that contributes. The
 * positive predicate is preferred over a not-stashed/not-lost shape because
 * a future state-enum addition would silently affect a negative predicate
 * (Concern #3).
 *
 * @param {object} c - character document
 * @param {function} [catalogueLookup] - (id) => catalogue entry | undefined.
 *   Default: the ECM-5 cache reader. Tests inject a synthetic map.
 * @returns {number} non-negative integer penalty magnitude
 */
export function armourDefencePenalty(c, catalogueLookup = getCatalogueEntry) {
  if (!c || !Array.isArray(c.equipment)) return 0;
  const penalties = [];
  for (const item of c.equipment) {
    if (!item || item.state !== 'worn') continue;
    const entry = catalogueLookup(item.catalogue_id);
    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
    const p = Number.isInteger(entry.defence_penalty) ? entry.defence_penalty : 0;
    if (p > 0) penalties.push(p);
  }
  if (penalties.length === 0) return 0;
  return Math.max(...penalties);
}

/**
 * Count of armour items currently in state==='worn'. Drives the editor hint
 * surfaced when >1 armour is worn (ADR-006 D2 + Concern #8 — wording verbatim
 * in editor/sheet.js armour-section header).
 *
 * @param {object} c - character document
 * @param {function} [catalogueLookup]
 * @returns {number} count of worn armour items
 */
export function wornArmourCount(c, catalogueLookup = getCatalogueEntry) {
  if (!c || !Array.isArray(c.equipment)) return 0;
  let n = 0;
  for (const item of c.equipment) {
    if (!item || item.state !== 'worn') continue;
    const entry = catalogueLookup(item.catalogue_id);
    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
  }
  return n;
}

/**
 * Compute and materialise `c.derived.defence` per ADR-006 D3 + D4.
 *
 *   c.derived.defence = max(0, calcDefence(c) - armourDefencePenalty(c))
 *
 * This is the armour-adjusted base. applyStMods (called AFTER this, per the
 * render-path orchestrator) reads `c.derived.defence` as base, applies the
 * STM delta on top, and writes back — no longer the pre-ADR-006 bug where
 * applyStMods treated missing base as 0 (so STM mods on derived.defence
 * silently composed against 0 instead of the real mechanical base).
 *
 * Per ADR-006 D2-FLOOR + Concern #9: floor lives HERE, only HERE. No
 * defensive clamp in applyStMods or the sheet renderer.
 *
 * @param {object} c - character document (mutated)
 * @param {function} [catalogueLookup]
 * @returns {number} the materialised armour-adjusted defence (pre-overlay)
 */
export function materialiseDerivedDefence(c, catalogueLookup = getCatalogueEntry) {
  if (!c) return 0;
  const base = calcDefence(c);
  const penalty = armourDefencePenalty(c, catalogueLookup);
  const adjusted = Math.max(0, base - penalty);
  c.derived = c.derived || {};
  c.derived.defence = adjusted;
  return adjusted;
}

/**
 * Read-site helper: returns the modded c.derived.defence when it's been
 * materialised (the common case post-boot per the D8 cache-entry invariant);
 * falls back to a fresh armour-adjusted computation when not materialised
 * (edge cases — fresh chars, test contexts). The fallback is the same
 * computation materialiseDerivedDefence performs; the difference is whether
 * the result is cached on c.derived.defence or computed on the fly.
 *
 * Used by sheet.js, suite/sheet.js, roll calc, DT player pool, DT admin
 * resolution view — every consumer that previously called calcDefence(c)
 * directly migrates here.
 *
 * @param {object} c - character document
 * @param {function} [catalogueLookup]
 * @returns {number} displayed defence (overlay-modded if applicable)
 */
export function defenceForDisplay(c, catalogueLookup = getCatalogueEntry) {
  if (c && c.derived && typeof c.derived.defence === 'number') return c.derived.defence;
  if (!c) return 0;
  return Math.max(0, calcDefence(c) - armourDefencePenalty(c, catalogueLookup));
}

/**
 * Export-site helper: canonical mechanical defence (armour-adjusted, no
 * STM overlay). Always computes fresh — does NOT read c.derived.defence
 * which may carry an overlay-modded value at export time.
 *
 * Per ADR-004: STM overlays are scene-only adjustments and NOT canonical
 * character state. Character export (JSON / CSV / snapshot) captures the
 * mechanical baseline; overlays are a separate concern.
 *
 * @param {object} c - character document
 * @param {function} [catalogueLookup]
 * @returns {number} mechanical defence baseline (armour-adjusted)
 */
export function defenceMechanicalBase(c, catalogueLookup = getCatalogueEntry) {
  if (!c) return 0;
  return Math.max(0, calcDefence(c) - armourDefencePenalty(c, catalogueLookup));
}

// ─────────────────────────────────────────────────────────────────────────────
// #896 — Equipment availability filter + Fixer errata (Peter 2026-06-18).
//
// Composition is simple subtraction with no overlay/scene-state interaction
// (per Khepri dispatch — no ADR needed). The helpers below are pure: take
// (item, c) → number / boolean. Read sites use them at render time; DT form
// uses them to gate dropdown affordability.
//
// Fixer errata: the 2-dot Social merit Fixer reduces the availability cost
// of all items by 1. Applies in all usage and displays — dropdown, sheet
// held-items, admin editor row labels (the admin editor BYPASSES the
// affordability gate but still SHOWS the reduced number for consistency).
//
// Out of scope (Peter / Khepri): admin catalogue page (ECM-6) shows raw
// catalogue availability; it is character-agnostic.
// ─────────────────────────────────────────────────────────────────────────────

/**
 * Resources cap = effective rating of the character's Resources merit, or 0
 * if absent. Uses meritEffectiveRating so cp / xp / free / bonus channels
 * compose correctly without re-walking the merit array.
 *
 * @param {object} c - character document
 * @returns {number} resources rating (0-5)
 */
export function availabilityCap(c) {
  if (!c) return 0;
  const m = (c.merits || []).find(x => x?.name === 'Resources');
  return m ? meritEffectiveRating(c, m) : 0;
}

/**
 * Fixer reduction = 1 if the character has Fixer merit with effective rating
 * >= 1, else 0. The Fixer merit is a fixed 2-dot Social merit per the rules,
 * but we gate on the effective-rating check rather than mere presence so a
 * suppressed / zeroed Fixer entry (edge case during merit editing) doesn't
 * spuriously grant the reduction.
 *
 * @param {object} c - character document
 * @returns {0 | 1} reduction magnitude
 */
export function fixerReduction(c) {
  if (!c) return 0;
  const m = (c.merits || []).find(x => x?.name === 'Fixer');
  if (!m) return 0;
  return meritEffectiveRating(c, m) >= 1 ? 1 : 0;
}

/**
 * Effective availability of a catalogue item for a specific character:
 *
 *   effectiveAvailability(item, c) = max(0, item.availability - fixerReduction(c))
 *
 * Floored at 0 so a level-0 item with Fixer doesn't surface as -1; the
 * floor lives here, no clamp needed at consumer sites (same single-floor
 * discipline as ADR-006 Concern #9).
 *
 * `item` may be a catalogue entry (post-ECM-1 shape with `availability` int
 * field) OR a partial; null/undefined item or missing availability defaults
 * to 0.
 *
 * @param {object} item - catalogue entry
 * @param {object} c - character document
 * @returns {number} effective availability (0-5)
 */
export function effectiveAvailability(item, c) {
  const raw = Number.isInteger(item?.availability) ? item.availability : 0;
  return Math.max(0, raw - fixerReduction(c));
}

/**
 * Convenience wrapper: is the item within the character's affordability gate?
 *
 *   isAffordable(item, c) = effectiveAvailability(item, c) <= availabilityCap(c)
 *
 * Used by the DT form dropdown to disable unaffordable options. The admin
 * character editor explicitly BYPASSES this gate (ST override) but still
 * displays the effective availability per Peter's dispatch.
 *
 * @param {object} item - catalogue entry
 * @param {object} c - character document
 * @returns {boolean}
 */
export function isAffordable(item, c) {
  return effectiveAvailability(item, c) <= availabilityCap(c);
}
FILE D:\Terra Mortis\TM Suite\public\js\suite\roll.js
// ══════════════════════════════════════════════
//  Roll Tab UI — pool display, modifiers, dice animation, history
// ══════════════════════════════════════════════

import state from './data.js';

import { d10, mkDie, mkChain, rollPool, cntSuc } from '../shared/dice.js';
import { skSpecs, skNineAgain } from '../data/accessors.js';
import { hasAoE } from '../data/helpers.js';
import { getCatalogueEntry } from '../data/equipment-catalogue-cache.js';

// ── Imports from other suite modules (will exist once extracted) ──
// showResistSec / updResist live in shared/resist.js
import { showResistSec, updResist } from '../shared/resist.js';

// ── DOM helpers (will be provided by a ui module or inlined) ──
function closePanel() {
  const overlay = document.getElementById('panel-overlay');
  if (overlay) overlay.classList.remove('on');
}

function toast(msg) {
  const t = document.getElementById('toast');
  if (!t) return;
  t.textContent = msg;
  t.classList.add('on');
  clearTimeout(toast._timer);
  toast._timer = setTimeout(() => t.classList.remove('on'), 2500);
}

// ── LOAD POOL ──

export function loadPool(total, name, pi) {
  state.PS = Math.max(0, total);
  state.MOD = 0;
  state.specBonuses = {};
  state.activeEquipBonus = null;
  state.activeWeaponId = null;
  state.POOL_INFO = pi || null;
  // Auto-set 9-Again when the pool source grants it
  if (pi?.nineAgain) setAgain(9);
  else setAgain(10);
  // Reset Rote — player toggles manually (PT dot-5 costs 1 WP)
  if (state.ROTE) togMod('rote');
  showResistSec();
  updPool();
  const banner = document.getElementById('pool-banner');
  const cn = state.rollChar ? state.rollChar.name.split(' ')[0] : '';
  banner.textContent = cn ? cn + ' \u00B7 ' + name + ' \u00B7 ' + total + 'd' : name + ' \u00B7 ' + total + 'd';
  banner.classList.add('on');
  document.getElementById('sc-disc-lbl').textContent = '';
  document.getElementById('sc-disc-val').textContent = name;
  document.getElementById('sc-disc').classList.add('loaded');
  closePanel();
}

// ── EFFECTIVE POOL ──

export function effPool() {
  const wpBonus = state.WP ? 3 : 0;
  return state.RESIST_MODE === '-'
    ? Math.max(0, state.PS + state.MOD + wpBonus - state.RESIST_VAL)
    : state.PS + state.MOD + wpBonus;
}

// ── POOL / MOD ADJUSTMENTS ──

export function chgPool(d) {
  state.PS = Math.max(-5, Math.min(40, state.PS + d));
  updPool();
}

export function chgMod(d) {
  state.MOD = Math.max(-10, Math.min(10, state.MOD + d));
  updPool();
}

// ── UPDATE POOL DISPLAY ──

export function updPool() {
  const eff = effPool();
  const pv = document.getElementById('pval');
  pv.textContent = state.PS <= 0 ? 'Chance' : state.PS;
  pv.className = 'cval' + (state.PS <= 0 ? ' chance' : '');

  const mv = document.getElementById('mval');
  const mod = state.MOD;
  mv.textContent = mod === 0 ? '0' : mod > 0 ? '+' + mod : mod;
  mv.className = 'bval' + (mod > 0 ? ' pos' : mod < 0 ? ' neg' : '');

  const el = document.getElementById('effline');
  const pi = state.POOL_INFO;

  if (!pi || !pi.attr) {
    if (eff <= 0) {
      el.innerHTML = 'Effective pool: <span class="effpool-seg effpool-seg--neg">Chance die</span>';
    } else {
      el.innerHTML = 'Effective pool: <span>' + eff + (eff === 1 ? ' die' : ' dice') + '</span>';
    }
    return;
  }

  const segs = [];
  if (pi.attr) segs.push('<span class="effpool-seg">' + pi.attr + ' <b>' + pi.attrV + '</b></span>');
  if (pi.skill) segs.push('<span class="effpool-seg">' + pi.skill + ' <b>' + pi.skillV + '</b></span>');
  if (pi.unskilled) segs.push('<span class="effpool-seg effpool-seg--neg">unskilled <b>' + pi.unskilled + '</b></span>');
  if (pi.discName && pi.discV) segs.push('<span class="effpool-seg">' + pi.discName + ' <b>' + pi.discV + '</b></span>');
  if (pi.meritBonus && pi.meritLabel) segs.push('<span class="effpool-seg effpool-seg--merit">' + pi.meritLabel + ' <b>+' + pi.meritBonus + '</b></span>');
  if (pi.roteEligible && !state.ROTE) segs.push('<span class="effpool-seg effpool-seg--rote" onclick="togMod(\'rote\')" title="PT dot 5: spend 1 WP for Rote quality">Rote \u2713</span>');
  if (state.WP) segs.push('<span class="effpool-seg effpool-seg--wp">WP <b>+3</b></span>');
  if (state.RESIST_MODE === '-' && state.RESIST_VAL > 0) {
    segs.push('<span class="effpool-seg effpool-seg--resist">\u2212Resist <b>' + state.RESIST_VAL + '</b></span>');
  }

  let html = segs.join('<span class="effpool-sep"> + </span>');

  if (pi.skill && state.rollChar) {
    const rc = state.rollChar;
    const specs = skSpecs(rc, pi.skill);
    const na = skNineAgain(rc, pi.skill);
    if (specs.length) {
      html += '<div class="effpool-specs">' + specs.map(s => {
        const aoe = hasAoE(rc, s);
        const bonusN = na || aoe ? 2 : 1;
        const bonusLbl = na ? '2 (9-again)' : aoe ? '2 (AoE)' : '1';
        const isOn = state.specBonuses[s] !== undefined;
        const cls = 'effpool-spec' + (isOn ? ' on' : '');
        const safe = String(s).replace(/"/g, '&quot;');
        return `<span class="${cls}" data-spec="${safe}" data-bonus="${bonusN}" `
             + `onclick="togSpec(this)" title="Click to add this specialty's bonus to your modifier">`
             + `${s} <span class="effpool-spec-bonus">+${bonusLbl}</span></span>`;
      }).join('') + '</div>';
    }
  }

  // Equipment bonus chips (EQ-3): one-active-at-a-time gear bonuses
  if (pi.skill && state.rollChar) {
    const rc = state.rollChar;
    const equip = (rc.equipment || []).filter(item => {
      const entry = getCatalogueEntry(item.catalogue_id);
      // #752: 'active' is the strongest in-use state — treat it identically
      // to carried/worn for chip eligibility (Khepri's option (a) — preserves
      // 'active' as a semantically-stronger marker an ST may have already used).
      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
      return entry && entry.bucket === 'skill_gear' &&
             entry.bonus_dice > 0 &&
             entry.skill_domain === pi.skill &&
             (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
    });
    if (equip.length) {
      html += '<div class="effpool-specs">' + equip.map(item => {
        const entry = getCatalogueEntry(item.catalogue_id);
        const isOn = state.activeEquipBonus &&
                     state.activeEquipBonus.catalogueId === entry.id;
        const cls = 'effpool-spec' + (isOn ? ' on' : '');
        const safe = String(entry.id).replace(/"/g, '&quot;');
        return `<span class="${cls}" data-equip="${safe}" data-bonus="${entry.bonus_dice}" `
             + `onclick="togEquipChip(this)" title="${entry.name}">`
             + `${entry.name} <span class="effpool-spec-bonus">+${entry.bonus_dice}</span></span>`;
      }).join('') + '</div>';
    }
  }

  el.innerHTML = html;
  updWeaponRef();
}

// ── SPECIALTY TOGGLE ──
//
// Click handler for the specialty badges under the effective-pool line.
// Toggling a badge on adds its dice bonus to MOD; toggling off subtracts
// the same amount. The per-spec bonus is stored in state.specBonuses so
// we can reverse the exact amount even if the displayed bonus would be
// different at toggle-off time (e.g. character data changed mid-roll).

export function togSpec(badge) {
  if (!badge) return;
  const name = badge.dataset.spec;
  const bonus = parseInt(badge.dataset.bonus, 10) || 0;
  if (!name || !bonus) return;
  const existing = state.specBonuses[name];
  if (existing !== undefined) {
    state.MOD = state.MOD - existing;
    delete state.specBonuses[name];
  } else {
    state.MOD = state.MOD + bonus;
    state.specBonuses[name] = bonus;
  }
  updPool();
}

// ── EQUIPMENT CHIP TOGGLE (EQ-3) ──

export function togEquipChip(badge) {
  if (!badge) return;
  const id = badge.dataset.equip;
  const bonus = parseInt(badge.dataset.bonus, 10) || 0;
  if (!id || !bonus) return;

  if (state.activeEquipBonus && state.activeEquipBonus.catalogueId === id) {
    state.MOD -= state.activeEquipBonus.bonus;
    state.activeEquipBonus = null;
  } else {
    if (state.activeEquipBonus) {
      state.MOD -= state.activeEquipBonus.bonus;
    }
    state.MOD += bonus;
    state.activeEquipBonus = { catalogueId: id, bonus };
  }
  updPool();
}

// ── WEAPON REFERENCE PANEL (EQ-3) ──

const COMBAT_SKILLS = ['Weaponry', 'Brawl', 'Firearms', 'Archery'];

export function updWeaponRef() {
  const panel = document.getElementById('weapon-ref');
  if (!panel) return;

  // Capture live selection before rebuilding innerHTML
  const liveSel = document.getElementById('weapon-sel');
  if (liveSel && liveSel.value) state.activeWeaponId = liveSel.value;
  else if (liveSel && !liveSel.value) state.activeWeaponId = null;

  const pi = state.POOL_INFO;
  if (!pi || !pi.skill || !COMBAT_SKILLS.includes(pi.skill) || !state.rollChar) {
    panel.style.display = 'none';
    return;
  }
  const weapons = (state.rollChar.equipment || []).filter(item => {
    // #752: 'active' is the strongest in-use state — include it in weapon-ref
    // eligibility (matches the chip predicate above).
    const entry = getCatalogueEntry(item.catalogue_id);
    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
    // is identified by weapon_type presence (armour-shaped entries leave it null).
    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
           (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
  });
  if (!weapons.length) {
    panel.style.display = 'none';
    return;
  }

  panel.style.display = '';
  const prevId = state.activeWeaponId;

  const optionsHtml = weapons.map(item => {
    const e = getCatalogueEntry(item.catalogue_id);
    const sel = prevId === e.id ? ' selected' : '';
    return `<option value="${e.id}"${sel}>${e.name}</option>`;
  }).join('');

  let statsHtml = '';
  if (prevId) {
    const e = getCatalogueEntry(prevId);
    if (e) {
      const DMGTYPE = { lethal: 'Lethal', bashing: 'Bashing', aggravated: 'Aggravated' };
      const WPNTYPE = { melee: 'Melee', ranged: 'Ranged', thrown: 'Thrown' };
      const mod = e.damage_mod > 0 ? '+' + e.damage_mod : String(e.damage_mod);
      statsHtml = `<div class="trait-qual">`
               + `${mod} · ${DMGTYPE[e.damage_type] || e.damage_type} · ${WPNTYPE[e.weapon_type] || e.weapon_type}`
               + `</div>`;
    }
  }

  panel.innerHTML = `<div class="slabel">Weapon</div>`
    + `<select id="weapon-sel" class="resist-sel" onchange="updWeaponRef()">`
    + `<option value="">-- select --</option>${optionsHtml}</select>${statsHtml}`;
}

// ── AGAIN / MODIFIER TOGGLES ──

export function setAgain(v) {
  state.AGAIN = v;
  [8, 9].forEach(n => {
    const el = document.getElementById('a' + n);
    if (el) el.classList.toggle('on', n === v);
  });
}

export function togMod(m) {
  if (m === 'rote') {
    state.ROTE = !state.ROTE;
    document.getElementById('rote-c').classList.toggle('on', state.ROTE);
  } else if (m === 'wp') {
    state.WP = !state.WP;
    document.getElementById('wp-c').classList.toggle('on', state.WP);
    updPool();
  } else {
    state.NA = !state.NA;
    document.getElementById('na-c').classList.toggle('on', state.NA);
  }
}

// ── DIE DOM ELEMENTS ──

export function mkDieEl(d, delay, isX) {
  const el = document.createElement('div');
  // Exploding re-roll dice use the same success/fail colour as initial dice,
  // plus an 'ed' marker class for the chain connector styling.
  let cls = d.s ? 'die sd' : 'die fd';
  if (isX) cls += ' ed';
  if (d.x) cls += ' xd';
  el.className = cls;
  el.textContent = d.v;
  el.style.animationDelay = (delay * 40) + 'ms';
  return el;
}

export function mkColsEl(cols, base) {
  const w = document.createElement('div');
  w.style.cssText = 'display:flex;flex-wrap:wrap;gap:5px;';
  let delay = base;
  cols.forEach(col => {
    const ce = document.createElement('div');
    ce.className = 'dcol';
    ce.appendChild(mkDieEl(col.r, delay++, false));
    col.ch.forEach(child => {
      const conn = document.createElement('div');
      conn.className = 'xconn';
      ce.appendChild(conn);
      ce.appendChild(mkDieEl(child, delay++, true));
    });
    w.appendChild(ce);
  });
  return w;
}

// ── MAIN ROLL ──

export function doRoll() {
  const eff = effPool();
  const area = document.getElementById('dice-area');
  const hdr = document.getElementById('res-hdr');
  area.innerHTML = '';
  hdr.classList.remove('on');

  if (eff <= 0) {
    const v = d10();
    const suc = v === 10;
    const dram = v === 1;
    const cls = dram ? 'd' : suc ? 'e' : 'f';
    const lbl = dram ? 'Dramatic Failure' : suc ? 'Success (Chance)' : 'Failure (Chance)';
    const cnt = dram ? '\u2014' : suc ? '1' : '0';
    hdr.innerHTML = `<div><span class="rcnt ${cls}">${cnt}</span><span class="rlbl ${cls}">${lbl}</span></div><div class="rverd">Chance die</div>`;
    hdr.classList.add('on');
    const del = document.createElement('div');
    del.className = 'die cd';
    del.textContent = v;
    area.appendChild(del);
    addHist('Chance', cls, lbl, cnt, 'Chance die');
    return;
  }

  const cA = rollPool(eff);
  const cB = state.ROTE ? rollPool(eff) : null;
  const sA = cntSuc(cA);
  const sB = state.ROTE ? cntSuc(cB) : 0;
  let wC, wS, lC, lS;

  if (state.ROTE && sB > sA) {
    wC = cB; wS = sB; lC = cA; lS = sA;
  } else {
    wC = cA; wS = sA;
    if (state.ROTE) { lC = cB; lS = sB; }
  }

  const mods = [];
  if (state.WP) mods.push('WP +3');
  if (state.ROTE) mods.push('rote');
  if (state.NA) mods.push('no again');

  const pi = state.POOL_INFO;
  const verdParts = [];
  if (pi && pi.attr) {
    if (pi.attr) verdParts.push(pi.attr + ' ' + pi.attrV);
    if (pi.skill) verdParts.push(pi.skill + ' ' + pi.skillV);
    if (pi.unskilled) verdParts.push('unskilled ' + pi.unskilled);
    if (pi.discName && pi.discV) verdParts.push(pi.discName + ' ' + pi.discV);
FILE D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js
// ══════════════════════════════════════════════
//  Roll Tab UI (v2 — parallel dev surface, #1018)
//
//  Gated by the `tm-use-new-dice-roller` localStorage flag. Started as
//  a byte-identical copy of ./roll.js.
//
//  Slice A+D (#1024) — divergence from roll.js:
//    A. Anchor number = `effPool()` (the count you'll ACTUALLY roll),
//       painted into `#rv2-eff` / `#rv2-eff-unit`, with a
//       always-visible sub-line at `#rv2-sub`
//       ("base 8 · +2 mod · 9-again"). The full skill-breakdown is
//       still written into `#effline` — moved inside a
//       `<details>` disclosure in the markup.
//    D. `setAgainSeg(v)` drives one exclusive segmented Again pill
//       ("10" / "9" / "8" / "None") that maps to `state.AGAIN` +
//       `state.NA`; replaces the old `#a8` / `#a9` / `#na-c` trio.
//       `setAgain()` is kept as an export (loadPool calls it) and
//       delegates to `setAgainSeg`.
//
//  Rote / WP chips are unchanged (they compose with Again).
// ══════════════════════════════════════════════

import state from './data.js';

import { d10, mkDie, mkChain, rollPool, cntSuc } from '../shared/dice.js';
import { skSpecs, skNineAgain } from '../data/accessors.js';
import { hasAoE } from '../data/helpers.js';
import { getCatalogueEntry } from '../data/equipment-catalogue-cache.js';

// ── Imports from other suite modules (will exist once extracted) ──
// showResistSec / updResist live in shared/resist.js
import { showResistSec, updResist } from '../shared/resist.js';

// ── DOM helpers (will be provided by a ui module or inlined) ──
function closePanel() {
  const overlay = document.getElementById('panel-overlay');
  if (overlay) overlay.classList.remove('on');
}

function toast(msg) {
  const t = document.getElementById('toast');
  if (!t) return;
  t.textContent = msg;
  t.classList.add('on');
  clearTimeout(toast._timer);
  toast._timer = setTimeout(() => t.classList.remove('on'), 2500);
}

// ── LOAD POOL ──

export function loadPool(total, name, pi) {
  state.PS = Math.max(0, total);
  state.MOD = 0;
  state.specBonuses = {};
  state.activeEquipBonus = null;
  state.activeWeaponId = null;
  state.POOL_INFO = pi || null;
  // Auto-set 9-Again when the pool source grants it
  if (pi?.nineAgain) setAgain(9);
  else setAgain(10);
  // Reset Rote — player toggles manually (PT dot-5 costs 1 WP)
  if (state.ROTE) togMod('rote');
  showResistSec();
  updPool();
  const banner = document.getElementById('pool-banner');
  const cn = state.rollChar ? state.rollChar.name.split(' ')[0] : '';
  banner.textContent = cn ? cn + ' \u00B7 ' + name + ' \u00B7 ' + total + 'd' : name + ' \u00B7 ' + total + 'd';
  banner.classList.add('on');
  document.getElementById('sc-disc-lbl').textContent = '';
  document.getElementById('sc-disc-val').textContent = name;
  document.getElementById('sc-disc').classList.add('loaded');
  closePanel();
}

// ── EFFECTIVE POOL ──

export function effPool() {
  const wpBonus = state.WP ? 3 : 0;
  return state.RESIST_MODE === '-'
    ? Math.max(0, state.PS + state.MOD + wpBonus - state.RESIST_VAL)
    : state.PS + state.MOD + wpBonus;
}

// ── POOL / MOD ADJUSTMENTS ──

export function chgPool(d) {
  state.PS = Math.max(-5, Math.min(40, state.PS + d));
  updPool();
}

export function chgMod(d) {
  state.MOD = Math.max(-10, Math.min(10, state.MOD + d));
  updPool();
}

// ── UPDATE POOL DISPLAY ──

// Human-readable "again" phrase for the anchor sub-line (slice A).
function _againPhrase() {
  if (state.NA) return 'no again';
  return String(state.AGAIN) + '-again';
}

// Paint the segmented Again pill so its `on` class tracks state.AGAIN + NA
// (slice D). Safe to call when the container isn't in the DOM (v1 tab).
function _paintAgainSeg() {
  const seg = document.getElementById('rv2-again-seg');
  if (!seg) return;
  const buttons = seg.querySelectorAll('button[data-again]');
  buttons.forEach(b => {
    const val = b.dataset.again;
    const on = val === 'none'
      ? state.NA
      : (!state.NA && String(state.AGAIN) === val);
    b.classList.toggle('on', on);
  });
}

export function updPool() {
  const eff = effPool();

  // v2 stepper values (small).
  const pv = document.getElementById('pval');
  if (pv) {
    pv.textContent = state.PS <= 0 ? 'Chance' : state.PS;
    pv.className = 'rv2-stepper-val' + (state.PS <= 0 ? ' chance' : '');
  }
  const mv = document.getElementById('mval');
  if (mv) {
    const mod = state.MOD;
    mv.textContent = mod === 0 ? '0' : mod > 0 ? '+' + mod : mod;
    mv.className = 'rv2-stepper-val' + (mod > 0 ? ' pos' : mod < 0 ? ' neg' : '');
  }

  // v2 anchor: huge effective count + sub-line (slice A).
  const effEl = document.getElementById('rv2-eff');
  const unitEl = document.getElementById('rv2-eff-unit');
  const subEl = document.getElementById('rv2-sub');
  const rollBtn = document.getElementById('roll-btn');
  const isChance = eff <= 0;
  if (effEl) {
    effEl.textContent = isChance ? 'CHANCE' : String(eff);
    effEl.classList.toggle('chance', isChance);
  }
  if (unitEl) {
    unitEl.textContent = isChance ? 'die' : (eff === 1 ? 'die' : 'dice');
  }
  if (subEl) {
    const parts = [
      'base ' + (state.PS <= 0 ? 'chance' : state.PS),
    ];
    if (state.MOD !== 0) parts.push((state.MOD > 0 ? '+' : '') + state.MOD + ' mod');
    parts.push(_againPhrase());
    if (state.WP) parts.push('WP +3');
    if (state.ROTE) parts.push('rote');
    if (state.RESIST_MODE === '-' && state.RESIST_VAL > 0) parts.push('−' + state.RESIST_VAL + ' resist');
    // U+00B7 middle-dot separator (matches sub-line separators elsewhere).
    subEl.textContent = parts.join(' · ');
  }
  if (rollBtn) {
    // ✦ = decorative four-point star.
    rollBtn.textContent = isChance ? '✦ ROLL CHANCE DIE' : ('✦ ROLL ' + eff + ' DICE');
  }

  _paintAgainSeg();

  const el = document.getElementById('effline');
  const pi = state.POOL_INFO;

  if (!pi || !pi.attr) {
    if (eff <= 0) {
      el.innerHTML = 'Effective pool: <span class="effpool-seg effpool-seg--neg">Chance die</span>';
    } else {
      el.innerHTML = 'Effective pool: <span>' + eff + (eff === 1 ? ' die' : ' dice') + '</span>';
    }
    return;
  }

  const segs = [];
  if (pi.attr) segs.push('<span class="effpool-seg">' + pi.attr + ' <b>' + pi.attrV + '</b></span>');
  if (pi.skill) segs.push('<span class="effpool-seg">' + pi.skill + ' <b>' + pi.skillV + '</b></span>');
  if (pi.unskilled) segs.push('<span class="effpool-seg effpool-seg--neg">unskilled <b>' + pi.unskilled + '</b></span>');
  if (pi.discName && pi.discV) segs.push('<span class="effpool-seg">' + pi.discName + ' <b>' + pi.discV + '</b></span>');
  if (pi.meritBonus && pi.meritLabel) segs.push('<span class="effpool-seg effpool-seg--merit">' + pi.meritLabel + ' <b>+' + pi.meritBonus + '</b></span>');
  if (pi.roteEligible && !state.ROTE) segs.push('<span class="effpool-seg effpool-seg--rote" onclick="togMod(\'rote\')" title="PT dot 5: spend 1 WP for Rote quality">Rote \u2713</span>');
  if (state.WP) segs.push('<span class="effpool-seg effpool-seg--wp">WP <b>+3</b></span>');
  if (state.RESIST_MODE === '-' && state.RESIST_VAL > 0) {
    segs.push('<span class="effpool-seg effpool-seg--resist">\u2212Resist <b>' + state.RESIST_VAL + '</b></span>');
  }

  let html = segs.join('<span class="effpool-sep"> + </span>');

  if (pi.skill && state.rollChar) {
    const rc = state.rollChar;
    const specs = skSpecs(rc, pi.skill);
    const na = skNineAgain(rc, pi.skill);
    if (specs.length) {
      html += '<div class="effpool-specs">' + specs.map(s => {
        const aoe = hasAoE(rc, s);
        const bonusN = na || aoe ? 2 : 1;
        const bonusLbl = na ? '2 (9-again)' : aoe ? '2 (AoE)' : '1';
        const isOn = state.specBonuses[s] !== undefined;
        const cls = 'effpool-spec' + (isOn ? ' on' : '');
        const safe = String(s).replace(/"/g, '&quot;');
        return `<span class="${cls}" data-spec="${safe}" data-bonus="${bonusN}" `
             + `onclick="togSpec(this)" title="Click to add this specialty's bonus to your modifier">`
             + `${s} <span class="effpool-spec-bonus">+${bonusLbl}</span></span>`;
      }).join('') + '</div>';
    }
  }

  // Equipment bonus chips (EQ-3): one-active-at-a-time gear bonuses
  if (pi.skill && state.rollChar) {
    const rc = state.rollChar;
    const equip = (rc.equipment || []).filter(item => {
      const entry = getCatalogueEntry(item.catalogue_id);
      // #752: 'active' is the strongest in-use state — treat it identically
      // to carried/worn for chip eligibility (Khepri's option (a) — preserves
      // 'active' as a semantically-stronger marker an ST may have already used).
      // EQC-1 (#1152): old 'equipment' bucket -> 'skill_gear', unchanged meaning.
      return entry && entry.bucket === 'skill_gear' &&
             entry.bonus_dice > 0 &&
             entry.skill_domain === pi.skill &&
             (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
    });
    if (equip.length) {
      html += '<div class="effpool-specs">' + equip.map(item => {
        const entry = getCatalogueEntry(item.catalogue_id);
        const isOn = state.activeEquipBonus &&
                     state.activeEquipBonus.catalogueId === entry.id;
        const cls = 'effpool-spec' + (isOn ? ' on' : '');
        const safe = String(entry.id).replace(/"/g, '&quot;');
        return `<span class="${cls}" data-equip="${safe}" data-bonus="${entry.bonus_dice}" `
             + `onclick="togEquipChip(this)" title="${entry.name}">`
             + `${entry.name} <span class="effpool-spec-bonus">+${entry.bonus_dice}</span></span>`;
      }).join('') + '</div>';
    }
  }

  el.innerHTML = html;
  updWeaponRef();
}

// ── SPECIALTY TOGGLE ──
//
// Click handler for the specialty badges under the effective-pool line.
// Toggling a badge on adds its dice bonus to MOD; toggling off subtracts
// the same amount. The per-spec bonus is stored in state.specBonuses so
// we can reverse the exact amount even if the displayed bonus would be
// different at toggle-off time (e.g. character data changed mid-roll).

export function togSpec(badge) {
  if (!badge) return;
  const name = badge.dataset.spec;
  const bonus = parseInt(badge.dataset.bonus, 10) || 0;
  if (!name || !bonus) return;
  const existing = state.specBonuses[name];
  if (existing !== undefined) {
    state.MOD = state.MOD - existing;
    delete state.specBonuses[name];
  } else {
    state.MOD = state.MOD + bonus;
    state.specBonuses[name] = bonus;
  }
  updPool();
}

// ── EQUIPMENT CHIP TOGGLE (EQ-3) ──

export function togEquipChip(badge) {
  if (!badge) return;
  const id = badge.dataset.equip;
  const bonus = parseInt(badge.dataset.bonus, 10) || 0;
  if (!id || !bonus) return;

  if (state.activeEquipBonus && state.activeEquipBonus.catalogueId === id) {
    state.MOD -= state.activeEquipBonus.bonus;
    state.activeEquipBonus = null;
  } else {
    if (state.activeEquipBonus) {
      state.MOD -= state.activeEquipBonus.bonus;
    }
    state.MOD += bonus;
    state.activeEquipBonus = { catalogueId: id, bonus };
  }
  updPool();
}

// ── WEAPON REFERENCE PANEL (EQ-3) ──

const COMBAT_SKILLS = ['Weaponry', 'Brawl', 'Firearms', 'Archery'];

export function updWeaponRef() {
  const panel = document.getElementById('weapon-ref');
  if (!panel) return;

  // Capture live selection before rebuilding innerHTML
  const liveSel = document.getElementById('weapon-sel');
  if (liveSel && liveSel.value) state.activeWeaponId = liveSel.value;
  else if (liveSel && !liveSel.value) state.activeWeaponId = null;

  const pi = state.POOL_INFO;
  if (!pi || !pi.skill || !COMBAT_SKILLS.includes(pi.skill) || !state.rollChar) {
    panel.style.display = 'none';
    return;
  }
  const weapons = (state.rollChar.equipment || []).filter(item => {
    // #752: 'active' is the strongest in-use state — include it in weapon-ref
    // eligibility (matches the chip predicate above).
    const entry = getCatalogueEntry(item.catalogue_id);
    // EQC-1 (#1152): 'weapon' bucket merged into 'combat_gear'; weapon-shaped
    // is identified by weapon_type presence (armour-shaped entries leave it null).
    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
           (item.state === 'carried' || item.state === 'worn' || item.state === 'active');
  });
  if (!weapons.length) {
    panel.style.display = 'none';
    return;
  }

  panel.style.display = '';
  const prevId = state.activeWeaponId;

  const optionsHtml = weapons.map(item => {
    const e = getCatalogueEntry(item.catalogue_id);
    const sel = prevId === e.id ? ' selected' : '';
    return `<option value="${e.id}"${sel}>${e.name}</option>`;
  }).join('');

  let statsHtml = '';
  if (prevId) {
    const e = getCatalogueEntry(prevId);
    if (e) {
      const DMGTYPE = { lethal: 'Lethal', bashing: 'Bashing', aggravated: 'Aggravated' };
      const WPNTYPE = { melee: 'Melee', ranged: 'Ranged', thrown: 'Thrown' };
      const mod = e.damage_mod > 0 ? '+' + e.damage_mod : String(e.damage_mod);
      statsHtml = `<div class="trait-qual">`
               + `${mod} · ${DMGTYPE[e.damage_type] || e.damage_type} · ${WPNTYPE[e.weapon_type] || e.weapon_type}`
               + `</div>`;
    }
  }

  panel.innerHTML = `<div class="slabel">Weapon</div>`
    + `<select id="weapon-sel" class="resist-sel" onchange="updWeaponRef()">`
    + `<option value="">-- select --</option>${optionsHtml}</select>${statsHtml}`;
}

// ── AGAIN / MODIFIER TOGGLES ──

// Segmented Again pill entry point (slice D). `v` is one of
// '10' | '9' | '8' | 'none' (or the numeric variants). 'none' disables
// die explosion via state.NA; the other values set state.AGAIN and clear
// NA. Repaints the pill and the sub-line via updPool().
export function setAgainSeg(v) {
  if (v === 'none' || v === 'None') {
    state.NA = true;
  } else {
    state.NA = false;
    state.AGAIN = Number(v);
  }
  // updPool paints the seg AND the anchor sub-line so both stay in sync.
  updPool();
}

// Kept as an export because loadPool() calls it. Delegates to the new
// segmented entry point so the pill + sub-line stay in sync when a pool
// with a `nineAgain` flag is loaded.
export function setAgain(v) {
  setAgainSeg(v);
}

export function togMod(m) {
  if (m === 'rote') {
    state.ROTE = !state.ROTE;
    const el = document.getElementById('rote-c');
    if (el) el.classList.toggle('on', state.ROTE);
    updPool();
  } else if (m === 'wp') {
    state.WP = !state.WP;
    const el = document.getElementById('wp-c');

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:42:32.316255Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$admin = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\public\\js\\admin\\equipment-catalogue-admin.js'; "'$admin[0..390]; $admin[390..520]' in D:\Terra Mortis\TM Suite
 succeeded in 2793ms:
/**
 * Equipment Catalogue admin view (ECM-6, issue #873).
 *
 * Epic ECM (specs/epic-ecm-equipment-catalogue-migration.md). Browser-side
 * CRUD over the Mongo-backed equipment_catalogue established by ECM-1.
 *
 * UX (epic D4 / D5 / D6):
 *   - List view: filter by bucket, search by name, sort by availability.
 *     Each row shows name / bucket / availability / holders count / actions.
 *   - Create form: bucket selector reveals bucket-specific fields. Soft
 *     duplicate warning when an existing item matches by name+bucket
 *     (case-insensitive). Non-blocking — ST can override.
 *   - Edit form: same field set + impact warning banner ("Held by N
 *     characters; edits will apply to all") sourced from
 *     GET /api/equipment_catalogue/:id/impact.
 *   - Delete control: hard-disabled with explanatory tooltip when
 *     holders > 0. API enforces 409 server-side; UI mirrors the gate.
 *
 * Holders count is derived **client-side** from the character set passed
 * by switchDomain. The server's /:id/impact endpoint is reused for the
 * edit-form banner only (it does the same join but is the SSOT for the
 * delete gate; we render its message inline if the API returns 409).
 *
 * Write paths broadcast `broadcastCatalogueUpdate` server-side (wired in
 * ECM-1); ECM-4 / ECM-5 dropdown clients refresh on receipt. This view
 * refreshes its own list via `loadItems()` on each successful mutation.
 */

import { apiGet, apiPost, apiPatch, apiRaw } from '../data/api.js';
import { esc } from '../data/helpers.js';

// EQC-1 (issue #1152, epic #1038, 2026-08-13): re-partitioned from the old
// four buckets (weapon/armour/equipment/asset) to these five. combat_gear
// absorbs both weapon and armour — an item fills whichever stat-field subset
// applies (weapon fields, armour fields, or in principle both) and leaves
// the rest null; see BUCKET_FIELDS below and equipment-derivation.js's own
// armour_value-based discriminator.
const BUCKETS = ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'];

// Bucket → list of optional fields shown in create/edit forms. Mirrors
// the EQ-1 null-template pattern (EQ_NULLS / WP_NULLS / AR_NULLS / AS_NULLS).
const BUCKET_FIELDS = {
  combat_gear:  ['damage_mod', 'damage_type', 'weapon_type', 'armour_value', 'defence_penalty'],
  skill_gear:   ['skill_domain', 'bonus_dice'],
  tool_utility: ['mechanical_effect'],
  narrative:    [],
  container:    ['mechanical_effect'],
};

// Module-level state — re-rendered on every mutation. Single-view module
// (admin sidebar is mutually exclusive), so a singleton is fine.
let _container = null;
let _allChars = [];
let _items = [];
let _filterBucket = '';
let _searchName = '';
let _sortKey = 'name';        // 'name' | 'availability' | 'bucket'
let _sortDir = 'asc';
let _editingId = null;        // ObjectId-string or null when create mode

/**
 * Compute holders count per catalogue _id from the character set.
 * Returns Map<string-id, number>. Pre-ECM-3 character.equipment[].catalogue_id
 * is still a string slug, so this returns 0 for everything; post-ECM-3 it
 * resolves correctly. The delete gate is API-enforced regardless.
 */
function buildHoldersIndex(chars) {
  const idx = new Map();
  for (const c of chars || []) {
    for (const item of c.equipment || []) {
      const key = String(item.catalogue_id);
      idx.set(key, (idx.get(key) || 0) + 1);
    }
  }
  return idx;
}

export async function initEquipmentCatalogueAdmin(container, chars) {
  _container = container;
  _allChars = Array.isArray(chars) ? chars : [];
  _editingId = null;
  _container.innerHTML = '<div class="ec-admin"><div class="ec-empty">Loading equipment catalogue…</div></div>';
  await loadItems();
  render();
}

async function loadItems() {
  try {
    _items = await apiGet('/api/equipment_catalogue');
  } catch (err) {
    console.error('[equipment-catalogue-admin] load failed:', err);
    _items = [];
  }
}

function filteredSorted() {
  const search = (_searchName || '').toLowerCase();
  let rows = _items.filter(it => {
    if (_filterBucket && it.bucket !== _filterBucket) return false;
    if (search && !(it.name || '').toLowerCase().includes(search)) return false;
    return true;
  });
  const dir = _sortDir === 'desc' ? -1 : 1;
  rows.sort((a, b) => {
    let av, bv;
    if (_sortKey === 'availability') { av = a.availability ?? -1; bv = b.availability ?? -1; }
    else if (_sortKey === 'bucket') { av = a.bucket || ''; bv = b.bucket || ''; }
    else { av = (a.name || '').toLowerCase(); bv = (b.name || '').toLowerCase(); }
    if (av < bv) return -1 * dir;
    if (av > bv) return  1 * dir;
    return 0;
  });
  return rows;
}

function render() {
  if (!_container) return;
  const holders = buildHoldersIndex(_allChars);
  const rows = filteredSorted();
  const bucketOpts = ['<option value="">All buckets</option>']
    .concat(BUCKETS.map(b => `<option value="${b}"${_filterBucket === b ? ' selected' : ''}>${b}</option>`))
    .join('');

  const html = `
    <div class="ec-admin">
      <div class="ec-toolbar">
        <label class="ec-toolbar-field">
          <span>Bucket</span>
          <select id="ec-filter-bucket">${bucketOpts}</select>
        </label>
        <label class="ec-toolbar-field">
          <span>Name search</span>
          <input id="ec-filter-search" type="text" value="${esc(_searchName)}" placeholder="filter…">
        </label>
        <label class="ec-toolbar-field">
          <span>Sort by</span>
          <select id="ec-sort-key">
            <option value="name"${_sortKey === 'name' ? ' selected' : ''}>Name</option>
            <option value="bucket"${_sortKey === 'bucket' ? ' selected' : ''}>Bucket</option>
            <option value="availability"${_sortKey === 'availability' ? ' selected' : ''}>Availability</option>
          </select>
          <button id="ec-sort-dir" class="ec-btn-sm" title="Toggle direction">${_sortDir === 'asc' ? '↑' : '↓'}</button>
        </label>
        <button id="ec-new" class="ec-btn-primary">+ New item</button>
      </div>

      <div class="ec-count">${rows.length} of ${_items.length} item${_items.length === 1 ? '' : 's'}</div>

      <table class="ec-table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Bucket</th>
            <th>Avail.</th>
            <th>Holders</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          ${rows.map(it => renderRow(it, holders.get(String(it._id)) || 0)).join('')}
        </tbody>
      </table>

      <div id="ec-form-host"></div>
    </div>
  `;
  _container.innerHTML = html;
  wireListEvents();
}

function renderRow(it, holdersCount) {
  const deleteAttrs = holdersCount > 0
    ? `disabled title="Held by ${holdersCount} character${holdersCount === 1 ? '' : 's'} — remove from holders before deleting."`
    : '';
  return `
    <tr data-id="${esc(String(it._id))}">
      <td>${esc(it.name || '')}</td>
      <td><span class="ec-bucket-tag ec-bucket-${esc(it.bucket || '')}">${esc(it.bucket || '')}</span></td>
      <td>${it.availability ?? '—'}</td>
      <td>${holdersCount > 0 ? `<strong>${holdersCount}</strong>` : '0'}</td>
      <td class="ec-row-actions">
        <button class="ec-btn-sm" data-action="edit">Edit</button>
        <button class="ec-btn-sm ec-btn-danger" data-action="delete" ${deleteAttrs}>Delete</button>
      </td>
    </tr>
  `;
}

function wireListEvents() {
  const $ = id => document.getElementById(id);
  $('ec-filter-bucket')?.addEventListener('change', e => { _filterBucket = e.target.value; render(); });
  $('ec-filter-search')?.addEventListener('input', e => { _searchName = e.target.value; render(); });
  $('ec-sort-key')?.addEventListener('change', e => { _sortKey = e.target.value; render(); });
  $('ec-sort-dir')?.addEventListener('click', () => { _sortDir = _sortDir === 'asc' ? 'desc' : 'asc'; render(); });
  $('ec-new')?.addEventListener('click', () => openCreateForm());

  _container.querySelectorAll('.ec-table tbody tr').forEach(row => {
    const id = row.dataset.id;
    row.querySelector('[data-action="edit"]')?.addEventListener('click', () => openEditForm(id));
    const delBtn = row.querySelector('[data-action="delete"]');
    if (delBtn && !delBtn.disabled) {
      delBtn.addEventListener('click', () => onDelete(id));
    }
  });
}

// ── Forms ──────────────────────────────────────────────────────────────────

function openCreateForm() {
  _editingId = null;
  renderForm({ bucket: 'skill_gear', tags: [] });
}

async function openEditForm(id) {
  _editingId = id;
  const item = _items.find(it => String(it._id) === String(id));
  if (!item) return;
  // Fetch authoritative impact info for the banner. The list-view holders
  // count is a client-side join (may be stale); the API count is SSOT.
  let impact = { holders: 0, character_names: [] };
  try {
    impact = await apiGet(`/api/equipment_catalogue/${id}/impact`);
  } catch (err) {
    console.warn('[equipment-catalogue-admin] impact fetch failed:', err);
  }
  renderForm(item, impact);
}

function renderForm(initial, impact = null) {
  const host = document.getElementById('ec-form-host');
  if (!host) return;
  const isEdit = !!_editingId;
  const bucket = initial.bucket || 'skill_gear';
  const bucketOpts = BUCKETS.map(b =>
    `<option value="${b}"${bucket === b ? ' selected' : ''}>${b}</option>`
  ).join('');

  const banner = isEdit && impact && impact.holders > 0
    ? `<div class="ec-impact-banner">
         <strong>Held by ${impact.holders} character${impact.holders === 1 ? '' : 's'}.</strong>
         Edits will apply to all current holders.
         <span class="ec-impact-list">(${impact.character_names.map(esc).join(', ')})</span>
       </div>`
    : '';

  host.innerHTML = `
    <div class="ec-form-panel">
      <div class="ec-form-header">
        <h3>${isEdit ? 'Edit item' : 'New item'}</h3>
        <button id="ec-form-close" class="ec-btn-sm">Close</button>
      </div>
      ${banner}
      <div class="ec-form-grid">
        ${isEdit
          ? `<div class="ec-form-field"><label>Bucket</label><div class="ec-form-readonly">${esc(bucket)} <span class="ec-form-hint">(immutable — delete + recreate to change bucket)</span></div></div>`
          : `<div class="ec-form-field"><label>Bucket</label><select id="ec-f-bucket">${bucketOpts}</select></div>`
        }
        <div class="ec-form-field">
          <label>Name *</label>
          <input id="ec-f-name" type="text" value="${esc(initial.name || '')}" required>
        </div>
        <div class="ec-form-field ec-form-field--wide">
          <label>Description</label>
          <textarea id="ec-f-description" rows="3">${esc(initial.description || '')}</textarea>
        </div>
        <div class="ec-form-field">
          <label>Availability (0-5)</label>
          <input id="ec-f-availability" type="number" min="0" max="5" value="${initial.availability ?? ''}">
        </div>
        <div class="ec-form-field">
          <label>Tags <span class="ec-form-hint">(comma-separated)</span></label>
          <input id="ec-f-tags" type="text" value="${esc((initial.tags || []).join(', '))}">
        </div>
        <div id="ec-bucket-fields" class="ec-form-field--wide">
          ${renderBucketFields(bucket, initial)}
        </div>
      </div>
      <div class="ec-form-actions">
        <span id="ec-form-msg" class="ec-form-msg"></span>
        <button id="ec-form-save" class="ec-btn-primary">${isEdit ? 'Save changes' : 'Create item'}</button>
      </div>
    </div>
  `;
  wireFormEvents(initial);
}

function renderBucketFields(bucket, initial) {
  const fields = BUCKET_FIELDS[bucket] || [];
  if (!fields.length) return '';
  const labels = {
    damage_mod: 'Damage mod',
    damage_type: 'Damage type (lethal / bashing)',
    weapon_type: 'Weapon type',
    armour_value: 'Armour value',
    defence_penalty: 'Defence penalty',
    skill_domain: 'Skill domain',
    bonus_dice: 'Bonus dice',
    mechanical_effect: 'Mechanical effect',
  };
  const numericFields = new Set(['damage_mod', 'armour_value', 'defence_penalty', 'bonus_dice']);
  const longFields = new Set(['mechanical_effect']);
  return `<fieldset class="ec-bucket-fieldset"><legend>${bucket}-specific</legend>${
    fields.map(f => {
      const val = initial[f];
      const type = numericFields.has(f) ? 'number' : 'text';
      const inputHtml = longFields.has(f)
        ? `<textarea id="ec-f-${f}" rows="2">${esc(val ?? '')}</textarea>`
        : `<input id="ec-f-${f}" type="${type}" value="${esc(val ?? '')}">`;
      return `<div class="ec-form-field"><label>${labels[f] || f}</label>${inputHtml}</div>`;
    }).join('')
  }</fieldset>`;
}

function wireFormEvents(initial) {
  const $ = id => document.getElementById(id);
  $('ec-form-close')?.addEventListener('click', () => {
    document.getElementById('ec-form-host').innerHTML = '';
    _editingId = null;
  });
  $('ec-f-bucket')?.addEventListener('change', e => {
    // Re-render only the bucket-specific fieldset; keep other field values intact.
    const fresh = collectFormValues(false);
    fresh.bucket = e.target.value;
    document.getElementById('ec-bucket-fields').innerHTML = renderBucketFields(fresh.bucket, fresh);
  });
  $('ec-form-save')?.addEventListener('click', () => onSave(initial));
}

/**
 * Read current form values. `coerce=true` casts numeric fields to numbers /
 * null for the API. `coerce=false` (used by bucket-switch re-render) keeps
 * raw strings so the form preserves user input across renders.
 */
function collectFormValues(coerce = true) {
  const $ = id => document.getElementById(id);
  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'skill_gear');
  const name = ($('ec-f-name')?.value || '').trim();
  const description = ($('ec-f-description')?.value || '').trim() || null;
  const availRaw = $('ec-f-availability')?.value;
  const availability = availRaw === '' || availRaw === undefined ? null : Number(availRaw);
  const tagsRaw = $('ec-f-tags')?.value || '';
  const tags = tagsRaw.split(',').map(t => t.trim()).filter(Boolean);

  const out = { bucket, name, description, availability, tags };

  const fields = BUCKET_FIELDS[bucket] || [];
  const numericFields = new Set(['damage_mod', 'armour_value', 'defence_penalty', 'bonus_dice']);
  for (const f of fields) {
    const raw = $(`ec-f-${f}`)?.value;
    if (raw === undefined) continue;
    if (!coerce) { out[f] = raw; continue; }
    if (raw === '' || raw === null) { out[f] = null; continue; }
    out[f] = numericFields.has(f) ? Number(raw) : raw;
  }
  return out;
}

function showFormMsg(text, kind = 'info') {
  const el = document.getElementById('ec-form-msg');
  if (!el) return;
  el.textContent = text;
  el.className = `ec-form-msg ec-form-msg--${kind}`;
}

async function onSave(initial) {
  const values = collectFormValues(true);
  if (!values.name) { showFormMsg('Name is required.', 'error'); return; }
  if (!values.bucket) { showFormMsg('Bucket is required.', 'error'); return; }

  if (!_editingId) {
    // Soft duplicate warning per epic D6 — non-blocking. Match by
    // name+bucket, case-insensitive.
    const dup = _items.find(it =>
      it.bucket === values.bucket &&
      (it.name || '').toLowerCase() === values.name.toLowerCase()
    );
    if (dup && !confirm(
      `An item with name "${dup.name}" already exists in bucket "${dup.bucket}". ` +
      `Continue anyway? (Inspect the existing item by closing this form.)`
    )) return;
  }

  showFormMsg('Saving…', 'info');
  try {
    if (_editingId) {
      // PATCH — only send updatable fields. The server allowlist also
      // strips, but pruning client-side keeps the payload small.
      const { bucket: _b, ...patchBody } = values;
      await apiPatch(`/api/equipment_catalogue/${_editingId}`, patchBody);
      showFormMsg('Saved.', 'ok');
    } else {
    } else {
      await apiPost('/api/equipment_catalogue', values);
      showFormMsg('Created.', 'ok');
    }
    await loadItems();
    _editingId = null;
    render();
  } catch (err) {
    showFormMsg(err.message || 'Save failed.', 'error');
  }
}

async function onDelete(id) {
  const item = _items.find(it => String(it._id) === String(id));
  if (!item) return;
  if (!confirm(`Delete "${item.name}" from the catalogue?`)) return;
  try {
    const res = await apiRaw('DELETE', `/api/equipment_catalogue/${id}`);
    if (res.status === 204) {
      await loadItems();
      render();
      return;
    }
    if (res.status === 409 && res.body) {
      // Server hard-block (epic D5 + ECM-6 HALT-DAR). Surface the holder
      // names so the ST knows where to clean up.
      alert(
        `Cannot delete — held by ${res.body.holders} character${res.body.holders === 1 ? '' : 's'}: ` +
        `${(res.body.character_names || []).join(', ')}.`
      );
      // Refresh the list so the holders column reflects fresh truth.
      await loadItems();
      render();
      return;
    }
    alert((res.body && (res.body.message || res.body.error)) || 'Delete failed.');
  } catch (err) {
    alert(err.message || 'Delete failed.');
  }
}

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:42:39.968606Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$n = 0; Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\public\\js\\admin\\equipment-catalogue-admin.js' | ForEach-Object { "'$n++; if ($n -ge 390 -and $n -le 430) { '"'{0,4}: {1}' -f "'$n, $_ } }' in D:\Terra Mortis\TM Suite
 succeeded in 3332ms:
 390:       showFormMsg('Saved.', 'ok');
 391:     } else {
 392:       await apiPost('/api/equipment_catalogue', values);
 393:       showFormMsg('Created.', 'ok');
 394:     }
 395:     await loadItems();
 396:     _editingId = null;
 397:     render();
 398:   } catch (err) {
 399:     showFormMsg(err.message || 'Save failed.', 'error');
 400:   }
 401: }
 402: 
 403: async function onDelete(id) {
 404:   const item = _items.find(it => String(it._id) === String(id));
 405:   if (!item) return;
 406:   if (!confirm(`Delete "${item.name}" from the catalogue?`)) return;
 407:   try {
 408:     const res = await apiRaw('DELETE', `/api/equipment_catalogue/${id}`);
 409:     if (res.status === 204) {
 410:       await loadItems();
 411:       render();
 412:       return;
 413:     }
 414:     if (res.status === 409 && res.body) {
 415:       // Server hard-block (epic D5 + ECM-6 HALT-DAR). Surface the holder
 416:       // names so the ST knows where to clean up.
 417:       alert(
 418:         `Cannot delete — held by ${res.body.holders} character${res.body.holders === 1 ? '' : 's'}: ` +
 419:         `${(res.body.character_names || []).join(', ')}.`
 420:       );
 421:       // Refresh the list so the holders column reflects fresh truth.
 422:       await loadItems();
 423:       render();
 424:       return;
 425:     }
 426:     alert((res.body && (res.body.message || res.body.error)) || 'Delete failed.');
 427:   } catch (err) {
 428:     alert(err.message || 'Delete failed.');
 429:   }
 430: }

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:42:48.441877Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\server\\routes\\equipment-catalogue.js'; Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\server\\schemas\\equipment_catalogue.schema.js'" in D:\Terra Mortis\TM Suite
 succeeded in 2836ms:
/**
 * Equipment Catalogue routes (ECM-1, issue #868).
 *
 * Epic ECM (specs/epic-ecm-equipment-catalogue-migration.md). Mongo-backed
 * replacement for the static EQUIPMENT_CATALOGUE module. Reads are public
 * (the DT form and player app both need them without a token); writes are
 * ST-gated.
 *
 * Mounted via a factory so the test app can substitute its mockAuth for the
 * production requireAuth without the router needing to know which one is
 * in play. The mixed-auth shape (no-auth reads + ST-auth writes) does not
 * fit the parent-route-mount pattern used elsewhere; per-handler auth chains
 * with an injected requireAuth keep the wiring explicit.
 *
 * Endpoints (per epic D3):
 *
 *   GET    /api/equipment_catalogue              public   list all
 *   GET    /api/equipment_catalogue/:id          public   single by ObjectId
 *   GET    /api/equipment_catalogue/:id/impact   public   holder count + names
 *   POST   /api/equipment_catalogue              ST       create
 *   PATCH  /api/equipment_catalogue/:id          ST       partial update
 *   DELETE /api/equipment_catalogue/:id          ST       409 if any holder
 *
 * Write paths broadcast `broadcastCatalogueUpdate(itemId, op)` on the WS so
 * ECM-4/5/6 dropdown clients can refetch the catalogue. Wired in ECM-1 even
 * though no client listens yet — the cost of firing into an empty WS is nil
 * and ECM-6 inherits the wiring rather than having to add it.
 */

import { Router } from 'express';
import { ObjectId } from 'mongodb';
import { getCollection } from '../db.js';
import { requireRole } from '../middleware/auth.js';
import { validate } from '../middleware/validate.js';
import {
  equipmentCatalogueSchema,
  EQUIPMENT_CATALOGUE_UPDATABLE_FIELDS,
} from '../schemas/equipment_catalogue.schema.js';
import { broadcastCatalogueUpdate } from '../ws.js';

/**
 * Validate :id param as a 24-char ObjectId hex. On failure short-circuit
 * with 404 — same surface a non-existent doc returns, so probing clients
 * can't distinguish "malformed id" from "no such id".
 */
function withObjectId(req, res, next) {
  if (!ObjectId.isValid(req.params.id) || String(new ObjectId(req.params.id)) !== req.params.id) {
    return res.status(404).json({ error: 'NOT_FOUND', message: 'Item not found' });
  }
  req._oid = new ObjectId(req.params.id);
  next();
}

/**
 * Factory: returns the equipment_catalogue router with the supplied auth
 * middleware applied to the write handlers. `authMiddleware` runs before
 * `requireRole('st')`; in prod that's `requireAuth`, in tests `mockAuth`.
 */
export default function buildEquipmentCatalogueRouter(authMiddleware) {
  const router = Router();
  const col = () => getCollection('equipment_catalogue');
  const chars = () => getCollection('characters');

  // ─────────────────────────────────────────────────────────────────────────
  //  Public reads
  // ─────────────────────────────────────────────────────────────────────────

  router.get('/', async (req, res) => {
    const docs = await col().find({}).sort({ bucket: 1, name: 1 }).toArray();
    res.json(docs);
  });

  router.get('/:id/impact', withObjectId, async (req, res) => {
    const item = await col().findOne({ _id: req._oid });
    if (!item) return res.status(404).json({ error: 'NOT_FOUND', message: 'Item not found' });
    const holders = await chars()
      .find({ 'equipment.catalogue_id': req._oid }, { projection: { name: 1 } })
      .toArray();
    res.json({
      holders: holders.length,
      character_names: holders.map(c => c.name).filter(Boolean),
    });
  });

  router.get('/:id', withObjectId, async (req, res) => {
    const doc = await col().findOne({ _id: req._oid });
    if (!doc) return res.status(404).json({ error: 'NOT_FOUND', message: 'Item not found' });
    res.json(doc);
  });

  // ─────────────────────────────────────────────────────────────────────────
  //  ST-only writes
  // ─────────────────────────────────────────────────────────────────────────

  router.post('/',
    authMiddleware, requireRole('st'),
    validate(equipmentCatalogueSchema),
    async (req, res) => {
      const now = new Date().toISOString();
      const doc = { ...req.body, created_at: now, updated_at: now };
      // Don't trust client-supplied _id on create.
      delete doc._id;
      const result = await col().insertOne(doc);
      const created = await col().findOne({ _id: result.insertedId });
      broadcastCatalogueUpdate(result.insertedId, 'create');
      res.status(201).json(created);
    }
  );

  router.patch('/:id',
    authMiddleware, requireRole('st'),
    withObjectId,
    async (req, res) => {
      const filtered = {};
      for (const [field, value] of Object.entries(req.body)) {
        if (EQUIPMENT_CATALOGUE_UPDATABLE_FIELDS.has(field)) filtered[field] = value;
      }
      if (!Object.keys(filtered).length) {
        return res.status(400).json({
          error: 'VALIDATION_ERROR',
          message: 'No updatable fields provided',
        });
      }
      filtered.updated_at = new Date().toISOString();
      const result = await col().findOneAndUpdate(
        { _id: req._oid },
        { $set: filtered },
        { returnDocument: 'after' }
      );
      if (!result) return res.status(404).json({ error: 'NOT_FOUND', message: 'Item not found' });
      broadcastCatalogueUpdate(req._oid, 'update');
      res.json(result);
    }
  );

  router.delete('/:id',
    authMiddleware, requireRole('st'),
    withObjectId,
    async (req, res) => {
      // D5 + ECM-6 HALT-DAR pin: hard-block at the API layer when held.
      const holders = await chars()
        .find({ 'equipment.catalogue_id': req._oid }, { projection: { name: 1 } })
        .toArray();
      if (holders.length > 0) {
        return res.status(409).json({
          error: 'CONFLICT',
          message: `Cannot delete — item is held by ${holders.length} character${holders.length === 1 ? '' : 's'}`,
          holders: holders.length,
          character_names: holders.map(c => c.name).filter(Boolean),
        });
      }
      const result = await col().deleteOne({ _id: req._oid });
      if (result.deletedCount === 0) {
        return res.status(404).json({ error: 'NOT_FOUND', message: 'Item not found' });
      }
      broadcastCatalogueUpdate(req._oid, 'delete');
      res.status(204).end();
    }
  );

  return router;
}
/**
 * JSON Schema (Draft-07) for TM Equipment Catalogue Item.
 * Collection: equipment_catalogue
 *
 * Epic ECM (issue #868 + epic spec specs/epic-ecm-equipment-catalogue-migration.md).
 * Mirrors the EQ-1 catalogue shape — `_id: ObjectId` is the only identity,
 * no slug field. Bucket-specific fields explicit-null where absent (matches
 * the EQ_NULLS / WP_NULLS / AR_NULLS / AS_NULLS pattern from
 * server/data/equipment-catalogue.js).
 *
 * Per epic Non-Goals: NO state-enum per-bucket schema validation in this epic
 * — that's a separate bugfix. Bucket-specific fields are loosely typed (nullable
 * primitives) so the schema accepts every existing seed entry without further
 * coercion in ECM-2.
 *
 * EQC-1 (issue #1152, epic #1038) — bucket re-partition, 2026-08-13. The old
 * four buckets (weapon/armour/equipment/asset) become FIVE new values:
 *
 *   combat_gear  — weapons AND armour, distinguished by which stat fields are
 *                  populated (damage_mod/damage_type/weapon_type = weapon-shaped;
 *                  armour_value/defence_penalty = armour-shaped). Both sets of
 *                  fields stay on the schema for this bucket; a given item
 *                  populates whichever subset applies and leaves the rest null.
 *   skill_gear   — old `equipment` bucket, unchanged meaning (skill_domain +
 *                  bonus_dice). Renamed only.
 *   tool_utility — NEW. "Does a thing, no bonus" per the epic description —
 *                  mechanical_effect (free text), no numeric bonus field.
 *   narrative    — NEW. Purely descriptive; no bucket-specific stat fields.
 *   container    — old `asset` bucket. "Assets are containers... no
 *                  stat-stacking on the asset itself" (epic #1038) — holds
 *                  other equipment (see character.schema.js's `container_id`)
 *                  rather than granting a bonus of its own. mechanical_effect
 *                  stays available for descriptive/narrative notes (e.g. what
 *                  a haven confers), not a combat bonus.
 *
 * Migration: server/scripts/migrate-eqc1-bucket-taxonomy.mjs maps every live
 * equipment_catalogue document from the old enum to the new one
 * (weapon→combat_gear, armour→combat_gear, equipment→skill_gear,
 * asset→container). Must run before/alongside this schema landing — see that
 * script's own header for why the two cannot ship independently.
 */

export const equipmentCatalogueSchema = {
  $schema: 'http://json-schema.org/draft-07/schema#',
  title: 'TM Equipment Catalogue Item',
  type: 'object',
  required: ['bucket', 'name'],
  additionalProperties: false,

  properties: {
    // MongoDB _id — injected on insert, present on read.
    _id: {},

    // ── Identity / core ──
    bucket:       { type: 'string', enum: ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container'] },
    name:         { type: 'string', minLength: 1 },
    description:  { type: ['string', 'null'] },
    availability: { type: ['integer', 'null'], minimum: 0, maximum: 5 },
    tags:         { type: 'array', items: { type: 'string' } },

    // ── Bucket-specific (explicit null where absent) ──
    damage_mod:        { type: ['integer', 'null'] },
    damage_type:       { type: ['string',  'null'] },
    weapon_type:       { type: ['string',  'null'] },
    armour_value:      { type: ['integer', 'null'] },
    defence_penalty:   { type: ['integer', 'null'] },
    skill_domain:      { type: ['string',  'null'] },
    bonus_dice:        { type: ['integer', 'null'] },
    // mechanical_effect — asset-bucket-only in practice (free-text note on
    // what the asset does mechanically); follows the same nullable shape as
    // the other bucket-specific fields. Widened in ECM-6 (#873) after the
    // ECM-2 seed surfaced that the source carries this field on asset
    // entries — ECM-1's properties block had omitted it, causing PATCH to
    // silently drop edits via the allowlist. Per epic Non-Goal "no state-
    // enum per-bucket schema validation", the schema does not enforce
    // bucket=asset; the field is nullable across all buckets.
    mechanical_effect: { type: ['string',  'null'] },

    // ── Audit-light metadata (kept minimal per epic D1 / Non-Goals) ──
    created_at: { type: 'string' },
    updated_at: { type: 'string' },
  },
};

// Allowlisted fields for PATCH updates. `_id`, `bucket`, `created_at` are
// immutable. `bucket` is excluded specifically: a bucket change would
// orphan bucket-specific field semantics and invalidate render assumptions
// in ECM-4 / ECM-5; force the ST to delete + recreate if bucket needs to change.
export const EQUIPMENT_CATALOGUE_UPDATABLE_FIELDS = new Set([
  'name', 'description', 'availability', 'tags',
  'damage_mod', 'damage_type', 'weapon_type',
  'armour_value', 'defence_penalty',
  'skill_domain', 'bonus_dice',
  'mechanical_effect',
]);

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:42:56.886808Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --glob '*.js' --glob '*.json' --glob '*.mjs' -e \"bucket: 'weapon'\" -e \"bucket: 'armour'\" -e \"bucket: 'equipment'\" -e \"bucket: 'asset'\" -e '\"bucket\": \"weapon\"' -e '\"bucket\": \"armour\"' -e '\"bucket\": \"equipment\"' -e '\"bucket\": \"asset\"' 'D:\\Terra Mortis\\TM Suite\\server' 'D:\\Terra Mortis\\TM Suite\\public'" in D:\Terra Mortis\TM Suite
 succeeded in 3071ms:
D:\Terra Mortis\TM Suite\server\schemas\character.schema.js:354:    // bucket: 'asset' items (Vehicle (Luxury), Safe House, etc.). The previous
D:\Terra Mortis\TM Suite\server\routes\characters.js:829:// via the catalogue's bucket: 'asset' items). Response shape is now just
D:\Terra Mortis\TM Suite\server\routes\characters.js:926:// consolidated into equipment[] via the catalogue's bucket: 'asset' items.
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:15: *   into equipment[] via catalogue bucket: 'asset' items.
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:51:    bucket: 'weapon', name: `Test Cat Item ${Math.random().toString(36).slice(2, 8)}`,
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:73:  catKnife      = await seedCatalogueItem({ name: 'Knife (test)',      bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:74:  catFlashlight = await seedCatalogueItem({ name: 'Flashlight (test)', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:75:  catRope       = await seedCatalogueItem({ name: 'Rope (test)',       bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:18:    const r = planBucketMigration({ bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:23:    const r = planBucketMigration({ bucket: 'armour' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:29:    const r = planBucketMigration({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:35:    const r = planBucketMigration({ bucket: 'asset' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:54:    bucket: 'equipment',
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:86:    await seedItem({ name: 'Alpha', bucket: 'weapon' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:87:    await seedItem({ name: 'Bravo', bucket: 'armour' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:97:    await seedItem({ name: 'Zebra', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:98:    await seedItem({ name: 'Aardvark', bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:154:      .send({ bucket: 'equipment', name: 'X' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:162:      .send({ bucket: 'equipment', name: 'X' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:170:      .send({ bucket: 'equipment' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:198:      .send({ _id: sentinel, bucket: 'equipment', name: 'Try-injection' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:230:    const seed = await seedItem({ bucket: 'equipment', name: 'Stable' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:234:      .send({ bucket: 'weapon', _id: new ObjectId(), created_at: '1999-01-01T00:00:00.000Z', name: 'OK' });
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:246:      .send({ bucket: 'weapon' });   // bucket is non-updatable
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:256:    const seed = await seedItem({ bucket: 'asset', name: 'Warehouse', mechanical_effect: null });
D:\Terra Mortis\TM Suite\server\tests\issue-896-availability-filter.test.js:43:  return { _id: 'item-' + availability, bucket: 'equipment', name: 'Item ' + availability, availability };

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:43:07.896406Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"bucket\" 'D:\\Terra Mortis\\TM Suite\\server\\data\\equipment-catalogue.js'; Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\server\\data\\equipment-catalogue.js' | Select-Object -First 160" in D:\Terra Mortis\TM Suite
2026-08-13T02:43:18.743353Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.7 seconds
Output:
rg: D:\Terra Mortis\TM Suite\server\data\equipment-catalogue.js: IO error for operation on D:\Terra Mortis\TM Suite\server\data\equipment-catalogue.js: The system cannot find the path specified. (os error 3)
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m … alogue.js'; [36;1mGet-Content -LiteralPath 'D:\Terra Mortis\TM Suite\server[0m …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m               ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'D:\Terra Mortis\TM Suite\server\data\equipment-catalogue.js' because it does not exist.[0m

 exited 1 in 2722ms:
rg: D:\Terra Mortis\TM Suite\server\data\equipment-catalogue.js: IO error for operation on D:\Terra Mortis\TM Suite\server\data\equipment-catalogue.js: The system cannot find the path specified. (os error 3)
[31;1mGet-Content: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m … alogue.js'; [36;1mGet-Content -LiteralPath 'D:\Terra Mortis\TM Suite\server[0m …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m               ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find path 'D:\Terra Mortis\TM Suite\server\data\equipment-catalogue.js' because it does not exist.[0m

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:43:18.778088Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; node --input-type=module -e \"import { planBucketMigration } from './scripts/migrate-eqc1-bucket-taxonomy.mjs'; for (const value of [null, undefined, {}, {bucket: null}, {bucket: ''}, {bucket: 7}, {bucket: []}, {bucket: ['weapon']}]) console.log(JSON.stringify(value), '=>', planBucketMigration(value));\"" in D:\Terra Mortis\TM Suite
 succeeded in 2660ms:
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit
null => {
  touched: false,
  fromBucket: undefined,
  toBucket: undefined,
  reason: 'malformed document'
}
undefined => {
  touched: false,
  fromBucket: undefined,
  toBucket: undefined,
  reason: 'malformed document'
}
{} => {
  touched: false,
  fromBucket: undefined,
  toBucket: undefined,
  reason: 'unrecognised bucket value "undefined"'
}
{"bucket":null} => {
  touched: false,
  fromBucket: null,
  toBucket: undefined,
  reason: 'unrecognised bucket value "null"'
}
{"bucket":""} => {
  touched: false,
  fromBucket: '',
  toBucket: undefined,
  reason: 'unrecognised bucket value ""'
}
{"bucket":7} => {
  touched: false,
  fromBucket: 7,
  toBucket: undefined,
  reason: 'unrecognised bucket value "7"'
}
{"bucket":[]} => {
  touched: false,
  fromBucket: [],
  toBucket: undefined,
  reason: 'unrecognised bucket value ""'
}
{"bucket":["weapon"]} => {
  touched: true,
  fromBucket: [ 'weapon' ],
  toBucket: 'combat_gear',
  reason: null
}

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:43:28.854990Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath "'$reviewRoot; node --input-type=module -e "globalThis.location={hostname:'"''}; const m=await import('./public/js/data/equipment-derivation.js'); const entries=new Map([['w',{bucket:'combat_gear',weapon_type:'melee',armour_value:null,defence_penalty:9}],['a',{bucket:'combat_gear',weapon_type:null,armour_value:2,defence_penalty:3}],['n',{bucket:'combat_gear',weapon_type:null,armour_value:null,defence_penalty:8}]]); const c={equipment:[{catalogue_id:'w',state:'worn'},{catalogue_id:'a',state:'worn'},{catalogue_id:'n',state:'worn'}]}; console.log({penalty:m.armourDefencePenalty(c,id=>entries.get(id)),count:m.wornArmourCount(c,id=>entries.get(id))});\"" in D:\Terra Mortis\TM Suite
 succeeded in 2602ms:
{ penalty: 3, count: 1 }

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:43:42.377187Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"\\.ec-bucket-(weapon|armour|equipment|asset|combat_gear|skill_gear|tool_utility|narrative|container)\" 'D:\\Terra Mortis\\TM Suite\\public\\css\\admin-layout.css'" in D:\Terra Mortis\TM Suite
 succeeded in 2670ms:
10149:.ec-bucket-combat_gear  { background: rgba(139, 0, 0, 0.18); color: var(--crim); }
10150:.ec-bucket-skill_gear   { background: rgba(108, 117, 125, 0.22); color: var(--txt2); }
10151:.ec-bucket-tool_utility { background: rgba(70, 90, 120, 0.22); color: var(--txt2); }
10152:.ec-bucket-narrative    { background: var(--surf2); color: var(--txt3); }
10153:.ec-bucket-container    { background: rgba(224, 196, 122, 0.18); color: var(--gold2); }

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:43:50.454936Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --glob '*.js' --glob '*.mjs' --glob '"'!server/tests/**'"' --glob '"'!tests/**'"' --glob '"'!specs/**'"' \"\\.bucket\\b|\\[['\\\"]bucket['\\\"]\\]\" 'D:\\Terra Mortis\\TM Suite\\public' 'D:\\Terra Mortis\\TM Suite\\server'" in D:\Terra Mortis\TM Suite
2026-08-13T02:43:57.719806Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.3 seconds
Output:

 exited 1 in 2319ms:
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:43:57.761993Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --glob '*.js' --glob '*.mjs' --glob '"'!server/tests/**'"' --glob '"'!tests/**'"' --glob '"'!specs/**'"' \"\\.bucket\\b\" 'D:\\Terra Mortis\\TM Suite\\public' 'D:\\Terra Mortis\\TM Suite\\server'" in D:\Terra Mortis\TM Suite
 succeeded in 2376ms:
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:99:    if (_filterBucket && it.bucket !== _filterBucket) return false;
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:107:    else if (_sortKey === 'bucket') { av = a.bucket || ''; bv = b.bucket || ''; }
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:178:      <td><span class="ec-bucket-tag ec-bucket-${esc(it.bucket || '')}">${esc(it.bucket || '')}</span></td>
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:233:  const bucket = initial.bucket || 'skill_gear';
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:323:    fresh.bucket = e.target.value;
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:324:    document.getElementById('ec-bucket-fields').innerHTML = renderBucketFields(fresh.bucket, fresh);
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:336:  const bucket = $('ec-f-bucket')?.value || (_editingId ? (_items.find(it => String(it._id) === String(_editingId))?.bucket) : 'skill_gear');
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:368:  if (!values.bucket) { showFormMsg('Bucket is required.', 'error'); return; }
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:374:      it.bucket === values.bucket &&
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:378:      `An item with name "${dup.name}" already exists in bucket "${dup.bucket}". ` +
D:\Terra Mortis\TM Suite\public\js\data\equipment-catalogue-cache.js:36:    if (!_byBucket.has(it.bucket)) _byBucket.set(it.bucket, []);
D:\Terra Mortis\TM Suite\public\js\data\equipment-catalogue-cache.js:37:    _byBucket.get(it.bucket).push(it);
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:68:    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:91:    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2598:    const bucket = (entry.bucket && byBucket[entry.bucket]) ? entry.bucket : 'skill_gear';
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:5519:    if (!byBucket.has(it.bucket)) byBucket.set(it.bucket, []);
D:\Terra Mortis\TM Suite\public\js\tabs\downtime-form.js:5520:    byBucket.get(it.bucket).push(it);
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:72:  const from = doc.bucket;
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:129:      for (const u of totals.unrecognised) console.log(`  ${u.name} (${u.id}): bucket="${u.bucket}"`);
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:221:      return entry && entry.bucket === 'skill_gear' &&
D:\Terra Mortis\TM Suite\public\js\suite\roll-v2.js:313:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:145:      return entry && entry.bucket === 'skill_gear' &&
D:\Terra Mortis\TM Suite\public\js\suite\roll.js:237:    return entry && entry.bucket === 'combat_gear' && entry.weapon_type != null &&

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5888bdccd92c595f928345c72a8530b2a4206daf
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,45 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:44:04.676676Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Pass 2 repository tracing confirms the three-item mechanics behave correctly: only the armour-shaped item contributes (penalty 3, worn count 1); both rollers include only the weapon-shaped item in weapon reference and none of the three in skill chips; the neither-shaped item reaches “Other Combat Gear.” The whole production-source search found no missed old-value comparison. New evidence does confirm the containment write-path gap, and it also exposed JavaScript key coercion in the migration planner for array-valued buckets.
apply patch
patch: completed
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-findings.md
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5771d418907718366a5964e567794faf344e2e86
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,77 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5771d418907718366a5964e567794faf344e2e86
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,77 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:44:49.642251Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
  ✓ Pass 1: inspect only the supplied diff and freeze blind findings
  ✓ Pass 2: inspect repository context, trace edge cases, and freeze findings
  → Pass 3a: read only pre-record story sections and freeze AC findings
  • Pass 3b: read Dev Agent Record, run verification gates, and audit claims
  • Finalize severity-grouped report and validation attestation
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5771d418907718366a5964e567794faf344e2e86
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,77 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:44:58.718703Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Pass 2 is frozen. I’m now opening the story for Pass 3a, but only through Acceptance Criteria 1–7; I will stop before the Dev Agent Record and freeze the acceptance audit before reading any of the author’s completion/test claims.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -LiteralPath 'D:\\Terra Mortis\\TM Suite\\specs\\stories\\feature.1152.eqc1-bucket-container-schema.story.md' -Pattern '"'^#{1,4} '"'" in D:\Terra Mortis\TM Suite
 succeeded in 3159ms:

specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:1:[7m# [0m[7m[0mStory [0m[7m[0mfeature.1152: [0m[7m[0mEQC-1 [0m[7m[0m— [0m[7m[0mBucket [0m[7m[0mRe-Partition, [0m
[7m[0mContainer [0m[7m[0mSchema, [0m[7m[0mData [0m[7m[0mMigration[0m
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:3:[7m## [0mStatus: review
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:11:[7m## [0mStory
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:17:[7m## [0mBackground
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:25:[7m## [0mThe new taxonomy
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:38:[7m## [0mAcceptance Criteria
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:54:[7m## [0mTasks / Subtasks
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:85:[7m## [0mDev Notes
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:92:[7m### [0mProject Structure Notes
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:98:[7m### [0mReferences
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:105:[7m## [0mDev Agent Record
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:107:[7m### [0mAgent Model Used
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:111:[7m### [0mDebug Log References
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:116:[7m### [0mCompletion Notes List
specs\stories\feature.1152.eqc1-bucket-container-schema.story.md:122:[7m### [0mChange Log


diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5771d418907718366a5964e567794faf344e2e86
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,77 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:45:06.923627Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$story = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\specs\\stories\\feature.1152.eqc1-bucket-container-schema.story.md'; "'$story[0..52]' in D:\Terra Mortis\TM Suite
 succeeded in 2985ms:
# Story feature.1152: EQC-1 — Bucket Re-Partition, Container Schema, Data Migration

## Status: review

---
issue: 1152
issue_url: https://github.com/angelusvmorningstar/TerraMortis/issues/1152
branch: ms/issue-1152-eqc1-bucket-container-schema
---

## Story

**As a** Storyteller,
**I want** the equipment catalogue re-partitioned onto the four-category taxonomy decided at the 2026-07-25 meeting (combat gear / skill gear / tools-utility / narrative), with assets becoming containers rather than a stat-bearing bucket,
**so that** EQC's later shards (display distinctions, ST CRUD, programmatic purchase, skill-acquisition removal) have a correct, live-migrated data foundation to build on — and so nothing built on the OLD taxonomy silently breaks the moment this lands.

## Background

Sharded from Epic EQC (issue #1038, itself created 2026-07-25 the same day as the design meeting that decided this taxonomy). Epic ECM (issues #868-#876, merged 17-18 June 2026) had already moved the equipment catalogue from static JS into MongoDB, but kept the OLD four-value taxonomy (`weapon|armour|equipment|asset`) — a technical migration only, never a re-design. EQC is that re-design, finally being picked up now that Angelus owns TM Suite dev directly (Peter stepped back 2026-08-09).

**This story's scope was expanded mid-session from what issue #1152 originally said (schema-only).** A survey of every direct reader of `equipment_catalogue.bucket` found FOURTEEN consumer files across server and client, several of which are load-bearing for actual combat math (armour defence-penalty derivation, both roll calculators' weapon/skill-bonus chip filters). Shipping the schema's new enum without updating those consumers, or without migrating existing live data onto it, would make every existing character's equipment silently invisible to armour/roll derivation the instant the schema landed — a live-game-breaking regression, not a cosmetic gap. Angelus ruled (via `AskUserQuestion`, this session): **atomic, not dual-support** — schema, data migration, and every direct consumer land together as one coherent unit, so the branch is never in a broken intermediate state at any commit. This absorbed what would have been a separate `EQC-6` (data migration) issue — closed as duplicate/absorbed (#1157).

**Timing**: Game 7 is Saturday 2026-08-15. Angelus explicitly ruled this session that build work should proceed now regardless — nothing merges to `main` without his explicit say-so (TM Suite's own hard rule), so there is no live-game risk from working on a branch.

## The new taxonomy

| Old bucket | New bucket | Meaning change |
|---|---|---|
| `weapon` | `combat_gear` | Merged with armour. Weapon-shaped identified by `weapon_type`/`damage_mod`/`damage_type` populated. |
| `armour` | `combat_gear` | Merged with weapon. Armour-shaped identified by `armour_value`/`defence_penalty` populated. |
| `equipment` | `skill_gear` | Unchanged meaning (skill_domain + bonus_dice) — renamed only. |
| — | `tool_utility` | NEW. "Does a thing, no bonus" (epic #1038). No numeric bonus field; `mechanical_effect` free text only. |
| — | `narrative` | NEW. Purely descriptive; no bucket-specific stat fields at all. |
| `asset` | `container` | "Assets are containers... no stat-stacking on the asset itself" (epic #1038). Holds other equipment via the new `container_id` field, rather than granting a bonus of its own. |

`combat_gear` deliberately has NO new sub-type discriminator field — weapon-shaped vs armour-shaped is inferred from which stat fields are populated, matching the epic's own wording ("combat gear (weapons/armour, distinct stat fields)") and requiring no schema addition.

## Acceptance Criteria

1. `equipment_catalogue.schema.js`'s `bucket` enum reads `['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']`.
2. `character.schema.js`'s `equipment[]` items gain an optional `container_id` field: nullable, 24-hex-string pattern when present, referencing another item in the SAME character's `equipment[]` array whose catalogue bucket is `container`. Single-level containment only (a container's contents are never themselves valid container targets) — explicitly scoped out, not a bug.
3. `server/scripts/migrate-eqc1-bucket-taxonomy.mjs` exists, is pure-testable (`planBucketMigration`), dry-run by default, idempotent, and correctly maps every old bucket value to its new equivalent per the table above. An unrecognised bucket value is reported, never guessed or silently dropped.
4. Every direct reader of `entry.bucket === 'weapon'|'armour'|'equipment'|'asset'` is updated to the new taxonomy, preserving its exact prior behaviour for correctly-migrated data:
   - `equipment-derivation.js`: `armourDefencePenalty`, `wornArmourCount` — armour-shaped `combat_gear` only.
   - `roll.js`, `roll-v2.js`: skill-bonus chip filter (`skill_gear`), weapon-reference filter (weapon-shaped `combat_gear`).
   - `editor/sheet.js`: equipment renderer regrouped into Weapons / Armour / Other Combat Gear / Skill Gear / Tools-Utility / Narrative / Containers; add-form bucket dropdown updated.
   - `tabs/downtime-form.js`: catalogue dropdown optgroups.
   - `admin/equipment-catalogue-admin.js`: `BUCKETS`, `BUCKET_FIELDS`, default-bucket literals.
   - `admin-layout.css`: `.ec-bucket-*` tag colours re-keyed to the new values.
5. No behavioural regression for correctly-migrated data: an armour item that would have contributed its `defence_penalty` under the old bucket contributes identically under the new one; the same for weapon/skill-gear chips.
6. `npm test` (vitest) passes in full, including new tests for the migration script's pure planning function and an integration pass against `tm_suite_test`.
7. TM Wiki, TM Cockpit, and TM Herald are completely untouched by this story — it is TM Suite-only.


diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5771d418907718366a5964e567794faf344e2e86
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,77 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:45:13.497821Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --glob '*.js' --glob '*.json' --glob '*.mjs' \"armour_value|defence_penalty\" 'D:\\Terra Mortis\\TM Suite\\server' 'D:\\Terra Mortis\\TM Suite\\public' | Select-Object -First 250" in D:\Terra Mortis\TM Suite
 succeeded in 7635ms:
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:37:// armour_value-based discriminator.
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:43:  combat_gear:  ['damage_mod', 'damage_type', 'weapon_type', 'armour_value', 'defence_penalty'],
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:294:    armour_value: 'Armour value',
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:295:    defence_penalty: 'Defence penalty',
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:300:  const numericFields = new Set(['damage_mod', 'armour_value', 'defence_penalty', 'bonus_dice']);
D:\Terra Mortis\TM Suite\public\js\admin\equipment-catalogue-admin.js:347:  const numericFields = new Set(['damage_mod', 'armour_value', 'defence_penalty', 'bonus_dice']);
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2577:// damage_mod = weapon-shaped; armour_value/defence_penalty = armour-shaped),
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2606:  const isArmourShaped = e => e.armour_value != null || e.defence_penalty != null;
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2642:      h += '<div class="sh-armour-hint" style="font-size:0.85em;opacity:0.75;margin-bottom:6px;">Only one armour applies; highest defence_penalty wins.</div>';
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2650:        entry.armour_value    != null ? `AR ${entry.armour_value}` : null,
D:\Terra Mortis\TM Suite\public\js\editor\sheet.js:2651:        entry.defence_penalty != null ? `Defence ${baseDefence}(${baseDefence - entry.defence_penalty})` : null,
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:8: *       by state === 'worn' + bucket === 'combat_gear' with armour_value != null;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:13: *       is identified by `entry.armour_value != null` rather than by bucket
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:16: *       `armour_value: null` and is correctly excluded.
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:49: * 0 when no qualifying armour is worn. Negative defence_penalty values are
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:68:    if (!entry || entry.bucket !== 'combat_gear' || entry.armour_value == null) continue;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:69:    const p = Number.isInteger(entry.defence_penalty) ? entry.defence_penalty : 0;
D:\Terra Mortis\TM Suite\public\js\data\equipment-derivation.js:91:    if (entry?.bucket === 'combat_gear' && entry.armour_value != null) n++;
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:86:      armour_value: null, defence_penalty: null, skill_domain: null, bonus_dice: null,
D:\Terra Mortis\TM Suite\server\tests\equipment.test.js:54:    armour_value: null, defence_penalty: null,
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:60:    armour_value: null, defence_penalty: null,
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:2: * Issue #879 — defence_penalty wire-in (ADR-006).
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:14: *      _"Only one armour applies; highest defence_penalty wins."_ Wording
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:94:  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:96:    expect(src).toMatch(/entry\.armour_value\s*==\s*null/);
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:140:  it('renders the soft hint exactly as: "Only one armour applies; highest defence_penalty wins."', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:142:    expect(src).toContain('Only one armour applies; highest defence_penalty wins.');
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:247:      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:248:      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:257:  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:261:      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:262:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:271:  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:275:      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:281:  it('D2: returns the MAX defence_penalty across multiple worn armour items (worst-case stacking)', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:285:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:286:      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:287:      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:297:  it('treats null/undefined/non-integer defence_penalty as 0', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:301:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:302:      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:303:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:322:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:323:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:342:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:357:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:408:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\scripts\migrate-eqc1-bucket-taxonomy.mjs:8: *   armour    → combat_gear   (armour-shaped: armour_value/defence_penalty)
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:21: *                  armour_value/defence_penalty = armour-shaped). Both sets of
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:65:    armour_value:      { type: ['integer', 'null'] },
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:66:    defence_penalty:   { type: ['integer', 'null'] },
D:\Terra Mortis\TM Suite\server\schemas\equipment_catalogue.schema.js:92:  'armour_value', 'defence_penalty',

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5771d418907718366a5964e567794faf344e2e86
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,77 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:45:50.924578Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --glob '*.test.js' \"skill_gear|combat_gear|tool_utility|narrative|container\" 'D:\\Terra Mortis\\TM Suite\\server\\tests'" in D:\Terra Mortis\TM Suite
 succeeded in 3131ms:
D:\Terra Mortis\TM Suite\server\tests\api-publish-cycle.test.js:8: *   4. Blank narrative is published correctly (player gets empty string — a known risk, not a silent bug)
D:\Terra Mortis\TM Suite\server\tests\api-publish-cycle.test.js:94:    // ST stages the narrative
D:\Terra Mortis\TM Suite\server\tests\api-publish-cycle.test.js:160:      .send({ 'st_review.outcome_text': 'Corrected narrative.' });
D:\Terra Mortis\TM Suite\server\tests\api-publish-cycle.test.js:167:    expect(found.published_outcome).toBe('Corrected narrative.');
D:\Terra Mortis\TM Suite\server\tests\compile-push-outcome-joint.test.js:127:    st_narrative: {
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:67:    // `bucket === 'skill_gear'` filter clause counts. We confirm BOTH the
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:68:    // skill_gear filter and the 'active' check coexist within a small
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:70:    // EQC-1 (#1152): the old 'equipment' bucket is now 'skill_gear'.
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:71:    expect(src).toMatch(/bucket === 'skill_gear'[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:76:    // EQC-1 (#1152): the old 'weapon' bucket merged into 'combat_gear',
D:\Terra Mortis\TM Suite\server\tests\equipment-client-fixes.test.js:78:    expect(src).toMatch(/bucket === 'combat_gear' && entry\.weapon_type != null[\s\S]{0,400}item\.state === 'active'/);
D:\Terra Mortis\TM Suite\server\tests\feat-784-confirm-outcome-btn.test.js:85:      expect(handlerBlock).toMatch(/renderProcessingMode\(container\)/);
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:7: *   AC2 — Each builder reads the revision_note from the correct st_narrative path.
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:10: *          feeding narrative.
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:31:    // #886: Feeding and Project Reports lost their narrative-drafting layer
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:42:    expect(storyJs).not.toContain('dt-feed-val-narrative-block');
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:43:    expect(storyJs).not.toContain('dt-feed-narrative-ta');
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:73:    const sub = { st_narrative: { project_responses: [{}, { revision_note: 'Needs more detail on the contact' }] } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:75:    const revisionNote = sub.st_narrative?.project_responses?.[idx]?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:80:    const sub = { st_narrative: { project_responses: [{ revision_note: '' }] } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:81:    const revisionNote = sub.st_narrative?.project_responses?.[0]?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:86:    const sub = { st_narrative: { action_responses: [{ revision_note: 'Rewrite — too passive' }] } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:87:    const revisionNote = sub.st_narrative?.action_responses?.[0]?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:93:      st_narrative: {
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:101:    const terrReports  = sub.st_narrative?.territory_reports || [];
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:108:    const sub = { st_narrative: { territory_reports: [{ territory_id: 'harbour', revision_note: 'OK' }] } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:109:    const terrReport   = (sub.st_narrative?.territory_reports || []).find(r => r?.territory_id === 'academy') || {};
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:115:    const sub = { st_narrative: { home_report: { revision_note: 'Too long — cut to 60 words' } } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:116:    const revisionNote = sub.st_narrative?.home_report?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:121:    const sub = { st_narrative: { cacophony_savvy: [null, { revision_note: 'Wrong district mentioned' }] } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:123:    const revisionNote = sub?.st_narrative?.cacophony_savvy?.[slotIdx]?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:128:    const sub = { st_narrative: { story_moment: { revision_note: 'Touchstone name spelled wrong' } } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:129:    const revisionNote = sub?.st_narrative?.story_moment?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:134:    const sub = { st_narrative: { story_moment: { revision_note: 'Vignette is too long' } } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:135:    const revisionNote = sub?.st_narrative?.story_moment?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:139:  it('handleCopyFeedingContext reads feeding_narrative.revision_note', () => {
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:140:    const sub = { st_narrative: { feeding_narrative: { revision_note: 'Remove the mechanical reference to successes' } } };
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:141:    const revisionNote = sub.st_narrative?.feeding_narrative?.revision_note || '';
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:145:  it('all builders are safe against missing st_narrative (no throw)', () => {
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:147:    expect(() => (sub.st_narrative?.project_responses?.[0]?.revision_note   || '')).not.toThrow();
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:148:    expect(() => (sub.st_narrative?.action_responses?.[0]?.revision_note    || '')).not.toThrow();
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:149:    expect(() => (sub.st_narrative?.territory_reports || []).find(() => false)).not.toThrow();
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:150:    expect(() => (sub.st_narrative?.home_report?.revision_note              || '')).not.toThrow();
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:151:    expect(() => (sub?.st_narrative?.cacophony_savvy?.[0]?.revision_note    || '')).not.toThrow();
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:152:    expect(() => (sub?.st_narrative?.story_moment?.revision_note            || '')).not.toThrow();
D:\Terra Mortis\TM Suite\server\tests\fix.398.revision-note-prompt-injection.test.js:153:    expect(() => (sub.st_narrative?.feeding_narrative?.revision_note        || '')).not.toThrow();
D:\Terra Mortis\TM Suite\server\tests\fix.792.dt-proc-roller-gate-stale-pool.test.js:120:      'const builderEl = container.querySelector(`.proc-pool-builder[data-proc-key="${key}"]`)',
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:3: * containers.
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:8: * letter-spacing:2.5px`). Six call sites sit inside containers that style
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:17: *   1. the CONTENTS of each container, byte-for-byte, against strings
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:20: *      small-type containers;
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:185:describe('#1128 AC1 — the six containers are byte-identical to origin/main', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:186:  it('every container matches its main-captured golden string exactly', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:199:  it('no .trait-dots span survives anywhere inside the three small-type containers', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:241:// AC2 — a suspension still reaches all six containers
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:258: * The dot run inside a container's contents. Containers legitimately carry
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:261: * not about the surrounding copy. Byte-identity of the whole container is
D:\Terra Mortis\TM Suite\server\tests\issue-1128-dot-wrapper.test.js:416: * Bucket A = containers that style their own dots -> MUST be the plain variant.
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:17:  it('maps weapon -> combat_gear', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:19:    expect(r).toEqual({ touched: true, fromBucket: 'weapon', toBucket: 'combat_gear', reason: null });
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:22:  it('maps armour -> combat_gear', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:25:    expect(r.toBucket).toBe('combat_gear');
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:28:  it('maps equipment -> skill_gear', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:31:    expect(r.toBucket).toBe('skill_gear');
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:34:  it('maps asset -> container', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:37:    expect(r.toBucket).toBe('container');
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:41:    for (const bucket of ['combat_gear', 'skill_gear', 'tool_utility', 'narrative', 'container']) {
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:111:    expect((await col.findOne({ _id: wId })).bucket).toBe('combat_gear');
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:112:    expect((await col.findOne({ _id: aId })).bucket).toBe('combat_gear');
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:113:    expect((await col.findOne({ _id: eId })).bucket).toBe('skill_gear');
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:114:    expect((await col.findOne({ _id: asId })).bucket).toBe('container');
D:\Terra Mortis\TM Suite\server\tests\issue-1152-eqc1-bucket-migration.test.js:118:    const stillOld = await col.find({ _id: { $in: [wId, aId, eId, asId] }, bucket: { $nin: ['combat_gear', 'skill_gear', 'container'] } }).toArray();
D:\Terra Mortis\TM Suite\server\tests\issue-827-subtitle-order.test.js:183:  it('Haven row: "Attached: X" appears BEFORE trait-right (dots container)', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-868-ecm-1-equipment-catalogue-api.test.js:142:      .send({ bucket: 'combat_gear', name: 'Glock 17', description: 'Pistol', availability: 2, damage_mod: 2, weapon_type: 'ranged' });
D:\Terra Mortis\TM Suite\server\tests\issue-871-876-ecm-4-9-bundle.test.js:61:  it('emits optgroups per bucket (combat_gear / skill_gear / tool_utility / narrative / container — EQC-1 #1152)', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-871-876-ecm-4-9-bundle.test.js:63:    expect(src).toMatch(/for\s*\(\s*const\s+bucket\s+of\s+\[\s*['"]combat_gear['"]\s*,\s*['"]skill_gear['"]\s*,\s*['"]tool_utility['"]\s*,\s*['"]narrative['"]\s*,\s*['"]container['"]\s*\]/);
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:94:  it('filters by bucket === \'combat_gear\' AND armour_value != null (EQC-1 #1152: armour merged into combat_gear, distinguished by populated stat fields)', () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:95:    expect(src).toMatch(/entry\.bucket\s*!==\s*['"]combat_gear['"]/);
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:247:      { _id: 'aaa1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:248:      { _id: 'aaa2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:257:  it('D1: filters by bucket === \'combat_gear\' with armour_value != null — ignores worn non-armour items', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:261:      { _id: 'wep1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null, defence_penalty: 5 },   // weapon-shaped combat_gear (armour_value null), ignored despite matching bucket AND a populated defence_penalty
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:262:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:271:  it('EQC-1 (#1152): a non-combat_gear bucket is ignored even with armour_value populated', async () => {
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:275:      { _id: 'x1', bucket: 'narrative', armour_value: 3, defence_penalty: 9 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:285:      { _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:286:      { _id: 'arm2', bucket: 'combat_gear', armour_value: 1, defence_penalty: 3 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:287:      { _id: 'arm3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:301:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1, defence_penalty: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:302:      { _id: 'a2', bucket: 'combat_gear', armour_value: 1, defence_penalty: undefined },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:303:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1, defence_penalty: 'bad' },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:322:      { _id: 'a1', bucket: 'combat_gear', armour_value: 1 }, { _id: 'a2', bucket: 'combat_gear', armour_value: 1 },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:323:      { _id: 'a3', bucket: 'combat_gear', armour_value: 1 }, { _id: 'w1', bucket: 'combat_gear', weapon_type: 'melee', armour_value: null },
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:342:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:357:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 5 }];
D:\Terra Mortis\TM Suite\server\tests\issue-879-defence-penalty-wirein.test.js:408:    const items = [{ _id: 'arm1', bucket: 'combat_gear', armour_value: 1, defence_penalty: 2 }];
D:\Terra Mortis\TM Suite\server\tests\issue-886-dt-story-resolved-push.test.js:4: * After #886 the Story tab no longer re-authors per-section narratives. The
D:\Terra Mortis\TM Suite\server\tests\issue-886-dt-story-resolved-push.test.js:9: * resolved fields instead of the deleted st_narrative.* drafts.
D:\Terra Mortis\TM Suite\server\tests\issue-886-dt-story-resolved-push.test.js:77:    st_narrative: {
D:\Terra Mortis\TM Suite\server\tests\issue-886-dt-story-resolved-push.test.js:99:    // The draft layer is gone — no DTSR-7 narrative should be consulted.
D:\Terra Mortis\TM Suite\server\tests\n7b-necro-input-suppression.test.js:88:// merit-bd-row container is one HTML chunk per merit so we can slice the

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..5771d418907718366a5964e567794faf344e2e86
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,77 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:46:00.236706Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Pass 3a is now frozen conceptually, still without the Dev Agent Record. The acceptance text makes one additional schema-level problem clear: `container_id` is a catalogue ID, but character equipment entries have no per-instance ID and duplicates are allowed, so two identical containers cannot be distinguished. AC #5’s parity claim is also not demonstrated end-to-end for roll chips; the changed roll tests are source-regex checks rather than old/new behavioral equivalence tests.
apply patch
patch: completed
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-findings.md
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:46:29.839699Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
  ✓ Pass 1: inspect only the supplied diff and freeze blind findings
  ✓ Pass 2: inspect repository context, trace edge cases, and freeze findings
  ✓ Pass 3a: read only pre-record story sections and freeze AC findings
  → Pass 3b: read Dev Agent Record, run verification gates, and audit claims
  • Finalize severity-grouped report and validation attestation
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:46:34.725129Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Pass 3a is written. I’m now crossing the information barrier and reading the Dev Agent Record in full, then I’ll run every named equipment suite individually and audit the claimed 10-file failure signature against the current committed state.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$story = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\specs\\stories\\feature.1152.eqc1-bucket-container-schema.story.md'; "'$story[104..($story.Length - 1)]' in D:\Terra Mortis\TM Suite
 succeeded in 3199ms:
## Dev Agent Record

### Agent Model Used

Claude Sonnet 5.

### Debug Log References

- Consumer blast-radius confirmed by `grep -rln "'weapon'\|'armour'\|..."` across `server/` and `public/js/` before any edit — 14 files, not the 2 originally scoped in issue #1152.
- Verified `equipment_catalogue.schema.js`'s `validate()` middleware only runs on POST (create); PATCH uses an allowlist that already excludes `bucket` (immutable); GET performs no schema validation at all. This means the schema change cannot retroactively invalidate existing documents on read — only new-item creation is gated by the new enum, and the data migration is what actually moves existing documents onto it.

### Completion Notes List

- All five consumer-file categories updated; every equipment-related vitest suite re-run and green after each change, not just at the end.
- Full `npm test`: 2463 total, 2458 pass, **5 pre-existing failures across 10 test files** (`epic.708.3-cycle-phase-controls`, `issue-1013-indomitable-rules-text`, `issue-1021-failed-breakpoint-merit`, `issue-811-sumchannels-rootcause`, `issue-826-cleanup-script-integration`, `issue-836-legacy-tracker-cache-removed`, `issue-837-xp-totals-deprecation`, `n7-n9-allocator-readers` — the one CLAUDE.md already documents by name — `n8-mandragora-prereq`, `oath-a-pledge-helpers`). **Verified pre-existing, not caused by this story**: `git stash push` on all 13 tracked EQC-1 files, re-ran the same 10 files against the clean base — identical failure signature (10 failed files, 5 failed tests, same test names). `git stash pop` restored EQC-1's changes; the 9 equipment-related suites (170 tests) re-confirmed green after the round-trip. None of the 5 pre-existing failures reference equipment/bucket/armour/weapon/container in any way.
- The live migration was NOT run against production `tm_suite` — see Project Structure Notes. This is a deliberate, disclosed gap for the deploy step, not an oversight.

### Change Log

- 2026-08-13: EQC-1 implemented, scope expanded from schema-only to atomic (schema + migration + all direct consumers) per Angelus's ruling this session. New files: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs`, `server/tests/issue-1152-eqc1-bucket-migration.test.js`. Modified: 9 production files + 5 test files (see Project Structure Notes for the full list).

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:46:42.500042Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$story = Get-Content -LiteralPath '"'D:\\Terra Mortis\\TM Suite\\specs\\stories\\feature.1152.eqc1-bucket-container-schema.story.md'; "'$story[53..103]' in D:\Terra Mortis\TM Suite
 succeeded in 3043ms:
## Tasks / Subtasks

- [x] **Task 1 — Schema** (AC #1, #2)
  - [x] `equipment_catalogue.schema.js`: new bucket enum + doc-comment explaining the taxonomy and the migration dependency.
  - [x] `character.schema.js`: `equipment[]` items gain `container_id` (nullable, 24-hex pattern), with a doc-comment on single-level containment and referential looseness (unresolvable `container_id` is display-inert, not a write-time hard failure).

- [x] **Task 2 — Migration script** (AC #3)
  - [x] `server/scripts/migrate-eqc1-bucket-taxonomy.mjs`, following `backfill-free-grants.js`'s own pattern (pure compute function + dry-run-default CLI driver).
  - [x] `BUCKET_MAP` + `planBucketMigration` pure function; `migrate()` integration driver.
  - [x] Tests: `issue-1152-eqc1-bucket-migration.test.js` — pure-function coverage + a `tm_suite_test` integration pass proving apply + idempotency + unrecognised-value reporting.

- [x] **Task 3 — Consumer updates** (AC #4, #5)
  - [x] `equipment-derivation.js`: `armourDefencePenalty`/`wornArmourCount` predicates.
  - [x] `roll.js` + `roll-v2.js`: skill-bonus chip filter, weapon-reference filter.
  - [x] `editor/sheet.js`: full renderer regroup (Weapons/Armour/Other Combat Gear split within combat_gear; Skill Gear, Tools/Utility, Narrative, Containers sections); add-form `BUCKETS`/`BUCKET_LABELS`.
  - [x] `tabs/downtime-form.js`: dropdown `EQUIPMENT_BUCKET_LABELS` + optgroup loop.
  - [x] `admin/equipment-catalogue-admin.js`: `BUCKETS`, `BUCKET_FIELDS`, three default-bucket literal sites.
  - [x] `admin-layout.css`: `.ec-bucket-*` colour classes.

- [x] **Task 4 — Update existing tests for the new taxonomy** (AC #6)
  - [x] `issue-879-defence-penalty-wirein.test.js`: source-string check + every fixture; added an explicit "weapon-shaped combat_gear is excluded from armour derivation" test (this is the genuinely NEW risk the merge introduces, previously impossible to get wrong since weapon/armour were different buckets).
  - [x] `equipment-client-fixes.test.js`: `#752` source-string checks for the new predicate shapes.
  - [x] `issue-871-876-ecm-4-9-bundle.test.js`: optgroup-loop source-string check.
  - [x] `issue-868-ecm-1-equipment-catalogue-api.test.js`: one POST fixture using a retired bucket value corrected (`weapon` → `combat_gear` + `weapon_type`).
  - [x] `equipment.test.js`, `issue-896-availability-filter.test.js`, `issue-872-ecm-5-editor-cache.test.js`: left with incidental old-bucket literal fixtures — these exercise bucket-AGNOSTIC behaviour (CRUD mechanics, availability maths, cache lookup-by-key) and are not semantically wrong, just cosmetically stale. Noted here as a candidate follow-up cleanup, not a correctness gap this story needs to close.

- [x] **Task 5 — Full regression** (AC #6, #7)
  - [x] `npx vitest run` on every equipment-touching test file individually — green.
  - [x] Full `npm test` (vitest, all 171+ suites) — run as a final check given the live-game stakes.
  - [x] Confirm zero diff under TM Wiki, TM Cockpit, TM Herald — this story only ever used `Read`/`Grep`/`Bash(git ...)` there, no `Edit`/`Write`.

## Dev Notes

- **This is genuinely bigger than its own issue originally said.** The re-scoping happened mid-session after a direct grep confirmed the consumer blast radius; see the Background section above and the GitHub issue's own edit history (`gh issue edit 1152`, 2026-08-13) for the paper trail.
- **`combat_gear`'s weapon/armour sub-classification is entirely field-presence-driven.** This is the one genuinely NEW correctness risk the merge introduces: before this story, a weapon item and an armour item could never be confused because they lived in different buckets. After this story, they share a bucket, and an item with NEITHER `weapon_type` nor `armour_value` populated (an ST creating an item and not filling in stats yet) needs to render SOMEWHERE, not silently vanish — hence `editor/sheet.js`'s "Other Combat Gear" fallback section.
- **`container_id` is intentionally NOT recursive.** A container's own contents can never themselves be valid container targets in this schema. The epic's own examples (a safe inside a haven, a security system inside a property) are all single-level; recursive containment is real complexity with no stated requirement. A future story can lift this if actually needed — do not build it speculatively.
- **The migration script cannot run standalone ahead of the schema/consumer changes**, unlike `backfill-free-grants.js`'s own union-sum-guarded independence. See the script's own header comment for why.

### Project Structure Notes

- New files: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs`, `server/tests/issue-1152-eqc1-bucket-migration.test.js`.
- Modified: `server/schemas/equipment_catalogue.schema.js`, `server/schemas/character.schema.js`, `public/js/data/equipment-derivation.js`, `public/js/suite/roll.js`, `public/js/suite/roll-v2.js`, `public/js/editor/sheet.js`, `public/js/tabs/downtime-form.js`, `public/js/admin/equipment-catalogue-admin.js`, `public/css/admin-layout.css`, plus the five test files named in Task 4.
- **The live migration itself has NOT been run against real `tm_suite` data** — this story ships the schema, the script, and the consumer code. Running `migrate-eqc1-bucket-taxonomy.mjs --apply` against production is a deploy-time action, deliberately not taken from a dev session. Whoever merges/deploys this must run it (dry-run first, per the script's own usage notes) before/immediately after the deploy, or existing characters' combat gear goes dark until it's run.

### References

- Epic EQC, issue #1038.
- `2026-07-25_meeting-lessons.md` §Equipment, assets, containers (umbrella root).
- `specs/epic-ecm-equipment-catalogue-migration.md` — the prior, superseded-in-taxonomy-only migration this one follows.
- `server/scripts/backfill-free-grants.js` — the migration-script pattern this story's own script follows.


diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:46:51.222852Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\Terra Mortis\\TM Suite' status --short --branch; git -C 'D:\\Terra Mortis\\TM Suite' diff --name-status ddf059f8 c7e6771b" in D:\Terra Mortis\TM Suite
 succeeded in 5871ms:
## ms/issue-1152-eqc1-bucket-container-schema...origin/main [ahead 1]
 M public/css/suite.css
 M public/js/tabs/office-tab.js
 M server/index.js
 M server/tests/helpers/test-app.js
 M server/tests/issue-1141-office-tab-render.test.js
 M specs/stories/sprint-status.yaml
?? .netlify/
?? "Position power notes.rtf"
?? _bmad/custom/
?? data/exports/
?? debug.log
?? server/routes/office-manoeuvre-rank.js
?? server/scripts/_acad-backup-pre-edge1.json
?? server/scripts/_acad-backup-pre-edge2.json
?? server/scripts/_acad-backup-pre-edge3.json
?? server/scripts/_acad-backup-pre-edge4.json
?? server/scripts/_acad-backup-pre-edge5.json
?? server/scripts/_acad-backup-pre-edge6.json
?? server/scripts/_acad-backup-pre-edge7.json
?? server/scripts/_acad-backup-pre-smooth.json
?? server/scripts/_acad-canal.mjs
?? server/scripts/_acad-edge1-build.mjs
?? server/scripts/_acad-edge1-look.mjs
?? server/scripts/_acad-edge1.json
?? server/scripts/_acad-edge1.png
?? server/scripts/_acad-edge2-build.mjs
?? server/scripts/_acad-edge2.json
?? server/scripts/_acad-edge2.png
?? server/scripts/_acad-edge3-build.mjs
?? server/scripts/_acad-edge3-look.mjs
?? server/scripts/_acad-edge3.json
?? server/scripts/_acad-edge3.png
?? server/scripts/_acad-edge4-build.mjs
?? server/scripts/_acad-edge4-layout.mjs
?? server/scripts/_acad-edge4-look.mjs
?? server/scripts/_acad-edge4.json
?? server/scripts/_acad-edge4.png
?? server/scripts/_acad-edge5-build.mjs
?? server/scripts/_acad-edge5-cand.json
?? server/scripts/_acad-edge5-cand.mjs
?? server/scripts/_acad-edge5-cand.png
?? server/scripts/_acad-edge5-legs.mjs
?? server/scripts/_acad-edge5-look.mjs
?? server/scripts/_acad-edge5-render.mjs
?? server/scripts/_acad-edge5-render.png
?? server/scripts/_acad-edge5.json
?? server/scripts/_acad-edge5.png
?? server/scripts/_acad-edge6-build.mjs
?? server/scripts/_acad-edge6-full.png
?? server/scripts/_acad-edge6-look.mjs
?? server/scripts/_acad-edge6-splice.mjs
?? server/scripts/_acad-edge6.json
?? server/scripts/_acad-edge6.png
?? server/scripts/_acad-edge7-build.mjs
?? server/scripts/_acad-edge7.json
?? server/scripts/_acad-edge7.png
?? server/scripts/_acad-look.mjs
?? server/scripts/_acad-look.png
?? server/scripts/_acadcand.html
?? server/scripts/_acadedge1.html
?? server/scripts/_acadedge2.html
?? server/scripts/_acadedge3.html
?? server/scripts/_acadedge4.html
?? server/scripts/_acadedge5.html
?? server/scripts/_acadedge5render.html
?? server/scripts/_acadedge6.html
?? server/scripts/_acadedge7.html
?? server/scripts/_acadfull.html
?? server/scripts/_acadlook.html
?? server/scripts/_add-necropolis.js
?? server/scripts/_apply-geography.js
?? server/scripts/_arrow_build.html
?? server/scripts/_arrow_build.mjs
?? server/scripts/_arrow_build.png
?? server/scripts/_arrow_build2.html
?? server/scripts/_arrow_build2.mjs
?? server/scripts/_arrow_build2.png
?? server/scripts/_arrow_build3.html
?? server/scripts/_arrow_build3.mjs
?? server/scripts/_arrow_build3.png
?? server/scripts/_arrow_chk.mjs
?? server/scripts/_arrow_chk_malabartyrwhitt.html
?? server/scripts/_arrow_chk_malabartyrwhitt.png
?? server/scripts/_arrow_chk_wassellburke.html
?? server/scripts/_arrow_chk_wassellburke.png
?? server/scripts/_arrow_coast.json
?? server/scripts/_arrow_inland.html
?? server/scripts/_arrow_inland.mjs
?? server/scripts/_arrow_inland.png
?? server/scripts/_arrow_ne.html
?? server/scripts/_arrow_ne.mjs
?? server/scripts/_arrow_ne.png
?? server/scripts/_arrow_q.txt
?? server/scripts/_arrow_q2.txt
?? server/scripts/_arrow_ring.json
?? server/scripts/_arrow_roads.json
?? server/scripts/_arrow_se.html
?? server/scripts/_arrow_se.mjs
?? server/scripts/_arrow_se.png
?? server/scripts/_arrow_streets.html
?? server/scripts/_arrow_streets.mjs
?? server/scripts/_arrow_streets.png
?? server/scripts/_arrow_subb.html
?? server/scripts/_arrow_subb.mjs
?? server/scripts/_arrow_subb.png
?? server/scripts/_arrow_tyr.html
?? server/scripts/_arrow_tyr.mjs
?? server/scripts/_arrow_tyr.png
?? server/scripts/_arrow_view.html
?? server/scripts/_arrow_view.mjs
?? server/scripts/_arrow_view.png
?? server/scripts/_ath-crude-backup.json
?? server/scripts/_ath-inspect.html
?? server/scripts/_ath-inspect.mjs
?? server/scripts/_ath-inspect.png
?? server/scripts/_ath-junction.html
?? server/scripts/_ath-junction.mjs
?? server/scripts/_ath-junction.png
?? server/scripts/_ath-leg-build.mjs
?? server/scripts/_ath-leg1.html
?? server/scripts/_ath-leg1.json
?? server/scripts/_ath-leg1.png
?? server/scripts/_ath-leg2.html
?? server/scripts/_ath-leg2.json
?? server/scripts/_ath-leg2.png
?? server/scripts/_ath-leg3.html
?? server/scripts/_ath-leg3.json
?? server/scripts/_ath-leg3.png
?? server/scripts/_ath-leg4.json
?? server/scripts/_ath-nineways.html
?? server/scripts/_ath-nineways.mjs
?? server/scripts/_ath-nineways.png
?? server/scripts/_ath-orient-render.mjs
?? server/scripts/_ath-orient.html
?? server/scripts/_ath-orient.mjs
?? server/scripts/_ath-orient.png
?? server/scripts/_ath-provisional.mjs
?? server/scripts/_ath-splice-final.mjs
?? server/scripts/_athbound.json
?? server/scripts/_athq.overpass
?? server/scripts/_audit-haven-merits.js
?? server/scripts/_audit-submission-shapes.js
?? server/scripts/_bareisland.json
?? server/scripts/_bareisland_q.txt
?? server/scripts/_bee.json
?? server/scripts/_bee.ql
?? server/scripts/_bh-inspect.html
?? server/scripts/_bh-inspect.mjs
?? server/scripts/_bh-inspect.png
?? server/scripts/_bh-nodes.mjs
?? server/scripts/_bh-nodes.png
?? server/scripts/_bh-ring.json
?? server/scripts/_bh.html
?? server/scripts/_bhgeom.json
?? server/scripts/_bhgeomq.overpass
?? server/scripts/_bhout.json
?? server/scripts/_bhout2.json
?? server/scripts/_bhq.overpass
?? server/scripts/_bhq2.overpass
?? server/scripts/_bi2.json
?? server/scripts/_bi2_q.txt
?? server/scripts/_bi_extend.html
?? server/scripts/_bi_extend.mjs
?? server/scripts/_bi_extend.png
?? server/scripts/_bj.json
?? server/scripts/_bj.ql
?? server/scripts/_bj2.json
?? server/scripts/_bj2.ql
?? server/scripts/_br-commit.mjs
?? server/scripts/_br-draw.html
?? server/scripts/_br-draw.mjs
?? server/scripts/_br-draw.png
?? server/scripts/_br-orient.html
?? server/scripts/_br-orient.mjs
?? server/scripts/_br-orient.png
?? server/scripts/_br-union.json
?? server/scripts/_br-zoom.html
?? server/scripts/_br-zoom.mjs
?? server/scripts/_br-zoom.png
?? server/scripts/_br.json
?? server/scripts/_br.ql
?? server/scripts/_br2.json
?? server/scripts/_br2.ql
?? server/scripts/_br3.json
?? server/scripts/_br3.ql
?? server/scripts/_brad-chain.json
?? server/scripts/_build-eastcliff-final.js
?? server/scripts/_build-eastcliff.js
?? server/scripts/_build-eastcliff3.js
?? server/scripts/_carthian-backup-crude.json
?? server/scripts/_carthian-build.mjs
?? server/scripts/_carthian-build2.mjs
?? server/scripts/_carthian-island.json
?? server/scripts/_carthian2.html
?? server/scripts/_carthian2.png
?? server/scripts/_char-canon-crossref.js
?? server/scripts/_char-data-audit.js
?? server/scripts/_check-lore-qextra.js
?? server/scripts/_check-qkey-mapping.js
?? server/scripts/_check-response-shape.js
?? server/scripts/_check-retired.js
?? server/scripts/_check-rules-complete.js
?? server/scripts/_check-wan-covenant.js
?? server/scripts/_coast2.json
?? server/scripts/_court.json
?? server/scripts/_court_preview.html
?? server/scripts/_court_preview.mjs
?? server/scripts/_court_preview.png
?? server/scripts/_court_q.txt
?? server/scripts/_covenant-structure.js
?? server/scripts/_coverage.html
?? server/scripts/_coverage.js
?? server/scripts/_coverage.mjs
?? server/scripts/_coverage.png
?? server/scripts/_cp2_bj.html
?? server/scripts/_cp2_bj.mjs
?? server/scripts/_cp2_bj.png
?? server/scripts/_cp2_build.html
?? server/scripts/_cp2_build.mjs
?? server/scripts/_cp2_build.png
?? server/scripts/_cp2_junctions.html
?? server/scripts/_cp2_junctions.mjs
?? server/scripts/_cp2_junctions.png
?? server/scripts/_cp2_q.txt
?? server/scripts/_cp2_ring.json
?? server/scripts/_cp2_roads.json
?? server/scripts/_cp2_syd.json
?? server/scripts/_cp_build.html
?? server/scripts/_cp_build.mjs
?? server/scripts/_cp_build.png
?? server/scripts/_cp_centennial.json
?? server/scripts/_cp_parks.json
?? server/scripts/_cp_precinct.json
?? server/scripts/_cp_q.txt
?? server/scripts/_cp_ring.html
?? server/scripts/_cp_ring.json
?? server/scripts/_cp_ring.mjs
?? server/scripts/_cp_ring.png
?? server/scripts/_cp_roads.json
?? server/scripts/_cp_roads_q.txt
?? server/scripts/_cp_rough.html
?? server/scripts/_cp_rough.json
?? server/scripts/_cp_rough.mjs
?? server/scripts/_cp_rough.png
?? server/scripts/_cp_sub_q.txt
?? server/scripts/_cp_subs_meta.json
?? server/scripts/_cr-crude-backup.json
?? server/scripts/_cr-final.png
?? server/scripts/_cr-inspect.html
?? server/scripts/_cr-inspect.mjs
?? server/scripts/_cr-inspect.png
?? server/scripts/_cr-leg1-build.mjs
?? server/scripts/_cr-leg1.html
?? server/scripts/_cr-leg1.json
?? server/scripts/_cr-leg1.png
?? server/scripts/_cr-leg2-build.mjs
?? server/scripts/_cr-leg2.html
?? server/scripts/_cr-leg2.json
?? server/scripts/_cr-leg2.png
?? server/scripts/_cr-leg3-build.mjs
?? server/scripts/_cr-leg3.html
?? server/scripts/_cr-leg3.json
?? server/scripts/_cr-leg3.png
?? server/scripts/_cr-leg4-build.mjs
?? server/scripts/_cr-leg4.html
?? server/scripts/_cr-leg4.json
?? server/scripts/_cr-leg4.png
?? server/scripts/_cr-leg5-build.mjs
?? server/scripts/_cr-leg5.html
?? server/scripts/_cr-leg5.json
?? server/scripts/_cr-leg5.png
?? server/scripts/_cr-orient-render.mjs
?? server/scripts/_cr-orient.html
?? server/scripts/_cr-orient.mjs
?? server/scripts/_cr-orient.png
?? server/scripts/_cr-provisional.mjs
?? server/scripts/_cr-splice-final.mjs
?? server/scripts/_crbound.json
?? server/scripts/_crbq.overpass
?? server/scripts/_crfeat.json
?? server/scripts/_crleg3q.overpass
?? server/scripts/_crleg3roads.json
?? server/scripts/_crleg5q.overpass
?? server/scripts/_crleg5roads.json
?? server/scripts/_crq.overpass
?? server/scripts/_curzon.json
?? server/scripts/_curzon_q.txt
?? server/scripts/_cycle-map.js
?? server/scripts/_dock-airport-build.mjs
?? server/scripts/_dock-airport-splice.mjs
?? server/scripts/_dock-airport.json
?? server/scripts/_dock-airport.png
?? server/scripts/_dock-backup-pre-airport.json
?? server/scripts/_dock-backup-pre-botany.json
?? server/scripts/_dock-backup-pre-bumborah.json
?? server/scripts/_dock-backup-pre-canal.json
?? server/scripts/_dock-backup-pre-corner.json
?? server/scripts/_dock-backup-pre-edge1.json
?? server/scripts/_dock-backup-pre-hayes.json
?? server/scripts/_dock-backup-pre-hayesfix.json
?? server/scripts/_dock-backup-pre-page.json
?? server/scripts/_dock-backup-pre-partial.json
?? server/scripts/_dock-backup-pre-scd.json
?? server/scripts/_dock-botany-build.mjs
?? server/scripts/_dock-botany-splice.mjs
?? server/scripts/_dock-botany.json
?? server/scripts/_dock-botany.png
?? server/scripts/_dock-bumborah-build.mjs
?? server/scripts/_dock-bumborah-pt.json
?? server/scripts/_dock-bumborah-splice.mjs
?? server/scripts/_dock-bumborah.json
?? server/scripts/_dock-bumborah.png
?? server/scripts/_dock-canal-build.mjs
?? server/scripts/_dock-canal-build.png
?? server/scripts/_dock-canal-look.mjs
?? server/scripts/_dock-canal-look.png
?? server/scripts/_dock-canal-splice.mjs
?? server/scripts/_dock-canal-trace.mjs
?? server/scripts/_dock-canal-trace.png
?? server/scripts/_dock-canal.json
?? server/scripts/_dock-close-build.mjs
?? server/scripts/_dock-close-canal.json
?? server/scripts/_dock-close-coast.json
?? server/scripts/_dock-close.png
?? server/scripts/_dock-closed.png
?? server/scripts/_dock-corner-fix.mjs
?? server/scripts/_dock-corner.png
?? server/scripts/_dock-edge1-build.mjs
?? server/scripts/_dock-edge1-look.mjs
?? server/scripts/_dock-edge1.json
?? server/scripts/_dock-edge1.png
?? server/scripts/_dock-foreshore-look.mjs
?? server/scripts/_dock-foreshore-look.png
?? server/scripts/_dock-foreshore2-look.mjs
?? server/scripts/_dock-foreshore2-look.png
?? server/scripts/_dock-full.png
?? server/scripts/_dock-hayes-build.mjs
?? server/scripts/_dock-hayes-fix.mjs
?? server/scripts/_dock-hayes-fix.png
?? server/scripts/_dock-hayes-look.mjs
?? server/scripts/_dock-hayes-look.png
?? server/scripts/_dock-hayes-splice.mjs
?? server/scripts/_dock-hayes.json
?? server/scripts/_dock-hayes.png
?? server/scripts/_dock-jct.png
?? server/scripts/_dock-look.mjs
?? server/scripts/_dock-look.png
?? server/scripts/_dock-page-build.mjs
?? server/scripts/_dock-page-splice.mjs
?? server/scripts/_dock-page.json
?? server/scripts/_dock-page.png
?? server/scripts/_dock-partial-build.mjs
?? server/scripts/_dock-partial-splice.mjs
?? server/scripts/_dock-partial.json
?? server/scripts/_dock-partial.png
?? server/scripts/_dock-scd-build.mjs
?? server/scripts/_dock-scd-splice.mjs
?? server/scripts/_dock-scd.json
?? server/scripts/_dock-scd.png
?? server/scripts/_dockairport.html
?? server/scripts/_dockbotany.html
?? server/scripts/_dockbum.html
?? server/scripts/_dockcanal.html
?? server/scripts/_dockcanalb.html
?? server/scripts/_dockcanaltr.html
?? server/scripts/_dockclose.html
?? server/scripts/_dockclosed.html
?? server/scripts/_dockcorner.html
?? server/scripts/_dockedge1.html
?? server/scripts/_dockfore.html
?? server/scripts/_dockfore2.html
?? server/scripts/_dockfull.html
?? server/scripts/_dockhayes.html
?? server/scripts/_dockhayesb.html
?? server/scripts/_dockhayesfix.html
?? server/scripts/_dockjct.html
?? server/scripts/_docklook.html
?? server/scripts/_dockpage.html
?? server/scripts/_dockpartial.html
?? server/scripts/_dockscd.html
?? server/scripts/_dossier-audit.js
?? server/scripts/_dossier-stub-cleanup.js
?? server/scripts/_dt-extract.js
?? server/scripts/_dt-link-spec.md
?? server/scripts/_dt-mongo-shape.js
?? server/scripts/_dt-survey.js
?? server/scripts/_dt1-assemble.js
?? server/scripts/_dt1-links-A.json
?? server/scripts/_dt1-links-B.json
?? server/scripts/_dt1-links-C.json
?? server/scripts/_dt1-links-D.json
?? server/scripts/_dt1-summary.js
?? server/scripts/_dt2-links-A.json
?? server/scripts/_dt2-links-B.json
?? server/scripts/_dt2-links-C.json
?? server/scripts/_dt2-links-D.json
?? server/scripts/_dt2-shape.js
?? server/scripts/_dt2-submissions.json
?? server/scripts/_dt234-extend.js
?? server/scripts/_dt3-backup-check.js
?? server/scripts/_dt3-canon-apply.js
?? server/scripts/_dt3-from-backup.js
?? server/scripts/_dt3-links-A.json
?? server/scripts/_dt3-links-B.json
?? server/scripts/_dt3-links-C.json
?? server/scripts/_dt3-submissions.json
?? server/scripts/_dt34-dump.js
?? server/scripts/_dt4-havens.js
?? server/scripts/_dt4-havens2.js
?? server/scripts/_dt4-links-A.json
?? server/scripts/_dt4-links-B.json
?? server/scripts/_dt4-links-C.json
?? server/scripts/_dt4-shape.js
?? server/scripts/_dt4-submissions.json
?? server/scripts/_dump-kmz-raw.js
?? server/scripts/_dump-rules-rubric.js
?? server/scripts/_east-roads-raw.json
?? server/scripts/_east-ways-topo.json
?? server/scripts/_ec-backup-pre-edge1.json
?? server/scripts/_ec-backup-pre-edge2.json
?? server/scripts/_ec-backup-pre-edge3.json
?? server/scripts/_ec-closed.png
?? server/scripts/_ec-edge1-build.mjs
?? server/scripts/_ec-edge1-ends.json
?? server/scripts/_ec-edge1-splice.mjs
?? server/scripts/_ec-edge1.json
?? server/scripts/_ec-edge1.png
?? server/scripts/_ec-edge2-build.mjs
?? server/scripts/_ec-edge2-ends.json
?? server/scripts/_ec-edge2-splice.mjs
?? server/scripts/_ec-edge2.json
?? server/scripts/_ec-edge2.png
?? server/scripts/_ec-edge3-build.mjs
?? server/scripts/_ec-edge3-ends.json
?? server/scripts/_ec-edge3-splice.mjs
?? server/scripts/_ec-edge3.json
?? server/scripts/_ec-edge3.png
?? server/scripts/_ec-shot.html
?? server/scripts/_ec-shot.mjs
?? server/scripts/_ec-shot.png
?? server/scripts/_ecclosed.html
?? server/scripts/_ecedge1.html
?? server/scripts/_ecedge2.html
?? server/scripts/_ecedge3.html
?? server/scripts/_einar-secret-fix.js
?? server/scripts/_ely-add.mjs
?? server/scripts/_ely-shot.html
?? server/scripts/_ely-shot.mjs
?? server/scripts/_ely-shot.png
?? server/scripts/_ely.json
?? server/scripts/_ely.ql
?? server/scripts/_ex-backup-pre-coast12.json
?? server/scripts/_ex-backup-pre-coast345.json
?? server/scripts/_ex-backup-pre-coasthug.json
?? server/scripts/_ex-backup-pre-edge1.json
?? server/scripts/_ex-backup-pre-sdfix.json
?? server/scripts/_ex-backup-pre-sside.json
?? server/scripts/_ex-closed.png
?? server/scripts/_ex-coast-hug.mjs
?? server/scripts/_ex-coast-leg12.mjs
?? server/scripts/_ex-coast-leg345.mjs
?? server/scripts/_ex-coast12-splice.mjs
?? server/scripts/_ex-coast12.json
?? server/scripts/_ex-coast12.png
?? server/scripts/_ex-coast345-ends.json
?? server/scripts/_ex-coast345-splice.mjs
?? server/scripts/_ex-coast345.json
?? server/scripts/_ex-coast345.png
?? server/scripts/_ex-coasthug-ends.json
?? server/scripts/_ex-coasthug-splice.mjs
?? server/scripts/_ex-coasthug.json
?? server/scripts/_ex-coasthug.png
?? server/scripts/_ex-creek-leg1.mjs
?? server/scripts/_ex-creek1-ends.json
?? server/scripts/_ex-creek1.json
?? server/scripts/_ex-creek1.png
?? server/scripts/_ex-edge1-build.mjs
?? server/scripts/_ex-edge1-ends.json
?? server/scripts/_ex-edge1-splice.mjs
?? server/scripts/_ex-edge1.json
?? server/scripts/_ex-edge1.png
?? server/scripts/_ex-look.mjs
?? server/scripts/_ex-look.png
?? server/scripts/_ex-sd-fix.mjs
?? server/scripts/_ex-sd-legs.mjs
?? server/scripts/_ex-sdcheck.mjs
?? server/scripts/_ex-sdcheck2.mjs
?? server/scripts/_ex-sdcheck2.png
?? server/scripts/_ex-sdfix.png
?? server/scripts/_ex-sside-ends.json
?? server/scripts/_ex-sside-splice.mjs
?? server/scripts/_ex-sside.json
?? server/scripts/_ex-sside.png
?? server/scripts/_exclosed.html
?? server/scripts/_excoast12.html
?? server/scripts/_excoast345.html
?? server/scripts/_excoasthug.html
?? server/scripts/_excreek1.html
?? server/scripts/_exedge1.html
?? server/scripts/_exlook.html
?? server/scripts/_export-kml-hqs.mjs
?? server/scripts/_export-kml-local.mjs
?? server/scripts/_export-kml.js
?? server/scripts/_exsdcheck2.html
?? server/scripts/_exsdfix.html
?? server/scripts/_exsside.html
?? server/scripts/_facts-A.json
?? server/scripts/_facts-B.json
?? server/scripts/_facts-C.json
?? server/scripts/_facts-D.json
?? server/scripts/_facts-review.js
?? server/scripts/_fernleigh.json
?? server/scripts/_fetch-coast2.js
?? server/scripts/_fetch-fernleigh.js
?? server/scripts/_fetch-map-img.js
?? server/scripts/_fetch-roads.js
?? server/scripts/_fetch-tile.js
?? server/scripts/_fetch-tile2.js
?? server/scripts/_fetch-tile3.js
?? server/scripts/_fetch-tile4.js
?? server/scripts/_fetch-towns.js
?? server/scripts/_ff.json
?? server/scripts/_ff_q.txt
?? server/scripts/_fix-carver-fact.js
?? server/scripts/_fp.json
?? server/scripts/_fp2.json
?? server/scripts/_fp_locate.html
?? server/scripts/_fp_locate.mjs
?? server/scripts/_fp_locate.png
?? server/scripts/_fp_q.txt
?? server/scripts/_fp_q2.txt
?? server/scripts/_frank.json
?? server/scripts/_frank_q.txt
?? server/scripts/_gap-probe.js
?? server/scripts/_gap.json
?? server/scripts/_gap_q.txt
?? server/scripts/_gb-15.png
?? server/scripts/_gb-backup-pre-edge1.json
?? server/scripts/_gb-backup-pre-legs15.json
?? server/scripts/_gb-backup-pre-legs67.json
?? server/scripts/_gb-commit.mjs
?? server/scripts/_gb-draw.html
?? server/scripts/_gb-draw.mjs
?? server/scripts/_gb-draw.png
?? server/scripts/_gb-draw2.html
?? server/scripts/_gb-draw2.mjs
?? server/scripts/_gb-draw2.png
?? server/scripts/_gb-draw3.html
?? server/scripts/_gb-draw3.mjs
?? server/scripts/_gb-draw3.png
?? server/scripts/_gb-draw4.html
?? server/scripts/_gb-draw4.mjs
?? server/scripts/_gb-draw4.png
?? server/scripts/_gb-edge1-build.mjs
?? server/scripts/_gb-edge1-ends.json
?? server/scripts/_gb-edge1-splice.mjs
?? server/scripts/_gb-edge1.json
?? server/scripts/_gb-edge1.png
?? server/scripts/_gb-full.png
?? server/scripts/_gb-leg1-build.mjs
?? server/scripts/_gb-leg1.json
?? server/scripts/_gb-leg1.png
?? server/scripts/_gb-leg2-build.mjs
?? server/scripts/_gb-leg2-exit.json
?? server/scripts/_gb-leg2.json
?? server/scripts/_gb-leg2.png
?? server/scripts/_gb-leg3-build.mjs
?? server/scripts/_gb-leg3-exit.json
?? server/scripts/_gb-leg3.json
?? server/scripts/_gb-leg3.png
?? server/scripts/_gb-leg4-build.mjs
?? server/scripts/_gb-leg4-exit.json
?? server/scripts/_gb-leg4.json
?? server/scripts/_gb-leg4.png
?? server/scripts/_gb-leg5-build.mjs
?? server/scripts/_gb-leg5-exit.json
?? server/scripts/_gb-leg5.json
?? server/scripts/_gb-leg5.png
?? server/scripts/_gb-leg6-build.mjs
?? server/scripts/_gb-leg6-ends.json
?? server/scripts/_gb-leg6-exit.json
?? server/scripts/_gb-leg6-look.mjs
?? server/scripts/_gb-leg6-look.png
?? server/scripts/_gb-leg6.json
?? server/scripts/_gb-leg6.png
?? server/scripts/_gb-leg6b-build.mjs
?? server/scripts/_gb-leg6b.png
?? server/scripts/_gb-leg7-build.mjs
?? server/scripts/_gb-leg7.json
?? server/scripts/_gb-leg7.png
?? server/scripts/_gb-leg7b-build.mjs
?? server/scripts/_gb-leg7b.png
?? server/scripts/_gb-legs15-splice.mjs
?? server/scripts/_gb-legs67-splice.mjs
?? server/scripts/_gb-look.mjs
?? server/scripts/_gb-look.png
?? server/scripts/_gb-orient.html
?? server/scripts/_gb-orient.mjs
?? server/scripts/_gb-orient.png
?? server/scripts/_gb-orient2.html
?? server/scripts/_gb-orient2.mjs
?? server/scripts/_gb-orient2.png
?? server/scripts/_gb-orient3.html
?? server/scripts/_gb-orient3.mjs
?? server/scripts/_gb-orient3.png
?? server/scripts/_gb-probe.mjs
?? server/scripts/_gb-reserves.json
?? server/scripts/_gb-reserves.mjs
?? server/scripts/_gb-reserves.png
?? server/scripts/_gb-se.html
?? server/scripts/_gb-se.mjs
?? server/scripts/_gb-se.png
?? server/scripts/_gb-union.json
?? server/scripts/_gb-w.png
?? server/scripts/_gb.json
?? server/scripts/_gb.ql
?? server/scripts/_gb15.html
?? server/scripts/_gb2.json
?? server/scripts/_gb2.ql
?? server/scripts/_gb3.json
?? server/scripts/_gb3.ql
?? server/scripts/_gb4.json
?? server/scripts/_gb4.ql
?? server/scripts/_gb5.json
?? server/scripts/_gb5.ql
?? server/scripts/_gb6.json
?? server/scripts/_gb6.ql
?? server/scripts/_gbedge1.html
?? server/scripts/_gbfull.html
?? server/scripts/_gbleg1.html
?? server/scripts/_gbleg2.html
?? server/scripts/_gbleg3.html
?? server/scripts/_gbleg4.html
?? server/scripts/_gbleg5.html
?? server/scripts/_gbleg6.html
?? server/scripts/_gbleg6b.html
?? server/scripts/_gbleg6look.html
?? server/scripts/_gbleg7.html
?? server/scripts/_gbleg7b.html
?? server/scripts/_gblook.html
?? server/scripts/_gbres.html
?? server/scripts/_gbw.html
?? server/scripts/_geist.json
?? server/scripts/_geist_preview.html
?? server/scripts/_geist_preview.mjs
?? server/scripts/_geist_preview.png
?? server/scripts/_geist_q.txt
?? server/scripts/_geocode-havens.js
?? server/scripts/_georges.json
?? server/scripts/_georges.ql
?? server/scripts/_glad.json
?? server/scripts/_glad.ql
?? server/scripts/_gr-commit.mjs
?? server/scripts/_gr-draw.html
?? server/scripts/_gr-draw.mjs
?? server/scripts/_gr-draw.png
?? server/scripts/_gr-draw2.mjs
?? server/scripts/_gr-draw2.png
?? server/scripts/_gr-inspect.html
?? server/scripts/_gr-inspect.mjs
?? server/scripts/_gr-inspect.png
?? server/scripts/_gr-ne.html
?? server/scripts/_gr-ne.mjs
?? server/scripts/_gr-ne.png
?? server/scripts/_gr-union.json
?? server/scripts/_gr.json
?? server/scripts/_gr.ql
?? server/scripts/_gw-chain0.json
?? server/scripts/_gw-close.mjs
?? server/scripts/_gw-coast.mjs
?? server/scripts/_gw-coastpath.html
?? server/scripts/_gw-coastpath.mjs
?? server/scripts/_gw-coastpath.png
?? server/scripts/_gw-crude-backup.json
?? server/scripts/_gw-fixne.mjs
?? server/scripts/_gw-fixne2.mjs
?? server/scripts/_gw-inspect.html
?? server/scripts/_gw-inspect.mjs
?? server/scripts/_gw-inspect.png
?? server/scripts/_gw-layout.html
?? server/scripts/_gw-layout.mjs
?? server/scripts/_gw-layout.png
?? server/scripts/_gw-leg1-build.mjs
?? server/scripts/_gw-leg1.html
?? server/scripts/_gw-leg1.json
?? server/scripts/_gw-leg1.png
?? server/scripts/_gw-leg2-build.mjs
?? server/scripts/_gw-leg2.html
?? server/scripts/_gw-leg2.json
?? server/scripts/_gw-leg2.png
?? server/scripts/_gw-leg3-build.mjs
?? server/scripts/_gw-leg3.html
?? server/scripts/_gw-leg3.json
?? server/scripts/_gw-leg3.png
?? server/scripts/_gw-lock.mjs
?? server/scripts/_gw-ne.html
?? server/scripts/_gw-ne.mjs
?? server/scripts/_gw-ne.png
?? server/scripts/_gw-orient.mjs
?? server/scripts/_gw-provisional.mjs
?? server/scripts/_gw-state.mjs
?? server/scripts/_gwbound.json
?? server/scripts/_gwkb.json
?? server/scripts/_gwkbq.overpass
?? server/scripts/_gwq.overpass
?? server/scripts/_gwwater_tags.json
?? server/scripts/_gwwq.overpass
?? server/scripts/_hallow.json
?? server/scripts/_hallow2.json
?? server/scripts/_hallow_q.txt
?? server/scripts/_hallow_q2.txt
?? server/scripts/_harbour-boundary-spec.md
?? server/scripts/_havens-and-locations.js
?? server/scripts/_hb-backup-cahill-intact.json
?? server/scripts/_hb-backup-pre-carve.json
?? server/scripts/_hb-backup-pre-conformpg.json
?? server/scripts/_hb-backup-pre-conformvp.json
?? server/scripts/_hb-backup-pre-e.json
?? server/scripts/_hb-backup-pre-mscfix.json
?? server/scripts/_hb-backup-pre-n.json
?? server/scripts/_hb-backup-pre-ne.json
?? server/scripts/_hb-backup-pre-p1.json
?? server/scripts/_hb-backup-pre-pierone.json
?? server/scripts/_hb-backup-pre-se.json
?? server/scripts/_hb-backup-pre-step3.json
?? server/scripts/_hb-backup-pre-sw.json
?? server/scripts/_hb-bara-hug.json
?? server/scripts/_hb-bara-hug.mjs
?? server/scripts/_hb-bara-hug.png
?? server/scripts/_hb-bara-look.mjs
?? server/scripts/_hb-bara-look.png
?? server/scripts/_hb-bp26-v2.json
?? server/scripts/_hb-bp26-v2.mjs
?? server/scripts/_hb-bp26-v3.json
?? server/scripts/_hb-bp26-v3.mjs
?? server/scripts/_hb-bridge-pier26.mjs
?? server/scripts/_hb-bridge-touch.json
?? server/scripts/_hb-bridge-touch.mjs
?? server/scripts/_hb-bridge-touch.png
?? server/scripts/_hb-carve-write.js
?? server/scripts/_hb-carved-render.mjs
?? server/scripts/_hb-carved.json
?? server/scripts/_hb-carved.png
?? server/scripts/_hb-check.mjs
?? server/scripts/_hb-check.png
?? server/scripts/_hb-clear.mjs
?? server/scripts/_hb-clear.png
?? server/scripts/_hb-conform-gp.mjs
?? server/scripts/_hb-conform-pg-write.js
?? server/scripts/_hb-conform-pg.mjs
?? server/scripts/_hb-conform-vp-write.js
?? server/scripts/_hb-conform-vp.mjs
?? server/scripts/_hb-conformed-gp.json
?? server/scripts/_hb-conformed-gp.png
?? server/scripts/_hb-conformed-pg.json
?? server/scripts/_hb-conformed-pg.png
?? server/scripts/_hb-conformed-vp.json
?? server/scripts/_hb-conformed-vp.png
?? server/scripts/_hb-crescent-probe.mjs
?? server/scripts/_hb-crescent-probe.png
?? server/scripts/_hb-crescent-raw.json
?? server/scripts/_hb-crescent-south.mjs
?? server/scripts/_hb-crescent-south.png
?? server/scripts/_hb-e-build.mjs
?? server/scripts/_hb-e-line.json
?? server/scripts/_hb-extend-bradfield.json
?? server/scripts/_hb-extend-bradfield.mjs
?? server/scripts/_hb-extend-bradfield.png
?? server/scripts/_hb-find-concave.mjs
?? server/scripts/_hb-find-concave.png
?? server/scripts/_hb-hug-jones-save.js
?? server/scripts/_hb-hug-jones.json
?? server/scripts/_hb-hug-jones.mjs
?? server/scripts/_hb-hug-jones.png
?? server/scripts/_hb-hug-jones2.json
?? server/scripts/_hb-hug-jones2.mjs
?? server/scripts/_hb-hug-jones2.png
?? server/scripts/_hb-hug-jp-save.js
?? server/scripts/_hb-hug-jp.json
?? server/scripts/_hb-hug-jp.mjs
?? server/scripts/_hb-hug-jp.png
?? server/scripts/_hb-hug-zoom.mjs
?? server/scripts/_hb-hug-zoom.png
?? server/scripts/_hb-landmarks.mjs
?? server/scripts/_hb-landmarks.png
?? server/scripts/_hb-move-node.mjs
?? server/scripts/_hb-move99.mjs
?? server/scripts/_hb-move99.png
?? server/scripts/_hb-moved-write.js
?? server/scripts/_hb-moved.json
?? server/scripts/_hb-moved.png
?? server/scripts/_hb-msc-tight.mjs
?? server/scripts/_hb-msc-tight.png
?? server/scripts/_hb-msc-zoom.mjs
?? server/scripts/_hb-msc-zoom.png
?? server/scripts/_hb-n-build.mjs
?? server/scripts/_hb-n-line.json
?? server/scripts/_hb-ne-build.mjs
?? server/scripts/_hb-ne-line.json
?? server/scripts/_hb-p1-build.mjs
?? server/scripts/_hb-p1-build2.mjs
?? server/scripts/_hb-p1-build2.png
?? server/scripts/_hb-p1-fix.mjs
?? server/scripts/_hb-p1-fixed.json
?? server/scripts/_hb-p1-fixed.png
?? server/scripts/_hb-p1-line.json
?? server/scripts/_hb-p1-probe.mjs
?? server/scripts/_hb-p1-probe.png
?? server/scripts/_hb-p1-water.json
?? server/scripts/_hb-p1-write.mjs
?? server/scripts/_hb-p2-build.mjs
?? server/scripts/_hb-p2-build.png
?? server/scripts/_hb-p2-fixed.json
?? server/scripts/_hb-p2-line.json
?? server/scripts/_hb-pbr-save.js
?? server/scripts/_hb-pbr-zoom.mjs
?? server/scripts/_hb-pbr-zoom.png
?? server/scripts/_hb-pier26-hb.json
?? server/scripts/_hb-pier26-hb.mjs
?? server/scripts/_hb-pier26-look.mjs
?? server/scripts/_hb-pier26-look.png
?? server/scripts/_hb-pierone.json
?? server/scripts/_hb-pierone.mjs
?? server/scripts/_hb-pierone.png
?? server/scripts/_hb-polish.mjs
?? server/scripts/_hb-polished.json
?? server/scripts/_hb-polished.png
?? server/scripts/_hb-pyrmont-bridge.json
?? server/scripts/_hb-pyrmont-bridge.mjs
?? server/scripts/_hb-pyrmont-bridge.png
?? server/scripts/_hb-railpde-raw.json
?? server/scripts/_hb-railpde.mjs
?? server/scripts/_hb-railpde.png
?? server/scripts/_hb-revert-p1.js
?? server/scripts/_hb-se-brzoom.mjs
?? server/scripts/_hb-se-brzoom.png
?? server/scripts/_hb-se-build.mjs
?? server/scripts/_hb-se-fix.mjs
?? server/scripts/_hb-se-fixed.json
?? server/scripts/_hb-se-fixed.png
?? server/scripts/_hb-se-full.png
?? server/scripts/_hb-se-line.json
?? server/scripts/_hb-se-line2.json
?? server/scripts/_hb-se-probe.mjs
?? server/scripts/_hb-se-probe.png
?? server/scripts/_hb-se-raw.json
?? server/scripts/_hb-se-se.png
?? server/scripts/_hb-se-write.js
?? server/scripts/_hb-se.json
?? server/scripts/_hb-shore-full.json
?? server/scripts/_hb-shore-full.mjs
?? server/scripts/_hb-sw-build.mjs
?? server/scripts/_hb-sw-full.png
?? server/scripts/_hb-sw-southzoom.mjs
?? server/scripts/_hb-sw-southzoom.png
?? server/scripts/_hb-sw-sw.png
?? server/scripts/_hb-sw-write.js
?? server/scripts/_hb-sw.json
?? server/scripts/_hb-top-look.mjs
?? server/scripts/_hb-top-look.png
?? server/scripts/_hb-vic-build.mjs
?? server/scripts/_hb-vic-full.png
?? server/scripts/_hb-vic-probe.mjs
?? server/scripts/_hb-vic-probe.png
?? server/scripts/_hb-vic-raw.json
?? server/scripts/_hb-vic-sw.png
?? server/scripts/_hb-vic-zoom.mjs
?? server/scripts/_hb-vic-zoom.png
?? server/scripts/_hb-vic.json
?? server/scripts/_hb-water-raw.json
?? server/scripts/_hb-water-zoom.mjs
?? server/scripts/_hb-water-zoom.png
?? server/scripts/_hb-water.mjs
?? server/scripts/_hb-water.png
?? server/scripts/_hb-water2-raw.json
?? server/scripts/_hb-water2.mjs
?? server/scripts/_hb-water2.png
?? server/scripts/_hb_kmz/
?? server/scripts/_hbbh.html
?? server/scripts/_hbbl.html
?? server/scripts/_hbbt.html
?? server/scripts/_hbbz.html
?? server/scripts/_hbcarved.html
?? server/scripts/_hbcgp.html
?? server/scripts/_hbck.html
?? server/scripts/_hbcl.html
?? server/scripts/_hbcpg.html
?? server/scripts/_hbcr.html
?? server/scripts/_hbcs.html
?? server/scripts/_hbcvp.html
?? server/scripts/_hbeb.html
?? server/scripts/_hbfc.html
?? server/scripts/_hbhug.html
?? server/scripts/_hbhug2.html
?? server/scripts/_hbhz.html
?? server/scripts/_hbjp.html
?? server/scripts/_hblm.html
?? server/scripts/_hbm99.html
?? server/scripts/_hbmn.html
?? server/scripts/_hbmsc.html
?? server/scripts/_hbmsct.html
?? server/scripts/_hbp1.html
?? server/scripts/_hbp1b2.html
?? server/scripts/_hbp1f.html
?? server/scripts/_hbp2.html
?? server/scripts/_hbp26look.html
?? server/scripts/_hbpbr.html
?? server/scripts/_hbpbz.html
?? server/scripts/_hbpo.html
?? server/scripts/_hbpol.html
?? server/scripts/_hbrp.html
?? server/scripts/_hbse.html
?? server/scripts/_hbseb.html
?? server/scripts/_hbsefix.html
?? server/scripts/_hbsw.html
?? server/scripts/_hbsz.html
?? server/scripts/_hbtl.html
?? server/scripts/_hbv.html
?? server/scripts/_hbvb.html
?? server/scripts/_hbvz.html
?? server/scripts/_hbw.html
?? server/scripts/_hbw2.html
?? server/scripts/_hbwz.html
?? server/scripts/_hc-build.mjs
?? server/scripts/_hc-context.html
?? server/scripts/_hc-context.mjs
?? server/scripts/_hc-context.png
?? server/scripts/_hc-final.html
?? server/scripts/_hc-final.mjs
?? server/scripts/_hc-final.png
?? server/scripts/_hc-rw-lock.mjs
?? server/scripts/_hd-assemble.mjs
?? server/scripts/_hd-assemble2.mjs
?? server/scripts/_hd-assemble3.mjs
?? server/scripts/_hd-commit.mjs
?? server/scripts/_hd-diag.mjs
?? server/scripts/_hd-rail-build.mjs
?? server/scripts/_hd-rebuild2.mjs
?? server/scripts/_hd-rebuild3.mjs
?? server/scripts/_hd-render.html
?? server/scripts/_hd-render.mjs
?? server/scripts/_hd-render.png
?? server/scripts/_hd-union.html
?? server/scripts/_hd-union.json
?? server/scripts/_hd-union.png
?? server/scripts/_hd-zoom.html
?? server/scripts/_hd-zoom.mjs
?? server/scripts/_hd-zoom.png
?? server/scripts/_hd.json
?? server/scripts/_hd.ql
?? server/scripts/_hdcoast.json
?? server/scripts/_hdcoast.ql
?? server/scripts/_hdrail.json
?? server/scripts/_hdrail.ql
?? server/scripts/_history-detail.js
?? server/scripts/_history-one.js
?? server/scripts/_history-survey.js
?? server/scripts/_hqs-backup-pre-fold.json
?? server/scripts/_hqs-upd.json
?? server/scripts/_hqs_kmz/
?? server/scripts/_hs-14.png
?? server/scripts/_hs-backup-pre-leg5.json
?? server/scripts/_hs-backup-pre-legs14.json
?? server/scripts/_hs-fix-leg34.mjs
?? server/scripts/_hs-fix34.png
?? server/scripts/_hs-full.png
?? server/scripts/_hs-leg1-build.mjs
?? server/scripts/_hs-leg1-exit.json
?? server/scripts/_hs-leg1.json
?? server/scripts/_hs-leg1.png
?? server/scripts/_hs-leg2-build.mjs
?? server/scripts/_hs-leg2-exit.json
?? server/scripts/_hs-leg2.json
?? server/scripts/_hs-leg2.png
?? server/scripts/_hs-leg3-build.mjs
?? server/scripts/_hs-leg3-exit.json
?? server/scripts/_hs-leg3.json
?? server/scripts/_hs-leg3.png
?? server/scripts/_hs-leg4-build.mjs
?? server/scripts/_hs-leg4-exit.json
?? server/scripts/_hs-leg4.json
?? server/scripts/_hs-leg4.png
?? server/scripts/_hs-leg5-build.mjs
?? server/scripts/_hs-leg5-splice.mjs
?? server/scripts/_hs-leg5.json
?? server/scripts/_hs-leg5.png
?? server/scripts/_hs-legs14-splice.mjs
?? server/scripts/_hs-look.mjs
?? server/scripts/_hs-look.png
?? server/scripts/_hs-probe.mjs
?? server/scripts/_hs.json
?? server/scripts/_hs.ql
?? server/scripts/_hs14.html
?? server/scripts/_hsfix34.html
?? server/scripts/_hsfull.html
?? server/scripts/_hsleg1.html
?? server/scripts/_hsleg2.html
?? server/scripts/_hsleg3.html
?? server/scripts/_hsleg4.html
?? server/scripts/_hsleg5.html
?? server/scripts/_hslook.html
?? server/scripts/_infer-havens.js
?? server/scripts/_ingest-input.json
?? server/scripts/_investigate-rene-stdom.js
?? server/scripts/_kt.json
?? server/scripts/_kt.ql
?? server/scripts/_lance-backup-crude.json
?? server/scripts/_lance-block.mjs
?? server/scripts/_lance-block.png
?? server/scripts/_lance-build.mjs
?? server/scripts/_lance-build.png
?? server/scripts/_lance-build2.mjs
?? server/scripts/_lance-build2.png
?? server/scripts/_lance-build3.mjs
?? server/scripts/_lance-build3.png
?? server/scripts/_lance-feats.json
?? server/scripts/_lance-grounds.json
?? server/scripts/_lance-look.mjs
?? server/scripts/_lance-look.png
?? server/scripts/_lance-square.json
?? server/scripts/_lance-square.mjs
?? server/scripts/_lance-square.png
?? server/scripts/_lanceb.html
?? server/scripts/_lanceb2.html
?? server/scripts/_lanceb3.html
?? server/scripts/_lanceblk.html
?? server/scripts/_lancelook.html
?? server/scripts/_lancesq.html
?? server/scripts/_lc-draw.html
?? server/scripts/_lc-draw.mjs
?? server/scripts/_lc-draw.png
?? server/scripts/_lc-inspect.html
?? server/scripts/_lc-inspect.mjs
?? server/scripts/_lc-inspect.png
?? server/scripts/_lc-inspect2.html
?? server/scripts/_lc-inspect2.mjs
?? server/scripts/_lc-inspect2.png
?? server/scripts/_lc-inspect3.html
?? server/scripts/_lc-inspect3.mjs
?? server/scripts/_lc-inspect3.png
?? server/scripts/_lc-rough.html
?? server/scripts/_lc-rough.mjs
?? server/scripts/_lc-rough.png
?? server/scripts/_lc-union.json
?? server/scripts/_lc.json
?? server/scripts/_lc.ql
?? server/scripts/_lc2.json
?? server/scripts/_lc2.ql
?? server/scripts/_lc3.json
?? server/scripts/_lc3.ql
?? server/scripts/_lch-build.mjs
?? server/scripts/_lch-commit.mjs
?? server/scripts/_lch-commit2.mjs
?? server/scripts/_lch-junctions.json
?? server/scripts/_lch-orient.html
?? server/scripts/_lch-orient.mjs
?? server/scripts/_lch-orient.png
?? server/scripts/_lch-u.html
?? server/scripts/_lch-u.mjs
?? server/scripts/_lch-u.png
?? server/scripts/_lch-union.json
?? server/scripts/_lch.json
?? server/scripts/_lch.ql
?? server/scripts/_ley_preview.html
?? server/scripts/_ley_preview.mjs
?? server/scripts/_ley_preview.png
?? server/scripts/_leylines.mjs
?? server/scripts/_loc-polys.js
?? server/scripts/_locate-ns.js
?? server/scripts/_locations-local.backup-pre-sliver.json
?? server/scripts/_locations.json
?? server/scripts/_locations2.json
?? server/scripts/_loci-add.mjs
?? server/scripts/_loci-check.mjs
?? server/scripts/_lock-eastcliff.js
?? server/scripts/_lore_dump.js
?? server/scripts/_ma_eastcliff_watchtower.html
?? server/scripts/_ma_eastcliff_watchtower.png
?? server/scripts/_ma_georges_wellspring.html
?? server/scripts/_ma_georges_wellspring.png
?? server/scripts/_ma_georges_zoom.html
?? server/scripts/_ma_georges_zoom.mjs
?? server/scripts/_ma_georges_zoom.png
?? server/scripts/_ma_the_sleepless_athenaeum.html
?? server/scripts/_ma_the_sleepless_athenaeum.png
?? server/scripts/_mage-audit.mjs
?? server/scripts/_mage-polys.json
?? server/scripts/_map-shot.mjs
?? server/scripts/_map-shot.png
?? server/scripts/_maroubra_q.txt
?? server/scripts/_maroubra_ways.json
?? server/scripts/_military-fetch.js
?? server/scripts/_minor.json
?? server/scripts/_minor_preview.html
?? server/scripts/_minor_preview.mjs
?? server/scripts/_minor_preview.png
?? server/scripts/_minor_q.txt
?? server/scripts/_mm_shared.json
?? server/scripts/_nc.json
?? server/scripts/_nc.ql
?? server/scripts/_nec-fix.mjs
?? server/scripts/_npc-refine.mjs
?? server/scripts/_npc-shot.html
?? server/scripts/_npc-shot.mjs
?? server/scripts/_npc-shot.png
?? server/scripts/_npc-sites-add.mjs
?? server/scripts/_npc-sites-add2.mjs
?? server/scripts/_npc-sites-add3.mjs
?? server/scripts/_ns-apply.js
?? server/scripts/_ns-backup-2026-06-22.json
?? server/scripts/_ns-backup-eastedge-v1.json
?? server/scripts/_ns-backup-eastedge-v2-jagged.json
?? server/scripts/_ns-backup-eastedge-v3.json
?? server/scripts/_ns-backup-pre-eastedge.json
?? server/scripts/_ns-backup-pre-sw.json
?? server/scripts/_ns-backup-pre-w.json
?? server/scripts/_ns-brook-fetch.js
?? server/scripts/_ns-coast-check.js
?? server/scripts/_ns-coast.json
?? server/scripts/_ns-combine.js
?? server/scripts/_ns-east-build.mjs
?? server/scripts/_ns-east-final.html
?? server/scripts/_ns-east-final.json
?? server/scripts/_ns-east-final.png
?? server/scripts/_ns-east-fix.mjs
?? server/scripts/_ns-east-proposed.html
?? server/scripts/_ns-east-proposed.json
?? server/scripts/_ns-east-proposed.png
?? server/scripts/_ns-east-render.mjs
?? server/scripts/_ns-east-render2.mjs
?? server/scripts/_ns-east-render3.mjs
?? server/scripts/_ns-east-write.js
?? server/scripts/_ns-east.html
?? server/scripts/_ns-east.png
?? server/scripts/_ns-east2.html
?? server/scripts/_ns-east2.png
?? server/scripts/_ns-east3.html
?? server/scripts/_ns-east3.png
?? server/scripts/_ns-edited-rings.json
?? server/scripts/_ns-edited.json
?? server/scripts/_ns-ingest.js
?? server/scripts/_ns-junction-zoom.mjs
?? server/scripts/_ns-junction.html
?? server/scripts/_ns-junction.png
?? server/scripts/_ns-keep.html
?? server/scripts/_ns-keep.png
?? server/scripts/_ns-lock.js
?? server/scripts/_ns-new.json
?? server/scripts/_ns-rings.html
?? server/scripts/_ns-rings.png
?? server/scripts/_ns-roads.json
?? server/scripts/_ns-snap.js
?? server/scripts/_ns-spec-fetch.js
?? server/scripts/_ns-splice-bradleys.png
?? server/scripts/_ns-splice-full.png
?? server/scripts/_ns-splice-north.png
?? server/scripts/_ns-splice-write.js
?? server/scripts/_ns-splice.json
?? server/scripts/_ns-splice.mjs
?? server/scripts/_ns-state.js
?? server/scripts/_ns-stitch.js
?? server/scripts/_ns-suburbs.js
?? server/scripts/_ns-sw-full.png
?? server/scripts/_ns-sw-sw.png
?? server/scripts/_ns-sw.json
?? server/scripts/_ns-tweak-north.png
?? server/scripts/_ns-tweak-south.png
?? server/scripts/_ns-tweak-write.js
?? server/scripts/_ns-tweak.mjs
?? server/scripts/_ns-tweaked.json
?? server/scripts/_ns-w.json
?? server/scripts/_ns_kmz_extract/
?? server/scripts/_nw-inspect.html
?? server/scripts/_nw-inspect.mjs
?? server/scripts/_nw-inspect.png
?? server/scripts/_nw.json
?? server/scripts/_nw.ql
?? server/scripts/_overlap-check.mjs
?? server/scripts/_parse-ns-updated.js
?? server/scripts/_patch-jack.js
?? server/scripts/_ph-inspect.html
?? server/scripts/_ph-inspect.mjs
?? server/scripts/_ph-inspect.png
?? server/scripts/_ph-ring.json
?? server/scripts/_ph2.json
?? server/scripts/_ph2.ql
?? server/scripts/_phwater.json
?? server/scripts/_phwater.ql
?? server/scripts/_place-inferred.js
?? server/scripts/_place-ludica.js
?? server/scripts/_players-dump.js
?? server/scripts/_poi-shot.mjs
?? server/scripts/_poi-shot.png
?? server/scripts/_princes.json
?? server/scripts/_princes.ql
?? server/scripts/_probe-relationships.mjs
?? server/scripts/_ptdanger.json
?? server/scripts/_ptdanger.ql
?? server/scripts/_pyrmont-bridge-shores.mjs
?? server/scripts/_quest-excel.json
?? server/scripts/_quest-one.js
?? server/scripts/_quest-reconcile.js
?? server/scripts/_quest-survey.js
?? server/scripts/_quest-two.js
?? server/scripts/_railN.json
?? server/scripts/_railN.ql
?? server/scripts/_raz-commit.mjs
?? server/scripts/_raz-draw.html
?? server/scripts/_raz-draw.mjs
?? server/scripts/_raz-draw.png
?? server/scripts/_raz-inspect.html
?? server/scripts/_raz-inspect.mjs
?? server/scripts/_raz-inspect.png
?? server/scripts/_raz-union.json
?? server/scripts/_raz.json
?? server/scripts/_raz.ql
?? server/scripts/_raz2.json
?? server/scripts/_raz2.ql
?? server/scripts/_read-ns-kmz.js
?? server/scripts/_rebuild-havens.js
?? server/scripts/_recon-eastcliff.js
?? server/scripts/_recover-havens.js
?? server/scripts/_region.mjs
?? server/scripts/_region_innerwest.html
?? server/scripts/_region_innerwest.png
?? server/scripts/_region_north.html
?? server/scripts/_region_north.png
?? server/scripts/_relabel-xavier.js
?? server/scripts/_render-keep.mjs
?? server/scripts/_render-rings.mjs
?? server/scripts/_rene-disambig.js
?? server/scripts/_resolve-slivers.mjs
?? server/scripts/_resweep-q1q19-dump.json
?? server/scripts/_reveal-carthian-hq-rene.mjs
?? server/scripts/_reveal-homebush-dt6.mjs
?? server/scripts/_reveal-lidcombe-morgue-dt6.mjs
?? server/scripts/_reveal-mystical-hubs-keeper.mjs
?? server/scripts/_reveal-old-dragon-hq-livia.mjs
?? server/scripts/_reveal-swift-manor-dt6.mjs
?? server/scripts/_roads.json
?? server/scripts/_rw-close.mjs
?? server/scripts/_rw-full.html
?? server/scripts/_rw-full.png
?? server/scripts/_rw-hb-declash.mjs
?? server/scripts/_rw-junctions.json
?? server/scripts/_rw-legW-build.mjs
?? server/scripts/_rw-legW.html
?? server/scripts/_rw-legW.json
?? server/scripts/_rw-legW.png
?? server/scripts/_rw-orient.html
?? server/scripts/_rw-orient.mjs
?? server/scripts/_rw-orient.png
?? server/scripts/_rw.ql
?? server/scripts/_rw2.ql
?? server/scripts/_rwW.json
?? server/scripts/_rwW.ql
?? server/scripts/_rwbound.json
?? server/scripts/_rwcorr.json
?? server/scripts/_rwcorr.ql
?? server/scripts/_rwroberts.json
?? server/scripts/_sc-backup-pre-edge1.json
?? server/scripts/_sc-backup-pre-edge2.json
?? server/scripts/_sc-backup-pre-edge3.json
?? server/scripts/_sc-backup-pre-edge4.json
?? server/scripts/_sc-backup-pre-edge5.json
?? server/scripts/_sc-backup-pre-edge6.json
?? server/scripts/_sc-backup-pre-edge7.json
?? server/scripts/_sc-backup-pre-edge8.json
?? server/scripts/_sc-backup-pre-edge9.json
?? server/scripts/_sc-closed.png
?? server/scripts/_sc-edge1-build.mjs
?? server/scripts/_sc-edge1-ends.json
?? server/scripts/_sc-edge1-splice.mjs
?? server/scripts/_sc-edge1.json
?? server/scripts/_sc-edge1.png
?? server/scripts/_sc-edge2-build.mjs
?? server/scripts/_sc-edge2-ends.json
?? server/scripts/_sc-edge2-splice.mjs
?? server/scripts/_sc-edge2.json
?? server/scripts/_sc-edge2.png
?? server/scripts/_sc-edge3-build.mjs
?? server/scripts/_sc-edge3-ends.json
?? server/scripts/_sc-edge3-splice.mjs
?? server/scripts/_sc-edge3.json
?? server/scripts/_sc-edge3.png
?? server/scripts/_sc-edge4-build.mjs
?? server/scripts/_sc-edge4-ends.json
?? server/scripts/_sc-edge4-splice.mjs
?? server/scripts/_sc-edge4.json
?? server/scripts/_sc-edge4.png
?? server/scripts/_sc-edge5-build.mjs
?? server/scripts/_sc-edge5-ends.json
?? server/scripts/_sc-edge5-splice.mjs
?? server/scripts/_sc-edge5.json
?? server/scripts/_sc-edge5.png
?? server/scripts/_sc-edge6-build.mjs
?? server/scripts/_sc-edge6-ends.json
?? server/scripts/_sc-edge6-splice.mjs
?? server/scripts/_sc-edge6.json
?? server/scripts/_sc-edge6.png
?? server/scripts/_sc-edge7-build.mjs
?? server/scripts/_sc-edge7-ends.json
?? server/scripts/_sc-edge7-splice.mjs
?? server/scripts/_sc-edge7.json
?? server/scripts/_sc-edge7.png
?? server/scripts/_sc-edge8-build.mjs
?? server/scripts/_sc-edge8-ends.json
?? server/scripts/_sc-edge8-splice.mjs
?? server/scripts/_sc-edge8.json
?? server/scripts/_sc-edge8.png
?? server/scripts/_sc-edge9-build.mjs
?? server/scripts/_sc-edge9-ends.json
?? server/scripts/_sc-edge9-splice.mjs
?? server/scripts/_sc-edge9.json
?? server/scripts/_sc-edge9.png
?? server/scripts/_sc-look.mjs
?? server/scripts/_sc-look.png
?? server/scripts/_scclosed.html
?? server/scripts/_scedge1.html
?? server/scripts/_scedge2.html
?? server/scripts/_scedge3.html
?? server/scripts/_scedge4.html
?? server/scripts/_scedge5.html
?? server/scripts/_scedge6.html
?? server/scripts/_scedge7.html
?? server/scripts/_scedge8.html
?? server/scripts/_scedge9.html
?? server/scripts/_sclook.html
?? server/scripts/_scot.json
?? server/scripts/_scot.ql
?? server/scripts/_sh-backup-crude.json
?? server/scripts/_sh-boatharbour-ar.json
?? server/scripts/_sh-fix-leg23.mjs
?? server/scripts/_sh-fix23.png
?? server/scripts/_sh-full.png
?? server/scripts/_sh-leg1-aqua-build.mjs
?? server/scripts/_sh-leg1-build.mjs
?? server/scripts/_sh-leg1-ends.json
?? server/scripts/_sh-leg1-finish.mjs
?? server/scripts/_sh-leg1.json
?? server/scripts/_sh-leg1.png
?? server/scripts/_sh-leg1b-build.mjs
?? server/scripts/_sh-leg1b.png
?? server/scripts/_sh-leg1f.png
?? server/scripts/_sh-leg2-build.mjs
?? server/scripts/_sh-leg2-exit.json
?? server/scripts/_sh-leg2.json
?? server/scripts/_sh-leg2.png
?? server/scripts/_sh-leg3-bh-build.mjs
?? server/scripts/_sh-leg3-build.mjs
?? server/scripts/_sh-leg3-exit.json
?? server/scripts/_sh-leg3-look.mjs
?? server/scripts/_sh-leg3-look.png
?? server/scripts/_sh-leg3-look2.mjs
?? server/scripts/_sh-leg3-look2.png
?? server/scripts/_sh-leg3.json
?? server/scripts/_sh-leg3.png
?? server/scripts/_sh-leg4-build.mjs
?? server/scripts/_sh-leg4-exit.json
?? server/scripts/_sh-leg4.json
?? server/scripts/_sh-leg4.png
?? server/scripts/_sh-leg5-build.mjs
?? server/scripts/_sh-leg5-exit.json
?? server/scripts/_sh-leg5.json
?? server/scripts/_sh-leg5.png
?? server/scripts/_sh-leg6-build.mjs
?? server/scripts/_sh-leg6.json
?? server/scripts/_sh-leg6.png
?? server/scripts/_sh-look.mjs
?? server/scripts/_sh-look.png
?? server/scripts/_sh-probe.mjs
?? server/scripts/_sh-reserves.json
?? server/scripts/_sh-splice.mjs
?? server/scripts/_shfix23.html
?? server/scripts/_shfull.html
?? server/scripts/_shleg1.html
?? server/scripts/_shleg1b.html
?? server/scripts/_shleg1f.html
?? server/scripts/_shleg2.html
?? server/scripts/_shleg3.html
?? server/scripts/_shleg3look.html
?? server/scripts/_shleg3look2.html
?? server/scripts/_shleg4.html
?? server/scripts/_shleg5.html
?? server/scripts/_shleg6.html
?? server/scripts/_shlook.html
?? server/scripts/_snapshot-locations.js
?? server/scripts/_spine-bradleys.png
?? server/scripts/_spine-debug.mjs
?? server/scripts/_spine-graph.mjs
?? server/scripts/_spine-graph2.mjs
?? server/scripts/_spine-spit.png
?? server/scripts/_spine-zooms.mjs
?? server/scripts/_spine.html
?? server/scripts/_spine.json
?? server/scripts/_spine.png
?? server/scripts/_spit-bradleys-fetch.js
?? server/scripts/_spit-bradleys-raw.json
?? server/scripts/_spit-chain.json
?? server/scripts/_subbound.json
?? server/scripts/_subbound2.json
?? server/scripts/_subbound2_q.txt
?? server/scripts/_subbound_line.json
?? server/scripts/_subbound_q.txt
?? server/scripts/_suburbs.json
?? server/scripts/_sw-build.mjs
?? server/scripts/_sw-council-raw.json
?? server/scripts/_sw-council.mjs
?? server/scripts/_sw-council.png
?? server/scripts/_sw-lga-segs.json
?? server/scripts/_sw-probe.mjs
?? server/scripts/_sw-probe.png
?? server/scripts/_sw-relation.mjs
?? server/scripts/_sw-relation.png
?? server/scripts/_sw-roads-raw.json
?? server/scripts/_sw-tight.mjs
?? server/scripts/_sw-tight.png
?? server/scripts/_sw-write.js
?? server/scripts/_sw.html
?? server/scripts/_swc.html
?? server/scripts/_swr.html
?? server/scripts/_swt.html
?? server/scripts/_syd_q.txt
?? server/scripts/_t-balmoral.png
?? server/scripts/_t-chowder.png
?? server/scripts/_t-doverheights.png
?? server/scripts/_t-fernleigh-east.png
?? server/scripts/_t-fernleigh-west.png
?? server/scripts/_t-southhead.png
?? server/scripts/_t-vaucluse.png
?? server/scripts/_terr-names.js
?? server/scripts/_threads-batch3-and-settles.js
?? server/scripts/_threads-rebuild-timelines.js
?? server/scripts/_tile-centre.png
?? server/scripts/_tile-north.png
?? server/scripts/_towns.json
?? server/scripts/_tweak-probe.mjs
?? server/scripts/_twgeom.json
?? server/scripts/_twout.json
?? server/scripts/_twq.overpass
?? server/scripts/_twq2.overpass
?? server/scripts/_union-eastcliff.js
?? server/scripts/_upd-polys.json
?? server/scripts/_upd_kmz/
?? server/scripts/_v2.js
?? server/scripts/_validate-poly.js
?? server/scripts/_validate-thread.js
?? server/scripts/_verify-entities.js
?? server/scripts/_verify-ns.js
?? server/scripts/_w-build.mjs
?? server/scripts/_w-build.png
?? server/scripts/_w-probe.mjs
?? server/scripts/_w-probe.png
?? server/scripts/_w-roads-raw.json
?? server/scripts/_w-verify.mjs
?? server/scripts/_w-verify.png
?? server/scripts/_w-write.js
?? server/scripts/_w-zoom.mjs
?? server/scripts/_w-zoom.png
?? server/scripts/_w.html
?? server/scripts/_wav.json
?? server/scripts/_wav_q.txt
?? server/scripts/_wayne.js
?? server/scripts/_wb.html
?? server/scripts/_wc.html
?? server/scripts/_whites-creek-raw.json
?? server/scripts/_whites-creek.mjs
?? server/scripts/_whites-creek.png
?? server/scripts/_wloci.json
?? server/scripts/_wloci_preview.html
?? server/scripts/_wloci_preview.mjs
?? server/scripts/_wloci_preview.png
?? server/scripts/_wloci_q.txt
?? server/scripts/_wolf-declash.mjs
?? server/scripts/_wolf-rough.mjs
?? server/scripts/_wolf-shot.html
?? server/scripts/_wolf-shot.mjs
?? server/scripts/_wolf-shot.png
?? server/scripts/_wor-commit.mjs
?? server/scripts/_wor-draw.html
?? server/scripts/_wor-draw.mjs
?? server/scripts/_wor-draw.png
?? server/scripts/_wor-draw2.mjs
?? server/scripts/_wor-inspect.html
?? server/scripts/_wor-inspect.mjs
?? server/scripts/_wor-inspect.png
?? server/scripts/_wor-union.json
?? server/scripts/_wor.json
?? server/scripts/_wor.ql
?? server/scripts/_wr.json
?? server/scripts/_wr.ql
?? server/scripts/_wraith-backup-crude.json
?? server/scripts/_wraith-build.mjs
?? server/scripts/_wraith-build.png
?? server/scripts/_wraith-gap.json
?? server/scripts/_wraith-look.mjs
?? server/scripts/_wraith-look.png
?? server/scripts/_wraithb.html
?? server/scripts/_wraithlook.html
?? server/scripts/_wv.html
?? server/scripts/_wyrm_preview.html
?? server/scripts/_wyrm_preview.mjs
?? server/scripts/_wyrm_preview.png
?? server/scripts/_wz.html
?? server/scripts/_z-north.mjs
?? server/scripts/_z.html
?? server/scripts/_z2.html
?? server/scripts/_zn.html
?? server/scripts/_zsw.html
?? server/scripts/_zt.html
?? server/scripts/archive/_backups/
?? server/tests/oxp-3-office-manoeuvre-rank.test.js
?? specs/handover-collective-pool-producer.md
?? specs/stories/code-review/issue-1143-acceptance-auditor-findings.md
?? specs/stories/code-review/issue-1143-acceptance-auditor.md
?? specs/stories/code-review/issue-1143-blind-hunter-run.log
?? specs/stories/code-review/issue-1143-blind-hunter.md
?? specs/stories/code-review/issue-1143-diff.txt
?? specs/stories/code-review/issue-1143-edge-case-hunter-findings.md
?? specs/stories/code-review/issue-1143-edge-case-hunter.md
?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md
?? specs/stories/code-review/issue-1152-eqc1-codex-raw-output.txt
?? specs/stories/code-review/issue-1152-eqc1-codex-review.md
?? specs/stories/code-review/issue-1152-eqc1-diff.txt
?? specs/stories/code-review/oaq-2-acceptance-auditor-findings.md
?? specs/stories/code-review/oaq-2-acceptance-auditor.md
?? specs/stories/code-review/oaq-2-blind-hunter-findings.md
?? specs/stories/code-review/oaq-2-blind-hunter.md
?? specs/stories/code-review/oaq-2-diff.txt
?? specs/stories/code-review/oaq-2-edge-case-hunter-findings.md
?? specs/stories/code-review/oaq-2-edge-case-hunter.md
?? specs/stories/code-review/oaq-3-codex-findings.md
?? specs/stories/code-review/oaq-3-codex-review.md
?? specs/stories/code-review/oaq-3-diff.txt
?? specs/stories/code-review/otc-2-codex-findings.md
?? specs/stories/code-review/otc-2-codex-review-1-blind-hunter.md
?? specs/stories/code-review/otc-2-codex-review-2-edge-case-hunter.md
?? specs/stories/code-review/otc-2-codex-review-3-acceptance-auditor.md
?? specs/stories/code-review/otc-2-diff.txt
?? specs/stories/code-review/otc-3-codex-findings.md
?? specs/stories/code-review/otc-3-codex-review-1-blind-hunter.md
?? specs/stories/code-review/otc-3-codex-review-2-edge-case-hunter.md
?? specs/stories/code-review/otc-3-codex-review-3-acceptance-auditor.md
?? specs/stories/code-review/otc-3-diff.txt
?? specs/stories/oxp-3-manoeuvre-purchase-graduated-merit.md
?? tm-map.html
M	public/css/admin-layout.css
M	public/js/admin/equipment-catalogue-admin.js
M	public/js/data/equipment-derivation.js
M	public/js/editor/sheet.js
M	public/js/suite/roll-v2.js
M	public/js/suite/roll.js
M	public/js/tabs/downtime-form.js
M	server/schemas/character.schema.js
M	server/schemas/equipment_catalogue.schema.js
A	server/scripts/migrate-eqc1-bucket-taxonomy.mjs
M	server/tests/equipment-client-fixes.test.js
A	server/tests/issue-1152-eqc1-bucket-migration.test.js
M	server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
M	server/tests/issue-871-876-ecm-4-9-bundle.test.js
M	server/tests/issue-879-defence-penalty-wirein.test.js
A	specs/stories/feature.1152.eqc1-bucket-container-schema.story.md
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:47:02.178561Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\Terra Mortis\\TM Suite' rev-parse HEAD; git -C 'D:\\Terra Mortis\\TM Suite' log -1 --oneline; git -C 'D:\\Terra Mortis\\TM Suite' diff --name-only ddf059f8 c7e6771b -- 'public' 'server' 'specs/stories/feature.1152.eqc1-bucket-container-schema.story.md'" in D:\Terra Mortis\TM Suite
 succeeded in 3271ms:
c7e6771b19367f0a6face5e675c7a3e2ac3737b4
c7e6771b feat(equipment): EQC-1 — re-partition bucket taxonomy, add containment, migrate live data (#1152)
public/css/admin-layout.css
public/js/admin/equipment-catalogue-admin.js
public/js/data/equipment-derivation.js
public/js/editor/sheet.js
public/js/suite/roll-v2.js
public/js/suite/roll.js
public/js/tabs/downtime-form.js
server/schemas/character.schema.js
server/schemas/equipment_catalogue.schema.js
server/scripts/migrate-eqc1-bucket-taxonomy.mjs
server/tests/equipment-client-fixes.test.js
server/tests/issue-1152-eqc1-bucket-migration.test.js
server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js
server/tests/issue-871-876-ecm-4-9-bundle.test.js
server/tests/issue-879-defence-penalty-wirein.test.js
specs/stories/feature.1152.eqc1-bucket-container-schema.story.md

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:47:17.626713Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Write-Output \"Review root: "'$reviewRoot"; Get-Command mongod -ErrorAction SilentlyContinue | Select-Object Name,Source; Get-Process mongod -ErrorAction SilentlyContinue | Select-Object ProcessName,Id,Path' in D:\Terra Mortis\TM Suite
 succeeded in 3573ms:
Review root: D:\Terra Mortis\TM Suite

[32;1mProcessName[0m[32;1m   Id[0m[32;1m Path[0m
[32;1m-----------[0m [32;1m  --[0m [32;1m----[0m
mongod      6436 


diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:47:26.506624Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
A local `mongod` process is running, so these suites should exercise their integration slices rather than skip for lack of a database. I’m running the nine equipment gates one file at a time exactly as requested; I’ll report each file’s observed test-file/test counts and any skips or failures.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; npx vitest run tests/equipment.test.js" in D:\Terra Mortis\TM Suite
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:47:47.822529Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
2026-08-13T02:47:50.221517Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 13.4 seconds
Output:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

stdout | tests/equipment.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  write to custom object with { processEnv: myObject }

 ❯ tests/equipment.test.js (14 tests | 14 skipped) 5056ms

 Test Files  1 failed (1)
      Tests  14 skipped (14)
   Start at  12:47:42
   Duration  7.88s (transform 732ms, setup 56ms, import 2.33s, tests 5.06s, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework
stderr | tests/equipment.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/equipment.test.js
[setupDb] connectDb() failed: connect EACCES 159.143.141.178:27017


⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
MongoServerSelectionError: connect EACCES 159.143.141.178:27017
 ❯ Topology.selectServer node_modules/mongodb/src/sdam/topology.ts:637:29
 ❯ Topology._connect node_modules/mongodb/src/sdam/topology.ts:469:21
 ❯ Topology.connect node_modules/mongodb/src/sdam/topology.ts:405:6
 ❯ topologyConnect node_modules/mongodb/src/mongo_client.ts:662:8
 ❯ MongoClient._connect node_modules/mongodb/src/mongo_client.ts:674:6
 ❯ MongoClient.connect node_modules/mongodb/src/mongo_client.ts:586:6
 ❯ connectDb db.js:33:3
     31|     tls: true,
     32|   });
     33|   await client.connect();
       |   ^
     34|   db = client.db(dbName);
     35|   console.log('MongoDB connected successfully');
 ❯ setupDb tests/helpers/db-setup.js:9:5
 ❯ tests/equipment.test.js:70:3

Caused by: MongoNetworkError: connect EACCES 159.143.141.178:27017
 ❯ TLSSocket.<anonymous> node_modules/mongodb/lib/cmap/connect.js:313:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errorLabelSet: { constructor: 'Function<Set>', has: 'Function<has>', add: 'Function<add>', delete: 'Function<delete>', difference: 'Function<difference>', clear: 'Function<clear>', entries: 'Function<entries>', forEach: 'Function<forEach>', intersection: 'Function<intersection>', isSubsetOf: 'Function<isSubsetOf>', isSupersetOf: 'Function<isSupersetOf>', isDisjointFrom: 'Function<isDisjointFrom>', size: 3, symmetricDifference: 'Function<symmetricDifference>', union: 'Function<union>', values: 'Function<values>', keys: 'Function<values>' }, beforeHandshake: false, errorLabels: [ 'SystemOverloadedError', 'RetryableError', 'ResetPool' ], errmsg: 'connect EACCES 159.143.141.178:27017', hasErrorLabel: 'Function<hasErrorLabel>', addErrorLabel: 'Function<addErrorLabel>' }
Caused by: Error: connect EACCES 159.143.141.178:27017
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errno: -4092, code: 'EACCES', syscall: 'connect', address: '159.143.141.178', port: 27017 }
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
Error: Database not connected — call connectDb() first
 ❯ getDb db.js:39:18
     37|
     38| export function getDb() {
     39|   if (!db) throw new Error('Database not connected — call connectDb() …
       |                  ^
     40|   return db;
     41| }
 ❯ getCollection db.js:44:10
 ❯ tests/equipment.test.js:79:28

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 exited 1 in 13438ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

stdout | tests/equipment.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  write to custom object with { processEnv: myObject }

 ❯ tests/equipment.test.js (14 tests | 14 skipped) 5056ms

 Test Files  1 failed (1)
      Tests  14 skipped (14)
   Start at  12:47:42
   Duration  7.88s (transform 732ms, setup 56ms, import 2.33s, tests 5.06s, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework
stderr | tests/equipment.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/equipment.test.js
[setupDb] connectDb() failed: connect EACCES 159.143.141.178:27017


⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
MongoServerSelectionError: connect EACCES 159.143.141.178:27017
 ❯ Topology.selectServer node_modules/mongodb/src/sdam/topology.ts:637:29
 ❯ Topology._connect node_modules/mongodb/src/sdam/topology.ts:469:21
 ❯ Topology.connect node_modules/mongodb/src/sdam/topology.ts:405:6
 ❯ topologyConnect node_modules/mongodb/src/mongo_client.ts:662:8
 ❯ MongoClient._connect node_modules/mongodb/src/mongo_client.ts:674:6
 ❯ MongoClient.connect node_modules/mongodb/src/mongo_client.ts:586:6
 ❯ connectDb db.js:33:3
     31|     tls: true,
     32|   });
     33|   await client.connect();
       |   ^
     34|   db = client.db(dbName);
     35|   console.log('MongoDB connected successfully');
 ❯ setupDb tests/helpers/db-setup.js:9:5
 ❯ tests/equipment.test.js:70:3

Caused by: MongoNetworkError: connect EACCES 159.143.141.178:27017
 ❯ TLSSocket.<anonymous> node_modules/mongodb/lib/cmap/connect.js:313:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errorLabelSet: { constructor: 'Function<Set>', has: 'Function<has>', add: 'Function<add>', delete: 'Function<delete>', difference: 'Function<difference>', clear: 'Function<clear>', entries: 'Function<entries>', forEach: 'Function<forEach>', intersection: 'Function<intersection>', isSubsetOf: 'Function<isSubsetOf>', isSupersetOf: 'Function<isSupersetOf>', isDisjointFrom: 'Function<isDisjointFrom>', size: 3, symmetricDifference: 'Function<symmetricDifference>', union: 'Function<union>', values: 'Function<values>', keys: 'Function<values>' }, beforeHandshake: false, errorLabels: [ 'SystemOverloadedError', 'RetryableError', 'ResetPool' ], errmsg: 'connect EACCES 159.143.141.178:27017', hasErrorLabel: 'Function<hasErrorLabel>', addErrorLabel: 'Function<addErrorLabel>' }
Caused by: Error: connect EACCES 159.143.141.178:27017
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errno: -4092, code: 'EACCES', syscall: 'connect', address: '159.143.141.178', port: 27017 }
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
Error: Database not connected — call connectDb() first
 ❯ getDb db.js:39:18
     37|
     38| export function getDb() {
     39|   if (!db) throw new Error('Database not connected — call connectDb() …
       |                  ^
     40|   return db;
     41| }
 ❯ getCollection db.js:44:10
 ❯ tests/equipment.test.js:79:28

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\server\\db.js'; Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\server\\tests\\helpers\\db-setup.js'" in D:\Terra Mortis\TM Suite
 succeeded in 3066ms:
import { MongoClient } from 'mongodb';
import { config } from './config.js';

let client;
let db;

// Pure decision logic for the test-DB safety guard — extracted so it can be
// unit-tested without a real MongoDB connection.
//
// Vitest sets process.env.VITEST truthy in every worker. If we're running
// under vitest, the resolved DB name MUST end with `_test` — otherwise a
// regression in setup-env.js ordering (or an env override) would silently
// point a test run at production.
export function assertTestDbSafety(dbName, isVitest) {
  if (isVitest && !dbName.endsWith('_test')) {
    throw new Error(
      `Refusing to connect: test context (VITEST) targeting non-test database '${dbName}'. ` +
        `Tests must use a *_test database — check tests/helpers/setup-env.js ordering.`
    );
  }
}

export async function connectDb() {
  if (db) return; // Already connected — idempotent for test suites sharing a process
  const dbName = process.env.MONGODB_DB || 'tm_suite';
  assertTestDbSafety(dbName, !!process.env.VITEST);
  // Strip legacy ssl= param — not accepted by MongoDB driver v7
  const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
  client = new MongoClient(uri, {
    serverSelectionTimeoutMS: 5000,
    tls: true,
  });
  await client.connect();
  db = client.db(dbName);
  console.log('MongoDB connected successfully');
}

export function getDb() {
  if (!db) throw new Error('Database not connected — call connectDb() first');
  return db;
}

export function getCollection(name) {
  return getDb().collection(name);
}

// issue-1143: needed to start a session for multi-document transactions
// (office-actions.js's atomic budget-claim + dedupe + write).
export function getClient() {
  if (!client) throw new Error('MongoDB client not connected — call connectDb() first');
  return client;
}

// Returns true if the DB connection is alive
export function isConnected() {
  try {
    return !!db;
  } catch {
    return false;
  }
}

export async function closeDb() {
  if (client) {
    await client.close();
    client = null;
    db = null;
    console.log('MongoDB connection closed');
  }
}
/**
 * Test DB setup/teardown — connects to real MongoDB for integration tests.
 */

import { connectDb, closeDb, getCollection, getDb } from '../../db.js';

export async function setupDb() {
  try {
    await connectDb(); // No-op if already connected (idempotent)
  } catch (err) {
    console.error('[setupDb] connectDb() failed:', err.message);
    throw err;
  }

  // Belt-and-braces: catches the case where a connection was already
  // established (by earlier-running code) before setup-env.js's override
  // took effect. connectDb()'s own guard only fires on the connection that
  // creates `db`; this re-checks whatever connection setupDb() ends up with.
  const dbName = getDb().databaseName;
  if (!dbName.endsWith('_test')) {
    throw new Error(
      `Refusing test run: connected database '${dbName}' does not end with '_test'. ` +
        `Tests must use a *_test database — check tests/helpers/setup-env.js ordering.`
    );
  }
}

export async function teardownDb() {
  // No-op — closing mid-run causes failures when test files share a process.
  // The connection closes naturally when the process exits.
}

/**
 * issue-1143 (AC5): non-throwing connectivity probe, for suites that want a
 * clean vitest skip rather than a failed beforeAll + a second erroring
 * afterAll cleanup. setupDb() itself keeps its existing throw-on-failure
 * contract unchanged (other suites already depend on that shape) — this is
 * a separate, additive helper. Usage:
 *
 *   const dbAvailable = await isDbAvailable();
 *   describe.skipIf(!dbAvailable)('suite name', () => {
 *     beforeAll(async () => { await setupDb(); ... });
 *     ...
 *   });
 *
 * The top-level `await` works because vitest test files are ESM modules.
 */
export async function isDbAvailable() {
  try {
    await setupDb();
    return true;
  } catch {
    return false;
  }
}

/**
 * Get a few character IDs for testing player filtering.
 *
 * Against tm_suite_test (the default under vitest), the characters collection
 * is usually empty. Auto-seed minimal stub characters when we don't have
 * enough, so tests that just need "some valid character IDs" work without
 * every test file having to roll its own fixtures.
 */
export async function getTestCharacterIds(count = 2) {
  const col = getCollection('characters');
  let chars = await col
    .find({ retired: { $ne: true } })
    .limit(count)
    .project({ _id: 1, name: 1 })
    .toArray();

  if (chars.length < count) {
    const need = count - chars.length;
    const stubs = Array.from({ length: need }, (_, i) => ({
      name: `Test Character ${Date.now()}_${i}`,
      retired: false,
      _test_seeded: true,
    }));
    const result = await col.insertMany(stubs);
    const seededIds = Object.values(result.insertedIds);
    const seededDocs = await col
      .find({ _id: { $in: seededIds } })
      .project({ _id: 1, name: 1 })
      .toArray();
    chars = [...chars, ...seededDocs];
  }

  return chars.map(c => ({ id: c._id.toString(), name: c.name }));
}

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:47:58.922530Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\server\\config.js'; Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\server\\tests\\helpers\\setup-env.js'" in D:\Terra Mortis\TM Suite
 succeeded in 3061ms:
import dotenv from 'dotenv';
import { fileURLToPath } from 'url';
import { dirname, resolve } from 'path';

const __dirname = dirname(fileURLToPath(import.meta.url));
dotenv.config({ path: resolve(__dirname, '..', '.env') });

export const config = {
  MONGODB_URI: process.env.MONGODB_URI || 'mongodb://localhost:27017/tm_suite',
  PORT: parseInt(process.env.PORT, 10) || 3000,
  NODE_ENV: process.env.NODE_ENV || 'development',
  CORS_ORIGIN: process.env.CORS_ORIGIN || 'http://localhost:8080',

  // Discord OAuth2
  DISCORD_CLIENT_ID: process.env.DISCORD_CLIENT_ID,
  DISCORD_CLIENT_SECRET: process.env.DISCORD_CLIENT_SECRET,
  DISCORD_REDIRECT_URI: process.env.DISCORD_REDIRECT_URI || 'http://localhost:8080/admin',

  // Allowed ST Discord IDs
  ST_IDS: (process.env.ST_IDS || '').split(',').filter(Boolean),
};
/**
 * Vitest global setup — forces test runs to use a separate Mongo database.
 *
 * Without this, integration tests hit `tm_suite` (the live DB) and any
 * seed/cleanup leak corrupts production data. Running against `tm_suite_test`
 * isolates every insert/update/delete so the worst-case is a polluted
 * throwaway DB you can simply drop.
 *
 * Set BEFORE db.js is imported, because db.js resolves MONGODB_DB at
 * connect time. Vitest's `setupFiles` runs once per worker before test files.
 */

import 'dotenv/config';

// Hard override — never let a test run touch tm_suite, even if a developer
// sets MONGODB_DB=tm_suite in their local env. Tests always use tm_suite_test.
process.env.MONGODB_DB = 'tm_suite_test';

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:48:08.244104Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The first gate did not reach the local daemon: the repository’s root `.env` points tests at a remote Mongo host, and network policy rejected it (`EACCES`), producing 14 skips plus a failed suite. I found the test DB override changes only the database name, not the URI. I’m retrying against `127.0.0.1` explicitly; the DB client also hard-codes TLS, so this may reveal a second local-environment incompatibility.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/equipment.test.js" in D:\Terra Mortis\TM Suite
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:48:27.638783Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
2026-08-13T02:48:29.536158Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 13 seconds
Output:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

stdout | tests/equipment.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  enable debug logging with { debug: true }

 ❯ tests/equipment.test.js (14 tests | 14 skipped) 5040ms

 Test Files  1 failed (1)
      Tests  14 skipped (14)
   Start at  12:48:21
   Duration  7.80s (transform 723ms, setup 51ms, import 2.34s, tests 5.04s, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework
stderr | tests/equipment.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/equipment.test.js
[setupDb] connectDb() failed: read ECONNRESET


⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
MongoServerSelectionError: read ECONNRESET
 ❯ Topology.selectServer node_modules/mongodb/src/sdam/topology.ts:637:29
 ❯ Topology._connect node_modules/mongodb/src/sdam/topology.ts:469:21
 ❯ Topology.connect node_modules/mongodb/src/sdam/topology.ts:405:6
 ❯ topologyConnect node_modules/mongodb/src/mongo_client.ts:662:8
 ❯ MongoClient._connect node_modules/mongodb/src/mongo_client.ts:674:6
 ❯ MongoClient.connect node_modules/mongodb/src/mongo_client.ts:586:6
 ❯ connectDb db.js:33:3
     31|     tls: true,
     32|   });
     33|   await client.connect();
       |   ^
     34|   db = client.db(dbName);
     35|   console.log('MongoDB connected successfully');
 ❯ setupDb tests/helpers/db-setup.js:9:5
 ❯ tests/equipment.test.js:70:3

Caused by: MongoNetworkError: read ECONNRESET
 ❯ TLSSocket.<anonymous> node_modules/mongodb/lib/cmap/connect.js:313:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errorLabelSet: { constructor: 'Function<Set>', has: 'Function<has>', add: 'Function<add>', delete: 'Function<delete>', difference: 'Function<difference>', clear: 'Function<clear>', entries: 'Function<entries>', forEach: 'Function<forEach>', intersection: 'Function<intersection>', isSubsetOf: 'Function<isSubsetOf>', isSupersetOf: 'Function<isSupersetOf>', isDisjointFrom: 'Function<isDisjointFrom>', size: 3, symmetricDifference: 'Function<symmetricDifference>', union: 'Function<union>', values: 'Function<values>', keys: 'Function<values>' }, beforeHandshake: false, errorLabels: [ 'SystemOverloadedError', 'RetryableError', 'ResetPool' ], errmsg: 'read ECONNRESET', hasErrorLabel: 'Function<hasErrorLabel>', addErrorLabel: 'Function<addErrorLabel>' }
Caused by: Error: read ECONNRESET
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errno: -4077, code: 'ECONNRESET', syscall: 'read' }
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
Error: Database not connected — call connectDb() first
 ❯ getDb db.js:39:18
     37|
     38| export function getDb() {
     39|   if (!db) throw new Error('Database not connected — call connectDb() …
       |                  ^
     40|   return db;
     41| }
 ❯ getCollection db.js:44:10
 ❯ tests/equipment.test.js:79:28

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 exited 1 in 12950ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

stdout | tests/equipment.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  enable debug logging with { debug: true }

 ❯ tests/equipment.test.js (14 tests | 14 skipped) 5040ms

 Test Files  1 failed (1)
      Tests  14 skipped (14)
   Start at  12:48:21
   Duration  7.80s (transform 723ms, setup 51ms, import 2.34s, tests 5.04s, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework
stderr | tests/equipment.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/equipment.test.js
[setupDb] connectDb() failed: read ECONNRESET


⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
MongoServerSelectionError: read ECONNRESET
 ❯ Topology.selectServer node_modules/mongodb/src/sdam/topology.ts:637:29
 ❯ Topology._connect node_modules/mongodb/src/sdam/topology.ts:469:21
 ❯ Topology.connect node_modules/mongodb/src/sdam/topology.ts:405:6
 ❯ topologyConnect node_modules/mongodb/src/mongo_client.ts:662:8
 ❯ MongoClient._connect node_modules/mongodb/src/mongo_client.ts:674:6
 ❯ MongoClient.connect node_modules/mongodb/src/mongo_client.ts:586:6
 ❯ connectDb db.js:33:3
     31|     tls: true,
     32|   });
     33|   await client.connect();
       |   ^
     34|   db = client.db(dbName);
     35|   console.log('MongoDB connected successfully');
 ❯ setupDb tests/helpers/db-setup.js:9:5
 ❯ tests/equipment.test.js:70:3

Caused by: MongoNetworkError: read ECONNRESET
 ❯ TLSSocket.<anonymous> node_modules/mongodb/lib/cmap/connect.js:313:44

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errorLabelSet: { constructor: 'Function<Set>', has: 'Function<has>', add: 'Function<add>', delete: 'Function<delete>', difference: 'Function<difference>', clear: 'Function<clear>', entries: 'Function<entries>', forEach: 'Function<forEach>', intersection: 'Function<intersection>', isSubsetOf: 'Function<isSubsetOf>', isSupersetOf: 'Function<isSupersetOf>', isDisjointFrom: 'Function<isDisjointFrom>', size: 3, symmetricDifference: 'Function<symmetricDifference>', union: 'Function<union>', values: 'Function<values>', keys: 'Function<values>' }, beforeHandshake: false, errorLabels: [ 'SystemOverloadedError', 'RetryableError', 'ResetPool' ], errmsg: 'read ECONNRESET', hasErrorLabel: 'Function<hasErrorLabel>', addErrorLabel: 'Function<addErrorLabel>' }
Caused by: Error: read ECONNRESET
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Serialized Error: { errno: -4077, code: 'ECONNRESET', syscall: 'read' }
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/equipment.test.js [ tests/equipment.test.js ]
Error: Database not connected — call connectDb() first
 ❯ getDb db.js:39:18
     37|
     38| export function getDb() {
     39|   if (!db) throw new Error('Database not connected — call connectDb() …
       |                  ^
     40|   return db;
     41| }
 ❯ getCollection db.js:44:10
 ❯ tests/equipment.test.js:79:28

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:48:29.802278Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\Terra Mortis\\TM Suite' status --short -- 'server/db.js'; git -C 'D:\\Terra Mortis\\TM Suite' diff -- 'server/db.js'" in D:\Terra Mortis\TM Suite
 succeeded in 3045ms:
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied

diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:48:37.983398Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The local retry reached `mongod` but failed its TLS handshake (`ECONNRESET`) because `server/db.js` hard-codes `tls: true`. I’m using the review’s explicitly allowed temporary-edit procedure: change that one option to `false`, run the gates against `127.0.0.1/tm_suite_test`, then restore `server/db.js` from the committed state and verify its diff is empty.
apply patch
patch: completed
D:\Terra Mortis\TM Suite\server\db.js
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:48:45.229278Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/equipment.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 8963ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  14 passed (14)
   Start at  12:48:58
   Duration  3.52s (transform 776ms, setup 59ms, import 2.53s, tests 561ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:49:02.128153Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/equipment-client-fixes.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 5626ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  6 passed (6)
   Start at  12:49:11
   Duration  445ms (transform 41ms, setup 56ms, import 31ms, tests 10ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:49:11.837599Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/issue-868-ecm-1-equipment-catalogue-api.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 8652ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  28 passed (28)
   Start at  12:49:23
   Duration  3.21s (transform 753ms, setup 58ms, import 2.35s, tests 444ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:49:27.014388Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/issue-871-876-ecm-4-9-bundle.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 5606ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  19 passed (19)
   Start at  12:49:36
   Duration  487ms (transform 43ms, setup 53ms, import 49ms, tests 17ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:49:37.285995Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/issue-872-ecm-5-editor-cache.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 5519ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  14 passed (14)
   Start at  12:49:49
   Duration  474ms (transform 66ms, setup 56ms, import 45ms, tests 33ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:49:50.597418Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/issue-896-availability-filter.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 6188ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  28 passed (28)
   Start at  12:50:06
   Duration  767ms (transform 220ms, setup 49ms, import 62ms, tests 265ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:50:07.098837Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/issue-879-defence-penalty-wirein.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 5951ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  36 passed (36)
   Start at  12:50:16
   Duration  741ms (transform 234ms, setup 53ms, import 68ms, tests 238ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:50:17.233942Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/issue-1152-eqc1-bucket-migration.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 6140ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  11 passed (11)
   Start at  12:50:29
   Duration  936ms (transform 96ms, setup 55ms, import 396ms, tests 122ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:50:30.263813Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/issue-873-ecm-6-admin-sidebar.test.js" in D:\Terra Mortis\TM Suite
 succeeded in 5495ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server


 Test Files  1 passed (1)
      Tests  14 passed (14)
   Start at  12:50:42
   Duration  453ms (transform 42ms, setup 51ms, import 35ms, tests 29ms, environment 0ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:50:42.656044Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
All nine equipment gates passed against the local test DB: 9 files, 170/170 tests, zero skips. That independently confirms the record’s equipment-suite count once the TLS/environment mismatch is worked around. I’m now running the ten files named as “pre-existing failures” together to compare their current-HEAD file count, test count, and failing test names with the record.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg --files 'D:\\Terra Mortis\\TM Suite\\server\\tests' | rg \"(epic\\.708\\.3-cycle-phase-controls|issue-1013-indomitable-rules-text|issue-1021-failed-breakpoint-merit|issue-811-sumchannels-rootcause|issue-826-cleanup-script-integration|issue-836-legacy-tracker-cache-removed|issue-837-xp-totals-deprecation|n7-n9-allocator-readers|n8-mandragora-prereq|oath-a-pledge-helpers)\\.test\\.js"'$"' in D:\Terra Mortis\TM Suite
 succeeded in 3006ms:
D:\Terra Mortis\TM Suite\server\tests\epic.708.3-cycle-phase-controls.test.js
D:\Terra Mortis\TM Suite\server\tests\issue-1021-failed-breakpoint-merit.test.js
D:\Terra Mortis\TM Suite\server\tests\issue-1013-indomitable-rules-text.test.js
D:\Terra Mortis\TM Suite\server\tests\issue-826-cleanup-script-integration.test.js
D:\Terra Mortis\TM Suite\server\tests\issue-811-sumchannels-rootcause.test.js
D:\Terra Mortis\TM Suite\server\tests\issue-837-xp-totals-deprecation.test.js
D:\Terra Mortis\TM Suite\server\tests\issue-836-legacy-tracker-cache-removed.test.js
D:\Terra Mortis\TM Suite\server\tests\n8-mandragora-prereq.test.js
D:\Terra Mortis\TM Suite\server\tests\n7-n9-allocator-readers.test.js
D:\Terra Mortis\TM Suite\server\tests\oath-a-pledge-helpers.test.js

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:50:55.954065Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/epic.708.3-cycle-phase-controls.test.js tests/issue-1013-indomitable-rules-text.test.js tests/issue-1021-failed-breakpoint-merit.test.js tests/issue-811-sumchannels-rootcause.test.js tests/issue-826-cleanup-script-integration.test.js tests/issue-836-legacy-tracker-cache-removed.test.js tests/issue-837-xp-totals-deprecation.test.js tests/n7-n9-allocator-readers.test.js tests/n8-mandragora-prereq.test.js tests/oath-a-pledge-helpers.test.js" in D:\Terra Mortis\TM Suite
2026-08-13T02:51:15.664233Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 10.2 seconds
Total output lines: 3441
Output:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

 ❯ tests/oath-a-pledge-helpers.test.js (29 tests | 1 failed) 41ms
     × meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form 28ms
 ❯ tests/n7-n9-allocator-readers.test.js (25 tests | 1 failed) 87ms
     × all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly) 9ms
 ❯ tests/epic.708.3-cycle-phase-controls.test.js (14 tests | 3 failed) 31ms
     × exports setGamePhase function 14ms
     × uses data-phase attribute on phase buttons 3ms
     × highlights active phase with gold2 colour 4ms
stdout | tests/issue-1013-indomitable-rules-text.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/issue-1013-indomitable-rules-text.test.js (0 test)
 ❯ tests/issue-1021-failed-breakpoint-merit.test.js (0 test)
 ❯ tests/issue-811-sumchannels-rootcause.test.js (0 test)
stdout | tests/issue-826-cleanup-script-integration.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/issue-826-cleanup-script-integration.test.js (0 test)
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js (0 test)
stdout | tests/issue-837-xp-totals-deprecation.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/issue-837-xp-totals-deprecation.test.js (0 test)
 ❯ tests/n8-mandragora-prereq.test.js (0 test)

 Test Files  10 failed (10)
      Tests  5 failed | 63 passed (68)
   Start at  12:51:10
   Duration  5.12s (transform 311ms, setup 246ms, import 156ms, tests 160ms, environment 1ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

⎯⎯⎯⎯⎯⎯ Failed Suites 7 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/issue-1013-indomitable-rules-text.test.js [ tests/issue-1013-indomitable-rules-text.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/12]⎯

 FAIL  tests/issue-1021-failed-breakpoint-merit.test.js [ tests/issue-1021-failed-breakpoint-merit.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/12]⎯

 FAIL  tests/issue-811-sumchannels-rootcause.test.js [ tests/issue-811-sumchannels-rootcause.test.js ]
 FAIL  tests/issue-826-cleanup-script-integration.test.js [ tests/issue-826-cleanup-script-integration.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/12]⎯

 FAIL  tests/issue-836-legacy-tracker-cache-removed.test.js [ tests/issue-836-legacy-tracker-cache-removed.test.js ]
Error: ENOENT: no such file or directory, open 'D:\Terra Mortis\TM Suite\public\js\suite\tracker.js'
 ❯ read tests/issue-836-legacy-tracker-cache-removed.test.js:30:32
     28| const __dirname = path.dirname(fileURLToPath(import.meta.url));
     29| const REPO_ROOT = path.resolve(__dirname, '..', '..');
     30| function read(rel) { return fs.readFileSync(path.join(REPO_ROOT, rel),…
       |                                ^
     31| function exists(rel) { return fs.existsSync(path.join(REPO_ROOT, rel))…
     32|
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js:69:15

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/12]⎯

 FAIL  tests/issue-837-xp-totals-deprecation.test.js [ tests/issue-837-xp-totals-deprecation.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/12]⎯

 FAIL  tests/n8-mandragora-prereq.test.js [ tests/n8-mandragora-prereq.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/12]⎯


⎯⎯⎯⎯⎯⎯⎯ Failed Tests 5 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > exports setGamePhase function
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'setGamePhase'

- Expected
+ Received

- setGamePhase
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Attendance…26582 tokens truncated…
    return n[0] === discName || (n.length > 1 && n.slice(0, -1).join(' ') === discName);
  });
}

/** Return powers not attributable to any known discipline on the character. */
export function otherPowers(c) {
  const discNames = Object.keys(c.disciplines || {});
  return (c.powers || []).filter(p => {
    if (!p.name) return false;
    const key = p.name.split('|')[0].trim().replace(/\\s*[●○]+$/, '').replace(/\\s+\\d+$/, '');
    if (RITUAL_DISCS.some(d => p.name.startsWith(d))) return false;
    return !discNames.some(d => key === d || key.startsWith(d + ' '));
  });
}

/** Returns a Set of cult names from a character's Mystery Cult Initiation merits. */
function getCharCults(c) {
  return new Set(
    (c.merits || [])
      .filter(m => m.name === 'Mystery Cult Initiation' && m.cult_name)
      .map(m => m.cult_name)
  );
}

/** Check whether a character meets a devotion's discipline prerequisites. */
export function meetsDevPrereqs(c, dev) {
  if (dev.bl && (c.bloodline || '') !== dev.bl) return false;
  if (dev.cult && !getCharCults(c).has(dev.cult)) return false;
  const discs = c.disciplines || {};
  if (!dev.p || !dev.p.length) return true;
  if (dev.or) {
    return dev.p.some(p => (discs[p.disc]?.dots || 0) >= p.dots);
  }
  return dev.p.every(p => (discs[p.disc]?.dots || 0) >= p.dots);
}

/** Format a devotion's prerequisite list as a human-readable string. */
export function devPrereqStr(dev) {
  const parts = [];
  if (dev.bl) parts.push(dev.bl + ' only');
  if (dev.cult) parts.push(dev.cult + ' members only');
  if (dev.p && dev.p.length) parts.push(dev.p.map(p => p.disc + ' ' + p.dots).join(dev.or ? ' or ' : ', '));
  return parts.join('; ') || 'None';
}
"

 ❯ tests/n7-n9-allocator-readers.test.js:246:17
    244|     const src = read('public/js/editor/merits.js');
    245|     // buildMeritOptions
    246|     expect(src).toMatch(/buildMeritOptions[\s\S]{0,600}meritPrereqOK\(…
       |                 ^
    247|     // buildSubCategoryMeritOptions
    248|     expect(src).toMatch(/buildSubCategoryMeritOptions[\s\S]{0,800}meri…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/12]⎯

 FAIL  tests/oath-a-pledge-helpers.test.js > OATH-A D2 — encumbrance changes no dot sum > meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form
AssertionError: expected '/**\r\n * XP calculations and merit r…' to contain 'export function meritRating(c, m) {\n…'

- Expected
+ Received

- export function meritRating(c, m) {
-   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+ /**
+  * XP calculations and merit rating/breakdown helpers.
+  * Pure functions — no DOM side-effects.
+  */
+ 
+ import { getRuleByKey } from '../data/loader.js';
+ // N-1: map-fallback shape for per-slug free reads (see rules-helpers.js).
+ import { freeOf } from '../data/rules-helpers.js';
+ // COLLECTIVE-2 (#1110): compound slug + source names reach the bd-row from
+ // rule data, so they are escaped like every other data-sourced label.
+ import { esc } from '../data/helpers.js';
+ 
+ /**
+  * Convert XP spent into dot count (flat rate).
+  * @param {number} xpSpent - total XP allocated
+  * @param {number} baseBeforeXP - dots from CP/free (unused in flat model, kept for API)
+  * @param {number} costPerDot - XP cost per single dot
+  * @returns {number}
+  */
+ export function xpToDots(xpSpent, baseBeforeXP, costPerDot) {
+   return Math.floor((xpSpent || 0) / costPerDot);
+ }
+ 
+ /**
+  * Convert a dot count into XP cost (flat rate).
+  * @param {number} numDots
+  * @param {number} baseBeforeXP - (unused in flat model, kept for API)
+  * @param {number} costPerDot
+  * @returns {number}
+  */
+ export function dotsToXP(numDots, baseBeforeXP, costPerDot) {
+   return (numDots || 0) * costPerDot;
+ }
+ 
+ /** Starting XP awarded on character creation. */
+ export function xpStarting() { return 10; }
+ 
+ /** XP from voluntary humanity drops: 2 per dot permanently lost. */
+ export function xpHumanityDrop(c) {
+   // Use explicit field if set; otherwise infer from stored humanity value
+   const lost = c.humanity_lost !== undefined
+     ? c.humanity_lost
+     : Math.max(0, (c.humanity_base || 7) - (c.humanity || 0));
+   return lost * 2;
+ }
+ 
+ /** XP from completed ordeals: 3 per ordeal. */
+ export function xpOrdeals(c) {
+   return ((c.ordeals || []).filter(o => o.complete).length) * 3;
+ }
+ 
+ /** XP from game attendance. Requires loadGameXP() to have run — returns 0 if not yet loaded. */
+ export function xpGame(c) {
+   return c._gameXP ?? 0;
+ }
+ 
+ /** XP bonus from Professional Training: 1 XP per asset skill at 5+ effective dots (requires PT ≥ 4).
+  *  Effective dots = base dots + PT dot 4 bonus (+1 to chosen asset skill). */
+ export function xpPT5(c) {
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   if (!ptM || meritRating(c, ptM) < 4) return 0;
+   const assets = (ptM.asset_skills || []).filter(Boolean);
+   if (!assets.length) return 0;
+   const ptBonus = c._pt_dot4_bonus_skills instanceof Set ? c._pt_dot4_bonus_skills : new Set();
+   return assets.filter(sk => {
+     const s = (c.skills || {})[sk];
+     const effective = (s?.dots || 0) + (ptBonus.has(sk) ? 1 : 0);
+     return effective >= 5;
+   }).length;
+ }
+ 
+ /**
+  * Total XP earned by a character (all sources, derived dynamically).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpEarned(c) {
+   return xpStarting() + xpHumanityDrop(c) + xpOrdeals(c) + xpGame(c) + xpPT5(c);
+ }
+ 
+ /** Sum XP from inline creation fields on an object (attributes, skills, disciplines). */
+ function sumInlineXP(obj) {
+   if (!obj) return 0;
+   return Object.values(obj).reduce((t, v) => t + (v?.xp || 0), 0);
+ }
+ 
+ /** XP spent on attributes — sum of .xp across all attribute objects. */
+ export function xpSpentAttrs(c) {
+   return sumInlineXP(c.attributes);
+ }
+ 
+ /** XP spent on skills + specialisations beyond free allowance. */
+ export function xpSpentSkills(c) {
+   const skillXP = sumInlineXP(c.skills);
+   // PT free specs (dot 3): 2 extra, but ONLY usable on asset skills — tracked separately
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   const ptFree = (ptM && ptM.rating >= 3) ? 2 : 0;
+   const ptAssets = new Set((ptM && ptM.rating >= 3 && ptM.asset_skills) ? (ptM.asset_skills || []).filter(Boolean) : []);
+   let assetSpecs = 0, nonAssetSpecs = 0;
+   Object.entries(c.skills || {}).forEach(([sk, skillObj]) => {
+     const count = (skillObj && skillObj.specs) ? skillObj.specs.length : 0;
+     if (ptAssets.has(sk)) assetSpecs += count;
+     else nonAssetSpecs += count;
+   });
+   // MCI dot 1 free specs: each active MCI with dot1_choice === 'speciality' grants 1 free spec
+   const mciFreeSpecs = (c._mci_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // Bloodline-granted specs are also exempt from XP cost
+   const bloodlineFreeSpecs = (c._bloodline_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // PT free covers asset specs first; baseline 3 covers everything else
+   const ptFreeCovered = Math.min(ptFree, assetSpecs);
+   const paidSpecs = nonAssetSpecs + Math.max(0, assetSpecs - ptFreeCovered);
+   const specXP = Math.max(0, paidSpecs - 3 - mciFreeSpecs - bloodlineFreeSpecs);
+   return skillXP + specXP;
+ }
+ 
+ /** XP spent on all merits (general, influence, domain, standing) + fighting styles + pact oaths. */
+ export function xpSpentMerits(c) {
+   const meritXP = (c.merits || []).reduce((t, m) => t + (m.xp || 0), 0);
+   const styleXP = (c.fighting_styles || []).reduce((t, fs) => t + (fs.xp || 0), 0);
+   const pactXP = (c.powers || []).filter(p => p.category === 'pact').reduce((t, p) => t + (p.xp || 0), 0);
+   return meritXP + styleXP + pactXP;
+ }
+ 
+ /** XP spent on powers — disciplines + devotions. */
+ export function xpSpentPowers(c) {
+   const discXP = sumInlineXP(c.disciplines);
+   // Devotion XP: look up each devotion's cost from DEVOTIONS_DB
+   const devXP = (c.powers || [])
+     .filter(p => p.category === 'devotion')
+     .reduce((t, p) => {
+       // Try rules cache first, fallback to _devotionsDB
+       const slug = 'devotion-' + p.name.toLowerCase().replace(/['']/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
+       const rule = getRuleByKey(slug);
+       if (rule) return t + (rule.xp_fixed || 0);
+       const db = _devotionsDB ? _devotionsDB.find(d => d.n === p.name) : null;
+       return t + (db ? db.xp || 0 : 0);
+     }, 0);
+   // Rite XP: paid rites cost 1 XP (rank 1-3) or 2 XP (rank 4-5)
+   const riteXP = (c.powers || [])
+     .filter(p => p.category === 'rite' && !p.free)
+     .reduce((t, p) => t + (p.level >= 4 ? 2 : 1), 0);
+   return discXP + devXP + riteXP;
+ }
+ 
+ /** XP spent on special: Blood Potency, Humanity, lost Willpower dots. */
+ export function xpSpentSpecial(c) {
+   // Blood Potency: tracked directly in bp_creation.xp (new model) or derived (legacy)
+   const bpXP = (c.bp_creation || {}).xp !== undefined
+     ? ((c.bp_creation || {}).xp || 0)
+     : Math.max(0, (c.blood_potency || 1) - 1 - Math.floor(((c.bp_creation || {}).cp || 0) / 5)) * 5;
+   // Humanity: XP spent raising dots (new model only; old model net is captured in xpHumanityDrop)
+   const humXP = c.humanity_xp || 0;
+   // Lost Willpower dots: stored in xp_log.spent.willpower
+   const wpXP = ((c.xp_log || {}).spent || {}).willpower || 0;
+   // Manual special: anything else tracked in xp_log
+   const manualXP = ((c.xp_log || {}).spent || {}).special || 0;
+   return bpXP + humXP + wpXP + manualXP;
+ }
+ 
+ // Devotions DB reference (set via setDevotionsDB)
+ let _devotionsDB = null;
+ export function setDevotionsDB(db) { _devotionsDB = db; }
+ 
+ /**
+  * Total XP spent by a character (all categories, fully dynamic).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpSpent(c) {
+   return xpSpentAttrs(c) + xpSpentSkills(c) + xpSpentMerits(c) + xpSpentPowers(c) + xpSpentSpecial(c);
+ }
+ 
+ /**
+  * Remaining unspent XP.
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpLeft(c) {
+   return xpEarned(c) - xpSpent(c);
+ }
+ 
+ /**
+  * Effective rating of a merit (sum of CP + free + grant pools + XP, inline on merit object).
+  * Falls back to stored rating if inline fields are absent.
+  * @param {object} c - character object
+  * @param {object} m - merit entry
+  * @returns {number}
+  */
+ export function meritRating(c, m) {
+   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+   return (m.cp || 0) + freeOf(m, 'bloodline') + freeOf(m, 'pet') + freeOf(m, 'mci') + freeOf(m, 'vm') + freeOf(m, 'lk')
+     + freeOf(m, 'ohm') + freeOf(m, 'inv') + freeOf(m, 'pt') + freeOf(m, 'mdb') + freeOf(m, 'sw') + (m.xp || 0);
+ }
+ 
+ /**
+  * Render the merit breakdown row: Fr + CP + XP = total
+  * @param {number} realIdx - index into c.merits
+  * @param {object} mc - merit object with inline creation fields {cp, free, xp, free_mci, ...}
+  * @param {number|null} fixedAt - if the merit has a fixed rating (e.g. 3 for VM), pass it here;
+  *   null for graduated merits. When fixedAt is set, the displayed total snaps to 0 until the
+  *   threshold is met, then shows fixedAt.
+  */
+ export function meritBdRow(realIdx, mc, fixedAt, opts = {}) {
+   const cp = mc.cp || 0, xp = mc.xp || 0, fbl = freeOf(mc, 'bloodline'), fret = freeOf(mc, 'pet'), fmci = freeOf(mc, 'mci'), fvm = freeOf(mc, 'vm'), flk = freeOf(mc, 'lk'), fohm = freeOf(mc, 'ohm'), finv = freeOf(mc, 'inv'), fpt = freeOf(mc, 'pt'), fmdb = freeOf(mc, 'mdb'), fsw = freeOf(mc, 'sw');
+   // COLLECTIVE-2 (issue #1110): Collective Compound allocation channels are
+   // data-driven — the caller passes the discovered slugs rather than this
+   // function naming one. Defaults to ['necro'] so call sites that predate
+   // the compound wiring keep their pre-#1110 total exactly.
+   const _cmpSlugs = Array.isArray(opts.compoundSlugs) ? opts.compoundSlugs : ['necro'];
+   const fcompound = _cmpSlugs.reduce((s, slug) => s + freeOf(mc, slug), 0);
+   const total = cp + xp + fbl + fret + fmci + fvm + flk + fohm + finv + fpt + fmdb + fsw + fcompound + (opts.attachBonus || 0);
+   // Effective display: for fixed merits, only show dots once the threshold is reached
+   const effective = (fixedAt != null) ? (total >= fixedAt ? fixedAt : 0) : total;
+   const needsHint = (fixedAt != null && total > 0 && total < fixedAt)
+     ? '<span class="bd-needs-hint">' + total + ' / ' + fixedAt + ' needed</span>' : '';
+   // N-7b (issue #768): Necropolis target merits are pool-funded only — the
+   // domain-renderer call site suppresses CP / XP / MCI / Bonus on these rows
+   // via opts.hideCP / opts.hideXP / opts.hideMCI (this block) + opts.hideBonus
+   // (below). Same opts-flag pattern as N-9's hideBonus on standing merits.
+   // The pre-N-7b shape rendered CP + XP + sep unconditionally; the guards
+   // here are defaults-false so existing call sites that don't pass the
+   // hide-flags get the legacy behaviour unchanged.
+   let h = '<div class="merit-bd-row">';
+   if (!opts.hideCP) h += '<div class="bd-grp"><span class="bd-lbl">CP</span><input class="merit-bd-input" type="number" min="0" value="' + cp + '" onchange="shEditMeritPt(' + realIdx + ',\'cp\',+this.value)"></div>';
+   if (!opts.hideXP) h += '<div class="bd-grp"><span class="bd-lbl">XP</span><input class="merit-bd-input" type="number" min="0" value="' + xp + '" onchange="shEditMeritPt(' + realIdx + ',\'xp\',+this.value)"></div>';
+   if (!opts.hideCP || !opts.hideXP) h += '<div class="bd-sep"></div>';
+   // N-9 (issue #762, Bug 1 "adjacent finding"): MCI input writes the map-shape
+   // `free_grants.mci` per the ADR-005 allocator-write-path amendment. The
+   // handler (shEditMeritPt) detects the `free_grants.` prefix and routes to
+   // the map. Pre-N-9 this wrote `free_mci`, creating a fresh divergence on
+   // every edit after the N-2 backfill.
+   // N-7b: showMCI is AND'd with !hideMCI at the call site so Necropolis
+   // targets don't surface the MCI allocator even when there's pool capacity.
+   // Issue #774: a11y — every stepper input carries id + name + aria-label
+   // matching the NECRO precedent at line 241 (added by N-7c #771). Same
+   // browser warning ("form field element should have an id or name
+   // attribute / No label associated with a form field") was firing on
+   // these 5 sibling steppers. Single-line additions per stepper.
+   if (opts.showMCI && !opts.hideMCI) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-mci-lbl-' + realIdx + '">MCI</span><input id="bd-mci-' + realIdx + '" name="bd-mci-' + realIdx + '" aria-label="Mystery Cult Initiation pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fmci + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.mci\',+this.value)"></div>';
+   if (opts.showVM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-vm-lbl-' + realIdx + '">VM</span><input id="bd-vm-' + realIdx + '" name="bd-vm-' + realIdx + '" aria-label="Viral Mythology pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fvm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_vm\',+this.value)"></div>';
+   if (opts.showLK) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-lk-lbl-' + realIdx + '">LK</span><input id="bd-lk-' + realIdx + '" name="bd-lk-' + realIdx + '" aria-label="Lorekeeper pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + flk + '" onchange="shEditMeritPt(' + realIdx + ',\'free_lk\',+this.value)"></div>';
+   if (opts.showOHM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-ohm-lbl-' + realIdx + '">OHM</span><input id="bd-ohm-' + realIdx + '" name="bd-ohm-' + realIdx + '" aria-label="Oath of the Hard Motherfucker pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fohm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_ohm\',+this.value)"></div>';
+   if (opts.showINV) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-inv-lbl-' + realIdx + '">INV</span><input id="bd-inv-' + realIdx + '" name="bd-inv-' + realIdx + '" aria-label="Invested pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + finv + '" onchange="shEditMeritPt(' + realIdx + ',\'free_inv\',+this.value)"></div>';
+   // N-7 (issue #760): Collective Compound allocator — writes directly to
+   // m.free_grants.<slug> (map shape, no new legacy free_<slug> field) per the
+   // ADR-005 allocator-write-path amendment.
+   // N-7c (issue #771): id + aria-label so browsers don't flag "form field
+   // element should have an id or name attribute / No label associated with a
+   // form field". The 5 sibling steppers (LK/INV/VM/OHM/MCI) got the same
+   // treatment in #774 — see lines above.
+   // COLLECTIVE-2 (issue #1110): one stepper per compound the character
+   // belongs to that claims this merit, each writing its OWN slug. Pre-#1110
+   // this was a single `showNECRO` flag hardwired to free_grants.necro, which
+   // would have written a Crone or Sanctified allocation into the Necropolis
+   // pool. The descriptor supplies slug + source; no name literals here.
+   for (const _cmp of (opts.compoundPools || [])) {
+     if (!_cmp || !_cmp.slug) continue;
+     h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-' + _cmp.slug + '-lbl-' + realIdx + '">' + esc(_cmp.slug.toUpperCase()) + '</span><input id="bd-' + _cmp.slug + '-' + realIdx + '" name="bd-' + _cmp.slug + '-' + realIdx + '" aria-label="' + esc(_cmp.source || _cmp.slug) + ' pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + freeOf(mc, _cmp.slug) + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.' + _cmp.slug + '\',+this.value)"></div>';
+   }
+   h += '<div class="bd-eq"><span class="bd-val">' + effective + ' dot' + (effective === 1 ? '' : 's') + '</span>' + needsHint + '</div>'
+     + '</div>';
+   // N-9 (issue #762, Bug 2): standing-merit render paths (MCI, PT) don't read
+   // m.bonus, so the Bonus row was visible-but-no-op. Standing call sites pass
+   // opts.hideBonus=true to suppress the row entirely. Default behaviour
+   // (general/influence/domain/style merits) is unchanged.
+   if (!opts.hideBonus) {
+     const bon = mc.bonus || 0;
+     h += '<div class="attr-derived-row"><span class="bd-lbl">Bonus</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',-1)"' + (bon === 0 ? ' disabled' : '') + '>&#x25BC;</button><span class="bd-src">' + (bon > 0 ? '+' + bon : '0') + '</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',1)">&#x25B2;</button></div>';
+   }
+   return h;
+ }
+

 ❯ tests/oath-a-pledge-helpers.test.js:388:16
    386|     const xp = read('public/js/editor/xp.js');
    387|     const dom = read('public/js/editor/domain.js');
    388|     expect(xp).toContain("export function meritRating(c, m) {\n  if (m…
       |                ^
    389|     expect(dom).toContain('export function meritEffectiveRating(c, m) …
    390|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/12]⎯


 exited 1 in 10154ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

 ❯ tests/oath-a-pledge-helpers.test.js (29 tests | 1 failed) 41ms
     × meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form 28ms
 ❯ tests/n7-n9-allocator-readers.test.js (25 tests | 1 failed) 87ms
     × all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly) 9ms
 ❯ tests/epic.708.3-cycle-phase-controls.test.js (14 tests | 3 failed) 31ms
     × exports setGamePhase function 14ms
     × uses data-phase attribute on phase buttons 3ms
     × highlights active phase with gold2 colour 4ms
stdout | tests/issue-1013-indomitable-rules-text.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/issue-1013-indomitable-rules-text.test.js (0 test)
 ❯ tests/issue-1021-failed-breakpoint-merit.test.js (0 test)
 ❯ tests/issue-811-sumchannels-rootcause.test.js (0 test)
stdout | tests/issue-826-cleanup-script-integration.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/issue-826-cleanup-script-integration.test.js (0 test)
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js (0 test)
stdout | tests/issue-837-xp-totals-deprecation.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/issue-837-xp-totals-deprecation.test.js (0 test)
 ❯ tests/n8-mandragora-prereq.test.js (0 test)

 Test Files  10 failed (10)
      Tests  5 failed | 63 passed (68)
   Start at  12:51:10
   Duration  5.12s (transform 311ms, setup 246ms, import 156ms, tests 160ms, environment 1ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

⎯⎯⎯⎯⎯⎯ Failed Suites 7 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/issue-1013-indomitable-rules-text.test.js [ tests/issue-1013-indomitable-rules-text.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/12]⎯

 FAIL  tests/issue-1021-failed-breakpoint-merit.test.js [ tests/issue-1021-failed-breakpoint-merit.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/12]⎯

 FAIL  tests/issue-811-sumchannels-rootcause.test.js [ tests/issue-811-sumchannels-rootcause.test.js ]
 FAIL  tests/issue-826-cleanup-script-integration.test.js [ tests/issue-826-cleanup-script-integration.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/12]⎯

 FAIL  tests/issue-836-legacy-tracker-cache-removed.test.js [ tests/issue-836-legacy-tracker-cache-removed.test.js ]
Error: ENOENT: no such file or directory, open 'D:\Terra Mortis\TM Suite\public\js\suite\tracker.js'
 ❯ read tests/issue-836-legacy-tracker-cache-removed.test.js:30:32
     28| const __dirname = path.dirname(fileURLToPath(import.meta.url));
     29| const REPO_ROOT = path.resolve(__dirname, '..', '..');
     30| function read(rel) { return fs.readFileSync(path.join(REPO_ROOT, rel),…
       |                                ^
     31| function exists(rel) { return fs.existsSync(path.join(REPO_ROOT, rel))…
     32|
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js:69:15

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/12]⎯

 FAIL  tests/issue-837-xp-totals-deprecation.test.js [ tests/issue-837-xp-totals-deprecation.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/12]⎯

 FAIL  tests/n8-mandragora-prereq.test.js [ tests/n8-mandragora-prereq.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/12]⎯


⎯⎯⎯⎯⎯⎯⎯ Failed Tests 5 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > exports setGamePhase function
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'setGamePhase'

- Expected
+ Received

- setGamePhase
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Attendance section ───────────────────────────────────────────────────────
+ 
+ function buildAttendanceSection(cy, sessions) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-attend';
+ 
+   const selectWrap = document.createElement('div');
+   selectWrap.className = 'cy-attend-head';
+ 
+   const lbl = document.createElement('label');
+   lbl.className = 'cy-attend-lbl';
+   lbl.textContent = 'Linked Session:';
+ 
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-attend-select';
+ 
+   const blank = document.createElement('option');
+   blank.value = '';
+   blank.textContent = '— not linked —';
+   sel.appendChild(blank);
+ 
+   const sorted = [...sessions].sort((a, b) => (a.game_number ?? 999) - (b.game_number ?? 999));
+   sorted.forEach(s => {
+     const opt = document.createElement('option');
+     opt.value = String(s._id);
+     let label = s.game_number ? 'Game ' + s.game_number : '';
+     if (s.title) label += (label ? ' — ' : '') + s.title;
+     if (!label) label = s.session_date || String(s._id);
+     opt.textContent = label;
+     sel.appendChild(opt);
+   });
+ 
+   sel.value = cy.session_id || '';
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   selectWrap.appendChild(lbl);
+   selectWrap.appendChild(sel);
+   selectWrap.appendChild(errEl);
+   wrap.appendChild(selectWrap);
+ 
+   const tableWrap = document.createElement('div');
+   wrap.appendChild(tableWrap);
+ 
+   function renderTable() {
+     tableWrap.innerHTML = '';
+     const session = sessions.find(s => String(s._id) === sel.value);
+     if (!session) return;
+     const att = session.attendance || [];
+     if (!att.length) {
+       const msg = document.createElement('p');
+       msg.className = 'cy-muted cy-attend-empty';
+       msg.textContent = 'No attendance recorded for this session.';
+       tableWrap.appendChild(msg);
+       return;
+     }
+ 
+     const rows = [...att].sort((a, b) => {
+       const na = (a.character_display || a.character_name || '').toLowerCase();
+       const nb = (b.character_display || b.character_name || '').toLowerCase();
+       return na < nb ? -1 : na > nb ? 1 : 0;
+     });
+ 
+     const table = document.createElement('table');
+     table.className = 'infl-table cy-attend-table';
+     table.innerHTML = `<thead><tr>
+       <th>Character</th>
+       <th class="cy-att-c">Attend</th>
+       <th class="cy-att-c">Costuming</th>
+       <th class="cy-att-c">DT</th>
+       <th class="cy-att-c">Extra</th>
+       <th class="cy-att-c">XP</th>
+     </tr></thead>`;
+ 
+     const tbody = document.createElement('tbody');
+     let totAtt = 0, totCos = 0, totDT = 0, totExtra = 0, totXP = 0;
+ 
+     rows.forEach(a => {
+       const xp = (a.attended ? 1 : 0) + (a.costuming ? 1 : 0) + (a.downtime ? 1 : 0) + (a.extra || 0);
+       totAtt   += a.attended  ? 1 : 0;
+       totCos   += a.costuming ? 1 : 0;
+       totDT    += a.downtime  ? 1 : 0;
+       totExtra += (a.extra || 0);
+       totXP    += xp;
+ 
+       const name = a.character_display || a.character_name || a.character_id || '?';
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${esc(name)}</td>
+         <td class="cy-att-c">${a.attended  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.costuming ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.downtime  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.extra || 0}</td>
+         <td class="cy-att-c cy-att-xp">${xp}</td>`;
+       tbody.appendChild(tr);
+     });
+ 
+     const totTr = document.createElement('tr');
+     totTr.className = 'cy-att-total';
+     totTr.innerHTML = `
+       <td class="cy-muted">Total (${rows.length})</td>
+       <td class="cy-att-c">${totAtt}</td>
+       <td class="cy-att-c">${totCos}</td>
+       <td class="cy-att-c">${totDT}</td>
+       <td class="cy-att-c">${totExtra}</td>
+       <td class="cy-att-c">${totXP}</td>`;
+     tbody.appendChild(totTr);
+ 
+     table.appendChild(tbody);
+     tableWrap.appendChild(table);
+   }
+ 
+   renderTable();
+ 
+   sel.addEventListener('change', async () => {
+     errEl.classList.remove('is-visible');
+     const newId = sel.value || null;
+     try {
+       await apiPut('/api/downtime_cycles/' + cy._id, { session_id: newId });
+       cy.session_id = newId;
+       renderTable();
+     } catch (err) {
+       sel.value = cy.session_id || '';
+       errEl.textContent = 'Link failed: ' + err.message;
+       errEl.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Game Cycles panel ───────────────────────────────────────────────────────
+ 
+ // Build a chapter <select>. Options-only; no persistence handler — used for
+ // the add-cycle form (which has no cycle to write to yet).
+ function buildChapterPicker(chapters, selectedId = '') {
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-chapter-select';
+   const none = document.createElement('option');
+   none.value = '';
+   none.textContent = '— none —';
+   sel.appendChild(none);
+   [...chapters].sort((a, b) => a.number - b.number).forEach(ch => {
+     const opt = document.createElement('option');
+     opt.value = String(ch._id);
+     opt.textContent = `${ch.number} — ${ch.label}`;
+     sel.appendChild(opt);
+   });
+   sel.value = selectedId ? String(selectedId) : '';
+   return sel;
+ }
+ 
+ // Row-level chapter select: persists chapter_id to an existing cycle on change.
+ function buildChapterSelect(cy, chapters) {
+   const sel = buildChapterPicker(chapters, cy.chapter_id);
+   sel.addEventListener('change', async () => {
+     const val = sel.value || null;
+     try {
+       await updateCycle(cy._id, { chapter_id: val });
+       cy.chapter_id = val;
+       renderRibbon();
+     } catch (err) {
+       sel.value = cy.chapter_id ? String(cy.chapter_id) : '';
+     }
+   });
+   return sel;
+ }
+ 
+ function buildLabelCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-label-cell';
+ 
+   function renderView() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const span = document.createElement('span');
+     span.className = 'cy-label-text';
+     span.textContent = cy.label || String(cy._id);
+     const editBtn = document.createElement('button');
+     editBtn.className = 'cy-icon-btn';
+     editBtn.title = 'Edit label';
+     editBtn.textContent = '✎';
+     editBtn.addEventListener('click', renderEdit);
+     inner.appendChild(span);
+     inner.appendChild(editBtn);
+     td.appendChild(inner);
+   }
+ 
+   function renderEdit() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const input = document.createElement('input');
+     input.type = 'text';
+     input.className = 'form-input cy-label-input';
+     input.value = cy.label || '';
+     const save = document.createElement('button');
+     save.className = 'btn-sm';
+     save.textContent = 'Save';
+     const cancel = document.createElement('button');
+     cancel.className = 'btn-sm btn-muted';
+     cancel.textContent = 'Cancel';
+ 
+     async function doSave() {
+       const newLabel = input.value.trim();
+       if (!newLabel) { input.focus(); return; }
+       try {
+         await updateCycle(cy._id, { label: newLabel });
+         cy.label = newLabel;
+         renderView();
+         renderRibbon();
+       } catch (_err) {
+         input.classList.add('is-error');
+       }
+     }
+     save.addEventListener('click', doSave);
+     cancel.addEventListener('click', renderView);
+     input.addEventListener('keydown', e => {
+       if (e.key === 'Enter') doSave();
+       else if (e.key === 'Escape') renderView();
+     });
+ 
+     inner.appendChild(input);
+     inner.appendChild(save);
+     inner.appendChild(cancel);
+     td.appendChild(inner);
+     input.focus();
+     input.select();
+   }
+ 
+   renderView();
+   return td;
+ }
+ 
+ function buildCyclesPanel(cycles, chapters, charList = [], sessions = []) {
+   const sorted = [...cycles].sort((a, b) => (a.game_number ?? 0) - (b.game_number ?? 0));
+ 
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Game Cycles</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Cycle';
+   header.appendChild(addBtn);
+   wrap.appendChild(header);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   // Inline add-cycle form
+   const addForm = document.createElement('div');
+   addForm.className = 'cy-inline-form';
+   const nextGame = (sorted.reduce((mx, c) => Math.max(mx, c.game_number ?? 0), 0) || 0) + 1;
+   addForm.innerHTML = `
+     <input id="new-cy-num" type="number" min="1" placeholder="Game #" value="${nextGame}" class="form-input cy-input-num">
+     <input id="new-cy-label" type="text" placeholder="Label (e.g. Downtime 5)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-cy-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-cy-cancel">Cancel</button>`;
+   const chapterPick = buildChapterPicker(chapters);
+   chapterPick.id = 'new-cy-chapter';
+   addForm.insertBefore(chapterPick, addForm.querySelector('#new-cy-save'));
+   wrap.appendChild(addForm);
+ 
+   addBtn.addEventListener('click', () => {
+     addForm.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     addForm.querySelector('#new-cy-label').value = `Downtime ${nextGame}`;
+     addForm.querySelector('#new-cy-label').focus();
+   });
+   addForm.querySelector('#new-cy-cancel').addEventListener('click', () => {
+     addForm.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+   addForm.querySelector('#new-cy-save').addEventListener('click', async () => {
+     const num = parseInt(addForm.querySelector('#new-cy-num').value, 10);
+     const label = addForm.querySelector('#new-cy-label').value.trim();
+     const chapterId = addForm.querySelector('#new-cy-chapter').value || null;
+     if (!num || !label) {
+       errMsg.textContent = 'Game number and label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       await createCycle(num, { label, chapterId });
+       await refresh();
+     } catch (err) {
+       errMsg.textContent = `Create failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   if (!sorted.length) {
+     const empty = document.createElement('p');
+     empty.className = 'cy-muted';
+     empty.textContent = 'No downtime cycles found.';
+     wrap.appendChild(errMsg);
+     wrap.appendChild(empty);
+     return wrap;
+   }
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th>Label</th>
+     <th class="cy-col-phase">Phase</th>
+     <th class="cy-col-chapter">Chapter</th>
+     <th class="cy-col-prep">Prep Access</th>
+     <th class="cy-col-pub">Publish</th>
+     <th class="cy-col-att">Attendance</th>
+     <th class="cy-col-del"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+ 
+   sorted.forEach(cy => {
+     const tr = document.createElement('tr');
+ 
+     tr.appendChild(buildLabelCell(cy));
+     tr.appendChild(buildPhaseCell(cy));
+ 
+     const tdChapter = document.createElement('td');
+     tdChapter.appendChild(buildChapterSelect(cy, chapters));
+     tr.appendChild(tdChapter);
+ 
+     // Prep Access toggle
+     const tdAccess = document.createElement('td');
+     const accessBtn = document.createElement('button');
+     accessBtn.className = 'btn-sm';
+     accessBtn.textContent = 'Prep Access';
+     tdAccess.appendChild(accessBtn);
+     tr.appendChild(tdAccess);
+ 
+     const detailTr = document.createElement('tr');
+     detailTr.className = 'cy-detail-row is-hidden';
+     const detailTd = document.createElement('td');
+     detailTd.colSpan = 7;
+     detailTd.className = 'cy-detail-cell';
+     detailTd.appendChild(buildAccessSection(cy, charList));
+     detailTr.appendChild(detailTd);
+ 
+     accessBtn.addEventListener('click', () => {
+       const open = !detailTr.classList.contains('is-hidden');
+       detailTr.classList.toggle('is-hidden', open);
+       accessBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Publish Reports button
+     const tdPublish = document.createElement('td');
+     const publishBtn = document.createElement('button');
+     publishBtn.className = 'btn-sm';
+     publishBtn.textContent = 'Publish Reports';
+     const publishResult = document.createElement('span');
+     publishResult.className = 'cy-publish-result';
+     tdPublish.appendChild(publishBtn);
+     tdPublish.appendChild(publishResult);
+     tr.appendChild(tdPublish);
+ 
+     publishBtn.addEventListener('click', async () => {
+       publishBtn.disabled = true;
+       publishResult.className = 'cy-publish-result';
+       publishResult.textContent = 'Publishing…';
+       try {
+         const result = await apiPost('/api/downtime_cycles/' + cy._id + '/publish', {});
+         if (result.published === 0) {
+           publishResult.textContent = 'No compiled reports found.';
+         } else {
+           publishResult.classList.add('is-ok');
+           publishResult.textContent = result.published + ' report' + (result.published === 1 ? '' : 's') + ' published.';
+         }
+       } catch (err) {
+         publishResult.classList.add('is-error');
+         publishResult.textContent = 'Publish failed: ' + err.message;
+       } finally {
+         publishBtn.disabled = false;
+       }
+     });
+ 
+     // Attendance toggle
+     const tdAtt = document.createElement('td');
+     const attBtn = document.createElement('button');
+     attBtn.className = 'btn-sm';
+     attBtn.textContent = 'Attendance';
+     tdAtt.appendChild(attBtn);
+     tr.appendChild(tdAtt);
+ 
+     const attendTr = document.createElement('tr');
+     attendTr.className = 'cy-detail-row is-hidden';
+     const attendTd = document.createElement('td');
+     attendTd.colSpan = 7;
+     attendTd.className = 'cy-detail-cell';
+     attendTd.appendChild(buildAttendanceSection(cy, sessions));
+     attendTr.appendChild(attendTd);
+ 
+     attBtn.addEventListener('click', () => {
+       const open = !attendTr.classList.contains('is-hidden');
+       attendTr.classList.toggle('is-hidden', open);
+       attBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Delete cycle
+     const tdDel = document.createElement('td');
+     const delBtn = document.createElement('button');
+     delBtn.className = 'btn-sm btn-danger';
+     delBtn.textContent = 'Delete';
+     tdDel.appendChild(delBtn);
+     tr.appendChild(tdDel);
+ 
+     delBtn.addEventListener('click', async () => {
+       if (!confirm(`Delete cycle "${cy.label || cy._id}"? This cannot be undone.`)) return;
+       errMsg.classList.remove('is-visible');
+       delBtn.disabled = true;
+       try {
+         await deleteCycle(cy._id);
+         await refresh();
+       } catch (err) {
+         delBtn.disabled = false;
+         // 409 CYCLE_HAS_SUBMISSIONS surfaces here with the server message.
+         errMsg.textContent = err.message || 'Delete failed.';
+         errMsg.classList.add('is-visible');
+       }
+     });
+ 
+     tbody.appendChild(tr);
+     tbody.appendChild(detailTr);
+     tbody.appendChild(attendTr);
+   });
+ 
+   table.appendChild(tbody);
+   wrap.appendChild(errMsg);
+   wrap.appendChild(table);
+   return wrap;
+ }
+

 ❯ tests/epic.708.3-cycle-phase-controls.test.js:42:25
     40|
     41|   it('exports setGamePhase function', () => {
     42|     expect(CYCLE_VIEWS).toContain('setGamePhase');
       |                         ^
     43|   });
     44|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/12]⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > uses data-phase attribute on phase buttons
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'data-phase'

- Expected
+ Received

- data-phase
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Attendance section ───────────────────────────────────────────────────────
+ 
+ function buildAttendanceSection(cy, sessions) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-attend';
+ 
+   const selectWrap = document.createElement('div');
+   selectWrap.className = 'cy-attend-head';
+ 
+   const lbl = document.createElement('label');
+   lbl.className = 'cy-attend-lbl';
+   lbl.textContent = 'Linked Session:';
+ 
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-attend-select';
+ 
+   const blank = document.createElement('option');
+   blank.value = '';
+   blank.textContent = '— not linked —';
+   sel.appendChild(blank);
+ 
+   const sorted = [...sessions].sort((a, b) => (a.game_number ?? 999) - (b.game_number ?? 999));
+   sorted.forEach(s => {
+     const opt = document.createElement('option');
+     opt.value = String(s._id);
+     let label = s.game_number ? 'Game ' + s.game_number : '';
+     if (s.title) label += (label ? ' — ' : '') + s.title;
+     if (!label) label = s.session_date || String(s._id);
+     opt.textContent = label;
+     sel.appendChild(opt);
+   });
+ 
+   sel.value = cy.session_id || '';
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   selectWrap.appendChild(lbl);
+   selectWrap.appendChild(sel);
+   selectWrap.appendChild(errEl);
+   wrap.appendChild(selectWrap);
+ 
+   const tableWrap = document.createElement('div');
+   wrap.appendChild(tableWrap);
+ 
+   function renderTable() {
+     tableWrap.innerHTML = '';
+     const session = sessions.find(s => String(s._id) === sel.value);
+     if (!session) return;
+     const att = session.attendance || [];
+     if (!att.length) {
+       const msg = document.createElement('p');
+       msg.className = 'cy-muted cy-attend-empty';
+       msg.textContent = 'No attendance recorded for this session.';
+       tableWrap.appendChild(msg);
+       return;
+     }
+ 
+     const rows = [...att].sort((a, b) => {
+       const na = (a.character_display || a.character_name || '').toLowerCase();
+       const nb = (b.character_display || b.character_name || '').toLowerCase();
+       return na < nb ? -1 : na > nb ? 1 : 0;
+     });
+ 
+     const table = document.createElement('table');
+     table.className = 'infl-table cy-attend-table';
+     table.innerHTML = `<thead><tr>
+       <th>Character</th>
+       <th class="cy-att-c">Attend</th>
+       <th class="cy-att-c">Costuming</th>
+       <th class="cy-att-c">DT</th>
+       <th class="cy-att-c">Extra</th>
+       <th class="cy-att-c">XP</th>
+     </tr></thead>`;
+ 
+     const tbody = document.createElement('tbody');
+     let totAtt = 0, totCos = 0, totDT = 0, totExtra = 0, totXP = 0;
+ 
+     rows.forEach(a => {
+       const xp = (a.attended ? 1 : 0) + (a.costuming ? 1 : 0) + (a.downtime ? 1 : 0) + (a.extra || 0);
+       totAtt   += a.attended  ? 1 : 0;
+       totCos   += a.costuming ? 1 : 0;
+       totDT    += a.downtime  ? 1 : 0;
+       totExtra += (a.extra || 0);
+       totXP    += xp;
+ 
+       const name = a.character_display || a.character_name || a.character_id || '?';
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${esc(name)}</td>
+         <td class="cy-att-c">${a.attended  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.costuming ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.downtime  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.extra || 0}</td>
+         <td class="cy-att-c cy-att-xp">${xp}</td>`;
+       tbody.appendChild(tr);
+     });
+ 
+     const totTr = document.createElement('tr');
+     totTr.className = 'cy-att-total';
+     totTr.innerHTML = `
+       <td class="cy-muted">Total (${rows.length})</td>
+       <td class="cy-att-c">${totAtt}</td>
+       <td class="cy-att-c">${totCos}</td>
+       <td class="cy-att-c">${totDT}</td>
+       <td class="cy-att-c">${totExtra}</td>
+       <td class="cy-att-c">${totXP}</td>`;
+     tbody.appendChild(totTr);
+ 
+     table.appendChild(tbody);
+     tableWrap.appendChild(table);
+   }
+ 
+   renderTable();
+ 
+   sel.addEventListener('change', async () => {
+     errEl.classList.remove('is-visible');
+     const newId = sel.value || null;
+     try {
+       await apiPut('/api/downtime_cycles/' + cy._id, { session_id: newId });
+       cy.session_id = newId;
+       renderTable();
+     } catch (err) {
+       sel.value = cy.session_id || '';
+       errEl.textContent = 'Link failed: ' + err.message;
+       errEl.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Game Cycles panel ───────────────────────────────────────────────────────
+ 
+ // Build a chapter <select>. Options-only; no persistence handler — used for
+ // the add-cycle form (which has no cycle to write to yet).
+ function buildChapterPicker(chapters, selectedId = '') {
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-chapter-select';
+   const none = document.createElement('option');
+   none.value = '';
+   none.textContent = '— none —';
+   sel.appendChild(none);
+   [...chapters].sort((a, b) => a.number - b.number).forEach(ch => {
+     const opt = document.createElement('option');
+     opt.value = String(ch._id);
+     opt.textContent = `${ch.number} — ${ch.label}`;
+     sel.appendChild(opt);
+   });
+   sel.value = selectedId ? String(selectedId) : '';
+   return sel;
+ }
+ 
+ // Row-level chapter select: persists chapter_id to an existing cycle on change.
+ function buildChapterSelect(cy, chapters) {
+   const sel = buildChapterPicker(chapters, cy.chapter_id);
+   sel.addEventListener('change', async () => {
+     const val = sel.value || null;
+     try {
+       await updateCycle(cy._id, { chapter_id: val });
+       cy.chapter_id = val;
+       renderRibbon();
+     } catch (err) {
+       sel.value = cy.chapter_id ? String(cy.chapter_id) : '';
+     }
+   });
+   return sel;
+ }
+ 
+ function buildLabelCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-label-cell';
+ 
+   function renderView() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const span = document.createElement('span');
+     span.className = 'cy-label-text';
+     span.textContent = cy.label || String(cy._id);
+     const editBtn = document.createElement('button');
+     editBtn.className = 'cy-icon-btn';
+     editBtn.title = 'Edit label';
+     editBtn.textContent = '✎';
+     editBtn.addEventListener('click', renderEdit);
+     inner.appendChild(span);
+     inner.appendChild(editBtn);
+     td.appendChild(inner);
+   }
+ 
+   function renderEdit() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const input = document.createElement('input');
+     input.type = 'text';
+     input.className = 'form-input cy-label-input';
+     input.value = cy.label || '';
+     const save = document.createElement('button');
+     save.className = 'btn-sm';
+     save.textContent = 'Save';
+     const cancel = document.createElement('button');
+     cancel.className = 'btn-sm btn-muted';
+     cancel.textContent = 'Cancel';
+ 
+     async function doSave() {
+       const newLabel = input.value.trim();
+       if (!newLabel) { input.focus(); return; }
+       try {
+         await updateCycle(cy._id, { label: newLabel });
+         cy.label = newLabel;
+         renderView();
+         renderRibbon();
+       } catch (_err) {
+         input.classList.add('is-error');
+       }
+     }
+     save.addEventListener('click', doSave);
+     cancel.addEventListener('click', renderView);
+     input.addEventListener('keydown', e => {
+       if (e.key === 'Enter') doSave();
+       else if (e.key === 'Escape') renderView();
+     });
+ 
+     inner.appendChild(input);
+     inner.appendChild(save);
+     inner.appendChild(cancel);
+     td.appendChild(inner);
+     input.focus();
+     input.select();
+   }
+ 
+   renderView();
+   return td;
+ }
+ 
+ function buildCyclesPanel(cycles, chapters, charList = [], sessions = []) {
+   const sorted = [...cycles].sort((a, b) => (a.game_number ?? 0) - (b.game_number ?? 0));
+ 
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Game Cycles</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Cycle';
+   header.appendChild(addBtn);
+   wrap.appendChild(header);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   // Inline add-cycle form
+   const addForm = document.createElement('div');
+   addForm.className = 'cy-inline-form';
+   const nextGame = (sorted.reduce((mx, c) => Math.max(mx, c.game_number ?? 0), 0) || 0) + 1;
+   addForm.innerHTML = `
+     <input id="new-cy-num" type="number" min="1" placeholder="Game #" value="${nextGame}" class="form-input cy-input-num">
+     <input id="new-cy-label" type="text" placeholder="Label (e.g. Downtime 5)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-cy-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-cy-cancel">Cancel</button>`;
+   const chapterPick = buildChapterPicker(chapters);
+   chapterPick.id = 'new-cy-chapter';
+   addForm.insertBefore(chapterPick, addForm.querySelector('#new-cy-save'));
+   wrap.appendChild(addForm);
+ 
+   addBtn.addEventListener('click', () => {
+     addForm.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     addForm.querySelector('#new-cy-label').value = `Downtime ${nextGame}`;
+     addForm.querySelector('#new-cy-label').focus();
+   });
+   addForm.querySelector('#new-cy-cancel').addEventListener('click', () => {
+     addForm.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+   addForm.querySelector('#new-cy-save').addEventListener('click', async () => {
+     const num = parseInt(addForm.querySelector('#new-cy-num').value, 10);
+     const label = addForm.querySelector('#new-cy-label').value.trim();
+     const chapterId = addForm.querySelector('#new-cy-chapter').value || null;
+     if (!num || !label) {
+       errMsg.textContent = 'Game number and label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       await createCycle(num, { label, chapterId });
+       await refresh();
+     } catch (err) {
+       errMsg.textContent = `Create failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   if (!sorted.length) {
+     const empty = document.createElement('p');
+     empty.className = 'cy-muted';
+     empty.textContent = 'No downtime cycles found.';
+     wrap.appendChild(errMsg);
+     wrap.appendChild(empty);
+     return wrap;
+   }
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th>Label</th>
+     <th class="cy-col-phase">Phase</th>
+     <th class="cy-col-chapter">Chapter</th>
+     <th class="cy-col-prep">Prep Access</th>
+     <th class="cy-col-pub">Publish</th>
+     <th class="cy-col-att">Attendance</th>
+     <th class="cy-col-del"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+ 
+   sorted.forEach(cy => {
+     const tr = document.createElement('tr');
+ 
+     tr.appendChild(buildLabelCell(cy));
+     tr.appendChild(buildPhaseCell(cy));
+ 
+     const tdChapter = document.createElement('td');
+     tdChapter.appendChild(buildChapterSelect(cy, chapters));
+     tr.appendChild(tdChapter);
+ 
+     // Prep Access toggle
+     const tdAccess = document.createElement('td');
+     const accessBtn = document.createElement('button');
+     accessBtn.className = 'btn-sm';
+     accessBtn.textContent = 'Prep Access';
+     tdAccess.appendChild(accessBtn);
+     tr.appendChild(tdAccess);
+ 
+     const detailTr = document.createElement('tr');
+     detailTr.className = 'cy-detail-row is-hidden';
+     const detailTd = document.createElement('td');
+     detailTd.colSpan = 7;
+     detailTd.className = 'cy-detail-cell';
+     detailTd.appendChild(buildAccessSection(cy, charList));
+     detailTr.appendChild(detailTd);
+ 
+     accessBtn.addEventListener('click', () => {
+       const open = !detailTr.classList.contains('is-hidden');
+       detailTr.classList.toggle('is-hidden', open);
+       accessBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Publish Reports button
+     const tdPublish = document.createElement('td');
+     const publishBtn = document.createElement('button');
+     publishBtn.className = 'btn-sm';
+     publishBtn.textContent = 'Publish Reports';
+     const publishResult = document.createElement('span');
+     publishResult.className = 'cy-publish-result';
+     tdPublish.appendChild(publishBtn);
+     tdPublish.appendChild(publishResult);
+     tr.appendChild(tdPublish);
+ 
+     publishBtn.addEventListener('click', async () => {
+       publishBtn.disabled = true;
+       publishResult.className = 'cy-publish-result';
+       publishResult.textContent = 'Publishing…';
+       try {
+         const result = await apiPost('/api/downtime_cycles/' + cy._id + '/publish', {});
+         if (result.published === 0) {
+           publishResult.textContent = 'No compiled reports found.';
+         } else {
+           publishResult.classList.add('is-ok');
+           publishResult.textContent = result.published + ' report' + (result.published === 1 ? '' : 's') + ' published.';
+         }
+       } catch (err) {
+         publishResult.classList.add('is-error');
+         publishResult.textContent = 'Publish failed: ' + err.message;
+       } finally {
+         publishBtn.disabled = false;
+       }
+     });
+ 
+     // Attendance toggle
+     const tdAtt = document.createElement('td');
+     const attBtn = document.createElement('button');
+     attBtn.className = 'btn-sm';
+     attBtn.textContent = 'Attendance';
+     tdAtt.appendChild(attBtn);
+     tr.appendChild(tdAtt);
+ 
+     const attendTr = document.createElement('tr');
+     attendTr.className = 'cy-detail-row is-hidden';
+     const attendTd = document.createElement('td');
+     attendTd.colSpan = 7;
+     attendTd.className = 'cy-detail-cell';
+     attendTd.appendChild(buildAttendanceSection(cy, sessions));
+     attendTr.appendChild(attendTd);
+ 
+     attBtn.addEventListener('click', () => {
+       const open = !attendTr.classList.contains('is-hidden');
+       attendTr.classList.toggle('is-hidden', open);
+       attBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Delete cycle
+     const tdDel = document.createElement('td');
+     const delBtn = document.createElement('button');
+     delBtn.className = 'btn-sm btn-danger';
+     delBtn.textContent = 'Delete';
+     tdDel.appendChild(delBtn);
+     tr.appendChild(tdDel);
+ 
+     delBtn.addEventListener('click', async () => {
+       if (!confirm(`Delete cycle "${cy.label || cy._id}"? This cannot be undone.`)) return;
+       errMsg.classList.remove('is-visible');
+       delBtn.disabled = true;
+       try {
+         await deleteCycle(cy._id);
+         await refresh();
+       } catch (err) {
+         delBtn.disabled = false;
+         // 409 CYCLE_HAS_SUBMISSIONS surfaces here with the server message.
+         errMsg.textContent = err.message || 'Delete failed.';
+         errMsg.classList.add('is-visible');
+       }
+     });
+ 
+     tbody.appendChild(tr);
+     tbody.appendChild(detailTr);
+     tbody.appendChild(attendTr);
+   });
+ 
+   table.appendChild(tbody);
+   wrap.appendChild(errMsg);
+   wrap.appendChild(table);
+   return wrap;
+ }
+

 ❯ tests/epic.708.3-cycle-phase-controls.test.js:54:25
     52|
     53|   it('uses data-phase attribute on phase buttons', () => {
     54|     expect(CYCLE_VIEWS).toContain('data-phase');
       |                         ^
     55|   });
     56|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/12]⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > highlights active phase with gold2 colour
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'gold2'

- Expected
+ Received

- gold2
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Attendance section ───────────────────────────────────────────────────────
+ 
+ function buildAttendanceSection(cy, sessions) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-attend';
+ 
+   const selectWrap = document.createElement('div');
+   selectWrap.className = 'cy-attend-head';
+ 
+   const lbl = document.createElement('label');
+   lbl.className = 'cy-attend-lbl';
+   lbl.textContent = 'Linked Session:';
+ 
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-attend-select';
+ 
+   const blank = document.createElement('option');
+   blank.value = '';
+   blank.textContent = '— not linked —';
+   sel.appendChild(blank);
+ 
+   const sorted = [...sessions].sort((a, b) => (a.game_number ?? 999) - (b.game_number ?? 999));
+   sorted.forEach(s => {
+     const opt = document.createElement('option');
+     opt.value = String(s._id);
+     let label = s.game_number ? 'Game ' + s.game_number : '';
+     if (s.title) label += (label ? ' — ' : '') + s.title;
+     if (!label) label = s.session_date || String(s._id);
+     opt.textContent = label;
+     sel.appendChild(opt);
+   });
+ 
+   sel.value = cy.session_id || '';
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   selectWrap.appendChild(lbl);
+   selectWrap.appendChild(sel);
+   selectWrap.appendChild(errEl);
+   wrap.appendChild(selectWrap);
+ 
+   const tableWrap = document.createElement('div');
+   wrap.appendChild(tableWrap);
+ 
+   function renderTable() {
+     tableWrap.innerHTML = '';
+     const session = sessions.find(s => String(s._id) === sel.value);
+     if (!session) return;
+     const att = session.attendance || [];
+     if (!att.length) {
+       const msg = document.createElement('p');
+       msg.className = 'cy-muted cy-attend-empty';
+       msg.textContent = 'No attendance recorded for this session.';
+       tableWrap.appendChild(msg);
+       return;
+     }
+ 
+     const rows = [...att].sort((a, b) => {
+       const na = (a.character_display || a.character_name || '').toLowerCase();
+       const nb = (b.character_display || b.character_name || '').toLowerCase();
+       return na < nb ? -1 : na > nb ? 1 : 0;
+     });
+ 
+     const table = document.createElement('table');
+     table.className = 'infl-table cy-attend-table';
+     table.innerHTML = `<thead><tr>
+       <th>Character</th>
+       <th class="cy-att-c">Attend</th>
+       <th class="cy-att-c">Costuming</th>
+       <th class="cy-att-c">DT</th>
+       <th class="cy-att-c">Extra</th>
+       <th class="cy-att-c">XP</th>
+     </tr></thead>`;
+ 
+     const tbody = document.createElement('tbody');
+     let totAtt = 0, totCos = 0, totDT = 0, totExtra = 0, totXP = 0;
+ 
+     rows.forEach(a => {
+       const xp = (a.attended ? 1 : 0) + (a.costuming ? 1 : 0) + (a.downtime ? 1 : 0) + (a.extra || 0);
+       totAtt   += a.attended  ? 1 : 0;
+       totCos   += a.costuming ? 1 : 0;
+       totDT    += a.downtime  ? 1 : 0;
+       totExtra += (a.extra || 0);
+       totXP    += xp;
+ 
+       const name = a.character_display || a.character_name || a.character_id || '?';
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${esc(name)}</td>
+         <td class="cy-att-c">${a.attended  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.costuming ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.downtime  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.extra || 0}</td>
+         <td class="cy-att-c cy-att-xp">${xp}</td>`;
+       tbody.appendChild(tr);
+     });
+ 
+     const totTr = document.createElement('tr');
+     totTr.className = 'cy-att-total';
+     totTr.innerHTML = `
+       <td class="cy-muted">Total (${rows.length})</td>
+       <td class="cy-att-c">${totAtt}</td>
+       <td class="cy-att-c">${totCos}</td>
+       <td class="cy-att-c">${totDT}</td>
+       <td class="cy-att-c">${totExtra}</td>
+       <td class="cy-att-c">${totXP}</td>`;
+     tbody.appendChild(totTr);
+ 
+     table.appendChild(tbody);
+     tableWrap.appendChild(table);
+   }
+ 
+   renderTable();
+ 
+   sel.addEventListener('change', async () => {
+     errEl.classList.remove('is-visible');
+     const newId = sel.value || null;
+     try {
+       await apiPut('/api/downtime_cycles/' + cy._id, { session_id: newId });
+       cy.session_id = newId;
+       renderTable();
+     } catch (err) {
+       sel.value = cy.session_id || '';
+       errEl.textContent = 'Link failed: ' + err.message;
+       errEl.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Game Cycles panel ───────────────────────────────────────────────────────
+ 
+ // Build a chapter <select>. Options-only; no persistence handler — used for
+ // the add-cycle form (which has no cycle to write to yet).
+ function buildChapterPicker(chapters, selectedId = '') {
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-chapter-select';
+   const none = document.createElement('option');
+   none.value = '';
+   none.textContent = '— none —';
+   sel.appendChild(none);
+   [...chapters].sort((a, b) => a.number - b.number).forEach(ch => {
+     const opt = document.createElement('option');
+     opt.value = String(ch._id);
+     opt.textContent = `${ch.number} — ${ch.label}`;
+     sel.appendChild(opt);
+   });
+   sel.value = selectedId ? String(selectedId) : '';
+   return sel;
+ }
+ 
+ // Row-level chapter select: persists chapter_id to an existing cycle on change.
+ function buildChapterSelect(cy, chapters) {
+   const sel = buildChapterPicker(chapters, cy.chapter_id);
+   sel.addEventListener('change', async () => {
+     const val = sel.value || null;
+     try {
+       await updateCycle(cy._id, { chapter_id: val });
+       cy.chapter_id = val;
+       renderRibbon();
+     } catch (err) {
+       sel.value = cy.chapter_id ? String(cy.chapter_id) : '';
+     }
+   });
+   return sel;
+ }
+ 
+ function buildLabelCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-label-cell';
+ 
+   function renderView() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const span = document.createElement('span');
+     span.className = 'cy-label-text';
+     span.textContent = cy.label || String(cy._id);
+     const editBtn = document.createElement('button');
+     editBtn.className = 'cy-icon-btn';
+     editBtn.title = 'Edit label';
+     editBtn.textContent = '✎';
+     editBtn.addEventListener('click', renderEdit);
+     inner.appendChild(span);
+     inner.appendChild(editBtn);
+     td.appendChild(inner);
+   }
+ 
+   function renderEdit() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const input = document.createElement('input');
+     input.type = 'text';
+     input.className = 'form-input cy-label-input';
+     input.value = cy.label || '';
+     const save = document.createElement('button');
+     save.className = 'btn-sm';
+     save.textContent = 'Save';
+     const cancel = document.createElement('button');
+     cancel.className = 'btn-sm btn-muted';
+     cancel.textContent = 'Cancel';
+ 
+     async function doSave() {
+       const newLabel = input.value.trim();
+       if (!newLabel) { input.focus(); return; }
+       try {
+         await updateCycle(cy._id, { label: newLabel });
+         cy.label = newLabel;
+         renderView();
+         renderRibbon();
+       } catch (_err) {
+         input.classList.add('is-error');
+       }
+     }
+     save.addEventListener('click', doSave);
+     cancel.addEventListener('click', renderView);
+     input.addEventListener('keydown', e => {
+       if (e.key === 'Enter') doSave();
+       else if (e.key === 'Escape') renderView();
+     });
+ 
+     inner.appendChild(input);
+     inner.appendChild(save);
+     inner.appendChild(cancel);
+     td.appendChild(inner);
+     input.focus();
+     input.select();
+   }
+ 
+   renderView();
+   return td;
+ }
+ 
+ function buildCyclesPanel(cycles, chapters, charList = [], sessions = []) {
+   const sorted = [...cycles].sort((a, b) => (a.game_number ?? 0) - (b.game_number ?? 0));
+ 
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Game Cycles</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Cycle';
+   header.appendChild(addBtn);
+   wrap.appendChild(header);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   // Inline add-cycle form
+   const addForm = document.createElement('div');
+   addForm.className = 'cy-inline-form';
+   const nextGame = (sorted.reduce((mx, c) => Math.max(mx, c.game_number ?? 0), 0) || 0) + 1;
+   addForm.innerHTML = `
+     <input id="new-cy-num" type="number" min="1" placeholder="Game #" value="${nextGame}" class="form-input cy-input-num">
+     <input id="new-cy-label" type="text" placeholder="Label (e.g. Downtime 5)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-cy-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-cy-cancel">Cancel</button>`;
+   const chapterPick = buildChapterPicker(chapters);
+   chapterPick.id = 'new-cy-chapter';
+   addForm.insertBefore(chapterPick, addForm.querySelector('#new-cy-save'));
+   wrap.appendChild(addForm);
+ 
+   addBtn.addEventListener('click', () => {
+     addForm.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     addForm.querySelector('#new-cy-label').value = `Downtime ${nextGame}`;
+     addForm.querySelector('#new-cy-label').focus();
+   });
+   addForm.querySelector('#new-cy-cancel').addEventListener('click', () => {
+     addForm.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+   addForm.querySelector('#new-cy-save').addEventListener('click', async () => {
+     const num = parseInt(addForm.querySelector('#new-cy-num').value, 10);
+     const label = addForm.querySelector('#new-cy-label').value.trim();
+     const chapterId = addForm.querySelector('#new-cy-chapter').value || null;
+     if (!num || !label) {
+       errMsg.textContent = 'Game number and label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       await createCycle(num, { label, chapterId });
+       await refresh();
+     } catch (err) {
+       errMsg.textContent = `Create failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   if (!sorted.length) {
+     const empty = document.createElement('p');
+     empty.className = 'cy-muted';
+     empty.textContent = 'No downtime cycles found.';
+     wrap.appendChild(errMsg);
+     wrap.appendChild(empty);
+     return wrap;
+   }
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th>Label</th>
+     <th class="cy-col-phase">Phase</th>
+     <th class="cy-col-chapter">Chapter</th>
+     <th class="cy-col-prep">Prep Access</th>
+     <th class="cy-col-pub">Publish</th>
+     <th class="cy-col-att">Attendance</th>
+     <th class="cy-col-del"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+ 
+   sorted.forEach(cy => {
+     const tr = document.createElement('tr');
+ 
+     tr.appendChild(buildLabelCell(cy));
+     tr.appendChild(buildPhaseCell(cy));
+ 
+     const tdChapter = document.createElement('td');
+     tdChapter.appendChild(buildChapterSelect(cy, chapters));
+     tr.appendChild(tdChapter);
+ 
+     // Prep Access toggle
+     const tdAccess = document.createElement('td');
+     const accessBtn = document.createElement('button');
+     accessBtn.className = 'btn-sm';
+     accessBtn.textContent = 'Prep Access';
+     tdAccess.appendChild(accessBtn);
+     tr.appendChild(tdAccess);
+ 
+     const detailTr = document.createElement('tr');
+     detailTr.className = 'cy-detail-row is-hidden';
+     const detailTd = document.createElement('td');
+     detailTd.colSpan = 7;
+     detailTd.className = 'cy-detail-cell';
+     detailTd.appendChild(buildAccessSection(cy, charList));
+     detailTr.appendChild(detailTd);
+ 
+     accessBtn.addEventListener('click', () => {
+       const open = !detailTr.classList.contains('is-hidden');
+       detailTr.classList.toggle('is-hidden', open);
+       accessBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Publish Reports button
+     const tdPublish = document.createElement('td');
+     const publishBtn = document.createElement('button');
+     publishBtn.className = 'btn-sm';
+     publishBtn.textContent = 'Publish Reports';
+     const publishResult = document.createElement('span');
+     publishResult.className = 'cy-publish-result';
+     tdPublish.appendChild(publishBtn);
+     tdPublish.appendChild(publishResult);
+     tr.appendChild(tdPublish);
+ 
+     publishBtn.addEventListener('click', async () => {
+       publishBtn.disabled = true;
+       publishResult.className = 'cy-publish-result';
+       publishResult.textContent = 'Publishing…';
+       try {
+         const result = await apiPost('/api/downtime_cycles/' + cy._id + '/publish', {});
+         if (result.published === 0) {
+           publishResult.textContent = 'No compiled reports found.';
+         } else {
+           publishResult.classList.add('is-ok');
+           publishResult.textContent = result.published + ' report' + (result.published === 1 ? '' : 's') + ' published.';
+         }
+       } catch (err) {
+         publishResult.classList.add('is-error');
+         publishResult.textContent = 'Publish failed: ' + err.message;
+       } finally {
+         publishBtn.disabled = false;
+       }
+     });
+ 
+     // Attendance toggle
+     const tdAtt = document.createElement('td');
+     const attBtn = document.createElement('button');
+     attBtn.className = 'btn-sm';
+     attBtn.textContent = 'Attendance';
+     tdAtt.appendChild(attBtn);
+     tr.appendChild(tdAtt);
+ 
+     const attendTr = document.createElement('tr');
+     attendTr.className = 'cy-detail-row is-hidden';
+     const attendTd = document.createElement('td');
+     attendTd.colSpan = 7;
+     attendTd.className = 'cy-detail-cell';
+     attendTd.appendChild(buildAttendanceSection(cy, sessions));
+     attendTr.appendChild(attendTd);
+ 
+     attBtn.addEventListener('click', () => {
+       const open = !attendTr.classList.contains('is-hidden');
+       attendTr.classList.toggle('is-hidden', open);
+       attBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Delete cycle
+     const tdDel = document.createElement('td');
+     const delBtn = document.createElement('button');
+     delBtn.className = 'btn-sm btn-danger';
+     delBtn.textContent = 'Delete';
+     tdDel.appendChild(delBtn);
+     tr.appendChild(tdDel);
+ 
+     delBtn.addEventListener('click', async () => {
+       if (!confirm(`Delete cycle "${cy.label || cy._id}"? This cannot be undone.`)) return;
+       errMsg.classList.remove('is-visible');
+       delBtn.disabled = true;
+       try {
+         await deleteCycle(cy._id);
+         await refresh();
+       } catch (err) {
+         delBtn.disabled = false;
+         // 409 CYCLE_HAS_SUBMISSIONS surfaces here with the server message.
+         errMsg.textContent = err.message || 'Delete failed.';
+         errMsg.classList.add('is-visible');
+       }
+     });
+ 
+     tbody.appendChild(tr);
+     tbody.appendChild(detailTr);
+     tbody.appendChild(attendTr);
+   });
+ 
+   table.appendChild(tbody);
+   wrap.appendChild(errMsg);
+   wrap.appendChild(table);
+   return wrap;
+ }
+

 ❯ tests/epic.708.3-cycle-phase-controls.test.js:63:25
     61|
     62|   it('highlights active phase with gold2 colour', () => {
     63|     expect(CYCLE_VIEWS).toContain('gold2');
       |                         ^
     64|   });
     65|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/12]⎯

 FAIL  tests/n7-n9-allocator-readers.test.js > N-9 — meritPrereqOK dropdown filter + current-row passthrough warn > all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly)
AssertionError: expected '/**\r\n * Merit utilities, prerequisi…' to match /buildMeritOptions[\s\S]{0,600}meritPr…/

- Expected:
/buildMeritOptions[\s\S]{0,600}meritPrereqOK\(c, rule\)/

+ Received:
"/**
 * Merit utilities, prerequisite system, and power/discipline helpers.
 * Depends on constants and the rules cache (purchasable_powers API).
 */

import {
  ATTR_NAMES, SKILL_NAMES, DISC_NAMES, COV_SHORT, CLAN_NAMES,
  RITUAL_DISCS, INFLUENCE_SPHERES, INFLUENCE_MERIT_TYPES
} from '../data/constants.js';
import { meetsPrereq as _meetsPrereq, prereqLabel as _prereqLabel } from '../data/prereq.js';
import { getRulesByCategory, getRuleByKey, getRulesDB } from '../data/loader.js';

// Re-export the new prereq engine for direct use by consumers
export { _meetsPrereq as meetsPrereq, _prereqLabel as prereqLabel };

/** Check if a merit is excluded by a merit the character already owns.
 *  Issue #188 (2026-05-08): exposed as `isMeritExcluded` so the DT form's
 *  XP Spend picker can apply the same exclusion logic the sheet's Merit
 *  Add picker uses, without duplicating the rule walk. */
export function isMeritExcluded(c, meritName) {
  for (const m of (c.merits || [])) {
    const rule = getRuleByKey(m.name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''));
    if (!rule || !rule.exclusive) continue;
    const excluded = rule.exclusive.split(',').map(s => s.trim().toLowerCase());
    if (excluded.includes(meritName.toLowerCase())) return rule.name;
  }
  return null;
}
// Local alias preserved so the existing internal callers (buildMeritOptions
// / buildSubCategoryMeritOptions / buildMCIGrantOptions) don't need to be
// touched.
const _isExcluded = isMeritExcluded;

/* ══════════════════════════════════════════════════════
   Merit string helpers
   ══════════════════════════════════════════════════════ */

/** Strip dot glyphs and pipe-suffix, returning the base merit name. */
export function meritBase(s) {
  return s.replace(/\\s*[●○]+.*/,'').replace(/\\s*\\|.*/,'').trim();
}

/** Count filled-dot glyphs in a merit string. */
export function meritDotCount(s) {
  return (s.match(/●/g) || []).length;
}

/** Return the pipe-suffix portion, or null. */
export function meritSuffix(s) {
  const m = s.match(/\\|\\s*(.+)$/);
  return m ? m[1].trim() : null;
}

/** Lowercase key from the base name. */
export function meritKey(s) {
  return meritBase(s).toLowerCase();
}

/** Lowercase key with parenthetical qualifiers stripped. */
export function meritKeyBase(s) {
  return meritKey(s).replace(/\\s*\\([^)]*\\)\\s*/g,'').trim();
}

/**
 * If a merit has a single fixed rating (e.g. Viral Mythology = 3), returns that number.
 * Returns null for graduated/range merits (e.g. Allies 1-5).
 */
export function meritFixedRating(name) {
  // Try rules cache first
  const slug = (name || '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
  const rule = getRuleByKey(slug);
  if (rule) {
    if (!rule.rating_range) return null;
    if (rule.rating_range[0] === rule.rating_range[1]) return rule.rating_range[0];
    return null; // range merit
  }
  return null;
}

/** Look up a merit by name string. Tries rules cache first, falls back to MERITS_DB. */
export function meritLookup(s) {
  // Try rules cache (unified schema)
  const slug = (s || '').toLowerCase().replace(/\\s*[●○]+.*/,'').replace(/\\s*\\|.*/,'').trim()
    .replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
  const rule = getRuleByKey(slug);
  if (rule) return { desc: rule.description, prereq: rule.prereq, rating: rule.rating_range ? `${rule.rating_range[0]}–${rule.rating_range[1]}` : null, type: rule.parent, sub_category: rule.sub_category, _rule: rule };
  return null;
}

/* ══════════════════════════════════════════════════════
   Merit array helpers (operate on character objects)
   ══════════════════════════════════════════════════════ */

/**
 * Find a merit by category and filtered index, returning both the merit
 * object and its real index in c.merits.
 */
export function meritByCategory(c, category, filteredIdx) {
  const filtered = (c.merits || []).filter(m => m.category === category);
  const m = filtered[filteredIdx];
  if (!m) return { merit: null, realIdx: -1 };
  return { merit: m, realIdx: c.merits.indexOf(m) };
}

/** Ensure every merit has inline creation fields (v3). */
export function ensureMeritSync(c) {
  if (!c.merits) c.merits = [];
  for (const m of c.merits) {
    if (m.cp === undefined) m.cp = 0;
    if (m.xp === undefined) m.xp = 0;
    // Issue #834: m.free is deprecated — no longer initialised here. New
    // merits don't carry the field; existing merits that have it set retain
    // it until the Phase 3 cleanup script zeros them. See memory
    // feedback_m_free_deprecated.
    if (m.free_mci === undefined) m.free_mci = 0;
    if (m.free_vm === undefined) m.free_vm = 0;
    if (m.free_lk === undefined) m.free_lk = 0;
    if (m.free_ohm === undefined) m.free_ohm = 0;
    if (m.free_inv === undefined) m.free_inv = 0;
    if (m.free_pt === undefined) m.free_pt = 0;
    if (m.free_mdb === undefined) m.free_mdb = 0;
    if (m.free_sw === undefined) m.free_sw = 0;
    if (m.bonus === undefined) m.bonus = 0;
  }
}

/** Append a merit with inline creation defaults. */
export function addMerit(c, merit) {
  if (!c.merits) c.merits = [];
  if (merit.cp === undefined) merit.cp = 0;
  if (merit.xp === undefined) merit.xp = 0;
  // Issue #834: m.free is deprecated — no longer initialised on new merits.
  if (merit.free_mci === undefined) merit.free_mci = 0;
  if (merit.free_vm === undefined) merit.free_vm = 0;
  if (merit.free_lk === undefined) merit.free_lk = 0;
  if (merit.free_ohm === undefined) merit.free_ohm = 0;
  if (merit.free_inv === undefined) merit.free_inv = 0;
  if (merit.free_pt === undefined) merit.free_pt = 0;
  if (merit.free_mdb === undefined) merit.free_mdb = 0;
  if (merit.free_sw === undefined) merit.free_sw = 0;
  if (merit.bonus === undefined) merit.bonus = 0;
  if (merit.rule_key === undefined) merit.rule_key = null;
  c.merits.push(merit);
}

/** Remove a merit by real index. */
export function removeMerit(c, realIdx) {
  if (realIdx < 0 || realIdx >= c.merits.length) return;
  c.merits.splice(realIdx, 1);
}

/* ══════════════════════════════════════════════════════
   Internal dot-lookup helpers (for prerequisite checks)
   ══════════════════════════════════════════════════════ */

function _getAttrDots(c, name) {
  const k = Object.keys(c.attributes || {}).find(a => a.toLowerCase() === name);
  return k ? (c.attributes[k].dots || 0) : 0;
}

function _getSkillDots(c, name) {
  const k = Object.keys(c.skills || {}).find(s => s.toLowerCase() === name);
  return k ? (c.skills[k].dots || 0) : 0;
}

function _getDiscDots(c, name) {
  const n = name.toLowerCase();
  const k = Object.keys(c.disciplines || {}).find(d => d.toLowerCase() === n);
  return k ? (c.disciplines[k]?.dots || 0) : 0;
}

function _getMeritRating(c, name) {
  const n = name.toLowerCase();
  const m = (c.merits || []).find(m => m.name.toLowerCase() === n);
  return m ? (m.rating || 1) : 0;
}

function _getCovStatus(c, covShort) {
  const fullName = COV_SHORT[covShort.toLowerCase()] || covShort;
  return c.status?.covenant?.[fullName] || 0;
}

/* ══════════════════════════════════════════════════════
   Prerequisite system
   ══════════════════════════════════════════════════════ */

/**
 * Check a single prerequisite token against a character.
 * Returns true if the character meets the requirement.
 */
export function checkSinglePrereq(c, token) {
  token = token.trim();
  if (!token || token === 'None' || token === '-') return true;

  // \"No X Status\"
  const noStatus = /^No\\s+(\\w+)\\s+Status$/i.exec(token);
  if (noStatus) {
    const cov = noStatus[1];
    return _getCovStatus(c, cov) === 0;
  }

  // \"City Status N\"
  const cityStatus = /^City\\s+Status\\s+(\\d+)$/i.exec(token);
  if (cityStatus) return (c.status?.city || 0) >= parseInt(cityStatus[1]);

  // \"Clan Status N\" or \"[ClanName] Status N\"
  const clanStatus = /^(?:Clan\\s+)?(\\w+)\\s+Status\\s+(\\d+)$/i.exec(token);
  if (clanStatus) {
    const word = clanStatus[1].toLowerCase();
    const n = parseInt(clanStatus[2]);
    if (word === 'clan') return (c.status?.clan || 0) >= n;
    if (COV_SHORT[word]) return _getCovStatus(c, word) >= n;
    return _getCovStatus(c, word) >= n;
  }

  // \"Humanity < N\"
  const humLt = /^Humanity\\s*<\\s*(\\d+)$/i.exec(token);
  if (humLt) return (c.humanity || 7) < parseInt(humLt[1]);

  // \"X Bloodline\" or \"Bloodline X\"
  if (/bloodline/i.test(token)) {
    const bl = (c.bloodline || '').toLowerCase();
    return bl && token.toLowerCase().includes(bl);
  }

  // Clan name alone
  if (CLAN_NAMES.has(token.toLowerCase())) return (c.clan || '').toLowerCase() === token.toLowerCase();

  // Specialisation references — too complex to verify, pass
  if (/speciali[sz]/i.test(token)) return true;

  // \"Attribute N\" / \"Skill N\" / \"Discipline N\" / \"Merit N\"
  const withNum = /^(.+?)\\s+(\\d+)$/.exec(token);
  if (withNum) {
    const name = withNum[1].trim().toLowerCase();
    const n = parseInt(withNum[2]);
    if (ATTR_NAMES.has(name)) return _getAttrDots(c, name) >= n;
    if (SKILL_NAMES.has(name)) return _getSkillDots(c, name) >= n;
    const nameNorm = name.normalize('NFD').replace(/[\\u0300-\\u036f]/g, '');
    if (DISC_NAMES.has(name) || DISC_NAMES.has(nameNorm)) return _getDiscDots(c, name) >= n || _getDiscDots(c, nameNorm) >= n;
    // Merit as prereq e.g. \"Safe Place 1\", \"Contacts 2\"
    return _getMeritRating(c, name) >= n;
  }

  // Token without number — presence check
  const nameLow = token.toLowerCase();
  if (ATTR_NAMES.has(nameLow)) return _getAttrDots(c, nameLow) > 0;
  if (SKILL_NAMES.has(nameLow)) return _getSkillDots(c, nameLow) > 0;
  if (DISC_NAMES.has(nameLow)) return _getDiscDots(c, nameLow) > 0;
  // Inequality operators — just pass
  if (/[≤≥<>]/.test(token)) return true;
  // Merit name without number — permissive fallback
  return _getMeritRating(c, nameLow) > 0 || true;
}

/**
 * Check a full prerequisite string (comma-separated AND, \"or\"-separated OR).
 * If the rules cache is available, looks up the structured prereq tree and
 * delegates to meetsPrereq(). Falls back to regex parsing if cache unavailable.
 */
export function meritQualifies(c, prereqStr, structuredPrereq) {
  if (!prereqStr || prereqStr === '-') return true;

  // If a structured prereq tree was passed directly, use the new engine
  if (structuredPrereq !== undefined) {
    return meritPrereqOK(c, { prereq: structuredPrereq });
  }

  // Fallback: regex-based parsing for legacy callers
  const andParts = prereqStr.split(/\\s*,\\s*/);
  return andParts.every(part => {
    const orParts = part.split(/\\s+or\\s+/i);
    return orParts.some(t => checkSinglePrereq(c, t.trim()));
  });
}

/**
 * N-9 (issue #762, Bug 3) — single seam for the prereq check across all
 * sub-category dropdown filters (buildMeritOptions, buildSubCategoryMeritOptions,
 * buildMCIGrantOptions). Wraps `_meetsPrereq` so future evolutions of the
 * prereq-check semantics happen in one place instead of three.
 *
 * Returns true when the character meets the rule's `prereq` tree. Empty /
 * missing prereq trees pass (existing `_meetsPrereq` semantics).
 *
 * Note: current-row passthrough — when a row's currently-selected merit name
 * fails its prereq (e.g. ST removed a prereq merit elsewhere on the sheet),
 * the picker keeps the selection visible per Ma'at's directive (legitimate
 * editing affordance). Callers handle that case inline with their `curLow`
 * check, and emit a `console.warn` so the situation surfaces in QA testing
 * instead of silently passing. The warn lives on the caller side because
 * \"current row\" is dropdown-builder concern, not a property of the rule.
 *
 * @param {object} c
 * @param {object} rule - merit rule doc (with optional `prereq` tree)
 * @returns {boolean}
 */
export function meritPrereqOK(c, rule) {
  return _meetsPrereq(c, rule && rule.prereq);
}

/**
 * Build <option> HTML for a merit select dropdown, filtered by prerequisites.
 * Excludes standing, domain, and influence merits (those have dedicated UI).
 */
export function buildMeritOptions(c, currentName) {
  // Try rules cache first
  const rulesDB = getRulesByCategory('merit');
  const qualified = [];

  if (rulesDB.length) {
    // Rules cache available — use structured data
    for (const rule of rulesDB) {
      if (rule.sub_category && rule.sub_category !== 'general') continue;
      if (INFLUENCE_MERIT_TYPES.includes(rule.name)) continue;
      // Issue #937: 'Style'-parent merits are plain merits — surface them.
      // 2026-06-30: 'Carthian Law' also removed. Carthian Laws are merits with
      // status-based prereqs and belong in the regular picker; the Pacts UI
      // continues to list them too as an alternate path. See parallel change
      // in downtime-form.js so DT XP-spend and sheet pickers stay aligned.
      if (rule.parent && ['Invictus Oath'].includes(rule.parent)) continue;
      if (!meritPrereqOK(c, rule)) continue;
      const excl = _isExcluded(c, rule.name);
      if (excl && rule.name.toLowerCase() !== (currentName || '').toLowerCase()) continue;
      qualified.push({ key: rule.name.toLowerCase(), label: rule.name });
    }
  } else {
    return '<option value=\"\">— rules loading —</option>';
  }
  qualified.sort((a, b) => a.label.localeCompare(b.label));
  const curLow = (currentName || '').toLowerCase();
  const esc = _esc;
  let opts = '<option value=\"\">' + (currentName ? '' : '— select merit —') + '</option>';
  if (currentName && !qualified.some(q => q.key === curLow)) {
    opts += '<option value=\"' + esc(currentName) + '\" selected>' + esc(currentName) + '</option>';
  }
  for (const { key, label } of qualified) {
    const sel = key === curLow || label.toLowerCase() === curLow ? ' selected' : '';
    opts += '<option value=\"' + esc(label) + '\"' + sel + '>' + esc(label) + '</option>';
  }
  return opts;
}

/**
 * Build <option> HTML for an influence or domain merit type dropdown, driven
 * by sub_category in the catalog. Enforces prereqs and exclusive lists, with
 * the show-if-current escape hatch so an existing row whose merit no longer
 * qualifies still displays in its own dropdown.
 * @param {object} c - character
 * @param {string} subCategory - 'influence' or 'domain'
 * @param {string} currentName - name currently selected on this row
 * @param {string[]} [extraNames] - additional names to include (e.g. legacy
 *   names not yet in the catalog) so the picker stays usable during migration
 */
export function buildSubCategoryMeritOptions(c, subCategory, currentName, extraNames = []) {
  const rulesDB = getRulesByCategory('merit');
  if (!rulesDB.length) return '<option value=\"\">— rules loading —</option>';
  const curLow = (currentName || '').toLowerCase();

  const qualified = [];
  const seen = new Set();
  for (const rule of rulesDB) {
    if (rule.sub_category !== subCategory) continue;
    // N-9 (issue #762, Bug 3): single-seam prereq check; current-row
    // passthrough preserved (legitimate editing affordance) but a
    // failing-prereq passthrough now emits a diagnostic warn so the
    // situation surfaces in QA testing instead of silently passing.
    const passes = meritPrereqOK(c, rule);
    const isCurrentRow = rule.name.toLowerCase() === curLow;
    if (!passes && !isCurrentRow) continue;
    if (!passes && isCurrentRow) {
      try {
        console.warn(`[meritPrereqOK] current selection \"${rule.name}\" (sub_category=${subCategory}) passes through filter but fails its prereq — investigate.`);
      } catch { /* console may be absent in test contexts */ }
    }
    if (_isExcluded(c, rule.name) && !isCurrentRow) continue;
    if (seen.has(rule.name)) continue;
    seen.add(rule.name);
    qualified.push(rule.name);
  }
  for (const n of extraNames) {
    if (!seen.has(n)) { seen.add(n); qualified.push(n); }
  }
  qualified.sort((a, b) => a.localeCompare(b));

  // Always include the current row's selected name even if filtered out.
  if (currentName && !seen.has(currentName)) qualified.push(currentName);

  return qualified.map(n => '<option value=\"' + _esc(n) + '\"' + (n === currentName ? ' selected' : '') + '>' + _esc(n) + '</option>').join('');
}

/**
 * Build <option> HTML for MCI grant dropdown — includes influence and domain merits.
 * Filters by prerequisites and dot-level rating.
 * MCI dot ratings: dot 1-2 = 1-dot merits, dot 3 = 2-dot, dot 4-5 = 3-dot.
 * Graduated merits (rating range) appear if their min ≤ dotRating.
 * @param {object} c - character
 * @param {number} dotLevel - 0-indexed MCI dot level
 * @param {string} currentName - currently selected merit name
 */
const MCI_DOT_RATING = [1, 1, 2, 3, 3];
export function buildMCIGrantOptions(c, dotLevel, currentName) {
  const maxR = MCI_DOT_RATING[dotLevel] || 1;
  const qualified = [];

  // Try rules cache first
  const rulesDB = getRulesByCategory('merit');
  if (rulesDB.length) {
    for (const rule of rulesDB) {
      if (rule.sub_category === 'standing') continue;
      if (rule.parent && ['Style', 'Invictus Oath', 'Carthian Law'].includes(rule.parent)) continue;
      if (!meritPrereqOK(c, rule)) continue;
      if (_isExcluded(c, rule.name) && rule.name.toLowerCase() !== (currentName || '').toLowerCase()) continue;
      const rr = rule.rating_range;
      if (rr) {
        if (rr[0] === rr[1]) { if (rr[0] !== maxR) continue; }
        else { if (rr[0] > maxR) continue; }
      } else { if (1 !== maxR) continue; }
      qualified.push({ key: rule.name.toLowerCase(), label: rule.name });
    }
  } else {
    return '<option value=\"\">— rules loading —</option>';
  }
  qualified.sort((a, b) => a.label.localeCompare(b.label));
  const curLow = (currentName || '').toLowerCase();
  let opts = '<option value=\"\">' + (currentName ? '' : '— select merit —') + '</option>';
  if (currentName && !qualified.some(q => q.key === curLow)) {
    opts += '<option value=\"' + _esc(currentName) + '\" selected>' + _esc(currentName) + '</option>';
  }
  for (const { key, label } of qualified) {
    const sel = key === curLow || label.toLowerCase() === curLow ? ' selected' : '';
    opts += '<option value=\"' + _esc(label) + '\"' + sel + '>' + _esc(label) + '</option>';
  }
  return opts;
}

/**
 * Walk a prereq tree and return true if every satisfying assignment requires
 * Carthian status — i.e. no non-Carthian character could legitimately have
 * the merit. `all` requires Carthian if any child does; `any` requires it
 * only if every branch does.
 */
function _everyPrereqPathRequiresCarthian(node) {
  if (!node) return false;
  if (node.all) return node.all.some(_everyPrereqPathRequiresCarthian);
  if (node.any) return node.any.every(_everyPrereqPathRequiresCarthian);
  if (node.type === 'status' && node.qualifier) return /carthian/i.test(node.qualifier);
  return false;
}

/**
 * Build <option> HTML for Fucking Thief — single-dot non-Carthian merits.
 * Excludes Carthian-locked merits (the thief is Carthian by prereq, so they
 * can only steal what a non-Carthian could plausibly hold).
 */
export function buildFThiefOptions(currentName) {
  const qualified = [];

  // Try rules cache first
  const rulesDB = getRulesByCategory('merit');
  if (rulesDB.length) {
    for (const rule of rulesDB) {
      if (rule.sub_category === 'standing') continue;
      const rr = rule.rating_range;
      const minR = rr ? rr[0] : 1;
      if (minR > 1) continue;
      if (rr && rr[0] === rr[1] && rr[0] !== 1) continue;
      if (_everyPrereqPathRequiresCarthian(rule.prereq)) continue;
      qualified.push({ key: rule.name.toLowerCase(), label: rule.name });
    }
  } else {
    return '<option value=\"\">— rules loading —</option>';
  }
  qualified.sort((a, b) => a.label.localeCompare(b.label));
  const curLow = (currentName || '').toLowerCase();
  let opts = '<option value=\"\">' + (currentName ? '' : '— choose stolen merit —') + '</option>';
  if (currentName && !qualified.some(q => q.key === curLow)) {
    opts += '<option value=\"' + _esc(currentName) + '\" selected>' + _esc(currentName) + '</option>';
  }
  for (const { key, label } of qualified) {
    const sel = key === curLow || label.toLowerCase() === curLow ? ' selected' : '';
    opts += '<option value=\"' + _esc(label) + '\"' + sel + '>' + _esc(label) + '</option>';
  }
  return opts;
}

/* ── Inline HTML escape (avoids circular dependency on a helpers module) ── */
function _esc(s) {
  return s == null ? '' : String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/\"/g, '&quot;');
}

/* ══════════════════════════════════════════════════════
   Power / discipline helpers
   ══════════════════════════════════════════════════════ */

/** Filter a powers array to those belonging to a specific discipline. */
export function powersForDisc(powers, discName) {
  if (!powers) return [];
  if (RITUAL_DISCS.includes(discName)) {
    return powers.filter(p => p.name && (p.name.startsWith(discName) || p.name.includes('| ' + discName)));
  }
  return powers.filter(p => {
    if (!p.name) return false;
    const n = p.name.split('|')[0].trim().split(/\\s+/);
    return n[0] === discName || (n.length > 1 && n.slice(0, -1).join(' ') === discName);
  });
}

/** Return powers not attributable to any known discipline on the character. */
export function otherPowers(c) {
  const discNames = Object.keys(c.disciplines || {});
  return (c.powers || []).filter(p => {
    if (!p.name) return false;
    const key = p.name.split('|')[0].trim().replace(/\\s*[●○]+$/, '').replace(/\\s+\\d+$/, '');
    if (RITUAL_DISCS.some(d => p.name.startsWith(d))) return false;
    return !discNames.some(d => key === d || key.startsWith(d + ' '));
  });
}

/** Returns a Set of cult names from a character's Mystery Cult Initiation merits. */
function getCharCults(c) {
  return new Set(
    (c.merits || [])
      .filter(m => m.name === 'Mystery Cult Initiation' && m.cult_name)
      .map(m => m.cult_name)
  );
}

/** Check whether a character meets a devotion's discipline prerequisites. */
export function meetsDevPrereqs(c, dev) {
  if (dev.bl && (c.bloodline || '') !== dev.bl) return false;
  if (dev.cult && !getCharCults(c).has(dev.cult)) return false;
  const discs = c.disciplines || {};
  if (!dev.p || !dev.p.length) return true;
  if (dev.or) {
    return dev.p.some(p => (discs[p.disc]?.dots || 0) >= p.dots);
  }
  return dev.p.every(p => (discs[p.disc]?.dots || 0) >= p.dots);
}

/** Format a devotion's prerequisite list as a human-readable string. */
export function devPrereqStr(dev) {
  const parts = [];
  if (dev.bl) parts.push(dev.bl + ' only');
  if (dev.cult) parts.push(dev.cult + ' members only');
  if (dev.p && dev.p.length) parts.push(dev.p.map(p => p.disc + ' ' + p.dots).join(dev.or ? ' or ' : ', '));
  return parts.join('; ') || 'None';
}
"

 ❯ tests/n7-n9-allocator-readers.test.js:246:17
    244|     const src = read('public/js/editor/merits.js');
    245|     // buildMeritOptions
    246|     expect(src).toMatch(/buildMeritOptions[\s\S]{0,600}meritPrereqOK\(…
       |                 ^
    247|     // buildSubCategoryMeritOptions
    248|     expect(src).toMatch(/buildSubCategoryMeritOptions[\s\S]{0,800}meri…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/12]⎯

 FAIL  tests/oath-a-pledge-helpers.test.js > OATH-A D2 — encumbrance changes no dot sum > meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form
AssertionError: expected '/**\r\n * XP calculations and merit r…' to contain 'export function meritRating(c, m) {\n…'

- Expected
+ Received

- export function meritRating(c, m) {
-   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+ /**
+  * XP calculations and merit rating/breakdown helpers.
+  * Pure functions — no DOM side-effects.
+  */
+ 
+ import { getRuleByKey } from '../data/loader.js';
+ // N-1: map-fallback shape for per-slug free reads (see rules-helpers.js).
+ import { freeOf } from '../data/rules-helpers.js';
+ // COLLECTIVE-2 (#1110): compound slug + source names reach the bd-row from
+ // rule data, so they are escaped like every other data-sourced label.
+ import { esc } from '../data/helpers.js';
+ 
+ /**
+  * Convert XP spent into dot count (flat rate).
+  * @param {number} xpSpent - total XP allocated
+  * @param {number} baseBeforeXP - dots from CP/free (unused in flat model, kept for API)
+  * @param {number} costPerDot - XP cost per single dot
+  * @returns {number}
+  */
+ export function xpToDots(xpSpent, baseBeforeXP, costPerDot) {
+   return Math.floor((xpSpent || 0) / costPerDot);
+ }
+ 
+ /**
+  * Convert a dot count into XP cost (flat rate).
+  * @param {number} numDots
+  * @param {number} baseBeforeXP - (unused in flat model, kept for API)
+  * @param {number} costPerDot
+  * @returns {number}
+  */
+ export function dotsToXP(numDots, baseBeforeXP, costPerDot) {
+   return (numDots || 0) * costPerDot;
+ }
+ 
+ /** Starting XP awarded on character creation. */
+ export function xpStarting() { return 10; }
+ 
+ /** XP from voluntary humanity drops: 2 per dot permanently lost. */
+ export function xpHumanityDrop(c) {
+   // Use explicit field if set; otherwise infer from stored humanity value
+   const lost = c.humanity_lost !== undefined
+     ? c.humanity_lost
+     : Math.max(0, (c.humanity_base || 7) - (c.humanity || 0));
+   return lost * 2;
+ }
+ 
+ /** XP from completed ordeals: 3 per ordeal. */
+ export function xpOrdeals(c) {
+   return ((c.ordeals || []).filter(o => o.complete).length) * 3;
+ }
+ 
+ /** XP from game attendance. Requires loadGameXP() to have run — returns 0 if not yet loaded. */
+ export function xpGame(c) {
+   return c._gameXP ?? 0;
+ }
+ 
+ /** XP bonus from Professional Training: 1 XP per asset skill at 5+ effective dots (requires PT ≥ 4).
+  *  Effective dots = base dots + PT dot 4 bonus (+1 to chosen asset skill). */
+ export function xpPT5(c) {
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   if (!ptM || meritRating(c, ptM) < 4) return 0;
+   const assets = (ptM.asset_skills || []).filter(Boolean);
+   if (!assets.length) return 0;
+   const ptBonus = c._pt_dot4_bonus_skills instanceof Set ? c._pt_dot4_bonus_skills : new Set();
+   return assets.filter(sk => {
+     const s = (c.skills || {})[sk];
+     const effective = (s?.dots || 0) + (ptBonus.has(sk) ? 1 : 0);
+     return effective >= 5;
+   }).length;
+ }
+ 
+ /**
+  * Total XP earned by a character (all sources, derived dynamically).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpEarned(c) {
+   return xpStarting() + xpHumanityDrop(c) + xpOrdeals(c) + xpGame(c) + xpPT5(c);
+ }
+ 
+ /** Sum XP from inline creation fields on an object (attributes, skills, disciplines). */
+ function sumInlineXP(obj) {
+   if (!obj) return 0;
+   return Object.values(obj).reduce((t, v) => t + (v?.xp || 0), 0);
+ }
+ 
+ /** XP spent on attributes — sum of .xp across all attribute objects. */
+ export function xpSpentAttrs(c) {
+   return sumInlineXP(c.attributes);
+ }
+ 
+ /** XP spent on skills + specialisations beyond free allowance. */
+ export function xpSpentSkills(c) {
+   const skillXP = sumInlineXP(c.skills);
+   // PT free specs (dot 3): 2 extra, but ONLY usable on asset skills — tracked separately
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   const ptFree = (ptM && ptM.rating >= 3) ? 2 : 0;
+   const ptAssets = new Set((ptM && ptM.rating >= 3 && ptM.asset_skills) ? (ptM.asset_skills || []).filter(Boolean) : []);
+   let assetSpecs = 0, nonAssetSpecs = 0;
+   Object.entries(c.skills || {}).forEach(([sk, skillObj]) => {
+     const count = (skillObj && skillObj.specs) ? skillObj.specs.length : 0;
+     if (ptAssets.has(sk)) assetSpecs += count;
+     else nonAssetSpecs += count;
+   });
+   // MCI dot 1 free specs: each active MCI with dot1_choice === 'speciality' grants 1 free spec
+   const mciFreeSpecs = (c._mci_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // Bloodline-granted specs are also exempt from XP cost
+   const bloodlineFreeSpecs = (c._bloodline_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // PT free covers asset specs first; baseline 3 covers everything else
+   const ptFreeCovered = Math.min(ptFree, assetSpecs);
+   const paidSpecs = nonAssetSpecs + Math.max(0, assetSpecs - ptFreeCovered);
+   const specXP = Math.max(0, paidSpecs - 3 - mciFreeSpecs - bloodlineFreeSpecs);
+   return skillXP + specXP;
+ }
+ 
+ /** XP spent on all merits (general, influence, domain, standing) + fighting styles + pact oaths. */
+ export function xpSpentMerits(c) {
+   const meritXP = (c.merits || []).reduce((t, m) => t + (m.xp || 0), 0);
+   const styleXP = (c.fighting_styles || []).reduce((t, fs) => t + (fs.xp || 0), 0);
+   const pactXP = (c.powers || []).filter(p => p.category === 'pact').reduce((t, p) => t + (p.xp || 0), 0);
+   return meritXP + styleXP + pactXP;
+ }
+ 
+ /** XP spent on powers — disciplines + devotions. */
+ export function xpSpentPowers(c) {
+   const discXP = sumInlineXP(c.disciplines);
+   // Devotion XP: look up each devotion's cost from DEVOTIONS_DB
+   const devXP = (c.powers || [])
+     .filter(p => p.category === 'devotion')
+     .reduce((t, p) => {
+       // Try rules cache first, fallback to _devotionsDB
+       const slug = 'devotion-' + p.name.toLowerCase().replace(/['']/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
+       const rule = getRuleByKey(slug);
+       if (rule) return t + (rule.xp_fixed || 0);
+       const db = _devotionsDB ? _devotionsDB.find(d => d.n === p.name) : null;
+       return t + (db ? db.xp || 0 : 0);
+     }, 0);
+   // Rite XP: paid rites cost 1 XP (rank 1-3) or 2 XP (rank 4-5)
+   const riteXP = (c.powers || [])
+     .filter(p => p.category === 'rite' && !p.free)
+     .reduce((t, p) => t + (p.level >= 4 ? 2 : 1), 0);
+   return discXP + devXP + riteXP;
+ }
+ 
+ /** XP spent on special: Blood Potency, Humanity, lost Willpower dots. */
+ export function xpSpentSpecial(c) {
+   // Blood Potency: tracked directly in bp_creation.xp (new model) or derived (legacy)
+   const bpXP = (c.bp_creation || {}).xp !== undefined
+     ? ((c.bp_creation || {}).xp || 0)
+     : Math.max(0, (c.blood_potency || 1) - 1 - Math.floor(((c.bp_creation || {}).cp || 0) / 5)) * 5;
+   // Humanity: XP spent raising dots (new model only; old model net is captured in xpHumanityDrop)
+   const humXP = c.humanity_xp || 0;
+   // Lost Willpower dots: stored in xp_log.spent.willpower
+   const wpXP = ((c.xp_log || {}).spent || {}).willpower || 0;
+   // Manual special: anything else tracked in xp_log
+   const manualXP = ((c.xp_log || {}).spent || {}).special || 0;
+   return bpXP + humXP + wpXP + manualXP;
+ }
+ 
+ // Devotions DB reference (set via setDevotionsDB)
+ let _devotionsDB = null;
+ export function setDevotionsDB(db) { _devotionsDB = db; }
+ 
+ /**
+  * Total XP spent by a character (all categories, fully dynamic).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpSpent(c) {
+   return xpSpentAttrs(c) + xpSpentSkills(c) + xpSpentMerits(c) + xpSpentPowers(c) + xpSpentSpecial(c);
+ }
+ 
+ /**
+  * Remaining unspent XP.
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpLeft(c) {
+   return xpEarned(c) - xpSpent(c);
+ }
+ 
+ /**
+  * Effective rating of a merit (sum of CP + free + grant pools + XP, inline on merit object).
+  * Falls back to stored rating if inline fields are absent.
+  * @param {object} c - character object
+  * @param {object} m - merit entry
+  * @returns {number}
+  */
+ export function meritRating(c, m) {
+   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+   return (m.cp || 0) + freeOf(m, 'bloodline') + freeOf(m, 'pet') + freeOf(m, 'mci') + freeOf(m, 'vm') + freeOf(m, 'lk')
+     + freeOf(m, 'ohm') + freeOf(m, 'inv') + freeOf(m, 'pt') + freeOf(m, 'mdb') + freeOf(m, 'sw') + (m.xp || 0);
+ }
+ 
+ /**
+  * Render the merit breakdown row: Fr + CP + XP = total
+  * @param {number} realIdx - index into c.merits
+  * @param {object} mc - merit object with inline creation fields {cp, free, xp, free_mci, ...}
+  * @param {number|null} fixedAt - if the merit has a fixed rating (e.g. 3 for VM), pass it here;
+  *   null for graduated merits. When fixedAt is set, the displayed total snaps to 0 until the
+  *   threshold is met, then shows fixedAt.
+  */
+ export function meritBdRow(realIdx, mc, fixedAt, opts = {}) {
+   const cp = mc.cp || 0, xp = mc.xp || 0, fbl = freeOf(mc, 'bloodline'), fret = freeOf(mc, 'pet'), fmci = freeOf(mc, 'mci'), fvm = freeOf(mc, 'vm'), flk = freeOf(mc, 'lk'), fohm = freeOf(mc, 'ohm'), finv = freeOf(mc, 'inv'), fpt = freeOf(mc, 'pt'), fmdb = freeOf(mc, 'mdb'), fsw = freeOf(mc, 'sw');
+   // COLLECTIVE-2 (issue #1110): Collective Compound allocation channels are
+   // data-driven — the caller passes the discovered slugs rather than this
+   // function naming one. Defaults to ['necro'] so call sites that predate
+   // the compound wiring keep their pre-#1110 total exactly.
+   const _cmpSlugs = Array.isArray(opts.compoundSlugs) ? opts.compoundSlugs : ['necro'];
+   const fcompound = _cmpSlugs.reduce((s, slug) => s + freeOf(mc, slug), 0);
+   const total = cp + xp + fbl + fret + fmci + fvm + flk + fohm + finv + fpt + fmdb + fsw + fcompound + (opts.attachBonus || 0);
+   // Effective display: for fixed merits, only show dots once the threshold is reached
+   const effective = (fixedAt != null) ? (total >= fixedAt ? fixedAt : 0) : total;
+   const needsHint = (fixedAt != null && total > 0 && total < fixedAt)
+     ? '<span class="bd-needs-hint">' + total + ' / ' + fixedAt + ' needed</span>' : '';
+   // N-7b (issue #768): Necropolis target merits are pool-funded only — the
+   // domain-renderer call site suppresses CP / XP / MCI / Bonus on these rows
+   // via opts.hideCP / opts.hideXP / opts.hideMCI (this block) + opts.hideBonus
+   // (below). Same opts-flag pattern as N-9's hideBonus on standing merits.
+   // The pre-N-7b shape rendered CP + XP + sep unconditionally; the guards
+   // here are defaults-false so existing call sites that don't pass the
+   // hide-flags get the legacy behaviour unchanged.
+   let h = '<div class="merit-bd-row">';
+   if (!opts.hideCP) h += '<div class="bd-grp"><span class="bd-lbl">CP</span><input class="merit-bd-input" type="number" min="0" value="' + cp + '" onchange="shEditMeritPt(' + realIdx + ',\'cp\',+this.value)"></div>';
+   if (!opts.hideXP) h += '<div class="bd-grp"><span class="bd-lbl">XP</span><input class="merit-bd-input" type="number" min="0" value="' + xp + '" onchange="shEditMeritPt(' + realIdx + ',\'xp\',+this.value)"></div>';
+   if (!opts.hideCP || !opts.hideXP) h += '<div class="bd-sep"></div>';
+   // N-9 (issue #762, Bug 1 "adjacent finding"): MCI input writes the map-shape
+   // `free_grants.mci` per the ADR-005 allocator-write-path amendment. The
+   // handler (shEditMeritPt) detects the `free_grants.` prefix and routes to
+   // the map. Pre-N-9 this wrote `free_mci`, creating a fresh divergence on
+   // every edit after the N-2 backfill.
+   // N-7b: showMCI is AND'd with !hideMCI at the call site so Necropolis
+   // targets don't surface the MCI allocator even when there's pool capacity.
+   // Issue #774: a11y — every stepper input carries id + name + aria-label
+   // matching the NECRO precedent at line 241 (added by N-7c #771). Same
+   // browser warning ("form field element should have an id or name
+   // attribute / No label associated with a form field") was firing on
+   // these 5 sibling steppers. Single-line additions per stepper.
+   if (opts.showMCI && !opts.hideMCI) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-mci-lbl-' + realIdx + '">MCI</span><input id="bd-mci-' + realIdx + '" name="bd-mci-' + realIdx + '" aria-label="Mystery Cult Initiation pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fmci + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.mci\',+this.value)"></div>';
+   if (opts.showVM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-vm-lbl-' + realIdx + '">VM</span><input id="bd-vm-' + realIdx + '" name="bd-vm-' + realIdx + '" aria-label="Viral Mythology pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fvm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_vm\',+this.value)"></div>';
+   if (opts.showLK) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-lk-lbl-' + realIdx + '">LK</span><input id="bd-lk-' + realIdx + '" name="bd-lk-' + realIdx + '" aria-label="Lorekeeper pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + flk + '" onchange="shEditMeritPt(' + realIdx + ',\'free_lk\',+this.value)"></div>';
+   if (opts.showOHM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-ohm-lbl-' + realIdx + '">OHM</span><input id="bd-ohm-' + realIdx + '" name="bd-ohm-' + realIdx + '" aria-label="Oath of the Hard Motherfucker pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fohm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_ohm\',+this.value)"></div>';
+   if (opts.showINV) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-inv-lbl-' + realIdx + '">INV</span><input id="bd-inv-' + realIdx + '" name="bd-inv-' + realIdx + '" aria-label="Invested pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + finv + '" onchange="shEditMeritPt(' + realIdx + ',\'free_inv\',+this.value)"></div>';
+   // N-7 (issue #760): Collective Compound allocator — writes directly to
+   // m.free_grants.<slug> (map shape, no new legacy free_<slug> field) per the
+   // ADR-005 allocator-write-path amendment.
+   // N-7c (issue #771): id + aria-label so browsers don't flag "form field
+   // element should have an id or name attribute / No label associated with a
+   // form field". The 5 sibling steppers (LK/INV/VM/OHM/MCI) got the same
+   // treatment in #774 — see lines above.
+   // COLLECTIVE-2 (issue #1110): one stepper per compound the character
+   // belongs to that claims this merit, each writing its OWN slug. Pre-#1110
+   // this was a single `showNECRO` flag hardwired to free_grants.necro, which
+   // would have written a Crone or Sanctified allocation into the Necropolis
+   // pool. The descriptor supplies slug + source; no name literals here.
+   for (const _cmp of (opts.compoundPools || [])) {
+     if (!_cmp || !_cmp.slug) continue;
+     h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-' + _cmp.slug + '-lbl-' + realIdx + '">' + esc(_cmp.slug.toUpperCase()) + '</span><input id="bd-' + _cmp.slug + '-' + realIdx + '" name="bd-' + _cmp.slug + '-' + realIdx + '" aria-label="' + esc(_cmp.source || _cmp.slug) + ' pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + freeOf(mc, _cmp.slug) + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.' + _cmp.slug + '\',+this.value)"></div>';
+   }
+   h += '<div class="bd-eq"><span class="bd-val">' + effective + ' dot' + (effective === 1 ? '' : 's') + '</span>' + needsHint + '</div>'
+     + '</div>';
+   // N-9 (issue #762, Bug 2): standing-merit render paths (MCI, PT) don't read
+   // m.bonus, so the Bonus row was visible-but-no-op. Standing call sites pass
+   // opts.hideBonus=true to suppress the row entirely. Default behaviour
+   // (general/influence/domain/style merits) is unchanged.
+   if (!opts.hideBonus) {
+     const bon = mc.bonus || 0;
+     h += '<div class="attr-derived-row"><span class="bd-lbl">Bonus</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',-1)"' + (bon === 0 ? ' disabled' : '') + '>&#x25BC;</button><span class="bd-src">' + (bon > 0 ? '+' + bon : '0') + '</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',1)">&#x25B2;</button></div>';
+   }
+   return h;
+ }
+

 ❯ tests/oath-a-pledge-helpers.test.js:388:16
    386|     const xp = read('public/js/editor/xp.js');
    387|     const dom = read('public/js/editor/domain.js');
    388|     expect(xp).toContain("export function meritRating(c, m) {\n  if (m…
       |                ^
    389|     expect(dom).toContain('export function meritEffectiveRating(c, m) …
    390|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/12]⎯


diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:51:15.731717Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -ni \"equipment|bucket|armour|weapon|container\" 'D:\\Terra Mortis\\TM Suite\\server\\tests\\epic.708.3-cycle-phase-controls.test.js' 'D:\\Terra Mortis\\TM Suite\\server\\tests\\n7-n9-allocator-readers.test.js' 'D:\\Terra Mortis\\TM Suite\\server\\tests\\oath-a-pledge-helpers.test.js'" in D:\Terra Mortis\TM Suite
2026-08-13T02:51:37.132211Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.9 seconds
Output:

 exited 1 in 2926ms:
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:51:37.168746Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The ten-file current-HEAD run exactly matches the record’s aggregate signature: 10 failed files, 5 failed tests, 63 passed (68 total in those files). The five failing test names are the three cycle-phase assertions, the allocator-reader assertion, and the OATH-A byte-identity assertion; a direct search found none of the equipment/bucket/armour/weapon/container terms in those three failing-test files. I’m running the full Vitest gate now to verify the claimed 2463/2458 totals, then I’ll reproduce the ten-file signature from base commit `ddf059f8` in an isolated temp export.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; "'$env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npm test" in D:\Terra Mortis\TM Suite
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:51:58.592677Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:52:11.858566Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:52:26.769720Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:52:40.504859Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:52:56.673644Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The full gate is still running against the local test DB; no result has been yielded yet. The individual equipment and ten-file audit results are already captured, and I’m keeping the temporary TLS change isolated until the full run completes so the environment is consistent across all counts.
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:53:18.099559Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:53:31.143896Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:53:46.841803Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:54:00.701760Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:54:14.545281Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:54:28.761972Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The full suite has passed two minutes without producing a summary, but it remains active rather than hung at the tool layer. I’ll let it finish within the five-minute cap; if it times out, I’ll report the full-gate count as unverified while preserving the independently verified 170 equipment tests and ten-file signature.
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:54:44.632140Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:55:04.292763Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:55:17.344067Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:55:34.870073Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:55:49.861299Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:56:02.989741Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:56:16.234955Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:56:32.027186Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:56:46.413617Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
2026-08-13T02:56:50.144804Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 302.6 seconds
Output:
command timed out after 302590 milliseconds

> tm-suite-api@1.0.0 test
> vitest run


 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

 ❯ tests/n7-n9-allocator-readers.test.js (25 tests | 1 failed) 28ms
     × all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly) 8ms
 ❯ tests/oath-a-pledge-helpers.test.js (29 tests | 1 failed) 46ms
     × meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form 31ms
 ❯ tests/epic.708.3-cycle-phase-controls.test.js (14 tests | 3 failed) 25ms
     × exports setGamePhase function 10ms
     × uses data-phase attribute on phase buttons 4ms
     × highlights active phase with gold2 colour 4ms
stdout | tests/otc-2-office-actions-api.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/otc-2-office-actions-api.test.js (8 tests | 8 skipped) 5037ms
stdout | tests/issue-1013-indomitable-rules-text.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  enable debug logging with { debug: true }

 ❯ tests/issue-1013-indomitable-rules-text.test.js (0 test)
 ❯ tests/issue-1021-failed-breakpoint-merit.test.js (0 test)
 ❯ tests/issue-811-sumchannels-rootcause.test.js (0 test)
stdout | tests/issue-826-cleanup-script-integration.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/issue-826-cleanup-script-integration.test.js (0 test)
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js (0 test)
stdout | tests/issue-837-xp-totals-deprecation.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/issue-837-xp-totals-deprecation.test.js (0 test)
 ❯ tests/n8-mandragora-prereq.test.js (0 test)
stdout | tests/api-downtime-regent-gate.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  suppress all logs with { quiet: true }

 ❯ tests/api-downtime-regent-gate.test.js (12 tests | 12 skipped) 5042ms
stdout | tests/api-joint-projects.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/api-joint-projects.test.js (36 tests | 36 skipped) 5033ms
stdout | tests/api-invitation-lifecycle.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  enable debug logging with { debug: true }

 ❯ tests/api-invitation-lifecycle.test.js (19 tests | 19 skipped) 5045ms
stdout | tests/api-st-mods.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-st-mods.test.js (40 tests | 40 skipped) 5041ms
stdout | tests/api-characters-carthian-pull.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  suppress all logs with { quiet: true }

 ❯ tests/api-characters-carthian-pull.test.js (26 tests | 26 skipped) 5046ms
stdout | tests/n3-necropolis.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/n3-necropolis.test.js (12 tests | 12 skipped) 5035ms
stdout | tests/api-npcs-quick-add.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  suppress all logs with { quiet: true }

 ❯ tests/api-npcs-quick-add.test.js (9 tests | 9 skipped) 5033ms
stdout | tests/api-relationships-for-character.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/api-relationships-for-character.test.js (20 tests | 20 skipped) 5036ms
stdout | tests/api-players.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-players.test.js (28 tests | 28 skipped) 5029ms
stdout | tests/api-ordeal-responses.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛡️ auth for agents: https://vestauth.com

 ❯ tests/api-ordeal-responses.test.js (17 tests | 17 skipped) 5035ms
stdout | tests/api-characters-crud.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚡️ secrets for agents: https://dotenvx.com/as2

 ❯ tests/api-characters-crud.test.js (34 tests | 34 skipped) 5039ms
stdout | tests/api-downtime.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  write to custom object with { processEnv: myObject }

 ❯ tests/api-downtime.test.js (22 tests | 22 skipped) 5045ms
stdout | tests/api-ranking-ballots.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-ranking-ballots.test.js (21 tests | 21 skipped) 5041ms
stdout | tests/api-territories-regent-write.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-territories-regent-write.test.js (15 tests | 15 skipped) 5046ms
stdout | tests/issue-834-m-free-deprecation.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/issue-834-m-free-deprecation.test.js (21 tests | 21 skipped) 5043ms
stdout | tests/api-territories.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-territories.test.js (21 tests | 21 skipped) 5037ms
stdout | tests/api-rules-engine.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-rules-engine.test.js (50 tests | 50 skipped) 5041ms
stdout | tests/api-touchstone-edges.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/api-touchstone-edges.test.js (19 tests | 19 skipped) 5042ms
stdout | tests/api-publish-cycle.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/api-publish-cycle.test.js (11 tests | 11 skipped) 5040ms
stdout | tests/stm-10-lifecycle.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/stm-10-lifecycle.test.js (13 tests | 13 skipped) 5042ms
stdout | tests/stm-8-pool-snapshot.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛡️ auth for agents: https://vestauth.com

 ❯ tests/stm-8-pool-snapshot.test.js (9 tests | 9 skipped) 5040ms
stdout | tests/api-territories-regent-lieutenant.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/api-territories-regent-lieutenant.test.js (14 tests | 14 skipped) 5040ms
stdout | tests/api-chapters.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-chapters.test.js (22 tests | 22 skipped) 5033ms
stdout | tests/api-npc-flags.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚡️ secrets for agents: https://dotenvx.com/as2

 ❯ tests/api-npc-flags.test.js (28 tests | 28 skipped) 5037ms
stdout | tests/api-relationships.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-relationships.test.js (34 tests | 34 skipped) 5045ms
stdout | tests/api-game-sessions.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/api-game-sessions.test.js (17 tests | 17 skipped) 5037ms
stdout | tests/issue-971-cyoa-passages.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/issue-971-cyoa-passages.test.js (14 tests | 14 skipped) 5048ms
stdout | tests/api-game-sessions-next.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-game-sessions-next.test.js (5 tests | 5 skipped) 5041ms
stdout | tests/oath-b-d6-api-roundtrip.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/oath-b-d6-api-roundtrip.test.js (15 tests | 15 skipped) 5048ms
stdout | tests/api-characters-safe-place-locations.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/api-characters-safe-place-locations.test.js (11 tests | 11 skipped) 5041ms
stdout | tests/fix.715.dt-manual-open-gate.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/fix.715.dt-manual-open-gate.test.js (5 tests | 5 skipped) 5035ms
stdout | tests/api-downtime-hold-flags.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/api-downtime-hold-flags.test.js (9 tests | 9 skipped) 5043ms
stdout | tests/api-rules-aggregate.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-rules-aggregate.test.js (9 tests | 9 skipped) 5046ms
stdout | tests/api-relationships-player-edit.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-relationships-player-edit.test.js (11 tests | 11 skipped) 5033ms
stdout | tests/api-relationships-mutual.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-relationships-mutual.test.js (11 tests | 11 skipped) 5044ms
stdout | tests/api-characters.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-characters.test.js (15 tests | 15 skipped) 5044ms
stdout | tests/stm-7-bulk-endpoint.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/stm-7-bulk-endpoint.test.js (14 tests | 14 skipped) 5038ms
 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework
stderr | tests/otc-2-office-actions-api.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/otc-2-office-actions-api.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-downtime-regent-gate.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-downtime-regent-gate.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-joint-projects.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-joint-projects.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-invitation-lifecycle.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-invitation-lifecycle.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-st-mods.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-st-mods.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters-carthian-pull.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters-carthian-pull.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/n3-necropolis.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/n3-necropolis.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-npcs-quick-add.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-npcs-quick-add.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships-for-character.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships-for-character.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-players.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-players.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-ordeal-responses.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-ordeal-responses.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters-crud.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters-crud.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-downtime.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-downtime.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-ranking-ballots.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-ranking-ballots.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-territories-regent-write.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-territories-regent-write.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/issue-834-m-free-deprecation.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-territories.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-territories.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-rules-engine.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-rules-engine.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-touchstone-edges.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-touchstone-edges.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-publish-cycle.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-publish-cycle.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/stm-10-lifecycle.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/stm-10-lifecycle.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/stm-8-pool-snapshot.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/stm-8-pool-snapshot.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-territories-regent-lieutenant.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-territories-regent-lieutenant.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-chapters.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-chapters.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-npc-flags.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-npc-flags.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-game-sessions.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-game-sessions.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/issue-971-cyoa-passages.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/issue-971-cyoa-passages.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-game-sessions-next.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-game-sessions-next.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/oath-b-d6-api-roundtrip.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/oath-b-d6-api-roundtrip.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters-safe-place-locations.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters-safe-place-locations.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/fix.715.dt-manual-open-gate.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/fix.715.dt-manual-open-gate.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-downtime-hold-flags.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-downtime-hold-flags.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-rules-aggregate.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-rules-aggregate.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships-player-edit.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships-player-edit.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships-mutual.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships-mutual.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/stm-7-bulk-endpoint.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/stm-7-bulk-endpoint.test.js
[setupDb] connectDb() failed: read ECONNRESET


 exited 124 in 302590ms:

> tm-suite-api@1.0.0 test
> vitest run


 RUN  v4.1.2 D:/Terra Mortis/TM Suite/server

 ❯ tests/n7-n9-allocator-readers.test.js (25 tests | 1 failed) 28ms
     × all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly) 8ms
 ❯ tests/oath-a-pledge-helpers.test.js (29 tests | 1 failed) 46ms
     × meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form 31ms
 ❯ tests/epic.708.3-cycle-phase-controls.test.js (14 tests | 3 failed) 25ms
     × exports setGamePhase function 10ms
     × uses data-phase attribute on phase buttons 4ms
     × highlights active phase with gold2 colour 4ms
stdout | tests/otc-2-office-actions-api.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/otc-2-office-actions-api.test.js (8 tests | 8 skipped) 5037ms
stdout | tests/issue-1013-indomitable-rules-text.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  enable debug logging with { debug: true }

 ❯ tests/issue-1013-indomitable-rules-text.test.js (0 test)
 ❯ tests/issue-1021-failed-breakpoint-merit.test.js (0 test)
 ❯ tests/issue-811-sumchannels-rootcause.test.js (0 test)
stdout | tests/issue-826-cleanup-script-integration.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/issue-826-cleanup-script-integration.test.js (0 test)
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js (0 test)
stdout | tests/issue-837-xp-totals-deprecation.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/issue-837-xp-totals-deprecation.test.js (0 test)
 ❯ tests/n8-mandragora-prereq.test.js (0 test)
stdout | tests/api-downtime-regent-gate.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  suppress all logs with { quiet: true }

 ❯ tests/api-downtime-regent-gate.test.js (12 tests | 12 skipped) 5042ms
stdout | tests/api-joint-projects.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/api-joint-projects.test.js (36 tests | 36 skipped) 5033ms
stdout | tests/api-invitation-lifecycle.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  enable debug logging with { debug: true }

 ❯ tests/api-invitation-lifecycle.test.js (19 tests | 19 skipped) 5045ms
stdout | tests/api-st-mods.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-st-mods.test.js (40 tests | 40 skipped) 5041ms
stdout | tests/api-characters-carthian-pull.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  suppress all logs with { quiet: true }

 ❯ tests/api-characters-carthian-pull.test.js (26 tests | 26 skipped) 5046ms
stdout | tests/n3-necropolis.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  specify custom .env file path with { path: '/custom/path/.env' }

 ❯ tests/n3-necropolis.test.js (12 tests | 12 skipped) 5035ms
stdout | tests/api-npcs-quick-add.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  suppress all logs with { quiet: true }

 ❯ tests/api-npcs-quick-add.test.js (9 tests | 9 skipped) 5033ms
stdout | tests/api-relationships-for-character.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/api-relationships-for-character.test.js (20 tests | 20 skipped) 5036ms
stdout | tests/api-players.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-players.test.js (28 tests | 28 skipped) 5029ms
stdout | tests/api-ordeal-responses.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛡️ auth for agents: https://vestauth.com

 ❯ tests/api-ordeal-responses.test.js (17 tests | 17 skipped) 5035ms
stdout | tests/api-characters-crud.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚡️ secrets for agents: https://dotenvx.com/as2

 ❯ tests/api-characters-crud.test.js (34 tests | 34 skipped) 5039ms
stdout | tests/api-downtime.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  write to custom object with { processEnv: myObject }

 ❯ tests/api-downtime.test.js (22 tests | 22 skipped) 5045ms
stdout | tests/api-ranking-ballots.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-ranking-ballots.test.js (21 tests | 21 skipped) 5041ms
stdout | tests/api-territories-regent-write.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-territories-regent-write.test.js (15 tests | 15 skipped) 5046ms
stdout | tests/issue-834-m-free-deprecation.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/issue-834-m-free-deprecation.test.js (21 tests | 21 skipped) 5043ms
stdout | tests/api-territories.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-territories.test.js (21 tests | 21 skipped) 5037ms
stdout | tests/api-rules-engine.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-rules-engine.test.js (50 tests | 50 skipped) 5041ms
stdout | tests/api-touchstone-edges.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/api-touchstone-edges.test.js (19 tests | 19 skipped) 5042ms
stdout | tests/api-publish-cycle.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/api-publish-cycle.test.js (11 tests | 11 skipped) 5040ms
stdout | tests/stm-10-lifecycle.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/stm-10-lifecycle.test.js (13 tests | 13 skipped) 5042ms
stdout | tests/stm-8-pool-snapshot.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛡️ auth for agents: https://vestauth.com

 ❯ tests/stm-8-pool-snapshot.test.js (9 tests | 9 skipped) 5040ms
stdout | tests/api-territories-regent-lieutenant.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/api-territories-regent-lieutenant.test.js (14 tests | 14 skipped) 5040ms
stdout | tests/api-chapters.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-chapters.test.js (22 tests | 22 skipped) 5033ms
stdout | tests/api-npc-flags.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚡️ secrets for agents: https://dotenvx.com/as2

 ❯ tests/api-npc-flags.test.js (28 tests | 28 skipped) 5037ms
stdout | tests/api-relationships.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-relationships.test.js (34 tests | 34 skipped) 5045ms
stdout | tests/api-game-sessions.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/api-game-sessions.test.js (17 tests | 17 skipped) 5037ms
stdout | tests/issue-971-cyoa-passages.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/issue-971-cyoa-passages.test.js (14 tests | 14 skipped) 5048ms
stdout | tests/api-game-sessions-next.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/api-game-sessions-next.test.js (5 tests | 5 skipped) 5041ms
stdout | tests/oath-b-d6-api-roundtrip.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/oath-b-d6-api-roundtrip.test.js (15 tests | 15 skipped) 5048ms
stdout | tests/api-characters-safe-place-locations.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/api-characters-safe-place-locations.test.js (11 tests | 11 skipped) 5041ms
stdout | tests/fix.715.dt-manual-open-gate.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🤖 agentic secret storage: https://dotenvx.com/as2

 ❯ tests/fix.715.dt-manual-open-gate.test.js (5 tests | 5 skipped) 5035ms
stdout | tests/api-downtime-hold-flags.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent building .env in docker: https://dotenvx.com/prebuild

 ❯ tests/api-downtime-hold-flags.test.js (9 tests | 9 skipped) 5043ms
stdout | tests/api-rules-aggregate.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-rules-aggregate.test.js (9 tests | 9 skipped) 5046ms
stdout | tests/api-relationships-player-edit.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-relationships-player-edit.test.js (11 tests | 11 skipped) 5033ms
stdout | tests/api-relationships-mutual.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🛠️  run anywhere with `dotenvx run -- yourcommand`

 ❯ tests/api-relationships-mutual.test.js (11 tests | 11 skipped) 5044ms
stdout | tests/api-characters.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: ⚙️  override existing env vars with { override: true }

 ❯ tests/api-characters.test.js (15 tests | 15 skipped) 5044ms
stdout | tests/stm-7-bulk-endpoint.test.js
[dotenv@17.3.1] injecting env (4) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/stm-7-bulk-endpoint.test.js (14 tests | 14 skipped) 5038ms
 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework
stderr | tests/otc-2-office-actions-api.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/otc-2-office-actions-api.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-downtime-regent-gate.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-downtime-regent-gate.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-joint-projects.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-joint-projects.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-invitation-lifecycle.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-invitation-lifecycle.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-st-mods.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-st-mods.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters-carthian-pull.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters-carthian-pull.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/n3-necropolis.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/n3-necropolis.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-npcs-quick-add.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-npcs-quick-add.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships-for-character.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships-for-character.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-players.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-players.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-ordeal-responses.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-ordeal-responses.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters-crud.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters-crud.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-downtime.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-downtime.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-ranking-ballots.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-ranking-ballots.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-territories-regent-write.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-territories-regent-write.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/issue-834-m-free-deprecation.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-territories.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-territories.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-rules-engine.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-rules-engine.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-touchstone-edges.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-touchstone-edges.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-publish-cycle.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-publish-cycle.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/stm-10-lifecycle.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/stm-10-lifecycle.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/stm-8-pool-snapshot.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/stm-8-pool-snapshot.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-territories-regent-lieutenant.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-territories-regent-lieutenant.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-chapters.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-chapters.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-npc-flags.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-npc-flags.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-game-sessions.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-game-sessions.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/issue-971-cyoa-passages.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/issue-971-cyoa-passages.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-game-sessions-next.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-game-sessions-next.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/oath-b-d6-api-roundtrip.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/oath-b-d6-api-roundtrip.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters-safe-place-locations.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters-safe-place-locations.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/fix.715.dt-manual-open-gate.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/fix.715.dt-manual-open-gate.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-downtime-hold-flags.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-downtime-hold-flags.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-rules-aggregate.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-rules-aggregate.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships-player-edit.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships-player-edit.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-relationships-mutual.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-relationships-mutual.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/api-characters.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/api-characters.test.js
[setupDb] connectDb() failed: read ECONNRESET

stderr | tests/stm-7-bulk-endpoint.test.js
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)
strict mode: use allowUnionTypes to allow union type keyword at "#/definitions/merit/properties/narrow" (strictTypes)

stderr | tests/stm-7-bulk-endpoint.test.js
[setupDb] connectDb() failed: read ECONNRESET


diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:56:50.224927Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Write-Output \"Review root: "'$reviewRoot"; Get-Process mongod -ErrorAction SilentlyContinue | Select-Object ProcessName,Id,Responding,StartTime' in D:\Terra Mortis\TM Suite
 succeeded in 3203ms:
Review root: D:\Terra Mortis\TM Suite

[32;1mProcessName[0m[32;1m   Id[0m[32;1m Responding[0m[32;1m StartTime[0m
[32;1m-----------[0m [32;1m  --[0m [32;1m----------[0m [32;1m---------[0m
mongod      6436       True 


diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:57:01.357546Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The full run did not yield a valid total: it timed out at five minutes after many integration suites skipped because parallel workers repeatedly lost the local Mongo connection (`ECONNRESET`). That cannot verify or refute 2463/2458, so I’ll disclose it as unverified. The targeted DB suites remain valid because they ran individually and passed. I’m proceeding with the isolated base-commit comparison, which does not depend on a full-suite aggregate.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; "'$baseZip = '"'D:\\tmp\\eqc1152-ddf059f8.zip'; "'$baseDir = '"'D:\\tmp\\eqc1152-ddf059f8'; if (Test-Path -LiteralPath "'$baseZip) { throw "Refusing to overwrite existing $baseZip" }; if (Test-Path -LiteralPath $baseDir) { throw "Refusing to overwrite existing $baseDir" }; git -C $reviewRoot archive --format=zip --output=$baseZip ddf059f8; Expand-Archive -LiteralPath $baseZip -DestinationPath $baseDir; New-Item -ItemType Junction -Path "$baseDir'"\\server\\node_modules\" -Target \""'$reviewRoot'"\\server\\node_modules\" | Select-Object FullName,LinkType,Target" in D:\Terra Mortis\TM Suite
2026-08-13T02:57:22.491454Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 5.9 seconds
Output:
fatal: could not open 'D:/tmp/eqc1152-ddf059f8.zip' for writing: Permission denied
[31;1mNew-Item: [31;1mAccess to the path 'D:\tmp\eqc1152-ddf059f8' is denied.[0m

 exited 1 in 5873ms:
fatal: could not open 'D:/tmp/eqc1152-ddf059f8.zip' for writing: Permission denied
[31;1mNew-Item: [31;1mAccess to the path 'D:\tmp\eqc1152-ddf059f8' is denied.[0m

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:57:22.544688Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; "'$baseZip = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8.zip\"; "'$baseDir = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8\"; if (Test-Path -LiteralPath "'$baseZip) { throw "Refusing to overwrite existing $baseZip" }; if (Test-Path -LiteralPath $baseDir) { throw "Refusing to overwrite existing $baseDir" }; git -C $reviewRoot archive --format=zip --output=$baseZip ddf059f8; Expand-Archive -LiteralPath $baseZip -DestinationPath $baseDir; New-Item -ItemType Junction -Path "$baseDir'"\\server\\node_modules\" -Target \""'$reviewRoot'"\\server\\node_modules\" | Select-Object FullName,LinkType,Target" in D:\Terra Mortis\TM Suite
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:57:44.041189Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:57:58.939106Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:58:14.373617Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:58:27.699119Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
 succeeded in 57910ms:

[32;1mFullName                                                                 [0m[32;1m LinkType[0m[32;1m Target[0m
[32;1m--------                                                                 [0m [32;1m--------[0m [32;1m------[0m
D:\Terra Mortis\TM Suite\.review-tmp-eqc1152-ddf059f8\server\node_modules Junction D:\Terra Mortis\TM Suite\server\nod…

warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied

diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:58:31.312716Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
apply patch
patch: completed
D:\Terra Mortis\TM Suite\.review-tmp-eqc1152-ddf059f8\server\db.js
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:58:37.322100Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; "'$baseServer = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8\\server\"; Set-Location -LiteralPath "'$baseServer; $env:MONGODB_URI='"'mongodb://127.0.0.1:27017'; npx vitest run tests/epic.708.3-cycle-phase-controls.test.js tests/issue-1013-indomitable-rules-text.test.js tests/issue-1021-failed-breakpoint-merit.test.js tests/issue-811-sumchannels-rootcause.test.js tests/issue-826-cleanup-script-integration.test.js tests/issue-836-legacy-tracker-cache-removed.test.js tests/issue-837-xp-totals-deprecation.test.js tests/n7-n9-allocator-readers.test.js tests/n8-mandragora-prereq.test.js tests/oath-a-pledge-helpers.test.js" in D:\Terra Mortis\TM Suite
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:58:56.866418Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
2026-08-13T02:58:56.906774Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 11.1 seconds
Total output lines: 3441
Output:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/.review-tmp-eqc1152-ddf059f8/server

 ❯ tests/n7-n9-allocator-readers.test.js (25 tests | 1 failed) 29ms
     × all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly) 9ms
 ❯ tests/oath-a-pledge-helpers.test.js (29 tests | 1 failed) 53ms
     × meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form 34ms
 ❯ tests/epic.708.3-cycle-phase-controls.test.js (14 tests | 3 failed) 22ms
     × exports setGamePhase function 10ms
     × uses data-phase attribute on phase buttons 3ms
     × highlights active phase with gold2 colour 3ms
stdout | tests/issue-1013-indomitable-rules-text.test.js
[dotenv@17.3.1] injecting env (0) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/issue-1013-indomitable-rules-text.test.js (0 test)
 ❯ tests/issue-1021-failed-breakpoint-merit.test.js (0 test)
 ❯ tests/issue-811-sumchannels-rootcause.test.js (0 test)
stdout | tests/issue-826-cleanup-script-integration.test.js
[dotenv@17.3.1] injecting env (0) from ..\.env -- tip: ⚙️  load multiple .env files with { path: ['.env.local', '.env'] }

 ❯ tests/issue-826-cleanup-script-integration.test.js (0 test)
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js (0 test)
stdout | tests/issue-837-xp-totals-deprecation.test.js
[dotenv@17.3.1] injecting env (0) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/issue-837-xp-totals-deprecation.test.js (0 test)
 ❯ tests/n8-mandragora-prereq.test.js (0 test)

 Test Files  10 failed (10)
      Tests  5 failed | 63 passed (68)
   Start at  12:58:51
   Duration  5.25s (transform 377ms, setup 261ms, import 164ms, tests 104ms, environment 2ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

⎯⎯⎯⎯⎯⎯ Failed Suites 7 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/issue-1013-indomitable-rules-text.test.js [ tests/issue-1013-indomitable-rules-text.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/12]⎯

 FAIL  tests/issue-1021-failed-breakpoint-merit.test.js [ tests/issue-1021-failed-breakpoint-merit.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/12]⎯

 FAIL  tests/issue-811-sumchannels-rootcause.test.js [ tests/issue-811-sumchannels-rootcause.test.js ]
 FAIL  tests/issue-826-cleanup-script-integration.test.js [ tests/issue-826-cleanup-script-integration.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/12]⎯

 FAIL  tests/issue-836-legacy-tracker-cache-removed.test.js [ tests/issue-836-legacy-tracker-cache-removed.test.js ]
Error: ENOENT: no such file or directory, open 'D:\Terra Mortis\TM Suite\.review-tmp-eqc1152-ddf059f8\public\js\suite\tracker.js'
 ❯ read tests/issue-836-legacy-tracker-cache-removed.test.js:30:32
     28| const __dirname = path.dirname(fileURLToPath(import.meta.url));
     29| const REPO_ROOT = path.resolve(__dirname, '..', '..');
     30| function read(rel) { return fs.readFileSync(path.join(REPO_ROOT, rel),…
       |                                ^
     31| function exists(rel) { return fs.existsSync(path.join(REPO_ROOT, rel))…
     32|
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js:69:15

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/12]⎯

 FAIL  tests/issue-837-xp-totals-deprecation.test.js [ tests/issue-837-xp-totals-deprecation.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/12]⎯

 FAIL  tests/n8-mandragora-prereq.test.js [ tests/n8-mandragora-prereq.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/12]⎯


⎯⎯⎯⎯⎯⎯⎯ Failed Tests 5 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > exports setGamePhase function
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'setGamePhase'

- Expected
+ Received

- setGamePhase
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return…26592 tokens truncated…
    return n[0] === discName || (n.length > 1 && n.slice(0, -1).join(' ') === discName);
  });
}

/** Return powers not attributable to any known discipline on the character. */
export function otherPowers(c) {
  const discNames = Object.keys(c.disciplines || {});
  return (c.powers || []).filter(p => {
    if (!p.name) return false;
    const key = p.name.split('|')[0].trim().replace(/\\s*[●○]+$/, '').replace(/\\s+\\d+$/, '');
    if (RITUAL_DISCS.some(d => p.name.startsWith(d))) return false;
    return !discNames.some(d => key === d || key.startsWith(d + ' '));
  });
}

/** Returns a Set of cult names from a character's Mystery Cult Initiation merits. */
function getCharCults(c) {
  return new Set(
    (c.merits || [])
      .filter(m => m.name === 'Mystery Cult Initiation' && m.cult_name)
      .map(m => m.cult_name)
  );
}

/** Check whether a character meets a devotion's discipline prerequisites. */
export function meetsDevPrereqs(c, dev) {
  if (dev.bl && (c.bloodline || '') !== dev.bl) return false;
  if (dev.cult && !getCharCults(c).has(dev.cult)) return false;
  const discs = c.disciplines || {};
  if (!dev.p || !dev.p.length) return true;
  if (dev.or) {
    return dev.p.some(p => (discs[p.disc]?.dots || 0) >= p.dots);
  }
  return dev.p.every(p => (discs[p.disc]?.dots || 0) >= p.dots);
}

/** Format a devotion's prerequisite list as a human-readable string. */
export function devPrereqStr(dev) {
  const parts = [];
  if (dev.bl) parts.push(dev.bl + ' only');
  if (dev.cult) parts.push(dev.cult + ' members only');
  if (dev.p && dev.p.length) parts.push(dev.p.map(p => p.disc + ' ' + p.dots).join(dev.or ? ' or ' : ', '));
  return parts.join('; ') || 'None';
}
"

 ❯ tests/n7-n9-allocator-readers.test.js:246:17
    244|     const src = read('public/js/editor/merits.js');
    245|     // buildMeritOptions
    246|     expect(src).toMatch(/buildMeritOptions[\s\S]{0,600}meritPrereqOK\(…
       |                 ^
    247|     // buildSubCategoryMeritOptions
    248|     expect(src).toMatch(/buildSubCategoryMeritOptions[\s\S]{0,800}meri…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/12]⎯

 FAIL  tests/oath-a-pledge-helpers.test.js > OATH-A D2 — encumbrance changes no dot sum > meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form
AssertionError: expected '/**\r\n * XP calculations and merit r…' to contain 'export function meritRating(c, m) {\n…'

- Expected
+ Received

- export function meritRating(c, m) {
-   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+ /**
+  * XP calculations and merit rating/breakdown helpers.
+  * Pure functions — no DOM side-effects.
+  */
+ 
+ import { getRuleByKey } from '../data/loader.js';
+ // N-1: map-fallback shape for per-slug free reads (see rules-helpers.js).
+ import { freeOf } from '../data/rules-helpers.js';
+ // COLLECTIVE-2 (#1110): compound slug + source names reach the bd-row from
+ // rule data, so they are escaped like every other data-sourced label.
+ import { esc } from '../data/helpers.js';
+ 
+ /**
+  * Convert XP spent into dot count (flat rate).
+  * @param {number} xpSpent - total XP allocated
+  * @param {number} baseBeforeXP - dots from CP/free (unused in flat model, kept for API)
+  * @param {number} costPerDot - XP cost per single dot
+  * @returns {number}
+  */
+ export function xpToDots(xpSpent, baseBeforeXP, costPerDot) {
+   return Math.floor((xpSpent || 0) / costPerDot);
+ }
+ 
+ /**
+  * Convert a dot count into XP cost (flat rate).
+  * @param {number} numDots
+  * @param {number} baseBeforeXP - (unused in flat model, kept for API)
+  * @param {number} costPerDot
+  * @returns {number}
+  */
+ export function dotsToXP(numDots, baseBeforeXP, costPerDot) {
+   return (numDots || 0) * costPerDot;
+ }
+ 
+ /** Starting XP awarded on character creation. */
+ export function xpStarting() { return 10; }
+ 
+ /** XP from voluntary humanity drops: 2 per dot permanently lost. */
+ export function xpHumanityDrop(c) {
+   // Use explicit field if set; otherwise infer from stored humanity value
+   const lost = c.humanity_lost !== undefined
+     ? c.humanity_lost
+     : Math.max(0, (c.humanity_base || 7) - (c.humanity || 0));
+   return lost * 2;
+ }
+ 
+ /** XP from completed ordeals: 3 per ordeal. */
+ export function xpOrdeals(c) {
+   return ((c.ordeals || []).filter(o => o.complete).length) * 3;
+ }
+ 
+ /** XP from game attendance. Requires loadGameXP() to have run — returns 0 if not yet loaded. */
+ export function xpGame(c) {
+   return c._gameXP ?? 0;
+ }
+ 
+ /** XP bonus from Professional Training: 1 XP per asset skill at 5+ effective dots (requires PT ≥ 4).
+  *  Effective dots = base dots + PT dot 4 bonus (+1 to chosen asset skill). */
+ export function xpPT5(c) {
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   if (!ptM || meritRating(c, ptM) < 4) return 0;
+   const assets = (ptM.asset_skills || []).filter(Boolean);
+   if (!assets.length) return 0;
+   const ptBonus = c._pt_dot4_bonus_skills instanceof Set ? c._pt_dot4_bonus_skills : new Set();
+   return assets.filter(sk => {
+     const s = (c.skills || {})[sk];
+     const effective = (s?.dots || 0) + (ptBonus.has(sk) ? 1 : 0);
+     return effective >= 5;
+   }).length;
+ }
+ 
+ /**
+  * Total XP earned by a character (all sources, derived dynamically).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpEarned(c) {
+   return xpStarting() + xpHumanityDrop(c) + xpOrdeals(c) + xpGame(c) + xpPT5(c);
+ }
+ 
+ /** Sum XP from inline creation fields on an object (attributes, skills, disciplines). */
+ function sumInlineXP(obj) {
+   if (!obj) return 0;
+   return Object.values(obj).reduce((t, v) => t + (v?.xp || 0), 0);
+ }
+ 
+ /** XP spent on attributes — sum of .xp across all attribute objects. */
+ export function xpSpentAttrs(c) {
+   return sumInlineXP(c.attributes);
+ }
+ 
+ /** XP spent on skills + specialisations beyond free allowance. */
+ export function xpSpentSkills(c) {
+   const skillXP = sumInlineXP(c.skills);
+   // PT free specs (dot 3): 2 extra, but ONLY usable on asset skills — tracked separately
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   const ptFree = (ptM && ptM.rating >= 3) ? 2 : 0;
+   const ptAssets = new Set((ptM && ptM.rating >= 3 && ptM.asset_skills) ? (ptM.asset_skills || []).filter(Boolean) : []);
+   let assetSpecs = 0, nonAssetSpecs = 0;
+   Object.entries(c.skills || {}).forEach(([sk, skillObj]) => {
+     const count = (skillObj && skillObj.specs) ? skillObj.specs.length : 0;
+     if (ptAssets.has(sk)) assetSpecs += count;
+     else nonAssetSpecs += count;
+   });
+   // MCI dot 1 free specs: each active MCI with dot1_choice === 'speciality' grants 1 free spec
+   const mciFreeSpecs = (c._mci_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // Bloodline-granted specs are also exempt from XP cost
+   const bloodlineFreeSpecs = (c._bloodline_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // PT free covers asset specs first; baseline 3 covers everything else
+   const ptFreeCovered = Math.min(ptFree, assetSpecs);
+   const paidSpecs = nonAssetSpecs + Math.max(0, assetSpecs - ptFreeCovered);
+   const specXP = Math.max(0, paidSpecs - 3 - mciFreeSpecs - bloodlineFreeSpecs);
+   return skillXP + specXP;
+ }
+ 
+ /** XP spent on all merits (general, influence, domain, standing) + fighting styles + pact oaths. */
+ export function xpSpentMerits(c) {
+   const meritXP = (c.merits || []).reduce((t, m) => t + (m.xp || 0), 0);
+   const styleXP = (c.fighting_styles || []).reduce((t, fs) => t + (fs.xp || 0), 0);
+   const pactXP = (c.powers || []).filter(p => p.category === 'pact').reduce((t, p) => t + (p.xp || 0), 0);
+   return meritXP + styleXP + pactXP;
+ }
+ 
+ /** XP spent on powers — disciplines + devotions. */
+ export function xpSpentPowers(c) {
+   const discXP = sumInlineXP(c.disciplines);
+   // Devotion XP: look up each devotion's cost from DEVOTIONS_DB
+   const devXP = (c.powers || [])
+     .filter(p => p.category === 'devotion')
+     .reduce((t, p) => {
+       // Try rules cache first, fallback to _devotionsDB
+       const slug = 'devotion-' + p.name.toLowerCase().replace(/['']/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
+       const rule = getRuleByKey(slug);
+       if (rule) return t + (rule.xp_fixed || 0);
+       const db = _devotionsDB ? _devotionsDB.find(d => d.n === p.name) : null;
+       return t + (db ? db.xp || 0 : 0);
+     }, 0);
+   // Rite XP: paid rites cost 1 XP (rank 1-3) or 2 XP (rank 4-5)
+   const riteXP = (c.powers || [])
+     .filter(p => p.category === 'rite' && !p.free)
+     .reduce((t, p) => t + (p.level >= 4 ? 2 : 1), 0);
+   return discXP + devXP + riteXP;
+ }
+ 
+ /** XP spent on special: Blood Potency, Humanity, lost Willpower dots. */
+ export function xpSpentSpecial(c) {
+   // Blood Potency: tracked directly in bp_creation.xp (new model) or derived (legacy)
+   const bpXP = (c.bp_creation || {}).xp !== undefined
+     ? ((c.bp_creation || {}).xp || 0)
+     : Math.max(0, (c.blood_potency || 1) - 1 - Math.floor(((c.bp_creation || {}).cp || 0) / 5)) * 5;
+   // Humanity: XP spent raising dots (new model only; old model net is captured in xpHumanityDrop)
+   const humXP = c.humanity_xp || 0;
+   // Lost Willpower dots: stored in xp_log.spent.willpower
+   const wpXP = ((c.xp_log || {}).spent || {}).willpower || 0;
+   // Manual special: anything else tracked in xp_log
+   const manualXP = ((c.xp_log || {}).spent || {}).special || 0;
+   return bpXP + humXP + wpXP + manualXP;
+ }
+ 
+ // Devotions DB reference (set via setDevotionsDB)
+ let _devotionsDB = null;
+ export function setDevotionsDB(db) { _devotionsDB = db; }
+ 
+ /**
+  * Total XP spent by a character (all categories, fully dynamic).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpSpent(c) {
+   return xpSpentAttrs(c) + xpSpentSkills(c) + xpSpentMerits(c) + xpSpentPowers(c) + xpSpentSpecial(c);
+ }
+ 
+ /**
+  * Remaining unspent XP.
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpLeft(c) {
+   return xpEarned(c) - xpSpent(c);
+ }
+ 
+ /**
+  * Effective rating of a merit (sum of CP + free + grant pools + XP, inline on merit object).
+  * Falls back to stored rating if inline fields are absent.
+  * @param {object} c - character object
+  * @param {object} m - merit entry
+  * @returns {number}
+  */
+ export function meritRating(c, m) {
+   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+   return (m.cp || 0) + freeOf(m, 'bloodline') + freeOf(m, 'pet') + freeOf(m, 'mci') + freeOf(m, 'vm') + freeOf(m, 'lk')
+     + freeOf(m, 'ohm') + freeOf(m, 'inv') + freeOf(m, 'pt') + freeOf(m, 'mdb') + freeOf(m, 'sw') + (m.xp || 0);
+ }
+ 
+ /**
+  * Render the merit breakdown row: Fr + CP + XP = total
+  * @param {number} realIdx - index into c.merits
+  * @param {object} mc - merit object with inline creation fields {cp, free, xp, free_mci, ...}
+  * @param {number|null} fixedAt - if the merit has a fixed rating (e.g. 3 for VM), pass it here;
+  *   null for graduated merits. When fixedAt is set, the displayed total snaps to 0 until the
+  *   threshold is met, then shows fixedAt.
+  */
+ export function meritBdRow(realIdx, mc, fixedAt, opts = {}) {
+   const cp = mc.cp || 0, xp = mc.xp || 0, fbl = freeOf(mc, 'bloodline'), fret = freeOf(mc, 'pet'), fmci = freeOf(mc, 'mci'), fvm = freeOf(mc, 'vm'), flk = freeOf(mc, 'lk'), fohm = freeOf(mc, 'ohm'), finv = freeOf(mc, 'inv'), fpt = freeOf(mc, 'pt'), fmdb = freeOf(mc, 'mdb'), fsw = freeOf(mc, 'sw');
+   // COLLECTIVE-2 (issue #1110): Collective Compound allocation channels are
+   // data-driven — the caller passes the discovered slugs rather than this
+   // function naming one. Defaults to ['necro'] so call sites that predate
+   // the compound wiring keep their pre-#1110 total exactly.
+   const _cmpSlugs = Array.isArray(opts.compoundSlugs) ? opts.compoundSlugs : ['necro'];
+   const fcompound = _cmpSlugs.reduce((s, slug) => s + freeOf(mc, slug), 0);
+   const total = cp + xp + fbl + fret + fmci + fvm + flk + fohm + finv + fpt + fmdb + fsw + fcompound + (opts.attachBonus || 0);
+   // Effective display: for fixed merits, only show dots once the threshold is reached
+   const effective = (fixedAt != null) ? (total >= fixedAt ? fixedAt : 0) : total;
+   const needsHint = (fixedAt != null && total > 0 && total < fixedAt)
+     ? '<span class="bd-needs-hint">' + total + ' / ' + fixedAt + ' needed</span>' : '';
+   // N-7b (issue #768): Necropolis target merits are pool-funded only — the
+   // domain-renderer call site suppresses CP / XP / MCI / Bonus on these rows
+   // via opts.hideCP / opts.hideXP / opts.hideMCI (this block) + opts.hideBonus
+   // (below). Same opts-flag pattern as N-9's hideBonus on standing merits.
+   // The pre-N-7b shape rendered CP + XP + sep unconditionally; the guards
+   // here are defaults-false so existing call sites that don't pass the
+   // hide-flags get the legacy behaviour unchanged.
+   let h = '<div class="merit-bd-row">';
+   if (!opts.hideCP) h += '<div class="bd-grp"><span class="bd-lbl">CP</span><input class="merit-bd-input" type="number" min="0" value="' + cp + '" onchange="shEditMeritPt(' + realIdx + ',\'cp\',+this.value)"></div>';
+   if (!opts.hideXP) h += '<div class="bd-grp"><span class="bd-lbl">XP</span><input class="merit-bd-input" type="number" min="0" value="' + xp + '" onchange="shEditMeritPt(' + realIdx + ',\'xp\',+this.value)"></div>';
+   if (!opts.hideCP || !opts.hideXP) h += '<div class="bd-sep"></div>';
+   // N-9 (issue #762, Bug 1 "adjacent finding"): MCI input writes the map-shape
+   // `free_grants.mci` per the ADR-005 allocator-write-path amendment. The
+   // handler (shEditMeritPt) detects the `free_grants.` prefix and routes to
+   // the map. Pre-N-9 this wrote `free_mci`, creating a fresh divergence on
+   // every edit after the N-2 backfill.
+   // N-7b: showMCI is AND'd with !hideMCI at the call site so Necropolis
+   // targets don't surface the MCI allocator even when there's pool capacity.
+   // Issue #774: a11y — every stepper input carries id + name + aria-label
+   // matching the NECRO precedent at line 241 (added by N-7c #771). Same
+   // browser warning ("form field element should have an id or name
+   // attribute / No label associated with a form field") was firing on
+   // these 5 sibling steppers. Single-line additions per stepper.
+   if (opts.showMCI && !opts.hideMCI) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-mci-lbl-' + realIdx + '">MCI</span><input id="bd-mci-' + realIdx + '" name="bd-mci-' + realIdx + '" aria-label="Mystery Cult Initiation pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fmci + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.mci\',+this.value)"></div>';
+   if (opts.showVM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-vm-lbl-' + realIdx + '">VM</span><input id="bd-vm-' + realIdx + '" name="bd-vm-' + realIdx + '" aria-label="Viral Mythology pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fvm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_vm\',+this.value)"></div>';
+   if (opts.showLK) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-lk-lbl-' + realIdx + '">LK</span><input id="bd-lk-' + realIdx + '" name="bd-lk-' + realIdx + '" aria-label="Lorekeeper pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + flk + '" onchange="shEditMeritPt(' + realIdx + ',\'free_lk\',+this.value)"></div>';
+   if (opts.showOHM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-ohm-lbl-' + realIdx + '">OHM</span><input id="bd-ohm-' + realIdx + '" name="bd-ohm-' + realIdx + '" aria-label="Oath of the Hard Motherfucker pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fohm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_ohm\',+this.value)"></div>';
+   if (opts.showINV) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-inv-lbl-' + realIdx + '">INV</span><input id="bd-inv-' + realIdx + '" name="bd-inv-' + realIdx + '" aria-label="Invested pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + finv + '" onchange="shEditMeritPt(' + realIdx + ',\'free_inv\',+this.value)"></div>';
+   // N-7 (issue #760): Collective Compound allocator — writes directly to
+   // m.free_grants.<slug> (map shape, no new legacy free_<slug> field) per the
+   // ADR-005 allocator-write-path amendment.
+   // N-7c (issue #771): id + aria-label so browsers don't flag "form field
+   // element should have an id or name attribute / No label associated with a
+   // form field". The 5 sibling steppers (LK/INV/VM/OHM/MCI) got the same
+   // treatment in #774 — see lines above.
+   // COLLECTIVE-2 (issue #1110): one stepper per compound the character
+   // belongs to that claims this merit, each writing its OWN slug. Pre-#1110
+   // this was a single `showNECRO` flag hardwired to free_grants.necro, which
+   // would have written a Crone or Sanctified allocation into the Necropolis
+   // pool. The descriptor supplies slug + source; no name literals here.
+   for (const _cmp of (opts.compoundPools || [])) {
+     if (!_cmp || !_cmp.slug) continue;
+     h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-' + _cmp.slug + '-lbl-' + realIdx + '">' + esc(_cmp.slug.toUpperCase()) + '</span><input id="bd-' + _cmp.slug + '-' + realIdx + '" name="bd-' + _cmp.slug + '-' + realIdx + '" aria-label="' + esc(_cmp.source || _cmp.slug) + ' pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + freeOf(mc, _cmp.slug) + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.' + _cmp.slug + '\',+this.value)"></div>';
+   }
+   h += '<div class="bd-eq"><span class="bd-val">' + effective + ' dot' + (effective === 1 ? '' : 's') + '</span>' + needsHint + '</div>'
+     + '</div>';
+   // N-9 (issue #762, Bug 2): standing-merit render paths (MCI, PT) don't read
+   // m.bonus, so the Bonus row was visible-but-no-op. Standing call sites pass
+   // opts.hideBonus=true to suppress the row entirely. Default behaviour
+   // (general/influence/domain/style merits) is unchanged.
+   if (!opts.hideBonus) {
+     const bon = mc.bonus || 0;
+     h += '<div class="attr-derived-row"><span class="bd-lbl">Bonus</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',-1)"' + (bon === 0 ? ' disabled' : '') + '>&#x25BC;</button><span class="bd-src">' + (bon > 0 ? '+' + bon : '0') + '</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',1)">&#x25B2;</button></div>';
+   }
+   return h;
+ }
+

 ❯ tests/oath-a-pledge-helpers.test.js:388:16
    386|     const xp = read('public/js/editor/xp.js');
    387|     const dom = read('public/js/editor/domain.js');
    388|     expect(xp).toContain("export function meritRating(c, m) {\n  if (m…
       |                ^
    389|     expect(dom).toContain('export function meritEffectiveRating(c, m) …
    390|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/12]⎯


 exited 1 in 11097ms:

 RUN  v4.1.2 D:/Terra Mortis/TM Suite/.review-tmp-eqc1152-ddf059f8/server

 ❯ tests/n7-n9-allocator-readers.test.js (25 tests | 1 failed) 29ms
     × all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly) 9ms
 ❯ tests/oath-a-pledge-helpers.test.js (29 tests | 1 failed) 53ms
     × meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form 34ms
 ❯ tests/epic.708.3-cycle-phase-controls.test.js (14 tests | 3 failed) 22ms
     × exports setGamePhase function 10ms
     × uses data-phase attribute on phase buttons 3ms
     × highlights active phase with gold2 colour 3ms
stdout | tests/issue-1013-indomitable-rules-text.test.js
[dotenv@17.3.1] injecting env (0) from ..\.env -- tip: 🔐 prevent committing .env to code: https://dotenvx.com/precommit

 ❯ tests/issue-1013-indomitable-rules-text.test.js (0 test)
 ❯ tests/issue-1021-failed-breakpoint-merit.test.js (0 test)
 ❯ tests/issue-811-sumchannels-rootcause.test.js (0 test)
stdout | tests/issue-826-cleanup-script-integration.test.js
[dotenv@17.3.1] injecting env (0) from ..\.env -- tip: ⚙️  load multiple .env files with { path: ['.env.local', '.env'] }

 ❯ tests/issue-826-cleanup-script-integration.test.js (0 test)
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js (0 test)
stdout | tests/issue-837-xp-totals-deprecation.test.js
[dotenv@17.3.1] injecting env (0) from ..\.env -- tip: 🔐 encrypt with Dotenvx: https://dotenvx.com

 ❯ tests/issue-837-xp-totals-deprecation.test.js (0 test)
 ❯ tests/n8-mandragora-prereq.test.js (0 test)

 Test Files  10 failed (10)
      Tests  5 failed | 63 passed (68)
   Start at  12:58:51
   Duration  5.25s (transform 377ms, setup 261ms, import 164ms, tests 104ms, environment 2ms)

 DEPRECATED  `test.poolOptions` was removed in Vitest 4. All previous `poolOptions` are now top-level options. Please, refer to the migration guide: https://vitest.dev/guide/migration#pool-rework

⎯⎯⎯⎯⎯⎯ Failed Suites 7 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/issue-1013-indomitable-rules-text.test.js [ tests/issue-1013-indomitable-rules-text.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/12]⎯

 FAIL  tests/issue-1021-failed-breakpoint-merit.test.js [ tests/issue-1021-failed-breakpoint-merit.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/12]⎯

 FAIL  tests/issue-811-sumchannels-rootcause.test.js [ tests/issue-811-sumchannels-rootcause.test.js ]
 FAIL  tests/issue-826-cleanup-script-integration.test.js [ tests/issue-826-cleanup-script-integration.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/12]⎯

 FAIL  tests/issue-836-legacy-tracker-cache-removed.test.js [ tests/issue-836-legacy-tracker-cache-removed.test.js ]
Error: ENOENT: no such file or directory, open 'D:\Terra Mortis\TM Suite\.review-tmp-eqc1152-ddf059f8\public\js\suite\tracker.js'
 ❯ read tests/issue-836-legacy-tracker-cache-removed.test.js:30:32
     28| const __dirname = path.dirname(fileURLToPath(import.meta.url));
     29| const REPO_ROOT = path.resolve(__dirname, '..', '..');
     30| function read(rel) { return fs.readFileSync(path.join(REPO_ROOT, rel),…
       |                                ^
     31| function exists(rel) { return fs.existsSync(path.join(REPO_ROOT, rel))…
     32|
 ❯ tests/issue-836-legacy-tracker-cache-removed.test.js:69:15

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/12]⎯

 FAIL  tests/issue-837-xp-totals-deprecation.test.js [ tests/issue-837-xp-totals-deprecation.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/12]⎯

 FAIL  tests/n8-mandragora-prereq.test.js [ tests/n8-mandragora-prereq.test.js ]
SyntaxError: Invalid or unexpected token
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/12]⎯


⎯⎯⎯⎯⎯⎯⎯ Failed Tests 5 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > exports setGamePhase function
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'setGamePhase'

- Expected
+ Received

- setGamePhase
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Attendance section ───────────────────────────────────────────────────────
+ 
+ function buildAttendanceSection(cy, sessions) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-attend';
+ 
+   const selectWrap = document.createElement('div');
+   selectWrap.className = 'cy-attend-head';
+ 
+   const lbl = document.createElement('label');
+   lbl.className = 'cy-attend-lbl';
+   lbl.textContent = 'Linked Session:';
+ 
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-attend-select';
+ 
+   const blank = document.createElement('option');
+   blank.value = '';
+   blank.textContent = '— not linked —';
+   sel.appendChild(blank);
+ 
+   const sorted = [...sessions].sort((a, b) => (a.game_number ?? 999) - (b.game_number ?? 999));
+   sorted.forEach(s => {
+     const opt = document.createElement('option');
+     opt.value = String(s._id);
+     let label = s.game_number ? 'Game ' + s.game_number : '';
+     if (s.title) label += (label ? ' — ' : '') + s.title;
+     if (!label) label = s.session_date || String(s._id);
+     opt.textContent = label;
+     sel.appendChild(opt);
+   });
+ 
+   sel.value = cy.session_id || '';
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   selectWrap.appendChild(lbl);
+   selectWrap.appendChild(sel);
+   selectWrap.appendChild(errEl);
+   wrap.appendChild(selectWrap);
+ 
+   const tableWrap = document.createElement('div');
+   wrap.appendChild(tableWrap);
+ 
+   function renderTable() {
+     tableWrap.innerHTML = '';
+     const session = sessions.find(s => String(s._id) === sel.value);
+     if (!session) return;
+     const att = session.attendance || [];
+     if (!att.length) {
+       const msg = document.createElement('p');
+       msg.className = 'cy-muted cy-attend-empty';
+       msg.textContent = 'No attendance recorded for this session.';
+       tableWrap.appendChild(msg);
+       return;
+     }
+ 
+     const rows = [...att].sort((a, b) => {
+       const na = (a.character_display || a.character_name || '').toLowerCase();
+       const nb = (b.character_display || b.character_name || '').toLowerCase();
+       return na < nb ? -1 : na > nb ? 1 : 0;
+     });
+ 
+     const table = document.createElement('table');
+     table.className = 'infl-table cy-attend-table';
+     table.innerHTML = `<thead><tr>
+       <th>Character</th>
+       <th class="cy-att-c">Attend</th>
+       <th class="cy-att-c">Costuming</th>
+       <th class="cy-att-c">DT</th>
+       <th class="cy-att-c">Extra</th>
+       <th class="cy-att-c">XP</th>
+     </tr></thead>`;
+ 
+     const tbody = document.createElement('tbody');
+     let totAtt = 0, totCos = 0, totDT = 0, totExtra = 0, totXP = 0;
+ 
+     rows.forEach(a => {
+       const xp = (a.attended ? 1 : 0) + (a.costuming ? 1 : 0) + (a.downtime ? 1 : 0) + (a.extra || 0);
+       totAtt   += a.attended  ? 1 : 0;
+       totCos   += a.costuming ? 1 : 0;
+       totDT    += a.downtime  ? 1 : 0;
+       totExtra += (a.extra || 0);
+       totXP    += xp;
+ 
+       const name = a.character_display || a.character_name || a.character_id || '?';
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${esc(name)}</td>
+         <td class="cy-att-c">${a.attended  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.costuming ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.downtime  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.extra || 0}</td>
+         <td class="cy-att-c cy-att-xp">${xp}</td>`;
+       tbody.appendChild(tr);
+     });
+ 
+     const totTr = document.createElement('tr');
+     totTr.className = 'cy-att-total';
+     totTr.innerHTML = `
+       <td class="cy-muted">Total (${rows.length})</td>
+       <td class="cy-att-c">${totAtt}</td>
+       <td class="cy-att-c">${totCos}</td>
+       <td class="cy-att-c">${totDT}</td>
+       <td class="cy-att-c">${totExtra}</td>
+       <td class="cy-att-c">${totXP}</td>`;
+     tbody.appendChild(totTr);
+ 
+     table.appendChild(tbody);
+     tableWrap.appendChild(table);
+   }
+ 
+   renderTable();
+ 
+   sel.addEventListener('change', async () => {
+     errEl.classList.remove('is-visible');
+     const newId = sel.value || null;
+     try {
+       await apiPut('/api/downtime_cycles/' + cy._id, { session_id: newId });
+       cy.session_id = newId;
+       renderTable();
+     } catch (err) {
+       sel.value = cy.session_id || '';
+       errEl.textContent = 'Link failed: ' + err.message;
+       errEl.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Game Cycles panel ───────────────────────────────────────────────────────
+ 
+ // Build a chapter <select>. Options-only; no persistence handler — used for
+ // the add-cycle form (which has no cycle to write to yet).
+ function buildChapterPicker(chapters, selectedId = '') {
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-chapter-select';
+   const none = document.createElement('option');
+   none.value = '';
+   none.textContent = '— none —';
+   sel.appendChild(none);
+   [...chapters].sort((a, b) => a.number - b.number).forEach(ch => {
+     const opt = document.createElement('option');
+     opt.value = String(ch._id);
+     opt.textContent = `${ch.number} — ${ch.label}`;
+     sel.appendChild(opt);
+   });
+   sel.value = selectedId ? String(selectedId) : '';
+   return sel;
+ }
+ 
+ // Row-level chapter select: persists chapter_id to an existing cycle on change.
+ function buildChapterSelect(cy, chapters) {
+   const sel = buildChapterPicker(chapters, cy.chapter_id);
+   sel.addEventListener('change', async () => {
+     const val = sel.value || null;
+     try {
+       await updateCycle(cy._id, { chapter_id: val });
+       cy.chapter_id = val;
+       renderRibbon();
+     } catch (err) {
+       sel.value = cy.chapter_id ? String(cy.chapter_id) : '';
+     }
+   });
+   return sel;
+ }
+ 
+ function buildLabelCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-label-cell';
+ 
+   function renderView() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const span = document.createElement('span');
+     span.className = 'cy-label-text';
+     span.textContent = cy.label || String(cy._id);
+     const editBtn = document.createElement('button');
+     editBtn.className = 'cy-icon-btn';
+     editBtn.title = 'Edit label';
+     editBtn.textContent = '✎';
+     editBtn.addEventListener('click', renderEdit);
+     inner.appendChild(span);
+     inner.appendChild(editBtn);
+     td.appendChild(inner);
+   }
+ 
+   function renderEdit() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const input = document.createElement('input');
+     input.type = 'text';
+     input.className = 'form-input cy-label-input';
+     input.value = cy.label || '';
+     const save = document.createElement('button');
+     save.className = 'btn-sm';
+     save.textContent = 'Save';
+     const cancel = document.createElement('button');
+     cancel.className = 'btn-sm btn-muted';
+     cancel.textContent = 'Cancel';
+ 
+     async function doSave() {
+       const newLabel = input.value.trim();
+       if (!newLabel) { input.focus(); return; }
+       try {
+         await updateCycle(cy._id, { label: newLabel });
+         cy.label = newLabel;
+         renderView();
+         renderRibbon();
+       } catch (_err) {
+         input.classList.add('is-error');
+       }
+     }
+     save.addEventListener('click', doSave);
+     cancel.addEventListener('click', renderView);
+     input.addEventListener('keydown', e => {
+       if (e.key === 'Enter') doSave();
+       else if (e.key === 'Escape') renderView();
+     });
+ 
+     inner.appendChild(input);
+     inner.appendChild(save);
+     inner.appendChild(cancel);
+     td.appendChild(inner);
+     input.focus();
+     input.select();
+   }
+ 
+   renderView();
+   return td;
+ }
+ 
+ function buildCyclesPanel(cycles, chapters, charList = [], sessions = []) {
+   const sorted = [...cycles].sort((a, b) => (a.game_number ?? 0) - (b.game_number ?? 0));
+ 
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Game Cycles</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Cycle';
+   header.appendChild(addBtn);
+   wrap.appendChild(header);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   // Inline add-cycle form
+   const addForm = document.createElement('div');
+   addForm.className = 'cy-inline-form';
+   const nextGame = (sorted.reduce((mx, c) => Math.max(mx, c.game_number ?? 0), 0) || 0) + 1;
+   addForm.innerHTML = `
+     <input id="new-cy-num" type="number" min="1" placeholder="Game #" value="${nextGame}" class="form-input cy-input-num">
+     <input id="new-cy-label" type="text" placeholder="Label (e.g. Downtime 5)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-cy-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-cy-cancel">Cancel</button>`;
+   const chapterPick = buildChapterPicker(chapters);
+   chapterPick.id = 'new-cy-chapter';
+   addForm.insertBefore(chapterPick, addForm.querySelector('#new-cy-save'));
+   wrap.appendChild(addForm);
+ 
+   addBtn.addEventListener('click', () => {
+     addForm.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     addForm.querySelector('#new-cy-label').value = `Downtime ${nextGame}`;
+     addForm.querySelector('#new-cy-label').focus();
+   });
+   addForm.querySelector('#new-cy-cancel').addEventListener('click', () => {
+     addForm.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+   addForm.querySelector('#new-cy-save').addEventListener('click', async () => {
+     const num = parseInt(addForm.querySelector('#new-cy-num').value, 10);
+     const label = addForm.querySelector('#new-cy-label').value.trim();
+     const chapterId = addForm.querySelector('#new-cy-chapter').value || null;
+     if (!num || !label) {
+       errMsg.textContent = 'Game number and label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       await createCycle(num, { label, chapterId });
+       await refresh();
+     } catch (err) {
+       errMsg.textContent = `Create failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   if (!sorted.length) {
+     const empty = document.createElement('p');
+     empty.className = 'cy-muted';
+     empty.textContent = 'No downtime cycles found.';
+     wrap.appendChild(errMsg);
+     wrap.appendChild(empty);
+     return wrap;
+   }
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th>Label</th>
+     <th class="cy-col-phase">Phase</th>
+     <th class="cy-col-chapter">Chapter</th>
+     <th class="cy-col-prep">Prep Access</th>
+     <th class="cy-col-pub">Publish</th>
+     <th class="cy-col-att">Attendance</th>
+     <th class="cy-col-del"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+ 
+   sorted.forEach(cy => {
+     const tr = document.createElement('tr');
+ 
+     tr.appendChild(buildLabelCell(cy));
+     tr.appendChild(buildPhaseCell(cy));
+ 
+     const tdChapter = document.createElement('td');
+     tdChapter.appendChild(buildChapterSelect(cy, chapters));
+     tr.appendChild(tdChapter);
+ 
+     // Prep Access toggle
+     const tdAccess = document.createElement('td');
+     const accessBtn = document.createElement('button');
+     accessBtn.className = 'btn-sm';
+     accessBtn.textContent = 'Prep Access';
+     tdAccess.appendChild(accessBtn);
+     tr.appendChild(tdAccess);
+ 
+     const detailTr = document.createElement('tr');
+     detailTr.className = 'cy-detail-row is-hidden';
+     const detailTd = document.createElement('td');
+     detailTd.colSpan = 7;
+     detailTd.className = 'cy-detail-cell';
+     detailTd.appendChild(buildAccessSection(cy, charList));
+     detailTr.appendChild(detailTd);
+ 
+     accessBtn.addEventListener('click', () => {
+       const open = !detailTr.classList.contains('is-hidden');
+       detailTr.classList.toggle('is-hidden', open);
+       accessBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Publish Reports button
+     const tdPublish = document.createElement('td');
+     const publishBtn = document.createElement('button');
+     publishBtn.className = 'btn-sm';
+     publishBtn.textContent = 'Publish Reports';
+     const publishResult = document.createElement('span');
+     publishResult.className = 'cy-publish-result';
+     tdPublish.appendChild(publishBtn);
+     tdPublish.appendChild(publishResult);
+     tr.appendChild(tdPublish);
+ 
+     publishBtn.addEventListener('click', async () => {
+       publishBtn.disabled = true;
+       publishResult.className = 'cy-publish-result';
+       publishResult.textContent = 'Publishing…';
+       try {
+         const result = await apiPost('/api/downtime_cycles/' + cy._id + '/publish', {});
+         if (result.published === 0) {
+           publishResult.textContent = 'No compiled reports found.';
+         } else {
+           publishResult.classList.add('is-ok');
+           publishResult.textContent = result.published + ' report' + (result.published === 1 ? '' : 's') + ' published.';
+         }
+       } catch (err) {
+         publishResult.classList.add('is-error');
+         publishResult.textContent = 'Publish failed: ' + err.message;
+       } finally {
+         publishBtn.disabled = false;
+       }
+     });
+ 
+     // Attendance toggle
+     const tdAtt = document.createElement('td');
+     const attBtn = document.createElement('button');
+     attBtn.className = 'btn-sm';
+     attBtn.textContent = 'Attendance';
+     tdAtt.appendChild(attBtn);
+     tr.appendChild(tdAtt);
+ 
+     const attendTr = document.createElement('tr');
+     attendTr.className = 'cy-detail-row is-hidden';
+     const attendTd = document.createElement('td');
+     attendTd.colSpan = 7;
+     attendTd.className = 'cy-detail-cell';
+     attendTd.appendChild(buildAttendanceSection(cy, sessions));
+     attendTr.appendChild(attendTd);
+ 
+     attBtn.addEventListener('click', () => {
+       const open = !attendTr.classList.contains('is-hidden');
+       attendTr.classList.toggle('is-hidden', open);
+       attBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Delete cycle
+     const tdDel = document.createElement('td');
+     const delBtn = document.createElement('button');
+     delBtn.className = 'btn-sm btn-danger';
+     delBtn.textContent = 'Delete';
+     tdDel.appendChild(delBtn);
+     tr.appendChild(tdDel);
+ 
+     delBtn.addEventListener('click', async () => {
+       if (!confirm(`Delete cycle "${cy.label || cy._id}"? This cannot be undone.`)) return;
+       errMsg.classList.remove('is-visible');
+       delBtn.disabled = true;
+       try {
+         await deleteCycle(cy._id);
+         await refresh();
+       } catch (err) {
+         delBtn.disabled = false;
+         // 409 CYCLE_HAS_SUBMISSIONS surfaces here with the server message.
+         errMsg.textContent = err.message || 'Delete failed.';
+         errMsg.classList.add('is-visible');
+       }
+     });
+ 
+     tbody.appendChild(tr);
+     tbody.appendChild(detailTr);
+     tbody.appendChild(attendTr);
+   });
+ 
+   table.appendChild(tbody);
+   wrap.appendChild(errMsg);
+   wrap.appendChild(table);
+   return wrap;
+ }
+

 ❯ tests/epic.708.3-cycle-phase-controls.test.js:42:25
     40|
     41|   it('exports setGamePhase function', () => {
     42|     expect(CYCLE_VIEWS).toContain('setGamePhase');
       |                         ^
     43|   });
     44|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/12]⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > uses data-phase attribute on phase buttons
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'data-phase'

- Expected
+ Received

- data-phase
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Attendance section ───────────────────────────────────────────────────────
+ 
+ function buildAttendanceSection(cy, sessions) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-attend';
+ 
+   const selectWrap = document.createElement('div');
+   selectWrap.className = 'cy-attend-head';
+ 
+   const lbl = document.createElement('label');
+   lbl.className = 'cy-attend-lbl';
+   lbl.textContent = 'Linked Session:';
+ 
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-attend-select';
+ 
+   const blank = document.createElement('option');
+   blank.value = '';
+   blank.textContent = '— not linked —';
+   sel.appendChild(blank);
+ 
+   const sorted = [...sessions].sort((a, b) => (a.game_number ?? 999) - (b.game_number ?? 999));
+   sorted.forEach(s => {
+     const opt = document.createElement('option');
+     opt.value = String(s._id);
+     let label = s.game_number ? 'Game ' + s.game_number : '';
+     if (s.title) label += (label ? ' — ' : '') + s.title;
+     if (!label) label = s.session_date || String(s._id);
+     opt.textContent = label;
+     sel.appendChild(opt);
+   });
+ 
+   sel.value = cy.session_id || '';
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   selectWrap.appendChild(lbl);
+   selectWrap.appendChild(sel);
+   selectWrap.appendChild(errEl);
+   wrap.appendChild(selectWrap);
+ 
+   const tableWrap = document.createElement('div');
+   wrap.appendChild(tableWrap);
+ 
+   function renderTable() {
+     tableWrap.innerHTML = '';
+     const session = sessions.find(s => String(s._id) === sel.value);
+     if (!session) return;
+     const att = session.attendance || [];
+     if (!att.length) {
+       const msg = document.createElement('p');
+       msg.className = 'cy-muted cy-attend-empty';
+       msg.textContent = 'No attendance recorded for this session.';
+       tableWrap.appendChild(msg);
+       return;
+     }
+ 
+     const rows = [...att].sort((a, b) => {
+       const na = (a.character_display || a.character_name || '').toLowerCase();
+       const nb = (b.character_display || b.character_name || '').toLowerCase();
+       return na < nb ? -1 : na > nb ? 1 : 0;
+     });
+ 
+     const table = document.createElement('table');
+     table.className = 'infl-table cy-attend-table';
+     table.innerHTML = `<thead><tr>
+       <th>Character</th>
+       <th class="cy-att-c">Attend</th>
+       <th class="cy-att-c">Costuming</th>
+       <th class="cy-att-c">DT</th>
+       <th class="cy-att-c">Extra</th>
+       <th class="cy-att-c">XP</th>
+     </tr></thead>`;
+ 
+     const tbody = document.createElement('tbody');
+     let totAtt = 0, totCos = 0, totDT = 0, totExtra = 0, totXP = 0;
+ 
+     rows.forEach(a => {
+       const xp = (a.attended ? 1 : 0) + (a.costuming ? 1 : 0) + (a.downtime ? 1 : 0) + (a.extra || 0);
+       totAtt   += a.attended  ? 1 : 0;
+       totCos   += a.costuming ? 1 : 0;
+       totDT    += a.downtime  ? 1 : 0;
+       totExtra += (a.extra || 0);
+       totXP    += xp;
+ 
+       const name = a.character_display || a.character_name || a.character_id || '?';
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${esc(name)}</td>
+         <td class="cy-att-c">${a.attended  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.costuming ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.downtime  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.extra || 0}</td>
+         <td class="cy-att-c cy-att-xp">${xp}</td>`;
+       tbody.appendChild(tr);
+     });
+ 
+     const totTr = document.createElement('tr');
+     totTr.className = 'cy-att-total';
+     totTr.innerHTML = `
+       <td class="cy-muted">Total (${rows.length})</td>
+       <td class="cy-att-c">${totAtt}</td>
+       <td class="cy-att-c">${totCos}</td>
+       <td class="cy-att-c">${totDT}</td>
+       <td class="cy-att-c">${totExtra}</td>
+       <td class="cy-att-c">${totXP}</td>`;
+     tbody.appendChild(totTr);
+ 
+     table.appendChild(tbody);
+     tableWrap.appendChild(table);
+   }
+ 
+   renderTable();
+ 
+   sel.addEventListener('change', async () => {
+     errEl.classList.remove('is-visible');
+     const newId = sel.value || null;
+     try {
+       await apiPut('/api/downtime_cycles/' + cy._id, { session_id: newId });
+       cy.session_id = newId;
+       renderTable();
+     } catch (err) {
+       sel.value = cy.session_id || '';
+       errEl.textContent = 'Link failed: ' + err.message;
+       errEl.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Game Cycles panel ───────────────────────────────────────────────────────
+ 
+ // Build a chapter <select>. Options-only; no persistence handler — used for
+ // the add-cycle form (which has no cycle to write to yet).
+ function buildChapterPicker(chapters, selectedId = '') {
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-chapter-select';
+   const none = document.createElement('option');
+   none.value = '';
+   none.textContent = '— none —';
+   sel.appendChild(none);
+   [...chapters].sort((a, b) => a.number - b.number).forEach(ch => {
+     const opt = document.createElement('option');
+     opt.value = String(ch._id);
+     opt.textContent = `${ch.number} — ${ch.label}`;
+     sel.appendChild(opt);
+   });
+   sel.value = selectedId ? String(selectedId) : '';
+   return sel;
+ }
+ 
+ // Row-level chapter select: persists chapter_id to an existing cycle on change.
+ function buildChapterSelect(cy, chapters) {
+   const sel = buildChapterPicker(chapters, cy.chapter_id);
+   sel.addEventListener('change', async () => {
+     const val = sel.value || null;
+     try {
+       await updateCycle(cy._id, { chapter_id: val });
+       cy.chapter_id = val;
+       renderRibbon();
+     } catch (err) {
+       sel.value = cy.chapter_id ? String(cy.chapter_id) : '';
+     }
+   });
+   return sel;
+ }
+ 
+ function buildLabelCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-label-cell';
+ 
+   function renderView() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const span = document.createElement('span');
+     span.className = 'cy-label-text';
+     span.textContent = cy.label || String(cy._id);
+     const editBtn = document.createElement('button');
+     editBtn.className = 'cy-icon-btn';
+     editBtn.title = 'Edit label';
+     editBtn.textContent = '✎';
+     editBtn.addEventListener('click', renderEdit);
+     inner.appendChild(span);
+     inner.appendChild(editBtn);
+     td.appendChild(inner);
+   }
+ 
+   function renderEdit() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const input = document.createElement('input');
+     input.type = 'text';
+     input.className = 'form-input cy-label-input';
+     input.value = cy.label || '';
+     const save = document.createElement('button');
+     save.className = 'btn-sm';
+     save.textContent = 'Save';
+     const cancel = document.createElement('button');
+     cancel.className = 'btn-sm btn-muted';
+     cancel.textContent = 'Cancel';
+ 
+     async function doSave() {
+       const newLabel = input.value.trim();
+       if (!newLabel) { input.focus(); return; }
+       try {
+         await updateCycle(cy._id, { label: newLabel });
+         cy.label = newLabel;
+         renderView();
+         renderRibbon();
+       } catch (_err) {
+         input.classList.add('is-error');
+       }
+     }
+     save.addEventListener('click', doSave);
+     cancel.addEventListener('click', renderView);
+     input.addEventListener('keydown', e => {
+       if (e.key === 'Enter') doSave();
+       else if (e.key === 'Escape') renderView();
+     });
+ 
+     inner.appendChild(input);
+     inner.appendChild(save);
+     inner.appendChild(cancel);
+     td.appendChild(inner);
+     input.focus();
+     input.select();
+   }
+ 
+   renderView();
+   return td;
+ }
+ 
+ function buildCyclesPanel(cycles, chapters, charList = [], sessions = []) {
+   const sorted = [...cycles].sort((a, b) => (a.game_number ?? 0) - (b.game_number ?? 0));
+ 
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Game Cycles</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Cycle';
+   header.appendChild(addBtn);
+   wrap.appendChild(header);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   // Inline add-cycle form
+   const addForm = document.createElement('div');
+   addForm.className = 'cy-inline-form';
+   const nextGame = (sorted.reduce((mx, c) => Math.max(mx, c.game_number ?? 0), 0) || 0) + 1;
+   addForm.innerHTML = `
+     <input id="new-cy-num" type="number" min="1" placeholder="Game #" value="${nextGame}" class="form-input cy-input-num">
+     <input id="new-cy-label" type="text" placeholder="Label (e.g. Downtime 5)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-cy-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-cy-cancel">Cancel</button>`;
+   const chapterPick = buildChapterPicker(chapters);
+   chapterPick.id = 'new-cy-chapter';
+   addForm.insertBefore(chapterPick, addForm.querySelector('#new-cy-save'));
+   wrap.appendChild(addForm);
+ 
+   addBtn.addEventListener('click', () => {
+     addForm.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     addForm.querySelector('#new-cy-label').value = `Downtime ${nextGame}`;
+     addForm.querySelector('#new-cy-label').focus();
+   });
+   addForm.querySelector('#new-cy-cancel').addEventListener('click', () => {
+     addForm.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+   addForm.querySelector('#new-cy-save').addEventListener('click', async () => {
+     const num = parseInt(addForm.querySelector('#new-cy-num').value, 10);
+     const label = addForm.querySelector('#new-cy-label').value.trim();
+     const chapterId = addForm.querySelector('#new-cy-chapter').value || null;
+     if (!num || !label) {
+       errMsg.textContent = 'Game number and label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       await createCycle(num, { label, chapterId });
+       await refresh();
+     } catch (err) {
+       errMsg.textContent = `Create failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   if (!sorted.length) {
+     const empty = document.createElement('p');
+     empty.className = 'cy-muted';
+     empty.textContent = 'No downtime cycles found.';
+     wrap.appendChild(errMsg);
+     wrap.appendChild(empty);
+     return wrap;
+   }
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th>Label</th>
+     <th class="cy-col-phase">Phase</th>
+     <th class="cy-col-chapter">Chapter</th>
+     <th class="cy-col-prep">Prep Access</th>
+     <th class="cy-col-pub">Publish</th>
+     <th class="cy-col-att">Attendance</th>
+     <th class="cy-col-del"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+ 
+   sorted.forEach(cy => {
+     const tr = document.createElement('tr');
+ 
+     tr.appendChild(buildLabelCell(cy));
+     tr.appendChild(buildPhaseCell(cy));
+ 
+     const tdChapter = document.createElement('td');
+     tdChapter.appendChild(buildChapterSelect(cy, chapters));
+     tr.appendChild(tdChapter);
+ 
+     // Prep Access toggle
+     const tdAccess = document.createElement('td');
+     const accessBtn = document.createElement('button');
+     accessBtn.className = 'btn-sm';
+     accessBtn.textContent = 'Prep Access';
+     tdAccess.appendChild(accessBtn);
+     tr.appendChild(tdAccess);
+ 
+     const detailTr = document.createElement('tr');
+     detailTr.className = 'cy-detail-row is-hidden';
+     const detailTd = document.createElement('td');
+     detailTd.colSpan = 7;
+     detailTd.className = 'cy-detail-cell';
+     detailTd.appendChild(buildAccessSection(cy, charList));
+     detailTr.appendChild(detailTd);
+ 
+     accessBtn.addEventListener('click', () => {
+       const open = !detailTr.classList.contains('is-hidden');
+       detailTr.classList.toggle('is-hidden', open);
+       accessBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Publish Reports button
+     const tdPublish = document.createElement('td');
+     const publishBtn = document.createElement('button');
+     publishBtn.className = 'btn-sm';
+     publishBtn.textContent = 'Publish Reports';
+     const publishResult = document.createElement('span');
+     publishResult.className = 'cy-publish-result';
+     tdPublish.appendChild(publishBtn);
+     tdPublish.appendChild(publishResult);
+     tr.appendChild(tdPublish);
+ 
+     publishBtn.addEventListener('click', async () => {
+       publishBtn.disabled = true;
+       publishResult.className = 'cy-publish-result';
+       publishResult.textContent = 'Publishing…';
+       try {
+         const result = await apiPost('/api/downtime_cycles/' + cy._id + '/publish', {});
+         if (result.published === 0) {
+           publishResult.textContent = 'No compiled reports found.';
+         } else {
+           publishResult.classList.add('is-ok');
+           publishResult.textContent = result.published + ' report' + (result.published === 1 ? '' : 's') + ' published.';
+         }
+       } catch (err) {
+         publishResult.classList.add('is-error');
+         publishResult.textContent = 'Publish failed: ' + err.message;
+       } finally {
+         publishBtn.disabled = false;
+       }
+     });
+ 
+     // Attendance toggle
+     const tdAtt = document.createElement('td');
+     const attBtn = document.createElement('button');
+     attBtn.className = 'btn-sm';
+     attBtn.textContent = 'Attendance';
+     tdAtt.appendChild(attBtn);
+     tr.appendChild(tdAtt);
+ 
+     const attendTr = document.createElement('tr');
+     attendTr.className = 'cy-detail-row is-hidden';
+     const attendTd = document.createElement('td');
+     attendTd.colSpan = 7;
+     attendTd.className = 'cy-detail-cell';
+     attendTd.appendChild(buildAttendanceSection(cy, sessions));
+     attendTr.appendChild(attendTd);
+ 
+     attBtn.addEventListener('click', () => {
+       const open = !attendTr.classList.contains('is-hidden');
+       attendTr.classList.toggle('is-hidden', open);
+       attBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Delete cycle
+     const tdDel = document.createElement('td');
+     const delBtn = document.createElement('button');
+     delBtn.className = 'btn-sm btn-danger';
+     delBtn.textContent = 'Delete';
+     tdDel.appendChild(delBtn);
+     tr.appendChild(tdDel);
+ 
+     delBtn.addEventListener('click', async () => {
+       if (!confirm(`Delete cycle "${cy.label || cy._id}"? This cannot be undone.`)) return;
+       errMsg.classList.remove('is-visible');
+       delBtn.disabled = true;
+       try {
+         await deleteCycle(cy._id);
+         await refresh();
+       } catch (err) {
+         delBtn.disabled = false;
+         // 409 CYCLE_HAS_SUBMISSIONS surfaces here with the server message.
+         errMsg.textContent = err.message || 'Delete failed.';
+         errMsg.classList.add('is-visible');
+       }
+     });
+ 
+     tbody.appendChild(tr);
+     tbody.appendChild(detailTr);
+     tbody.appendChild(attendTr);
+   });
+ 
+   table.appendChild(tbody);
+   wrap.appendChild(errMsg);
+   wrap.appendChild(table);
+   return wrap;
+ }
+

 ❯ tests/epic.708.3-cycle-phase-controls.test.js:54:25
     52|
     53|   it('uses data-phase attribute on phase buttons', () => {
     54|     expect(CYCLE_VIEWS).toContain('data-phase');
       |                         ^
     55|   });
     56|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[8/12]⎯

 FAIL  tests/epic.708.3-cycle-phase-controls.test.js > epic.708.3 — cycle-views.js: phase control UI > highlights active phase with gold2 colour
AssertionError: expected 'import { apiGet, apiPost, apiDelete, …' to contain 'gold2'

- Expected
+ Received

- gold2
+ import { apiGet, apiPost, apiDelete, apiPut } from '../data/api.js';
+ import { createCycle, updateCycle, deleteCycle, deriveCycleStatus, getSubmissionsForCycle, zeroSubmissionFlipWarning, zeroSubmissionFlipMessage, setCyclePhase } from '../downtime/db.js';
+ import { resetOnTransition } from '../downtime/cycle-phase.js';
+ 
+ const PHASE_LABELS = {
+   game:       'Game',
+   downtime:   'Downtime',
+   processing: 'Processing',
+   prep:       'Prep',
+ };
+ 
+ // CM-1 (#1028): buttons follow the cycle order (cycle-model.md Rev 2 section
+ // 1): downtime, processing, prep, game. Prep is the game-prep window in which
+ // feeding is open. CM-5a: entering PREP is what resets the live tracker (once
+ // per chapter, from a preceding phase only); prep -> game is non-destructive
+ // so the prep week's confirmed feeds survive into the session.
+ const PHASES = ['downtime', 'processing', 'prep', 'game'];
+ 
+ // The phase a row's UI reflects: the new `phase` field when set, else the
+ // legacy game_phase override, else none. (game_phase's vocabulary is a subset
+ // of phase's, so the labels map covers both.) Guarded against junk values
+ // from hand-edited documents - anything outside the label map renders as
+ // "no phase" rather than leaking into labels and CSS class names (Codex
+ // review finding, 2026-08-10).
+ function uiPhase(cy) {
+   const p = cy.phase || cy.game_phase || null;
+   return PHASE_LABELS[p] ? p : null;
+ }
+ 
+ // Module-level view state so the status ribbon can refresh after any
+ // label / chapter / phase / add / delete mutation without a full re-fetch.
+ const view = { chapters: [], cycles: [], sessions: [], charList: [], ribbonEl: null };
+ 
+ // (byIdDesc removed by CM-1: _id is creation order, not game order - proven
+ // wrong for ordering in this data. All cycle ordering uses game_number.)
+ 
+ export async function initCycleView(charList) {
+   const el = document.getElementById('cycle-content');
+   el.innerHTML = '<p class="placeholder">Loading…</p>';
+ 
+   let chapters, cycles, sessions;
+   try {
+     [chapters, cycles, sessions] = await Promise.all([
+       apiGet('/api/chapters'),
+       apiGet('/api/downtime_cycles'),
+       apiGet('/api/game_sessions'),
+     ]);
+   } catch (err) {
+     el.innerHTML = `<p class="cy-error">Failed to load cycle data: ${err.message}</p>`;
+     return;
+   }
+ 
+   view.chapters = chapters;
+   view.cycles = cycles;
+   view.sessions = sessions;
+   view.charList = charList;
+ 
+   el.innerHTML = '';
+   el.appendChild(buildRibbon());
+   el.appendChild(buildChaptersPanel(chapters));
+   el.appendChild(buildCyclesPanel(cycles, chapters, charList, sessions));
+ }
+ 
+ /** Re-fetch and rebuild the whole tab (used after add / delete). */
+ async function refresh() {
+   await initCycleView(view.charList);
+ }
+ 
+ // ── Status ribbon ─────────────────────────────────────────────────────────────
+ 
+ function buildRibbon() {
+   const el = document.createElement('div');
+   el.className = 'cy-ribbon';
+   view.ribbonEl = el;
+   renderRibbon();
+   return el;
+ }
+ 
+ /**
+  * Current cycle: the highest-game_number cycle carrying any phase, else the
+  * highest-game_number non-closed cycle. game_number is THE ordering field -
+  * never _id/creation order, which is proven wrong for game order in this
+  * data (2,3,1,4,5). Codex review finding, 2026-08-10: the previous
+  * game-phase-first, _id-ordered logic let any stale game_phase override
+  * outrank the actual current cycle in the ST's ribbon.
+  */
+ const byGameNumberDesc = (a, b) => (b.game_number || 0) - (a.game_number || 0);
+ function deriveCurrentCycle() {
+   const cycles = view.cycles || [];
+   const phased = cycles.filter(c => uiPhase(c)).sort(byGameNumberDesc)[0];
+   if (phased) return phased;
+   const nonClosed = cycles.filter(c => deriveCycleStatus(c) !== 'closed').sort(byGameNumberDesc)[0];
+   return nonClosed || null;
+ }
+ 
+ function renderRibbon() {
+   const el = view.ribbonEl;
+   if (!el) return;
+   const cy = deriveCurrentCycle();
+ 
+   if (!cy) {
+     el.innerHTML = '<span class="cy-ribbon-empty">No active cycle.</span>';
+     return;
+   }
+ 
+   const chapter = cy.chapter_id
+     ? view.chapters.find(ch => String(ch._id) === String(cy.chapter_id))
+     : null;
+   const chapterText = chapter ? `${chapter.number} — ${chapter.label}` : 'No chapter';
+   const _ribbonPhase = uiPhase(cy);
+   const phaseText = _ribbonPhase ? PHASE_LABELS[_ribbonPhase] : 'No phase set';
+   const phaseMod = _ribbonPhase ? ` cy-phase--${_ribbonPhase}` : ' cy-phase--none';
+ 
+   el.innerHTML = `
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Chapter</span>
+       <span class="cy-ribbon-val">${esc(chapterText)}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Game Cycle</span>
+       <span class="cy-ribbon-val">${esc(cy.label || String(cy._id))}</span>
+     </div>
+     <div class="cy-ribbon-item">
+       <span class="cy-ribbon-label">Phase</span>
+       <span class="cy-ribbon-chip${phaseMod}">${esc(phaseText)}</span>
+     </div>`;
+ }
+ 
+ function esc(s) {
+   return String(s ?? '').replace(/[&<>"']/g, c => (
+     { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
+   ));
+ }
+ 
+ // ── Chapters panel ──────────────────────────────────────────────────────────
+ 
+ function buildChaptersPanel(chapters) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Chapters</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Chapter';
+   header.appendChild(addBtn);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   wrap.appendChild(header);
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th class="cy-col-num">#</th>
+     <th>Label</th>
+     <th class="cy-col-act"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+   table.appendChild(tbody);
+   wrap.appendChild(table);
+   wrap.appendChild(errMsg);
+ 
+   function renderRows(list) {
+     tbody.innerHTML = '';
+     if (!list.length) {
+       tbody.innerHTML = '<tr><td colspan="3" class="cy-empty-cell">No chapters yet.</td></tr>';
+       return;
+     }
+     list.forEach(ch => {
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${ch.number}</td>
+         <td>${esc(ch.label)}</td>
+         <td><button class="btn-sm btn-danger" data-id="${ch._id}">Delete</button></td>`;
+       tr.querySelector('button').addEventListener('click', async () => {
+         errMsg.classList.remove('is-visible');
+         try {
+           await apiDelete(`/api/chapters/${ch._id}`);
+           renderRows(list.filter(c => c._id !== ch._id));
+         } catch (err) {
+           if (err.message && err.message.includes('cycle')) {
+             errMsg.textContent = 'Chapter is linked to cycle(s) — remove the link before deleting.';
+           } else {
+             errMsg.textContent = `Delete failed: ${err.message}`;
+           }
+           errMsg.classList.add('is-visible');
+         }
+       });
+       tbody.appendChild(tr);
+     });
+   }
+ 
+   renderRows(chapters);
+ 
+   // Inline new-chapter form
+   const form = document.createElement('div');
+   form.className = 'cy-inline-form';
+   form.innerHTML = `
+     <input id="new-ch-num" type="number" min="1" placeholder="Number" class="form-input cy-input-num">
+     <input id="new-ch-label" type="text" placeholder="Label (e.g. Chapter Two: The Price of Power)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-ch-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-ch-cancel">Cancel</button>`;
+   wrap.appendChild(form);
+ 
+   addBtn.addEventListener('click', () => {
+     form.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     form.querySelector('#new-ch-num').value = '';
+     form.querySelector('#new-ch-label').value = '';
+     form.querySelector('#new-ch-num').focus();
+   });
+ 
+   form.querySelector('#new-ch-cancel').addEventListener('click', () => {
+     form.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+ 
+   form.querySelector('#new-ch-save').addEventListener('click', async () => {
+     const number = parseInt(form.querySelector('#new-ch-num').value, 10);
+     const label  = form.querySelector('#new-ch-label').value.trim();
+     if (!number || !label) {
+       errMsg.textContent = 'Both Number and Label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       const created = await apiPost('/api/chapters', { number, label });
+       chapters = [...chapters, created].sort((a, b) => a.number - b.number);
+       view.chapters = chapters;
+       renderRows(chapters);
+       form.classList.remove('is-open');
+       addBtn.classList.remove('is-hidden');
+       renderRibbon();
+     } catch (err) {
+       errMsg.textContent = `Save failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Phase controls ───────────────────────────────────────────────────────────
+ 
+ // Write a phase to a cycle. `phaseOrNull === null` clears the phase (neutral).
+ // The live-tracker reset is decided by resetOnTransition (CM-5a): entering
+ // prep from a preceding phase, or entering game from anywhere except prep.
+ // Clearing to neutral never resets. Returns false if the ST cancels either
+ // the zero-submission flip warning or the tracker-reset confirmation.
+ async function writePhase(cy, phaseOrNull) {
+   if (phaseOrNull === 'game') {
+     // #1003: warn if flipping an empty cycle to game while another live cycle
+     // holds submissions (feeding pulls from the game-phase cycle).
+     const warn = await zeroSubmissionFlipWarning(
+       cy, view.cycles || [], async id => (await getSubmissionsForCycle(id)).length);
+     if (warn && !confirm(zeroSubmissionFlipMessage(warn))) return false;
+   }
+   // CM-5a: the slate-wipe moves to PREP entry, so feed rolls made during the
+   // prep week survive into game (prep -> game is non-destructive). Entering
+   // game from any non-prep state keeps the legacy reset. Cancelling the
+   // dialog aborts the phase change entirely.
+   if (resetOnTransition(uiPhase(cy), phaseOrNull)) {
+     const label = PHASE_LABELS[phaseOrNull];
+     if (!confirm(`Setting to ${label} phase will reset the live tracker (all characters reload with default states). Continue?`)) return false;
+     try {
+       await apiDelete('/api/tracker_state');
+     } catch (err) {
+       throw new Error('Tracker reset failed: ' + err.message);
+     }
+   }
+   // CM-1 (#1028): the canonical writer sets all three representations in one
+   // PUT (phase + game_phase + status, per the cycle-phase.js mirror table),
+   // absorbing the #1001 status-alongside fix and extending it to the new
+   // `phase` field. `null` clears to neutral, preserving #918 semantics.
+   await setCyclePhase(cy, phaseOrNull);
+   return true;
+ }
+ 
+ function buildPhaseCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-phase-cell';
+ 
+   const group = document.createElement('div');
+   group.className = 'cy-phase-group';
+   td.appendChild(group);
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   PHASES.forEach(phase => {
+     const btn = document.createElement('button');
+     btn.className = 'cy-phase-btn' + (uiPhase(cy) === phase ? ' is-active' : '');
+     btn.textContent = PHASE_LABELS[phase];
+     btn.dataset.phase = phase;
+     btn.title = uiPhase(cy) === phase ? 'Click to clear this phase' : `Set ${PHASE_LABELS[phase]} phase`;
+ 
+     btn.addEventListener('click', async () => {
+       errEl.classList.remove('is-visible');
+       // Active phase toggles OFF to neutral; otherwise switch to the clicked phase.
+       const target = (uiPhase(cy) === phase) ? null : phase;
+       try {
+         const ok = await writePhase(cy, target);
+         if (!ok) return;
+         group.querySelectorAll('.cy-phase-btn').forEach(b => {
+           const active = b.dataset.phase === uiPhase(cy);
+           b.classList.toggle('is-active', active);
+           b.title = active ? 'Click to clear this phase' : `Set ${PHASE_LABELS[b.dataset.phase]} phase`;
+         });
+         renderRibbon();
+       } catch (err) {
+         errEl.textContent = 'Phase change failed: ' + err.message;
+         errEl.classList.add('is-visible');
+       }
+     });
+     group.appendChild(btn);
+   });
+ 
+   td.appendChild(errEl);
+   return td;
+ }
+ 
+ // ── Prep Access section ──────────────────────────────────────────────────────
+ 
+ function buildAccessSection(cy, charList) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-detail-scroll';
+ 
+   const activeChars = charList
+     .filter(c => !c.retired)
+     .sort((a, b) => (a.moniker || a.name || '').localeCompare(b.moniker || b.name || ''));
+ 
+   if (!activeChars.length) {
+     wrap.textContent = 'No active characters.';
+     wrap.classList.add('cy-muted');
+     return wrap;
+   }
+ 
+   const oowIds = new Set((cy.out_of_window_player_ids || []).map(String));
+ 
+   activeChars.forEach(c => {
+     const id = String(c._id);
+     const label = document.createElement('label');
+     label.className = 'cy-check-label';
+ 
+     const cb = document.createElement('input');
+     cb.type = 'checkbox';
+     cb.checked = oowIds.has(id);
+ 
+     const span = document.createElement('span');
+     span.textContent = c.moniker || c.name || String(c._id);
+ 
+     label.appendChild(cb);
+     label.appendChild(span);
+     wrap.appendChild(label);
+ 
+     cb.addEventListener('change', async () => {
+       const current = new Set((cy.out_of_window_player_ids || []).map(String));
+       if (cb.checked) current.add(id); else current.delete(id);
+       const updated = [...current];
+       try {
+         await apiPut('/api/downtime_cycles/' + cy._id, { out_of_window_player_ids: updated });
+         cy.out_of_window_player_ids = updated;
+       } catch (_err) {
+         cb.checked = !cb.checked;
+       }
+     });
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Attendance section ───────────────────────────────────────────────────────
+ 
+ function buildAttendanceSection(cy, sessions) {
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-attend';
+ 
+   const selectWrap = document.createElement('div');
+   selectWrap.className = 'cy-attend-head';
+ 
+   const lbl = document.createElement('label');
+   lbl.className = 'cy-attend-lbl';
+   lbl.textContent = 'Linked Session:';
+ 
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-attend-select';
+ 
+   const blank = document.createElement('option');
+   blank.value = '';
+   blank.textContent = '— not linked —';
+   sel.appendChild(blank);
+ 
+   const sorted = [...sessions].sort((a, b) => (a.game_number ?? 999) - (b.game_number ?? 999));
+   sorted.forEach(s => {
+     const opt = document.createElement('option');
+     opt.value = String(s._id);
+     let label = s.game_number ? 'Game ' + s.game_number : '';
+     if (s.title) label += (label ? ' — ' : '') + s.title;
+     if (!label) label = s.session_date || String(s._id);
+     opt.textContent = label;
+     sel.appendChild(opt);
+   });
+ 
+   sel.value = cy.session_id || '';
+ 
+   const errEl = document.createElement('span');
+   errEl.className = 'cy-error cy-error--inline';
+ 
+   selectWrap.appendChild(lbl);
+   selectWrap.appendChild(sel);
+   selectWrap.appendChild(errEl);
+   wrap.appendChild(selectWrap);
+ 
+   const tableWrap = document.createElement('div');
+   wrap.appendChild(tableWrap);
+ 
+   function renderTable() {
+     tableWrap.innerHTML = '';
+     const session = sessions.find(s => String(s._id) === sel.value);
+     if (!session) return;
+     const att = session.attendance || [];
+     if (!att.length) {
+       const msg = document.createElement('p');
+       msg.className = 'cy-muted cy-attend-empty';
+       msg.textContent = 'No attendance recorded for this session.';
+       tableWrap.appendChild(msg);
+       return;
+     }
+ 
+     const rows = [...att].sort((a, b) => {
+       const na = (a.character_display || a.character_name || '').toLowerCase();
+       const nb = (b.character_display || b.character_name || '').toLowerCase();
+       return na < nb ? -1 : na > nb ? 1 : 0;
+     });
+ 
+     const table = document.createElement('table');
+     table.className = 'infl-table cy-attend-table';
+     table.innerHTML = `<thead><tr>
+       <th>Character</th>
+       <th class="cy-att-c">Attend</th>
+       <th class="cy-att-c">Costuming</th>
+       <th class="cy-att-c">DT</th>
+       <th class="cy-att-c">Extra</th>
+       <th class="cy-att-c">XP</th>
+     </tr></thead>`;
+ 
+     const tbody = document.createElement('tbody');
+     let totAtt = 0, totCos = 0, totDT = 0, totExtra = 0, totXP = 0;
+ 
+     rows.forEach(a => {
+       const xp = (a.attended ? 1 : 0) + (a.costuming ? 1 : 0) + (a.downtime ? 1 : 0) + (a.extra || 0);
+       totAtt   += a.attended  ? 1 : 0;
+       totCos   += a.costuming ? 1 : 0;
+       totDT    += a.downtime  ? 1 : 0;
+       totExtra += (a.extra || 0);
+       totXP    += xp;
+ 
+       const name = a.character_display || a.character_name || a.character_id || '?';
+       const tr = document.createElement('tr');
+       tr.innerHTML = `
+         <td>${esc(name)}</td>
+         <td class="cy-att-c">${a.attended  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.costuming ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.downtime  ? '●' : '○'}</td>
+         <td class="cy-att-c">${a.extra || 0}</td>
+         <td class="cy-att-c cy-att-xp">${xp}</td>`;
+       tbody.appendChild(tr);
+     });
+ 
+     const totTr = document.createElement('tr');
+     totTr.className = 'cy-att-total';
+     totTr.innerHTML = `
+       <td class="cy-muted">Total (${rows.length})</td>
+       <td class="cy-att-c">${totAtt}</td>
+       <td class="cy-att-c">${totCos}</td>
+       <td class="cy-att-c">${totDT}</td>
+       <td class="cy-att-c">${totExtra}</td>
+       <td class="cy-att-c">${totXP}</td>`;
+     tbody.appendChild(totTr);
+ 
+     table.appendChild(tbody);
+     tableWrap.appendChild(table);
+   }
+ 
+   renderTable();
+ 
+   sel.addEventListener('change', async () => {
+     errEl.classList.remove('is-visible');
+     const newId = sel.value || null;
+     try {
+       await apiPut('/api/downtime_cycles/' + cy._id, { session_id: newId });
+       cy.session_id = newId;
+       renderTable();
+     } catch (err) {
+       sel.value = cy.session_id || '';
+       errEl.textContent = 'Link failed: ' + err.message;
+       errEl.classList.add('is-visible');
+     }
+   });
+ 
+   return wrap;
+ }
+ 
+ // ── Game Cycles panel ───────────────────────────────────────────────────────
+ 
+ // Build a chapter <select>. Options-only; no persistence handler — used for
+ // the add-cycle form (which has no cycle to write to yet).
+ function buildChapterPicker(chapters, selectedId = '') {
+   const sel = document.createElement('select');
+   sel.className = 'form-select cy-chapter-select';
+   const none = document.createElement('option');
+   none.value = '';
+   none.textContent = '— none —';
+   sel.appendChild(none);
+   [...chapters].sort((a, b) => a.number - b.number).forEach(ch => {
+     const opt = document.createElement('option');
+     opt.value = String(ch._id);
+     opt.textContent = `${ch.number} — ${ch.label}`;
+     sel.appendChild(opt);
+   });
+   sel.value = selectedId ? String(selectedId) : '';
+   return sel;
+ }
+ 
+ // Row-level chapter select: persists chapter_id to an existing cycle on change.
+ function buildChapterSelect(cy, chapters) {
+   const sel = buildChapterPicker(chapters, cy.chapter_id);
+   sel.addEventListener('change', async () => {
+     const val = sel.value || null;
+     try {
+       await updateCycle(cy._id, { chapter_id: val });
+       cy.chapter_id = val;
+       renderRibbon();
+     } catch (err) {
+       sel.value = cy.chapter_id ? String(cy.chapter_id) : '';
+     }
+   });
+   return sel;
+ }
+ 
+ function buildLabelCell(cy) {
+   const td = document.createElement('td');
+   td.className = 'cy-label-cell';
+ 
+   function renderView() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const span = document.createElement('span');
+     span.className = 'cy-label-text';
+     span.textContent = cy.label || String(cy._id);
+     const editBtn = document.createElement('button');
+     editBtn.className = 'cy-icon-btn';
+     editBtn.title = 'Edit label';
+     editBtn.textContent = '✎';
+     editBtn.addEventListener('click', renderEdit);
+     inner.appendChild(span);
+     inner.appendChild(editBtn);
+     td.appendChild(inner);
+   }
+ 
+   function renderEdit() {
+     td.innerHTML = '';
+     const inner = document.createElement('div');
+     inner.className = 'cy-label-inner';
+     const input = document.createElement('input');
+     input.type = 'text';
+     input.className = 'form-input cy-label-input';
+     input.value = cy.label || '';
+     const save = document.createElement('button');
+     save.className = 'btn-sm';
+     save.textContent = 'Save';
+     const cancel = document.createElement('button');
+     cancel.className = 'btn-sm btn-muted';
+     cancel.textContent = 'Cancel';
+ 
+     async function doSave() {
+       const newLabel = input.value.trim();
+       if (!newLabel) { input.focus(); return; }
+       try {
+         await updateCycle(cy._id, { label: newLabel });
+         cy.label = newLabel;
+         renderView();
+         renderRibbon();
+       } catch (_err) {
+         input.classList.add('is-error');
+       }
+     }
+     save.addEventListener('click', doSave);
+     cancel.addEventListener('click', renderView);
+     input.addEventListener('keydown', e => {
+       if (e.key === 'Enter') doSave();
+       else if (e.key === 'Escape') renderView();
+     });
+ 
+     inner.appendChild(input);
+     inner.appendChild(save);
+     inner.appendChild(cancel);
+     td.appendChild(inner);
+     input.focus();
+     input.select();
+   }
+ 
+   renderView();
+   return td;
+ }
+ 
+ function buildCyclesPanel(cycles, chapters, charList = [], sessions = []) {
+   const sorted = [...cycles].sort((a, b) => (a.game_number ?? 0) - (b.game_number ?? 0));
+ 
+   const wrap = document.createElement('div');
+   wrap.className = 'cy-section';
+ 
+   const header = document.createElement('div');
+   header.className = 'cy-section-head';
+   header.innerHTML = '<h3 class="cy-section-title">Game Cycles</h3>';
+ 
+   const addBtn = document.createElement('button');
+   addBtn.className = 'btn-sm';
+   addBtn.textContent = '+ New Cycle';
+   header.appendChild(addBtn);
+   wrap.appendChild(header);
+ 
+   const errMsg = document.createElement('p');
+   errMsg.className = 'cy-error cy-error--inline';
+ 
+   // Inline add-cycle form
+   const addForm = document.createElement('div');
+   addForm.className = 'cy-inline-form';
+   const nextGame = (sorted.reduce((mx, c) => Math.max(mx, c.game_number ?? 0), 0) || 0) + 1;
+   addForm.innerHTML = `
+     <input id="new-cy-num" type="number" min="1" placeholder="Game #" value="${nextGame}" class="form-input cy-input-num">
+     <input id="new-cy-label" type="text" placeholder="Label (e.g. Downtime 5)" class="form-input cy-input-grow">
+     <button class="btn-sm" id="new-cy-save">Save</button>
+     <button class="btn-sm btn-muted" id="new-cy-cancel">Cancel</button>`;
+   const chapterPick = buildChapterPicker(chapters);
+   chapterPick.id = 'new-cy-chapter';
+   addForm.insertBefore(chapterPick, addForm.querySelector('#new-cy-save'));
+   wrap.appendChild(addForm);
+ 
+   addBtn.addEventListener('click', () => {
+     addForm.classList.add('is-open');
+     addBtn.classList.add('is-hidden');
+     errMsg.classList.remove('is-visible');
+     addForm.querySelector('#new-cy-label').value = `Downtime ${nextGame}`;
+     addForm.querySelector('#new-cy-label').focus();
+   });
+   addForm.querySelector('#new-cy-cancel').addEventListener('click', () => {
+     addForm.classList.remove('is-open');
+     addBtn.classList.remove('is-hidden');
+   });
+   addForm.querySelector('#new-cy-save').addEventListener('click', async () => {
+     const num = parseInt(addForm.querySelector('#new-cy-num').value, 10);
+     const label = addForm.querySelector('#new-cy-label').value.trim();
+     const chapterId = addForm.querySelector('#new-cy-chapter').value || null;
+     if (!num || !label) {
+       errMsg.textContent = 'Game number and label are required.';
+       errMsg.classList.add('is-visible');
+       return;
+     }
+     errMsg.classList.remove('is-visible');
+     try {
+       await createCycle(num, { label, chapterId });
+       await refresh();
+     } catch (err) {
+       errMsg.textContent = `Create failed: ${err.message}`;
+       errMsg.classList.add('is-visible');
+     }
+   });
+ 
+   if (!sorted.length) {
+     const empty = document.createElement('p');
+     empty.className = 'cy-muted';
+     empty.textContent = 'No downtime cycles found.';
+     wrap.appendChild(errMsg);
+     wrap.appendChild(empty);
+     return wrap;
+   }
+ 
+   const table = document.createElement('table');
+   table.className = 'infl-table cy-table';
+   table.innerHTML = `<thead><tr>
+     <th>Label</th>
+     <th class="cy-col-phase">Phase</th>
+     <th class="cy-col-chapter">Chapter</th>
+     <th class="cy-col-prep">Prep Access</th>
+     <th class="cy-col-pub">Publish</th>
+     <th class="cy-col-att">Attendance</th>
+     <th class="cy-col-del"></th>
+   </tr></thead>`;
+   const tbody = document.createElement('tbody');
+ 
+   sorted.forEach(cy => {
+     const tr = document.createElement('tr');
+ 
+     tr.appendChild(buildLabelCell(cy));
+     tr.appendChild(buildPhaseCell(cy));
+ 
+     const tdChapter = document.createElement('td');
+     tdChapter.appendChild(buildChapterSelect(cy, chapters));
+     tr.appendChild(tdChapter);
+ 
+     // Prep Access toggle
+     const tdAccess = document.createElement('td');
+     const accessBtn = document.createElement('button');
+     accessBtn.className = 'btn-sm';
+     accessBtn.textContent = 'Prep Access';
+     tdAccess.appendChild(accessBtn);
+     tr.appendChild(tdAccess);
+ 
+     const detailTr = document.createElement('tr');
+     detailTr.className = 'cy-detail-row is-hidden';
+     const detailTd = document.createElement('td');
+     detailTd.colSpan = 7;
+     detailTd.className = 'cy-detail-cell';
+     detailTd.appendChild(buildAccessSection(cy, charList));
+     detailTr.appendChild(detailTd);
+ 
+     accessBtn.addEventListener('click', () => {
+       const open = !detailTr.classList.contains('is-hidden');
+       detailTr.classList.toggle('is-hidden', open);
+       accessBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Publish Reports button
+     const tdPublish = document.createElement('td');
+     const publishBtn = document.createElement('button');
+     publishBtn.className = 'btn-sm';
+     publishBtn.textContent = 'Publish Reports';
+     const publishResult = document.createElement('span');
+     publishResult.className = 'cy-publish-result';
+     tdPublish.appendChild(publishBtn);
+     tdPublish.appendChild(publishResult);
+     tr.appendChild(tdPublish);
+ 
+     publishBtn.addEventListener('click', async () => {
+       publishBtn.disabled = true;
+       publishResult.className = 'cy-publish-result';
+       publishResult.textContent = 'Publishing…';
+       try {
+         const result = await apiPost('/api/downtime_cycles/' + cy._id + '/publish', {});
+         if (result.published === 0) {
+           publishResult.textContent = 'No compiled reports found.';
+         } else {
+           publishResult.classList.add('is-ok');
+           publishResult.textContent = result.published + ' report' + (result.published === 1 ? '' : 's') + ' published.';
+         }
+       } catch (err) {
+         publishResult.classList.add('is-error');
+         publishResult.textContent = 'Publish failed: ' + err.message;
+       } finally {
+         publishBtn.disabled = false;
+       }
+     });
+ 
+     // Attendance toggle
+     const tdAtt = document.createElement('td');
+     const attBtn = document.createElement('button');
+     attBtn.className = 'btn-sm';
+     attBtn.textContent = 'Attendance';
+     tdAtt.appendChild(attBtn);
+     tr.appendChild(tdAtt);
+ 
+     const attendTr = document.createElement('tr');
+     attendTr.className = 'cy-detail-row is-hidden';
+     const attendTd = document.createElement('td');
+     attendTd.colSpan = 7;
+     attendTd.className = 'cy-detail-cell';
+     attendTd.appendChild(buildAttendanceSection(cy, sessions));
+     attendTr.appendChild(attendTd);
+ 
+     attBtn.addEventListener('click', () => {
+       const open = !attendTr.classList.contains('is-hidden');
+       attendTr.classList.toggle('is-hidden', open);
+       attBtn.classList.toggle('is-active', !open);
+     });
+ 
+     // Delete cycle
+     const tdDel = document.createElement('td');
+     const delBtn = document.createElement('button');
+     delBtn.className = 'btn-sm btn-danger';
+     delBtn.textContent = 'Delete';
+     tdDel.appendChild(delBtn);
+     tr.appendChild(tdDel);
+ 
+     delBtn.addEventListener('click', async () => {
+       if (!confirm(`Delete cycle "${cy.label || cy._id}"? This cannot be undone.`)) return;
+       errMsg.classList.remove('is-visible');
+       delBtn.disabled = true;
+       try {
+         await deleteCycle(cy._id);
+         await refresh();
+       } catch (err) {
+         delBtn.disabled = false;
+         // 409 CYCLE_HAS_SUBMISSIONS surfaces here with the server message.
+         errMsg.textContent = err.message || 'Delete failed.';
+         errMsg.classList.add('is-visible');
+       }
+     });
+ 
+     tbody.appendChild(tr);
+     tbody.appendChild(detailTr);
+     tbody.appendChild(attendTr);
+   });
+ 
+   table.appendChild(tbody);
+   wrap.appendChild(errMsg);
+   wrap.appendChild(table);
+   return wrap;
+ }
+

 ❯ tests/epic.708.3-cycle-phase-controls.test.js:63:25
     61|
     62|   it('highlights active phase with gold2 colour', () => {
     63|     expect(CYCLE_VIEWS).toContain('gold2');
       |                         ^
     64|   });
     65|

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[9/12]⎯

 FAIL  tests/n7-n9-allocator-readers.test.js > N-9 — meritPrereqOK dropdown filter + current-row passthrough warn > all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly)
AssertionError: expected '/**\r\n * Merit utilities, prerequisi…' to match /buildMeritOptions[\s\S]{0,600}meritPr…/

- Expected:
/buildMeritOptions[\s\S]{0,600}meritPrereqOK\(c, rule\)/

+ Received:
"/**
 * Merit utilities, prerequisite system, and power/discipline helpers.
 * Depends on constants and the rules cache (purchasable_powers API).
 */

import {
  ATTR_NAMES, SKILL_NAMES, DISC_NAMES, COV_SHORT, CLAN_NAMES,
  RITUAL_DISCS, INFLUENCE_SPHERES, INFLUENCE_MERIT_TYPES
} from '../data/constants.js';
import { meetsPrereq as _meetsPrereq, prereqLabel as _prereqLabel } from '../data/prereq.js';
import { getRulesByCategory, getRuleByKey, getRulesDB } from '../data/loader.js';

// Re-export the new prereq engine for direct use by consumers
export { _meetsPrereq as meetsPrereq, _prereqLabel as prereqLabel };

/** Check if a merit is excluded by a merit the character already owns.
 *  Issue #188 (2026-05-08): exposed as `isMeritExcluded` so the DT form's
 *  XP Spend picker can apply the same exclusion logic the sheet's Merit
 *  Add picker uses, without duplicating the rule walk. */
export function isMeritExcluded(c, meritName) {
  for (const m of (c.merits || [])) {
    const rule = getRuleByKey(m.name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''));
    if (!rule || !rule.exclusive) continue;
    const excluded = rule.exclusive.split(',').map(s => s.trim().toLowerCase());
    if (excluded.includes(meritName.toLowerCase())) return rule.name;
  }
  return null;
}
// Local alias preserved so the existing internal callers (buildMeritOptions
// / buildSubCategoryMeritOptions / buildMCIGrantOptions) don't need to be
// touched.
const _isExcluded = isMeritExcluded;

/* ══════════════════════════════════════════════════════
   Merit string helpers
   ══════════════════════════════════════════════════════ */

/** Strip dot glyphs and pipe-suffix, returning the base merit name. */
export function meritBase(s) {
  return s.replace(/\\s*[●○]+.*/,'').replace(/\\s*\\|.*/,'').trim();
}

/** Count filled-dot glyphs in a merit string. */
export function meritDotCount(s) {
  return (s.match(/●/g) || []).length;
}

/** Return the pipe-suffix portion, or null. */
export function meritSuffix(s) {
  const m = s.match(/\\|\\s*(.+)$/);
  return m ? m[1].trim() : null;
}

/** Lowercase key from the base name. */
export function meritKey(s) {
  return meritBase(s).toLowerCase();
}

/** Lowercase key with parenthetical qualifiers stripped. */
export function meritKeyBase(s) {
  return meritKey(s).replace(/\\s*\\([^)]*\\)\\s*/g,'').trim();
}

/**
 * If a merit has a single fixed rating (e.g. Viral Mythology = 3), returns that number.
 * Returns null for graduated/range merits (e.g. Allies 1-5).
 */
export function meritFixedRating(name) {
  // Try rules cache first
  const slug = (name || '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
  const rule = getRuleByKey(slug);
  if (rule) {
    if (!rule.rating_range) return null;
    if (rule.rating_range[0] === rule.rating_range[1]) return rule.rating_range[0];
    return null; // range merit
  }
  return null;
}

/** Look up a merit by name string. Tries rules cache first, falls back to MERITS_DB. */
export function meritLookup(s) {
  // Try rules cache (unified schema)
  const slug = (s || '').toLowerCase().replace(/\\s*[●○]+.*/,'').replace(/\\s*\\|.*/,'').trim()
    .replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
  const rule = getRuleByKey(slug);
  if (rule) return { desc: rule.description, prereq: rule.prereq, rating: rule.rating_range ? `${rule.rating_range[0]}–${rule.rating_range[1]}` : null, type: rule.parent, sub_category: rule.sub_category, _rule: rule };
  return null;
}

/* ══════════════════════════════════════════════════════
   Merit array helpers (operate on character objects)
   ══════════════════════════════════════════════════════ */

/**
 * Find a merit by category and filtered index, returning both the merit
 * object and its real index in c.merits.
 */
export function meritByCategory(c, category, filteredIdx) {
  const filtered = (c.merits || []).filter(m => m.category === category);
  const m = filtered[filteredIdx];
  if (!m) return { merit: null, realIdx: -1 };
  return { merit: m, realIdx: c.merits.indexOf(m) };
}

/** Ensure every merit has inline creation fields (v3). */
export function ensureMeritSync(c) {
  if (!c.merits) c.merits = [];
  for (const m of c.merits) {
    if (m.cp === undefined) m.cp = 0;
    if (m.xp === undefined) m.xp = 0;
    // Issue #834: m.free is deprecated — no longer initialised here. New
    // merits don't carry the field; existing merits that have it set retain
    // it until the Phase 3 cleanup script zeros them. See memory
    // feedback_m_free_deprecated.
    if (m.free_mci === undefined) m.free_mci = 0;
    if (m.free_vm === undefined) m.free_vm = 0;
    if (m.free_lk === undefined) m.free_lk = 0;
    if (m.free_ohm === undefined) m.free_ohm = 0;
    if (m.free_inv === undefined) m.free_inv = 0;
    if (m.free_pt === undefined) m.free_pt = 0;
    if (m.free_mdb === undefined) m.free_mdb = 0;
    if (m.free_sw === undefined) m.free_sw = 0;
    if (m.bonus === undefined) m.bonus = 0;
  }
}

/** Append a merit with inline creation defaults. */
export function addMerit(c, merit) {
  if (!c.merits) c.merits = [];
  if (merit.cp === undefined) merit.cp = 0;
  if (merit.xp === undefined) merit.xp = 0;
  // Issue #834: m.free is deprecated — no longer initialised on new merits.
  if (merit.free_mci === undefined) merit.free_mci = 0;
  if (merit.free_vm === undefined) merit.free_vm = 0;
  if (merit.free_lk === undefined) merit.free_lk = 0;
  if (merit.free_ohm === undefined) merit.free_ohm = 0;
  if (merit.free_inv === undefined) merit.free_inv = 0;
  if (merit.free_pt === undefined) merit.free_pt = 0;
  if (merit.free_mdb === undefined) merit.free_mdb = 0;
  if (merit.free_sw === undefined) merit.free_sw = 0;
  if (merit.bonus === undefined) merit.bonus = 0;
  if (merit.rule_key === undefined) merit.rule_key = null;
  c.merits.push(merit);
}

/** Remove a merit by real index. */
export function removeMerit(c, realIdx) {
  if (realIdx < 0 || realIdx >= c.merits.length) return;
  c.merits.splice(realIdx, 1);
}

/* ══════════════════════════════════════════════════════
   Internal dot-lookup helpers (for prerequisite checks)
   ══════════════════════════════════════════════════════ */

function _getAttrDots(c, name) {
  const k = Object.keys(c.attributes || {}).find(a => a.toLowerCase() === name);
  return k ? (c.attributes[k].dots || 0) : 0;
}

function _getSkillDots(c, name) {
  const k = Object.keys(c.skills || {}).find(s => s.toLowerCase() === name);
  return k ? (c.skills[k].dots || 0) : 0;
}

function _getDiscDots(c, name) {
  const n = name.toLowerCase();
  const k = Object.keys(c.disciplines || {}).find(d => d.toLowerCase() === n);
  return k ? (c.disciplines[k]?.dots || 0) : 0;
}

function _getMeritRating(c, name) {
  const n = name.toLowerCase();
  const m = (c.merits || []).find(m => m.name.toLowerCase() === n);
  return m ? (m.rating || 1) : 0;
}

function _getCovStatus(c, covShort) {
  const fullName = COV_SHORT[covShort.toLowerCase()] || covShort;
  return c.status?.covenant?.[fullName] || 0;
}

/* ══════════════════════════════════════════════════════
   Prerequisite system
   ══════════════════════════════════════════════════════ */

/**
 * Check a single prerequisite token against a character.
 * Returns true if the character meets the requirement.
 */
export function checkSinglePrereq(c, token) {
  token = token.trim();
  if (!token || token === 'None' || token === '-') return true;

  // \"No X Status\"
  const noStatus = /^No\\s+(\\w+)\\s+Status$/i.exec(token);
  if (noStatus) {
    const cov = noStatus[1];
    return _getCovStatus(c, cov) === 0;
  }

  // \"City Status N\"
  const cityStatus = /^City\\s+Status\\s+(\\d+)$/i.exec(token);
  if (cityStatus) return (c.status?.city || 0) >= parseInt(cityStatus[1]);

  // \"Clan Status N\" or \"[ClanName] Status N\"
  const clanStatus = /^(?:Clan\\s+)?(\\w+)\\s+Status\\s+(\\d+)$/i.exec(token);
  if (clanStatus) {
    const word = clanStatus[1].toLowerCase();
    const n = parseInt(clanStatus[2]);
    if (word === 'clan') return (c.status?.clan || 0) >= n;
    if (COV_SHORT[word]) return _getCovStatus(c, word) >= n;
    return _getCovStatus(c, word) >= n;
  }

  // \"Humanity < N\"
  const humLt = /^Humanity\\s*<\\s*(\\d+)$/i.exec(token);
  if (humLt) return (c.humanity || 7) < parseInt(humLt[1]);

  // \"X Bloodline\" or \"Bloodline X\"
  if (/bloodline/i.test(token)) {
    const bl = (c.bloodline || '').toLowerCase();
    return bl && token.toLowerCase().includes(bl);
  }

  // Clan name alone
  if (CLAN_NAMES.has(token.toLowerCase())) return (c.clan || '').toLowerCase() === token.toLowerCase();

  // Specialisation references — too complex to verify, pass
  if (/speciali[sz]/i.test(token)) return true;

  // \"Attribute N\" / \"Skill N\" / \"Discipline N\" / \"Merit N\"
  const withNum = /^(.+?)\\s+(\\d+)$/.exec(token);
  if (withNum) {
    const name = withNum[1].trim().toLowerCase();
    const n = parseInt(withNum[2]);
    if (ATTR_NAMES.has(name)) return _getAttrDots(c, name) >= n;
    if (SKILL_NAMES.has(name)) return _getSkillDots(c, name) >= n;
    const nameNorm = name.normalize('NFD').replace(/[\\u0300-\\u036f]/g, '');
    if (DISC_NAMES.has(name) || DISC_NAMES.has(nameNorm)) return _getDiscDots(c, name) >= n || _getDiscDots(c, nameNorm) >= n;
    // Merit as prereq e.g. \"Safe Place 1\", \"Contacts 2\"
    return _getMeritRating(c, name) >= n;
  }

  // Token without number — presence check
  const nameLow = token.toLowerCase();
  if (ATTR_NAMES.has(nameLow)) return _getAttrDots(c, nameLow) > 0;
  if (SKILL_NAMES.has(nameLow)) return _getSkillDots(c, nameLow) > 0;
  if (DISC_NAMES.has(nameLow)) return _getDiscDots(c, nameLow) > 0;
  // Inequality operators — just pass
  if (/[≤≥<>]/.test(token)) return true;
  // Merit name without number — permissive fallback
  return _getMeritRating(c, nameLow) > 0 || true;
}

/**
 * Check a full prerequisite string (comma-separated AND, \"or\"-separated OR).
 * If the rules cache is available, looks up the structured prereq tree and
 * delegates to meetsPrereq(). Falls back to regex parsing if cache unavailable.
 */
export function meritQualifies(c, prereqStr, structuredPrereq) {
  if (!prereqStr || prereqStr === '-') return true;

  // If a structured prereq tree was passed directly, use the new engine
  if (structuredPrereq !== undefined) {
    return meritPrereqOK(c, { prereq: structuredPrereq });
  }

  // Fallback: regex-based parsing for legacy callers
  const andParts = prereqStr.split(/\\s*,\\s*/);
  return andParts.every(part => {
    const orParts = part.split(/\\s+or\\s+/i);
    return orParts.some(t => checkSinglePrereq(c, t.trim()));
  });
}

/**
 * N-9 (issue #762, Bug 3) — single seam for the prereq check across all
 * sub-category dropdown filters (buildMeritOptions, buildSubCategoryMeritOptions,
 * buildMCIGrantOptions). Wraps `_meetsPrereq` so future evolutions of the
 * prereq-check semantics happen in one place instead of three.
 *
 * Returns true when the character meets the rule's `prereq` tree. Empty /
 * missing prereq trees pass (existing `_meetsPrereq` semantics).
 *
 * Note: current-row passthrough — when a row's currently-selected merit name
 * fails its prereq (e.g. ST removed a prereq merit elsewhere on the sheet),
 * the picker keeps the selection visible per Ma'at's directive (legitimate
 * editing affordance). Callers handle that case inline with their `curLow`
 * check, and emit a `console.warn` so the situation surfaces in QA testing
 * instead of silently passing. The warn lives on the caller side because
 * \"current row\" is dropdown-builder concern, not a property of the rule.
 *
 * @param {object} c
 * @param {object} rule - merit rule doc (with optional `prereq` tree)
 * @returns {boolean}
 */
export function meritPrereqOK(c, rule) {
  return _meetsPrereq(c, rule && rule.prereq);
}

/**
 * Build <option> HTML for a merit select dropdown, filtered by prerequisites.
 * Excludes standing, domain, and influence merits (those have dedicated UI).
 */
export function buildMeritOptions(c, currentName) {
  // Try rules cache first
  const rulesDB = getRulesByCategory('merit');
  const qualified = [];

  if (rulesDB.length) {
    // Rules cache available — use structured data
    for (const rule of rulesDB) {
      if (rule.sub_category && rule.sub_category !== 'general') continue;
      if (INFLUENCE_MERIT_TYPES.includes(rule.name)) continue;
      // Issue #937: 'Style'-parent merits are plain merits — surface them.
      // 2026-06-30: 'Carthian Law' also removed. Carthian Laws are merits with
      // status-based prereqs and belong in the regular picker; the Pacts UI
      // continues to list them too as an alternate path. See parallel change
      // in downtime-form.js so DT XP-spend and sheet pickers stay aligned.
      if (rule.parent && ['Invictus Oath'].includes(rule.parent)) continue;
      if (!meritPrereqOK(c, rule)) continue;
      const excl = _isExcluded(c, rule.name);
      if (excl && rule.name.toLowerCase() !== (currentName || '').toLowerCase()) continue;
      qualified.push({ key: rule.name.toLowerCase(), label: rule.name });
    }
  } else {
    return '<option value=\"\">— rules loading —</option>';
  }
  qualified.sort((a, b) => a.label.localeCompare(b.label));
  const curLow = (currentName || '').toLowerCase();
  const esc = _esc;
  let opts = '<option value=\"\">' + (currentName ? '' : '— select merit —') + '</option>';
  if (currentName && !qualified.some(q => q.key === curLow)) {
    opts += '<option value=\"' + esc(currentName) + '\" selected>' + esc(currentName) + '</option>';
  }
  for (const { key, label } of qualified) {
    const sel = key === curLow || label.toLowerCase() === curLow ? ' selected' : '';
    opts += '<option value=\"' + esc(label) + '\"' + sel + '>' + esc(label) + '</option>';
  }
  return opts;
}

/**
 * Build <option> HTML for an influence or domain merit type dropdown, driven
 * by sub_category in the catalog. Enforces prereqs and exclusive lists, with
 * the show-if-current escape hatch so an existing row whose merit no longer
 * qualifies still displays in its own dropdown.
 * @param {object} c - character
 * @param {string} subCategory - 'influence' or 'domain'
 * @param {string} currentName - name currently selected on this row
 * @param {string[]} [extraNames] - additional names to include (e.g. legacy
 *   names not yet in the catalog) so the picker stays usable during migration
 */
export function buildSubCategoryMeritOptions(c, subCategory, currentName, extraNames = []) {
  const rulesDB = getRulesByCategory('merit');
  if (!rulesDB.length) return '<option value=\"\">— rules loading —</option>';
  const curLow = (currentName || '').toLowerCase();

  const qualified = [];
  const seen = new Set();
  for (const rule of rulesDB) {
    if (rule.sub_category !== subCategory) continue;
    // N-9 (issue #762, Bug 3): single-seam prereq check; current-row
    // passthrough preserved (legitimate editing affordance) but a
    // failing-prereq passthrough now emits a diagnostic warn so the
    // situation surfaces in QA testing instead of silently passing.
    const passes = meritPrereqOK(c, rule);
    const isCurrentRow = rule.name.toLowerCase() === curLow;
    if (!passes && !isCurrentRow) continue;
    if (!passes && isCurrentRow) {
      try {
        console.warn(`[meritPrereqOK] current selection \"${rule.name}\" (sub_category=${subCategory}) passes through filter but fails its prereq — investigate.`);
      } catch { /* console may be absent in test contexts */ }
    }
    if (_isExcluded(c, rule.name) && !isCurrentRow) continue;
    if (seen.has(rule.name)) continue;
    seen.add(rule.name);
    qualified.push(rule.name);
  }
  for (const n of extraNames) {
    if (!seen.has(n)) { seen.add(n); qualified.push(n); }
  }
  qualified.sort((a, b) => a.localeCompare(b));

  // Always include the current row's selected name even if filtered out.
  if (currentName && !seen.has(currentName)) qualified.push(currentName);

  return qualified.map(n => '<option value=\"' + _esc(n) + '\"' + (n === currentName ? ' selected' : '') + '>' + _esc(n) + '</option>').join('');
}

/**
 * Build <option> HTML for MCI grant dropdown — includes influence and domain merits.
 * Filters by prerequisites and dot-level rating.
 * MCI dot ratings: dot 1-2 = 1-dot merits, dot 3 = 2-dot, dot 4-5 = 3-dot.
 * Graduated merits (rating range) appear if their min ≤ dotRating.
 * @param {object} c - character
 * @param {number} dotLevel - 0-indexed MCI dot level
 * @param {string} currentName - currently selected merit name
 */
const MCI_DOT_RATING = [1, 1, 2, 3, 3];
export function buildMCIGrantOptions(c, dotLevel, currentName) {
  const maxR = MCI_DOT_RATING[dotLevel] || 1;
  const qualified = [];

  // Try rules cache first
  const rulesDB = getRulesByCategory('merit');
  if (rulesDB.length) {
    for (const rule of rulesDB) {
      if (rule.sub_category === 'standing') continue;
      if (rule.parent && ['Style', 'Invictus Oath', 'Carthian Law'].includes(rule.parent)) continue;
      if (!meritPrereqOK(c, rule)) continue;
      if (_isExcluded(c, rule.name) && rule.name.toLowerCase() !== (currentName || '').toLowerCase()) continue;
      const rr = rule.rating_range;
      if (rr) {
        if (rr[0] === rr[1]) { if (rr[0] !== maxR) continue; }
        else { if (rr[0] > maxR) continue; }
      } else { if (1 !== maxR) continue; }
      qualified.push({ key: rule.name.toLowerCase(), label: rule.name });
    }
  } else {
    return '<option value=\"\">— rules loading —</option>';
  }
  qualified.sort((a, b) => a.label.localeCompare(b.label));
  const curLow = (currentName || '').toLowerCase();
  let opts = '<option value=\"\">' + (currentName ? '' : '— select merit —') + '</option>';
  if (currentName && !qualified.some(q => q.key === curLow)) {
    opts += '<option value=\"' + _esc(currentName) + '\" selected>' + _esc(currentName) + '</option>';
  }
  for (const { key, label } of qualified) {
    const sel = key === curLow || label.toLowerCase() === curLow ? ' selected' : '';
    opts += '<option value=\"' + _esc(label) + '\"' + sel + '>' + _esc(label) + '</option>';
  }
  return opts;
}

/**
 * Walk a prereq tree and return true if every satisfying assignment requires
 * Carthian status — i.e. no non-Carthian character could legitimately have
 * the merit. `all` requires Carthian if any child does; `any` requires it
 * only if every branch does.
 */
function _everyPrereqPathRequiresCarthian(node) {
  if (!node) return false;
  if (node.all) return node.all.some(_everyPrereqPathRequiresCarthian);
  if (node.any) return node.any.every(_everyPrereqPathRequiresCarthian);
  if (node.type === 'status' && node.qualifier) return /carthian/i.test(node.qualifier);
  return false;
}

/**
 * Build <option> HTML for Fucking Thief — single-dot non-Carthian merits.
 * Excludes Carthian-locked merits (the thief is Carthian by prereq, so they
 * can only steal what a non-Carthian could plausibly hold).
 */
export function buildFThiefOptions(currentName) {
  const qualified = [];

  // Try rules cache first
  const rulesDB = getRulesByCategory('merit');
  if (rulesDB.length) {
    for (const rule of rulesDB) {
      if (rule.sub_category === 'standing') continue;
      const rr = rule.rating_range;
      const minR = rr ? rr[0] : 1;
      if (minR > 1) continue;
      if (rr && rr[0] === rr[1] && rr[0] !== 1) continue;
      if (_everyPrereqPathRequiresCarthian(rule.prereq)) continue;
      qualified.push({ key: rule.name.toLowerCase(), label: rule.name });
    }
  } else {
    return '<option value=\"\">— rules loading —</option>';
  }
  qualified.sort((a, b) => a.label.localeCompare(b.label));
  const curLow = (currentName || '').toLowerCase();
  let opts = '<option value=\"\">' + (currentName ? '' : '— choose stolen merit —') + '</option>';
  if (currentName && !qualified.some(q => q.key === curLow)) {
    opts += '<option value=\"' + _esc(currentName) + '\" selected>' + _esc(currentName) + '</option>';
  }
  for (const { key, label } of qualified) {
    const sel = key === curLow || label.toLowerCase() === curLow ? ' selected' : '';
    opts += '<option value=\"' + _esc(label) + '\"' + sel + '>' + _esc(label) + '</option>';
  }
  return opts;
}

/* ── Inline HTML escape (avoids circular dependency on a helpers module) ── */
function _esc(s) {
  return s == null ? '' : String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/\"/g, '&quot;');
}

/* ══════════════════════════════════════════════════════
   Power / discipline helpers
   ══════════════════════════════════════════════════════ */

/** Filter a powers array to those belonging to a specific discipline. */
export function powersForDisc(powers, discName) {
  if (!powers) return [];
  if (RITUAL_DISCS.includes(discName)) {
    return powers.filter(p => p.name && (p.name.startsWith(discName) || p.name.includes('| ' + discName)));
  }
  return powers.filter(p => {
    if (!p.name) return false;
    const n = p.name.split('|')[0].trim().split(/\\s+/);
    return n[0] === discName || (n.length > 1 && n.slice(0, -1).join(' ') === discName);
  });
}

/** Return powers not attributable to any known discipline on the character. */
export function otherPowers(c) {
  const discNames = Object.keys(c.disciplines || {});
  return (c.powers || []).filter(p => {
    if (!p.name) return false;
    const key = p.name.split('|')[0].trim().replace(/\\s*[●○]+$/, '').replace(/\\s+\\d+$/, '');
    if (RITUAL_DISCS.some(d => p.name.startsWith(d))) return false;
    return !discNames.some(d => key === d || key.startsWith(d + ' '));
  });
}

/** Returns a Set of cult names from a character's Mystery Cult Initiation merits. */
function getCharCults(c) {
  return new Set(
    (c.merits || [])
      .filter(m => m.name === 'Mystery Cult Initiation' && m.cult_name)
      .map(m => m.cult_name)
  );
}

/** Check whether a character meets a devotion's discipline prerequisites. */
export function meetsDevPrereqs(c, dev) {
  if (dev.bl && (c.bloodline || '') !== dev.bl) return false;
  if (dev.cult && !getCharCults(c).has(dev.cult)) return false;
  const discs = c.disciplines || {};
  if (!dev.p || !dev.p.length) return true;
  if (dev.or) {
    return dev.p.some(p => (discs[p.disc]?.dots || 0) >= p.dots);
  }
  return dev.p.every(p => (discs[p.disc]?.dots || 0) >= p.dots);
}

/** Format a devotion's prerequisite list as a human-readable string. */
export function devPrereqStr(dev) {
  const parts = [];
  if (dev.bl) parts.push(dev.bl + ' only');
  if (dev.cult) parts.push(dev.cult + ' members only');
  if (dev.p && dev.p.length) parts.push(dev.p.map(p => p.disc + ' ' + p.dots).join(dev.or ? ' or ' : ', '));
  return parts.join('; ') || 'None';
}
"

 ❯ tests/n7-n9-allocator-readers.test.js:246:17
    244|     const src = read('public/js/editor/merits.js');
    245|     // buildMeritOptions
    246|     expect(src).toMatch(/buildMeritOptions[\s\S]{0,600}meritPrereqOK\(…
       |                 ^
    247|     // buildSubCategoryMeritOptions
    248|     expect(src).toMatch(/buildSubCategoryMeritOptions[\s\S]{0,800}meri…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[10/12]⎯

 FAIL  tests/oath-a-pledge-helpers.test.js > OATH-A D2 — encumbrance changes no dot sum > meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form
AssertionError: expected '/**\r\n * XP calculations and merit r…' to contain 'export function meritRating(c, m) {\n…'

- Expected
+ Received

- export function meritRating(c, m) {
-   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+ /**
+  * XP calculations and merit rating/breakdown helpers.
+  * Pure functions — no DOM side-effects.
+  */
+ 
+ import { getRuleByKey } from '../data/loader.js';
+ // N-1: map-fallback shape for per-slug free reads (see rules-helpers.js).
+ import { freeOf } from '../data/rules-helpers.js';
+ // COLLECTIVE-2 (#1110): compound slug + source names reach the bd-row from
+ // rule data, so they are escaped like every other data-sourced label.
+ import { esc } from '../data/helpers.js';
+ 
+ /**
+  * Convert XP spent into dot count (flat rate).
+  * @param {number} xpSpent - total XP allocated
+  * @param {number} baseBeforeXP - dots from CP/free (unused in flat model, kept for API)
+  * @param {number} costPerDot - XP cost per single dot
+  * @returns {number}
+  */
+ export function xpToDots(xpSpent, baseBeforeXP, costPerDot) {
+   return Math.floor((xpSpent || 0) / costPerDot);
+ }
+ 
+ /**
+  * Convert a dot count into XP cost (flat rate).
+  * @param {number} numDots
+  * @param {number} baseBeforeXP - (unused in flat model, kept for API)
+  * @param {number} costPerDot
+  * @returns {number}
+  */
+ export function dotsToXP(numDots, baseBeforeXP, costPerDot) {
+   return (numDots || 0) * costPerDot;
+ }
+ 
+ /** Starting XP awarded on character creation. */
+ export function xpStarting() { return 10; }
+ 
+ /** XP from voluntary humanity drops: 2 per dot permanently lost. */
+ export function xpHumanityDrop(c) {
+   // Use explicit field if set; otherwise infer from stored humanity value
+   const lost = c.humanity_lost !== undefined
+     ? c.humanity_lost
+     : Math.max(0, (c.humanity_base || 7) - (c.humanity || 0));
+   return lost * 2;
+ }
+ 
+ /** XP from completed ordeals: 3 per ordeal. */
+ export function xpOrdeals(c) {
+   return ((c.ordeals || []).filter(o => o.complete).length) * 3;
+ }
+ 
+ /** XP from game attendance. Requires loadGameXP() to have run — returns 0 if not yet loaded. */
+ export function xpGame(c) {
+   return c._gameXP ?? 0;
+ }
+ 
+ /** XP bonus from Professional Training: 1 XP per asset skill at 5+ effective dots (requires PT ≥ 4).
+  *  Effective dots = base dots + PT dot 4 bonus (+1 to chosen asset skill). */
+ export function xpPT5(c) {
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   if (!ptM || meritRating(c, ptM) < 4) return 0;
+   const assets = (ptM.asset_skills || []).filter(Boolean);
+   if (!assets.length) return 0;
+   const ptBonus = c._pt_dot4_bonus_skills instanceof Set ? c._pt_dot4_bonus_skills : new Set();
+   return assets.filter(sk => {
+     const s = (c.skills || {})[sk];
+     const effective = (s?.dots || 0) + (ptBonus.has(sk) ? 1 : 0);
+     return effective >= 5;
+   }).length;
+ }
+ 
+ /**
+  * Total XP earned by a character (all sources, derived dynamically).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpEarned(c) {
+   return xpStarting() + xpHumanityDrop(c) + xpOrdeals(c) + xpGame(c) + xpPT5(c);
+ }
+ 
+ /** Sum XP from inline creation fields on an object (attributes, skills, disciplines). */
+ function sumInlineXP(obj) {
+   if (!obj) return 0;
+   return Object.values(obj).reduce((t, v) => t + (v?.xp || 0), 0);
+ }
+ 
+ /** XP spent on attributes — sum of .xp across all attribute objects. */
+ export function xpSpentAttrs(c) {
+   return sumInlineXP(c.attributes);
+ }
+ 
+ /** XP spent on skills + specialisations beyond free allowance. */
+ export function xpSpentSkills(c) {
+   const skillXP = sumInlineXP(c.skills);
+   // PT free specs (dot 3): 2 extra, but ONLY usable on asset skills — tracked separately
+   const ptM = (c.merits || []).find(m => m.name === 'Professional Training');
+   const ptFree = (ptM && ptM.rating >= 3) ? 2 : 0;
+   const ptAssets = new Set((ptM && ptM.rating >= 3 && ptM.asset_skills) ? (ptM.asset_skills || []).filter(Boolean) : []);
+   let assetSpecs = 0, nonAssetSpecs = 0;
+   Object.entries(c.skills || {}).forEach(([sk, skillObj]) => {
+     const count = (skillObj && skillObj.specs) ? skillObj.specs.length : 0;
+     if (ptAssets.has(sk)) assetSpecs += count;
+     else nonAssetSpecs += count;
+   });
+   // MCI dot 1 free specs: each active MCI with dot1_choice === 'speciality' grants 1 free spec
+   const mciFreeSpecs = (c._mci_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // Bloodline-granted specs are also exempt from XP cost
+   const bloodlineFreeSpecs = (c._bloodline_free_specs || []).filter(fs =>
+     fs.skill && fs.spec && (c.skills || {})[fs.skill] && ((c.skills[fs.skill].specs || []).includes(fs.spec))
+   ).length;
+   // PT free covers asset specs first; baseline 3 covers everything else
+   const ptFreeCovered = Math.min(ptFree, assetSpecs);
+   const paidSpecs = nonAssetSpecs + Math.max(0, assetSpecs - ptFreeCovered);
+   const specXP = Math.max(0, paidSpecs - 3 - mciFreeSpecs - bloodlineFreeSpecs);
+   return skillXP + specXP;
+ }
+ 
+ /** XP spent on all merits (general, influence, domain, standing) + fighting styles + pact oaths. */
+ export function xpSpentMerits(c) {
+   const meritXP = (c.merits || []).reduce((t, m) => t + (m.xp || 0), 0);
+   const styleXP = (c.fighting_styles || []).reduce((t, fs) => t + (fs.xp || 0), 0);
+   const pactXP = (c.powers || []).filter(p => p.category === 'pact').reduce((t, p) => t + (p.xp || 0), 0);
+   return meritXP + styleXP + pactXP;
+ }
+ 
+ /** XP spent on powers — disciplines + devotions. */
+ export function xpSpentPowers(c) {
+   const discXP = sumInlineXP(c.disciplines);
+   // Devotion XP: look up each devotion's cost from DEVOTIONS_DB
+   const devXP = (c.powers || [])
+     .filter(p => p.category === 'devotion')
+     .reduce((t, p) => {
+       // Try rules cache first, fallback to _devotionsDB
+       const slug = 'devotion-' + p.name.toLowerCase().replace(/['']/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
+       const rule = getRuleByKey(slug);
+       if (rule) return t + (rule.xp_fixed || 0);
+       const db = _devotionsDB ? _devotionsDB.find(d => d.n === p.name) : null;
+       return t + (db ? db.xp || 0 : 0);
+     }, 0);
+   // Rite XP: paid rites cost 1 XP (rank 1-3) or 2 XP (rank 4-5)
+   const riteXP = (c.powers || [])
+     .filter(p => p.category === 'rite' && !p.free)
+     .reduce((t, p) => t + (p.level >= 4 ? 2 : 1), 0);
+   return discXP + devXP + riteXP;
+ }
+ 
+ /** XP spent on special: Blood Potency, Humanity, lost Willpower dots. */
+ export function xpSpentSpecial(c) {
+   // Blood Potency: tracked directly in bp_creation.xp (new model) or derived (legacy)
+   const bpXP = (c.bp_creation || {}).xp !== undefined
+     ? ((c.bp_creation || {}).xp || 0)
+     : Math.max(0, (c.blood_potency || 1) - 1 - Math.floor(((c.bp_creation || {}).cp || 0) / 5)) * 5;
+   // Humanity: XP spent raising dots (new model only; old model net is captured in xpHumanityDrop)
+   const humXP = c.humanity_xp || 0;
+   // Lost Willpower dots: stored in xp_log.spent.willpower
+   const wpXP = ((c.xp_log || {}).spent || {}).willpower || 0;
+   // Manual special: anything else tracked in xp_log
+   const manualXP = ((c.xp_log || {}).spent || {}).special || 0;
+   return bpXP + humXP + wpXP + manualXP;
+ }
+ 
+ // Devotions DB reference (set via setDevotionsDB)
+ let _devotionsDB = null;
+ export function setDevotionsDB(db) { _devotionsDB = db; }
+ 
+ /**
+  * Total XP spent by a character (all categories, fully dynamic).
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpSpent(c) {
+   return xpSpentAttrs(c) + xpSpentSkills(c) + xpSpentMerits(c) + xpSpentPowers(c) + xpSpentSpecial(c);
+ }
+ 
+ /**
+  * Remaining unspent XP.
+  * @param {object} c - character object
+  * @returns {number}
+  */
+ export function xpLeft(c) {
+   return xpEarned(c) - xpSpent(c);
+ }
+ 
+ /**
+  * Effective rating of a merit (sum of CP + free + grant pools + XP, inline on merit object).
+  * Falls back to stored rating if inline fields are absent.
+  * @param {object} c - character object
+  * @param {object} m - merit entry
+  * @returns {number}
+  */
+ export function meritRating(c, m) {
+   if (m.cp === undefined && m.xp === undefined) return m.rating || 0;
+   return (m.cp || 0) + freeOf(m, 'bloodline') + freeOf(m, 'pet') + freeOf(m, 'mci') + freeOf(m, 'vm') + freeOf(m, 'lk')
+     + freeOf(m, 'ohm') + freeOf(m, 'inv') + freeOf(m, 'pt') + freeOf(m, 'mdb') + freeOf(m, 'sw') + (m.xp || 0);
+ }
+ 
+ /**
+  * Render the merit breakdown row: Fr + CP + XP = total
+  * @param {number} realIdx - index into c.merits
+  * @param {object} mc - merit object with inline creation fields {cp, free, xp, free_mci, ...}
+  * @param {number|null} fixedAt - if the merit has a fixed rating (e.g. 3 for VM), pass it here;
+  *   null for graduated merits. When fixedAt is set, the displayed total snaps to 0 until the
+  *   threshold is met, then shows fixedAt.
+  */
+ export function meritBdRow(realIdx, mc, fixedAt, opts = {}) {
+   const cp = mc.cp || 0, xp = mc.xp || 0, fbl = freeOf(mc, 'bloodline'), fret = freeOf(mc, 'pet'), fmci = freeOf(mc, 'mci'), fvm = freeOf(mc, 'vm'), flk = freeOf(mc, 'lk'), fohm = freeOf(mc, 'ohm'), finv = freeOf(mc, 'inv'), fpt = freeOf(mc, 'pt'), fmdb = freeOf(mc, 'mdb'), fsw = freeOf(mc, 'sw');
+   // COLLECTIVE-2 (issue #1110): Collective Compound allocation channels are
+   // data-driven — the caller passes the discovered slugs rather than this
+   // function naming one. Defaults to ['necro'] so call sites that predate
+   // the compound wiring keep their pre-#1110 total exactly.
+   const _cmpSlugs = Array.isArray(opts.compoundSlugs) ? opts.compoundSlugs : ['necro'];
+   const fcompound = _cmpSlugs.reduce((s, slug) => s + freeOf(mc, slug), 0);
+   const total = cp + xp + fbl + fret + fmci + fvm + flk + fohm + finv + fpt + fmdb + fsw + fcompound + (opts.attachBonus || 0);
+   // Effective display: for fixed merits, only show dots once the threshold is reached
+   const effective = (fixedAt != null) ? (total >= fixedAt ? fixedAt : 0) : total;
+   const needsHint = (fixedAt != null && total > 0 && total < fixedAt)
+     ? '<span class="bd-needs-hint">' + total + ' / ' + fixedAt + ' needed</span>' : '';
+   // N-7b (issue #768): Necropolis target merits are pool-funded only — the
+   // domain-renderer call site suppresses CP / XP / MCI / Bonus on these rows
+   // via opts.hideCP / opts.hideXP / opts.hideMCI (this block) + opts.hideBonus
+   // (below). Same opts-flag pattern as N-9's hideBonus on standing merits.
+   // The pre-N-7b shape rendered CP + XP + sep unconditionally; the guards
+   // here are defaults-false so existing call sites that don't pass the
+   // hide-flags get the legacy behaviour unchanged.
+   let h = '<div class="merit-bd-row">';
+   if (!opts.hideCP) h += '<div class="bd-grp"><span class="bd-lbl">CP</span><input class="merit-bd-input" type="number" min="0" value="' + cp + '" onchange="shEditMeritPt(' + realIdx + ',\'cp\',+this.value)"></div>';
+   if (!opts.hideXP) h += '<div class="bd-grp"><span class="bd-lbl">XP</span><input class="merit-bd-input" type="number" min="0" value="' + xp + '" onchange="shEditMeritPt(' + realIdx + ',\'xp\',+this.value)"></div>';
+   if (!opts.hideCP || !opts.hideXP) h += '<div class="bd-sep"></div>';
+   // N-9 (issue #762, Bug 1 "adjacent finding"): MCI input writes the map-shape
+   // `free_grants.mci` per the ADR-005 allocator-write-path amendment. The
+   // handler (shEditMeritPt) detects the `free_grants.` prefix and routes to
+   // the map. Pre-N-9 this wrote `free_mci`, creating a fresh divergence on
+   // every edit after the N-2 backfill.
+   // N-7b: showMCI is AND'd with !hideMCI at the call site so Necropolis
+   // targets don't surface the MCI allocator even when there's pool capacity.
+   // Issue #774: a11y — every stepper input carries id + name + aria-label
+   // matching the NECRO precedent at line 241 (added by N-7c #771). Same
+   // browser warning ("form field element should have an id or name
+   // attribute / No label associated with a form field") was firing on
+   // these 5 sibling steppers. Single-line additions per stepper.
+   if (opts.showMCI && !opts.hideMCI) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-mci-lbl-' + realIdx + '">MCI</span><input id="bd-mci-' + realIdx + '" name="bd-mci-' + realIdx + '" aria-label="Mystery Cult Initiation pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fmci + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.mci\',+this.value)"></div>';
+   if (opts.showVM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-vm-lbl-' + realIdx + '">VM</span><input id="bd-vm-' + realIdx + '" name="bd-vm-' + realIdx + '" aria-label="Viral Mythology pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fvm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_vm\',+this.value)"></div>';
+   if (opts.showLK) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-lk-lbl-' + realIdx + '">LK</span><input id="bd-lk-' + realIdx + '" name="bd-lk-' + realIdx + '" aria-label="Lorekeeper pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + flk + '" onchange="shEditMeritPt(' + realIdx + ',\'free_lk\',+this.value)"></div>';
+   if (opts.showOHM) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-ohm-lbl-' + realIdx + '">OHM</span><input id="bd-ohm-' + realIdx + '" name="bd-ohm-' + realIdx + '" aria-label="Oath of the Hard Motherfucker pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + fohm + '" onchange="shEditMeritPt(' + realIdx + ',\'free_ohm\',+this.value)"></div>';
+   if (opts.showINV) h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-inv-lbl-' + realIdx + '">INV</span><input id="bd-inv-' + realIdx + '" name="bd-inv-' + realIdx + '" aria-label="Invested pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + finv + '" onchange="shEditMeritPt(' + realIdx + ',\'free_inv\',+this.value)"></div>';
+   // N-7 (issue #760): Collective Compound allocator — writes directly to
+   // m.free_grants.<slug> (map shape, no new legacy free_<slug> field) per the
+   // ADR-005 allocator-write-path amendment.
+   // N-7c (issue #771): id + aria-label so browsers don't flag "form field
+   // element should have an id or name attribute / No label associated with a
+   // form field". The 5 sibling steppers (LK/INV/VM/OHM/MCI) got the same
+   // treatment in #774 — see lines above.
+   // COLLECTIVE-2 (issue #1110): one stepper per compound the character
+   // belongs to that claims this merit, each writing its OWN slug. Pre-#1110
+   // this was a single `showNECRO` flag hardwired to free_grants.necro, which
+   // would have written a Crone or Sanctified allocation into the Necropolis
+   // pool. The descriptor supplies slug + source; no name literals here.
+   for (const _cmp of (opts.compoundPools || [])) {
+     if (!_cmp || !_cmp.slug) continue;
+     h += '<div class="bd-grp"><span class="bd-lbl bd-bonus-lbl" id="bd-' + _cmp.slug + '-lbl-' + realIdx + '">' + esc(_cmp.slug.toUpperCase()) + '</span><input id="bd-' + _cmp.slug + '-' + realIdx + '" name="bd-' + _cmp.slug + '-' + realIdx + '" aria-label="' + esc(_cmp.source || _cmp.slug) + ' pool allocation" class="merit-bd-input bd-bonus-input" type="number" min="0" value="' + freeOf(mc, _cmp.slug) + '" onchange="shEditMeritPt(' + realIdx + ',\'free_grants.' + _cmp.slug + '\',+this.value)"></div>';
+   }
+   h += '<div class="bd-eq"><span class="bd-val">' + effective + ' dot' + (effective === 1 ? '' : 's') + '</span>' + needsHint + '</div>'
+     + '</div>';
+   // N-9 (issue #762, Bug 2): standing-merit render paths (MCI, PT) don't read
+   // m.bonus, so the Bonus row was visible-but-no-op. Standing call sites pass
+   // opts.hideBonus=true to suppress the row entirely. Default behaviour
+   // (general/influence/domain/style merits) is unchanged.
+   if (!opts.hideBonus) {
+     const bon = mc.bonus || 0;
+     h += '<div class="attr-derived-row"><span class="bd-lbl">Bonus</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',-1)"' + (bon === 0 ? ' disabled' : '') + '>&#x25BC;</button><span class="bd-src">' + (bon > 0 ? '+' + bon : '0') + '</span><button class="sh-stat-adj" onclick="shAdjMeritBonus(' + realIdx + ',1)">&#x25B2;</button></div>';
+   }
+   return h;
+ }
+

 ❯ tests/oath-a-pledge-helpers.test.js:388:16
    386|     const xp = read('public/js/editor/xp.js');
    387|     const dom = read('public/js/editor/domain.js');
    388|     expect(xp).toContain("export function meritRating(c, m) {\n  if (m…
       |                ^
    389|     expect(dom).toContain('export function meritEffectiveRating(c, m) …
    390|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[11/12]⎯


diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:58:58.847580Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The isolated `ddf059f8` export reproduced the ten-file result exactly—same 10 failed files, same 5 failing tests, same 63 passes and same seven suite-load failures—so the author’s pre-existing-failure comparison is independently verified. I’m restoring the one-line TLS edit from Git now and deleting only the temporary base export/archive I created after validating their exact paths.
2026-08-13T02:59:13.323550Z ERROR codex_core::tools::router: error=`"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; git -C "'$reviewRoot checkout -- '"'server/db.js'; "'$baseDir = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8\"; "'$baseZip = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8.zip\"; "'$junction = "$baseDir'"\\server\\node_modules\"; if ((Get-Item -LiteralPath "'$baseDir).FullName -ne $baseDir) { throw '"'Base directory path validation failed' }; if ((Get-Item -LiteralPath "'$baseZip).FullName -ne $baseZip) { throw '"'Archive path validation failed' }; if (Test-Path -LiteralPath "'$junction) { Remove-Item -LiteralPath $junction -Force }; Remove-Item -LiteralPath $baseDir -Recurse -Force; Remove-Item -LiteralPath $baseZip -Force; git -C $reviewRoot diff -- '"'server/db.js'; Write-Output \"baseDirExists="'$(Test-Path -LiteralPath $baseDir) baseZipExists=$(Test-Path -LiteralPath $baseZip)"'` rejected: blocked by policy
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; git -C "'$reviewRoot checkout -- '"'server/db.js'; "'$baseDir = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8\"; "'$baseZip = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8.zip\"; "'$junction = "$baseDir'"\\server\\node_modules\"; if ((Get-Item -LiteralPath "'$baseDir).FullName -ne $baseDir) { throw '"'Base directory path validation failed' }; if ((Get-Item -LiteralPath "'$baseZip).FullName -ne $baseZip) { throw '"'Archive path validation failed' }; if (Test-Path -LiteralPath "'$junction) { Remove-Item -LiteralPath $junction -Force }; Remove-Item -LiteralPath $baseDir -Recurse -Force; Remove-Item -LiteralPath $baseZip -Force; git -C $reviewRoot diff -- '"'server/db.js'; Write-Output \"baseDirExists="'$(Test-Path -LiteralPath $baseDir) baseZipExists=$(Test-Path -LiteralPath $baseZip)"' in D:\Terra Mortis\TM Suite
 declined in 0ms:
`"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; git -C "'$reviewRoot checkout -- '"'server/db.js'; "'$baseDir = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8\"; "'$baseZip = "$reviewRoot'"\\.review-tmp-eqc1152-ddf059f8.zip\"; "'$junction = "$baseDir'"\\server\\node_modules\"; if ((Get-Item -LiteralPath "'$baseDir).FullName -ne $baseDir) { throw '"'Base directory path validation failed' }; if ((Get-Item -LiteralPath "'$baseZip).FullName -ne $baseZip) { throw '"'Archive path validation failed' }; if (Test-Path -LiteralPath "'$junction) { Remove-Item -LiteralPath $junction -Force }; Remove-Item -LiteralPath $baseDir -Recurse -Force; Remove-Item -LiteralPath $baseZip -Force; git -C $reviewRoot diff -- '"'server/db.js'; Write-Output \"baseDirExists="'$(Test-Path -LiteralPath $baseDir) baseZipExists=$(Test-Path -LiteralPath $baseZip)"'` rejected: blocked by policy
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:59:13.434127Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\Terra Mortis\\TM Suite' checkout -- 'server/db.js'; git -C 'D:\\Terra Mortis\\TM Suite' diff -- 'server/db.js'" in D:\Terra Mortis\TM Suite
 succeeded in 3037ms:
diff --git a/server/db.js b/server/db.js
index c45f6a1f..92569cd7 100644
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@ export async function connectDb() {
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
fatal: Unable to create 'D:/Terra Mortis/TM Suite/.git/index.lock': Permission denied

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:59:20.219398Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Git could not restore `server/db.js` because this sandbox has read-only `.git` metadata (`index.lock: Permission denied`). The failed checkout changed nothing. I’m restoring the exact committed line with the patch tool instead, then confirming `git diff -- server/db.js` is empty.
apply patch
patch: completed
D:\Terra Mortis\TM Suite\server\db.js
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:59:28.345301Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\Terra Mortis\\TM Suite' diff -- 'server/db.js'; git -C 'D:\\Terra Mortis\\TM Suite' status --short -- 'server/db.js'" in D:\Terra Mortis\TM Suite
 succeeded in 3133ms:
warning: in the working copy of 'server/db.js', LF will be replaced by CRLF the next time Git touches it
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:59:38.363581Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Item -LiteralPath 'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8','D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8.zip','D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8\\server\\node_modules' | Select-Object FullName,PSIsContainer,LinkType,Target" in D:\Terra Mortis\TM Suite
 succeeded in 3125ms:

[32;1mFullName                                                                 [0m[32;1m PSIsContainer[0m[32;1m LinkType[0m[32;1m Target[0m
[32;1m--------                                                                 [0m [32;1m-------------[0m [32;1m--------[0m [32;1m------[0m
D:\Terra Mortis\TM Suite\.review-tmp-eqc1152-ddf059f8                              True          
D:\Terra Mortis\TM Suite\.review-tmp-eqc1152-ddf059f8.zip                         False          
D:\Terra Mortis\TM Suite\.review-tmp-eqc1152-ddf059f8\server\node_modules          True Junction D:\Terra Mortis\TM Su…


diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:59:47.764230Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
2026-08-13T02:59:51.143797Z ERROR codex_core::tools::router: error=`"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Remove-Item -LiteralPath 'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8\\server\\node_modules' -Force"` rejected: blocked by policy
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Remove-Item -LiteralPath 'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8\\server\\node_modules' -Force" in D:\Terra Mortis\TM Suite
 declined in 0ms:
`"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Remove-Item -LiteralPath 'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8\\server\\node_modules' -Force"` rejected: blocked by policy
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T02:59:51.265227Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "[System.IO.Directory]::Delete('D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8\\server\\node_modules')" in D:\Terra Mortis\TM Suite
 succeeded in 3092ms:
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T03:00:00.868728Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "[System.IO.Directory]::Delete('D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8', "'$true); [System.IO.File]::Delete('"'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8.zip'); Write-Output \"baseDirExists="'$([System.IO.Directory]::Exists('"'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8')) baseZipExists="'$([System.IO.File]::Exists('"'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8.zip'))\"" in D:\Terra Mortis\TM Suite
 succeeded in 4694ms:
baseDirExists=False baseZipExists=False

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T03:00:16.982634Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Before finalizing ship readiness, I’m doing one additional read-only check against the local `tm_suite` catalogue: whether legacy weapon/armour rows actually have the discriminator fields the new consumers require. This matters because the migration changes only `bucket`; an old row with missing shape fields would be “mapped” yet lose its old weapon/armour behavior. No production migration or writes will be run.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; node --input-type=module -e \"import { MongoClient } from 'mongodb'; const c=new MongoClient('mongodb://127.0.0.1:27017'); await c.connect(); const col=c.db('tm_suite').collection('equipment_catalogue'); const buckets=await col.aggregate([{\\"'$group:{_id:'"'\\"'$bucket'"',n:{\\"'$sum:1}}},{'"\\"'$sort:{_id:1}}]).toArray(); const weakWeapons=await col.find({bucket:'"'weapon',weapon_type:null}).project({_id:1,name:1,weapon_type:1,damage_mod:1,damage_type:1}).toArray(); const weakArmour=await col.find({bucket:'armour',armour_value:null}).project({_id:1,name:1,armour_value:1,defence_penalty:1}).toArray(); console.log(JSON.stringify({buckets,weakWeapons,weakArmour},null,2)); await c.close();\"" in D:\Terra Mortis\TM Suite
2026-08-13T03:01:01.390506Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 3.2 seconds
Output:
[31;1mParserError: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m . ent_catalogue'); const buckets=await col.aggregate([{\[36;1m$group:[0m{_id:'\$ .[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m                                                         ~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mVariable reference is not valid. ':' was not followed by a valid variable name character. Consider using ${} to[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m[31;1m[36;1m     | [31;1mdelimit the name.[0m

 exited 1 in 3224ms:
[31;1mParserError: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m . ent_catalogue'); const buckets=await col.aggregate([{\[36;1m$group:[0m{_id:'\$ .[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m                                                         ~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mVariable reference is not valid. ':' was not followed by a valid variable name character. Consider using ${} to[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m[31;1m[36;1m     | [31;1mdelimit the name.[0m

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T03:01:01.435660Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath \""'$reviewRoot'"\\server\"; node --input-type=module -e 'import { MongoClient } from \"mongodb\"; const c=new MongoClient(\"mongodb://127.0.0.1:27017\"); await c.connect(); const col=c.db(\"tm_suite\").collection(\"equipment_catalogue\"); const buckets=await col.aggregate([{"'$group:{_id:"$bucket",n:{$sum:1}}},{$sort:{_id:1}}]).toArray(); const weakWeapons=await col.find({bucket:"weapon",weapon_type:null}).project({_id:1,name:1,weapon_type:1,damage_mod:1,damage_type:1}).toArray(); const weakArmour=await col.find({bucket:"armour",armour_value:null}).project({_id:1,name:1,armour_value:1,defence_penalty:1}).toArray(); console.log(JSON.stringify({buckets,weakWeapons,weakArmour},null,2)); await c.close();'"'" in D:\Terra Mortis\TM Suite
 succeeded in 3809ms:
{
  "buckets": [],
  "weakWeapons": [],
  "weakArmour": []
}

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T03:01:16.824555Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath "'$reviewRoot; node --input-type=module -e '"'globalThis.location={hostname:\"\"}; const {armourDefencePenalty}=await import(\"./public/js/data/equipment-derivation.js\"); const legacy={bucket:\"armour\",armour_value:null,defence_penalty:2}; const migrated={...legacy,bucket:\"combat_gear\"}; const c={equipment:[{catalogue_id:\"a\",state:\"worn\"}]}; const oldPenalty=(legacy.bucket===\"armour\"&&Number.isInteger(legacy.defence_penalty)&&legacy.defence_penalty>0)?legacy.defence_penalty:0; console.log({oldPenalty,newPenalty:armourDefencePenalty(c,()=>migrated)});'" in D:\Terra Mortis\TM Suite
 succeeded in 3674ms:
{ oldPenalty: 2, newPenalty: 0 }

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$reviewRoot = '"'D:\\Terra Mortis\\TM Suite'; Set-Location -LiteralPath "'$reviewRoot; node -e "const legacy={bucket:'"'weapon',weapon_type:null,damage_mod:2}; const migrated={...legacy,bucket:'combat_gear'}; console.log({oldWeaponRef:legacy.bucket==='weapon',newWeaponRef:migrated.bucket==='combat_gear'&&migrated.weapon_type"'!=null});"' in D:\Terra Mortis\TM Suite
 succeeded in 3222ms:
{ oldWeaponRef: true, newWeaponRef: false }

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..c11942e22531f248fac7166fb1778b96c9f48ae7
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,101 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T03:01:52.171571Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
The acceptance audit found a ship-level data risk the record does not address: legacy discriminator fields were optional. A schema-valid old armour row with `armour_value: null, defence_penalty: 2` contributed 2 before migration and contributes 0 afterward; a schema-valid old weapon with `weapon_type: null` was in the weapon panel before and is excluded afterward. The migration changes only `bucket` and does not audit these rows, so “correctly mapped” does not guarantee behavior preservation. I’m recording this as High before final attestation.
apply patch
patch: completed
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-findings.md
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..2bfce7c627a2d570750a579badcb8cfd774160cc
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,131 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..2bfce7c627a2d570750a579badcb8cfd774160cc
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,131 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw -LiteralPath 'D:\\Terra Mortis\\TM Suite\\specs\\stories\\code-review\\issue-1152-eqc1-codex-findings.md'" in D:\Terra Mortis\TM Suite
 succeeded in 2923ms:
# Adversarial review findings — issue #1152 / EQC-1

Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.

## High

### Pass 1

- None found.

### Pass 2

- None found.

### Pass 3a

- None found.

### Pass 3b

#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field

- **Severity**: High
- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.

## Medium

### Pass 1

#### [Pass 1] The advertised single-level/reference containment contract is not enforced

- **Severity**: Medium
- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.

#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs

- **Severity**: Medium
- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.

### Pass 2

#### [Pass 2] The full-character PUT route persists invalid containment references and nesting

- **Severity**: Medium
- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.

### Pass 3a

#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat

- **Severity**: Medium
- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.

### Pass 3b

#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files

- **Severity**: Medium
- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
- **Confidence**: High.

## Low

### Pass 1

#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows

- **Severity**: Low
- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
- **Confidence**: High.

### Pass 2

#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys

- **Severity**: Low
- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.

#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy

- **Severity**: Low
- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.

### Pass 3a

#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally

- **Severity**: Low
- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.

### Pass 3b

#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment

- **Severity**: Low
- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.

## Validation notes

_To be completed after Pass 3b. No validation attestation is frozen yet._


diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..2bfce7c627a2d570750a579badcb8cfd774160cc
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,131 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\Terra Mortis\\TM Suite' status --short -- 'server/db.js' 'specs/stories/code-review/issue-1152-eqc1-codex-findings.md' '.review-tmp-eqc1152-ddf059f8' '.review-tmp-eqc1152-ddf059f8.zip'; git -C 'D:\\Terra Mortis\\TM Suite' diff -- 'server/db.js'; Write-Output \"tempDirExists="'$([System.IO.Directory]::Exists('"'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8')) tempZipExists="'$([System.IO.File]::Exists('"'D:\\Terra Mortis\\TM Suite\\.review-tmp-eqc1152-ddf059f8.zip'))\"" in D:\Terra Mortis\TM Suite
 succeeded in 3139ms:
?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md
tempDirExists=False tempZipExists=False
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..2bfce7c627a2d570750a579badcb8cfd774160cc
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,131 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+_To be completed after Pass 3b. No validation attestation is frozen yet._

2026-08-13T03:03:21.177233Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
All temporary material is now removed and `server/db.js` has an empty diff; the only path attributable to this review is the requested findings file. I’m completing the report with the pass-by-pass file attestation, all command outcomes—including failed attempts—and an explicit “needs patches / do not trust against production today” ship assessment.
2026-08-13T03:05:00.667951Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
apply patch
patch: completed
D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-findings.md
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..4b692c2f41127bf086610419ccf2e8a821338d8a
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,179 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+### Ship assessment
+
+**Needs patches; do not ship or run the production migration as-is.** The direct readers and ordinary migrated fixtures behave correctly, and the nine equipment gates are green, but the migration does not preflight legacy weapon/armour rows for the discriminator fields on which the new mechanics depend. I would not trust it against real character data today until a production dry-run reports/audits every old weapon lacking `weapon_type` and every old armour lacking `armour_value` (with an explicit remediation policy), and containment's stated reference/single-level contract is either enforced or narrowed in the AC/comments. The live production migration was not run.
+
+### Information-barrier attestation and files opened
+
+- **Pass 1**: Opened only `specs/stories/code-review/issue-1152-eqc1-diff.txt`, in chunks after the first full read was output-truncated. I did not list or open repository source, the story, or the Dev Agent Record before writing the complete Pass 1 text to this file.
+- **Pass 2**: After Pass 1 was frozen, opened/searched production and test context including `public/js/data/equipment-derivation.js`, `public/js/suite/roll.js`, `public/js/suite/roll-v2.js`, `public/js/editor/sheet.js`, `public/js/admin/equipment-catalogue-admin.js`, `public/css/admin-layout.css`, `server/schemas/character.schema.js`, `server/schemas/equipment_catalogue.schema.js`, `server/routes/characters.js`, `server/routes/equipment-catalogue.js`, `server/db.js`, `server/config.js`, `server/tests/helpers/db-setup.js`, `server/tests/helpers/setup-env.js`, and repository-wide grep results under `public/`, `server/`, `tests/`, and `specs/` excluding the issue-1152 story. An attempted open of the nonexistent `server/data/equipment-catalogue.js` failed. I did not open the issue-1152 story until Pass 2 was frozen.
+- **Pass 3a**: Opened only story lines 1-53 (Story, Background, taxonomy, and AC 1-7) before freezing Pass 3a. I did not read Tasks, Dev Notes, or the Dev Agent Record during 3a.
+- **Pass 3b**: After 3a was frozen, opened the Dev Agent Record (lines 105-end), then the intervening Tasks/Dev Notes/Project Structure/References (lines 54-104) to read the author's account in full. Also opened the named tests and source lines surfaced by test failures, plus Git metadata/diffs for the specified commits. I did not read or modify any sibling repository.
+
+### Commands run and real results
+
+- Read the supplied diff with `Get-Content` and located/chunked its file sections with `Select-String`/array slicing. The first full output was truncated; the subsequent three chunks covered all 938 lines.
+- Ran repository searches with `rg` for old bucket comparisons, all `.bucket` readers, `container_id`, CSS bucket classes, discriminator fields, and new-taxonomy test coverage. One first regex command failed with a PowerShell parser error; one broad grep timed out after 22 seconds but returned useful partial results; narrower production-source searches completed. No production old-bucket comparison was found. All five new CSS classes exist and no old class remains. `container_id` has no production read site.
+- Directly executed `planBucketMigration` on `null`, `undefined`, `{}`, null/empty/number/array buckets. It did not throw; `['weapon']` was coerced to the mapping-table key and migrated.
+- Directly executed the three-item armour trace. Result: `{ penalty: 3, count: 1 }`; only the armour-shaped item contributed, while weapon-shaped and neither-shaped items contributed nothing. Static tracing of both roller predicates showed only the weapon-shaped item enters weapon reference and none of the three enters skill chips. Sheet predicates make the neither-shaped item reach `Other Combat Gear`.
+- Checked `git status`, HEAD, and `git diff --name-status ddf059f8 c7e6771b`. HEAD is `c7e6771b19367f0a6face5e675c7a3e2ac3737b4`. The EQC-1 source/tooling diff has 2 new files and 14 modified files; including the deliberately excluded story, the commit has 17 paths. The workspace already contained numerous unrelated tracked/untracked user changes, which were not touched.
+- Checked Mongo availability. A `mongod` process existed. The first `equipment.test.js` run used the repository's remote URI and failed with `EACCES 159.143.141.178:27017` (1 failed file, 14 skipped tests). Retrying with `MONGODB_URI=mongodb://127.0.0.1:27017` failed TLS negotiation with `ECONNRESET` because `server/db.js` hard-codes `tls: true` (1 failed file, 14 skipped tests).
+- Temporarily changed only `server/db.js` from `tls: true` to `tls: false` to use the local non-TLS daemon. With `MONGODB_URI=mongodb://127.0.0.1:27017`, the nine requested suites were run individually and produced the exact current gate totals below (all integration slices ran; no skips):
+
+  - `tests/equipment.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/equipment-client-fixes.test.js`: 1 file passed; **6/6 tests passed**.
+  - `tests/issue-868-ecm-1-equipment-catalogue-api.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-871-876-ecm-4-9-bundle.test.js`: 1 file passed; **19/19 tests passed**.
+  - `tests/issue-872-ecm-5-editor-cache.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/issue-896-availability-filter.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-879-defence-penalty-wirein.test.js`: 1 file passed; **36/36 tests passed**.
+  - `tests/issue-1152-eqc1-bucket-migration.test.js`: 1 file passed; **11/11 tests passed**.
+  - `tests/issue-873-ecm-6-admin-sidebar.test.js`: 1 file passed; **14/14 tests passed**.
+  - Aggregate: **9/9 files passed; 170/170 tests passed; 0 skipped**.
+
+- Ran the ten named historical-failure files together on current HEAD. Result: **10 failed files; 5 failed tests; 63 passed (68 tests)**. The failing tests were exactly: `epic.708.3-cycle-phase-controls` — “exports setGamePhase function”, “uses data-phase attribute on phase buttons”, “highlights active phase with gold2 colour”; `n7-n9-allocator-readers` — “all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly)”; `oath-a-pledge-helpers` — “meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form”. Seven other named files failed during suite loading. Searching the three files containing the five failed tests found none of `equipment|bucket|armour|weapon|container`.
+- Attempted full `npm test` with the local TLS workaround. It **did not complete**: the command timed out after 302.6 seconds, many integration suites reported `ECONNRESET` and skipped, and Vitest emitted no final aggregate. Therefore this review could not reproduce the claimed **2463 total / 2458 pass / 5 fail** full-suite numbers.
+- Independently reproduced the pre-existing-failure claim without stashing the dirty workspace: `git archive ddf059f8` into an isolated workspace temp directory, junctioned its `server/node_modules`, applied the same temporary local-TLS change inside that disposable export, and ran the same ten files. Result was identical: **10 failed files; 5 failed tests; 63 passed (68 tests), same five test names and same seven suite-load failures**. A first attempt to archive under `D:\tmp` failed with permission denied; the workspace-local isolated export succeeded.
+- Queried the local `tm_suite.equipment_catalogue` read-only. It contained zero documents, so production prevalence of missing discriminator fields could not be assessed. No live migration was run.
+- Directly compared legacy/new discriminator behavior: old armour `{ armour_value: null, defence_penalty: 2 }` yielded old penalty 2 versus new penalty 0; old weapon `{ weapon_type: null, damage_mod: 2 }` yielded old weapon-reference eligibility `true` versus new `false`.
+
+### Could not run or verify
+
+- Could not query the real production `tm_suite` catalogue; the configured remote Mongo endpoint is blocked by this environment's network policy. The local database was empty.
+- Could not obtain a valid full-suite aggregate because the five-minute run timed out after local Mongo connection resets under parallel load. Consequently the historical 2463/2458 total remains unverified here.
+- Did not run the live migration against production, per the story's explicit deploy-time scope and this review's no-write constraint.
+- Could not use `git checkout -- server/db.js` to restore the temporary edit because `.git` is read-only in this sandbox (`index.lock: Permission denied`). I restored the exact committed line with `apply_patch` instead; `git diff -- server/db.js` and path-scoped `git status --short` are empty afterward.
+
+### Workspace integrity
+
+- The temporary base archive/export and its `node_modules` junction were deleted after exact-path checks; existence checks returned false for both archive and directory.
+- No source change remains from this review. The requested findings file is the only created review artifact. Final path-scoped status showed only `?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md`; `server/db.js` had no diff. The pre-existing unrelated dirty-worktree entries were preserved.

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..4b692c2f41127bf086610419ccf2e8a821338d8a
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,179 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+### Ship assessment
+
+**Needs patches; do not ship or run the production migration as-is.** The direct readers and ordinary migrated fixtures behave correctly, and the nine equipment gates are green, but the migration does not preflight legacy weapon/armour rows for the discriminator fields on which the new mechanics depend. I would not trust it against real character data today until a production dry-run reports/audits every old weapon lacking `weapon_type` and every old armour lacking `armour_value` (with an explicit remediation policy), and containment's stated reference/single-level contract is either enforced or narrowed in the AC/comments. The live production migration was not run.
+
+### Information-barrier attestation and files opened
+
+- **Pass 1**: Opened only `specs/stories/code-review/issue-1152-eqc1-diff.txt`, in chunks after the first full read was output-truncated. I did not list or open repository source, the story, or the Dev Agent Record before writing the complete Pass 1 text to this file.
+- **Pass 2**: After Pass 1 was frozen, opened/searched production and test context including `public/js/data/equipment-derivation.js`, `public/js/suite/roll.js`, `public/js/suite/roll-v2.js`, `public/js/editor/sheet.js`, `public/js/admin/equipment-catalogue-admin.js`, `public/css/admin-layout.css`, `server/schemas/character.schema.js`, `server/schemas/equipment_catalogue.schema.js`, `server/routes/characters.js`, `server/routes/equipment-catalogue.js`, `server/db.js`, `server/config.js`, `server/tests/helpers/db-setup.js`, `server/tests/helpers/setup-env.js`, and repository-wide grep results under `public/`, `server/`, `tests/`, and `specs/` excluding the issue-1152 story. An attempted open of the nonexistent `server/data/equipment-catalogue.js` failed. I did not open the issue-1152 story until Pass 2 was frozen.
+- **Pass 3a**: Opened only story lines 1-53 (Story, Background, taxonomy, and AC 1-7) before freezing Pass 3a. I did not read Tasks, Dev Notes, or the Dev Agent Record during 3a.
+- **Pass 3b**: After 3a was frozen, opened the Dev Agent Record (lines 105-end), then the intervening Tasks/Dev Notes/Project Structure/References (lines 54-104) to read the author's account in full. Also opened the named tests and source lines surfaced by test failures, plus Git metadata/diffs for the specified commits. I did not read or modify any sibling repository.
+
+### Commands run and real results
+
+- Read the supplied diff with `Get-Content` and located/chunked its file sections with `Select-String`/array slicing. The first full output was truncated; the subsequent three chunks covered all 938 lines.
+- Ran repository searches with `rg` for old bucket comparisons, all `.bucket` readers, `container_id`, CSS bucket classes, discriminator fields, and new-taxonomy test coverage. One first regex command failed with a PowerShell parser error; one broad grep timed out after 22 seconds but returned useful partial results; narrower production-source searches completed. No production old-bucket comparison was found. All five new CSS classes exist and no old class remains. `container_id` has no production read site.
+- Directly executed `planBucketMigration` on `null`, `undefined`, `{}`, null/empty/number/array buckets. It did not throw; `['weapon']` was coerced to the mapping-table key and migrated.
+- Directly executed the three-item armour trace. Result: `{ penalty: 3, count: 1 }`; only the armour-shaped item contributed, while weapon-shaped and neither-shaped items contributed nothing. Static tracing of both roller predicates showed only the weapon-shaped item enters weapon reference and none of the three enters skill chips. Sheet predicates make the neither-shaped item reach `Other Combat Gear`.
+- Checked `git status`, HEAD, and `git diff --name-status ddf059f8 c7e6771b`. HEAD is `c7e6771b19367f0a6face5e675c7a3e2ac3737b4`. The EQC-1 source/tooling diff has 2 new files and 14 modified files; including the deliberately excluded story, the commit has 17 paths. The workspace already contained numerous unrelated tracked/untracked user changes, which were not touched.
+- Checked Mongo availability. A `mongod` process existed. The first `equipment.test.js` run used the repository's remote URI and failed with `EACCES 159.143.141.178:27017` (1 failed file, 14 skipped tests). Retrying with `MONGODB_URI=mongodb://127.0.0.1:27017` failed TLS negotiation with `ECONNRESET` because `server/db.js` hard-codes `tls: true` (1 failed file, 14 skipped tests).
+- Temporarily changed only `server/db.js` from `tls: true` to `tls: false` to use the local non-TLS daemon. With `MONGODB_URI=mongodb://127.0.0.1:27017`, the nine requested suites were run individually and produced the exact current gate totals below (all integration slices ran; no skips):
+
+  - `tests/equipment.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/equipment-client-fixes.test.js`: 1 file passed; **6/6 tests passed**.
+  - `tests/issue-868-ecm-1-equipment-catalogue-api.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-871-876-ecm-4-9-bundle.test.js`: 1 file passed; **19/19 tests passed**.
+  - `tests/issue-872-ecm-5-editor-cache.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/issue-896-availability-filter.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-879-defence-penalty-wirein.test.js`: 1 file passed; **36/36 tests passed**.
+  - `tests/issue-1152-eqc1-bucket-migration.test.js`: 1 file passed; **11/11 tests passed**.
+  - `tests/issue-873-ecm-6-admin-sidebar.test.js`: 1 file passed; **14/14 tests passed**.
+  - Aggregate: **9/9 files passed; 170/170 tests passed; 0 skipped**.
+
+- Ran the ten named historical-failure files together on current HEAD. Result: **10 failed files; 5 failed tests; 63 passed (68 tests)**. The failing tests were exactly: `epic.708.3-cycle-phase-controls` — “exports setGamePhase function”, “uses data-phase attribute on phase buttons”, “highlights active phase with gold2 colour”; `n7-n9-allocator-readers` — “all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly)”; `oath-a-pledge-helpers` — “meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form”. Seven other named files failed during suite loading. Searching the three files containing the five failed tests found none of `equipment|bucket|armour|weapon|container`.
+- Attempted full `npm test` with the local TLS workaround. It **did not complete**: the command timed out after 302.6 seconds, many integration suites reported `ECONNRESET` and skipped, and Vitest emitted no final aggregate. Therefore this review could not reproduce the claimed **2463 total / 2458 pass / 5 fail** full-suite numbers.
+- Independently reproduced the pre-existing-failure claim without stashing the dirty workspace: `git archive ddf059f8` into an isolated workspace temp directory, junctioned its `server/node_modules`, applied the same temporary local-TLS change inside that disposable export, and ran the same ten files. Result was identical: **10 failed files; 5 failed tests; 63 passed (68 tests), same five test names and same seven suite-load failures**. A first attempt to archive under `D:\tmp` failed with permission denied; the workspace-local isolated export succeeded.
+- Queried the local `tm_suite.equipment_catalogue` read-only. It contained zero documents, so production prevalence of missing discriminator fields could not be assessed. No live migration was run.
+- Directly compared legacy/new discriminator behavior: old armour `{ armour_value: null, defence_penalty: 2 }` yielded old penalty 2 versus new penalty 0; old weapon `{ weapon_type: null, damage_mod: 2 }` yielded old weapon-reference eligibility `true` versus new `false`.
+
+### Could not run or verify
+
+- Could not query the real production `tm_suite` catalogue; the configured remote Mongo endpoint is blocked by this environment's network policy. The local database was empty.
+- Could not obtain a valid full-suite aggregate because the five-minute run timed out after local Mongo connection resets under parallel load. Consequently the historical 2463/2458 total remains unverified here.
+- Did not run the live migration against production, per the story's explicit deploy-time scope and this review's no-write constraint.
+- Could not use `git checkout -- server/db.js` to restore the temporary edit because `.git` is read-only in this sandbox (`index.lock: Permission denied`). I restored the exact committed line with `apply_patch` instead; `git diff -- server/db.js` and path-scoped `git status --short` are empty afterward.
+
+### Workspace integrity
+
+- The temporary base archive/export and its `node_modules` junction were deleted after exact-path checks; existence checks returned false for both archive and directory.
+- No source change remains from this review. The requested findings file is the only created review artifact. Final path-scoped status showed only `?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md`; `server/db.js` had no diff. The pre-existing unrelated dirty-worktree entries were preserved.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git -C 'D:\\Terra Mortis\\TM Suite' diff --check -- 'specs/stories/code-review/issue-1152-eqc1-codex-findings.md' 'server/db.js'; git -C 'D:\\Terra Mortis\\TM Suite' status --short -- 'server/db.js' 'specs/stories/code-review/issue-1152-eqc1-codex-findings.md' '.review-tmp-eqc1152-ddf059f8' '.review-tmp-eqc1152-ddf059f8.zip'; (Get-Content -LiteralPath 'D:\\Terra Mortis\\TM Suite\\specs\\stories\\code-review\\issue-1152-eqc1-codex-findings.md' | Measure-Object -Line).Lines" in D:\Terra Mortis\TM Suite
 succeeded in 3343ms:
?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md
125
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\angel/.config/git/ignore': Permission denied

diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..4b692c2f41127bf086610419ccf2e8a821338d8a
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,179 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+### Ship assessment
+
+**Needs patches; do not ship or run the production migration as-is.** The direct readers and ordinary migrated fixtures behave correctly, and the nine equipment gates are green, but the migration does not preflight legacy weapon/armour rows for the discriminator fields on which the new mechanics depend. I would not trust it against real character data today until a production dry-run reports/audits every old weapon lacking `weapon_type` and every old armour lacking `armour_value` (with an explicit remediation policy), and containment's stated reference/single-level contract is either enforced or narrowed in the AC/comments. The live production migration was not run.
+
+### Information-barrier attestation and files opened
+
+- **Pass 1**: Opened only `specs/stories/code-review/issue-1152-eqc1-diff.txt`, in chunks after the first full read was output-truncated. I did not list or open repository source, the story, or the Dev Agent Record before writing the complete Pass 1 text to this file.
+- **Pass 2**: After Pass 1 was frozen, opened/searched production and test context including `public/js/data/equipment-derivation.js`, `public/js/suite/roll.js`, `public/js/suite/roll-v2.js`, `public/js/editor/sheet.js`, `public/js/admin/equipment-catalogue-admin.js`, `public/css/admin-layout.css`, `server/schemas/character.schema.js`, `server/schemas/equipment_catalogue.schema.js`, `server/routes/characters.js`, `server/routes/equipment-catalogue.js`, `server/db.js`, `server/config.js`, `server/tests/helpers/db-setup.js`, `server/tests/helpers/setup-env.js`, and repository-wide grep results under `public/`, `server/`, `tests/`, and `specs/` excluding the issue-1152 story. An attempted open of the nonexistent `server/data/equipment-catalogue.js` failed. I did not open the issue-1152 story until Pass 2 was frozen.
+- **Pass 3a**: Opened only story lines 1-53 (Story, Background, taxonomy, and AC 1-7) before freezing Pass 3a. I did not read Tasks, Dev Notes, or the Dev Agent Record during 3a.
+- **Pass 3b**: After 3a was frozen, opened the Dev Agent Record (lines 105-end), then the intervening Tasks/Dev Notes/Project Structure/References (lines 54-104) to read the author's account in full. Also opened the named tests and source lines surfaced by test failures, plus Git metadata/diffs for the specified commits. I did not read or modify any sibling repository.
+
+### Commands run and real results
+
+- Read the supplied diff with `Get-Content` and located/chunked its file sections with `Select-String`/array slicing. The first full output was truncated; the subsequent three chunks covered all 938 lines.
+- Ran repository searches with `rg` for old bucket comparisons, all `.bucket` readers, `container_id`, CSS bucket classes, discriminator fields, and new-taxonomy test coverage. One first regex command failed with a PowerShell parser error; one broad grep timed out after 22 seconds but returned useful partial results; narrower production-source searches completed. No production old-bucket comparison was found. All five new CSS classes exist and no old class remains. `container_id` has no production read site.
+- Directly executed `planBucketMigration` on `null`, `undefined`, `{}`, null/empty/number/array buckets. It did not throw; `['weapon']` was coerced to the mapping-table key and migrated.
+- Directly executed the three-item armour trace. Result: `{ penalty: 3, count: 1 }`; only the armour-shaped item contributed, while weapon-shaped and neither-shaped items contributed nothing. Static tracing of both roller predicates showed only the weapon-shaped item enters weapon reference and none of the three enters skill chips. Sheet predicates make the neither-shaped item reach `Other Combat Gear`.
+- Checked `git status`, HEAD, and `git diff --name-status ddf059f8 c7e6771b`. HEAD is `c7e6771b19367f0a6face5e675c7a3e2ac3737b4`. The EQC-1 source/tooling diff has 2 new files and 14 modified files; including the deliberately excluded story, the commit has 17 paths. The workspace already contained numerous unrelated tracked/untracked user changes, which were not touched.
+- Checked Mongo availability. A `mongod` process existed. The first `equipment.test.js` run used the repository's remote URI and failed with `EACCES 159.143.141.178:27017` (1 failed file, 14 skipped tests). Retrying with `MONGODB_URI=mongodb://127.0.0.1:27017` failed TLS negotiation with `ECONNRESET` because `server/db.js` hard-codes `tls: true` (1 failed file, 14 skipped tests).
+- Temporarily changed only `server/db.js` from `tls: true` to `tls: false` to use the local non-TLS daemon. With `MONGODB_URI=mongodb://127.0.0.1:27017`, the nine requested suites were run individually and produced the exact current gate totals below (all integration slices ran; no skips):
+
+  - `tests/equipment.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/equipment-client-fixes.test.js`: 1 file passed; **6/6 tests passed**.
+  - `tests/issue-868-ecm-1-equipment-catalogue-api.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-871-876-ecm-4-9-bundle.test.js`: 1 file passed; **19/19 tests passed**.
+  - `tests/issue-872-ecm-5-editor-cache.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/issue-896-availability-filter.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-879-defence-penalty-wirein.test.js`: 1 file passed; **36/36 tests passed**.
+  - `tests/issue-1152-eqc1-bucket-migration.test.js`: 1 file passed; **11/11 tests passed**.
+  - `tests/issue-873-ecm-6-admin-sidebar.test.js`: 1 file passed; **14/14 tests passed**.
+  - Aggregate: **9/9 files passed; 170/170 tests passed; 0 skipped**.
+
+- Ran the ten named historical-failure files together on current HEAD. Result: **10 failed files; 5 failed tests; 63 passed (68 tests)**. The failing tests were exactly: `epic.708.3-cycle-phase-controls` — “exports setGamePhase function”, “uses data-phase attribute on phase buttons”, “highlights active phase with gold2 colour”; `n7-n9-allocator-readers` — “all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly)”; `oath-a-pledge-helpers` — “meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form”. Seven other named files failed during suite loading. Searching the three files containing the five failed tests found none of `equipment|bucket|armour|weapon|container`.
+- Attempted full `npm test` with the local TLS workaround. It **did not complete**: the command timed out after 302.6 seconds, many integration suites reported `ECONNRESET` and skipped, and Vitest emitted no final aggregate. Therefore this review could not reproduce the claimed **2463 total / 2458 pass / 5 fail** full-suite numbers.
+- Independently reproduced the pre-existing-failure claim without stashing the dirty workspace: `git archive ddf059f8` into an isolated workspace temp directory, junctioned its `server/node_modules`, applied the same temporary local-TLS change inside that disposable export, and ran the same ten files. Result was identical: **10 failed files; 5 failed tests; 63 passed (68 tests), same five test names and same seven suite-load failures**. A first attempt to archive under `D:\tmp` failed with permission denied; the workspace-local isolated export succeeded.
+- Queried the local `tm_suite.equipment_catalogue` read-only. It contained zero documents, so production prevalence of missing discriminator fields could not be assessed. No live migration was run.
+- Directly compared legacy/new discriminator behavior: old armour `{ armour_value: null, defence_penalty: 2 }` yielded old penalty 2 versus new penalty 0; old weapon `{ weapon_type: null, damage_mod: 2 }` yielded old weapon-reference eligibility `true` versus new `false`.
+
+### Could not run or verify
+
+- Could not query the real production `tm_suite` catalogue; the configured remote Mongo endpoint is blocked by this environment's network policy. The local database was empty.
+- Could not obtain a valid full-suite aggregate because the five-minute run timed out after local Mongo connection resets under parallel load. Consequently the historical 2463/2458 total remains unverified here.
+- Did not run the live migration against production, per the story's explicit deploy-time scope and this review's no-write constraint.
+- Could not use `git checkout -- server/db.js` to restore the temporary edit because `.git` is read-only in this sandbox (`index.lock: Permission denied`). I restored the exact committed line with `apply_patch` instead; `git diff -- server/db.js` and path-scoped `git status --short` are empty afterward.
+
+### Workspace integrity
+
+- The temporary base archive/export and its `node_modules` junction were deleted after exact-path checks; existence checks returned false for both archive and directory.
+- No source change remains from this review. The requested findings file is the only created review artifact. Final path-scoped status showed only `?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md`; `server/db.js` had no diff. The pre-existing unrelated dirty-worktree entries were preserved.

2026-08-13T03:06:08.015877Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
  ✓ Pass 1: inspect only the supplied diff and freeze blind findings
  ✓ Pass 2: inspect repository context, trace edge cases, and freeze findings
  ✓ Pass 3a: read only pre-record story sections and freeze AC findings
  ✓ Pass 3b: read Dev Agent Record, run verification gates, and audit claims
  ✓ Finalize severity-grouped report and validation attestation
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..4b692c2f41127bf086610419ccf2e8a821338d8a
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,179 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+### Ship assessment
+
+**Needs patches; do not ship or run the production migration as-is.** The direct readers and ordinary migrated fixtures behave correctly, and the nine equipment gates are green, but the migration does not preflight legacy weapon/armour rows for the discriminator fields on which the new mechanics depend. I would not trust it against real character data today until a production dry-run reports/audits every old weapon lacking `weapon_type` and every old armour lacking `armour_value` (with an explicit remediation policy), and containment's stated reference/single-level contract is either enforced or narrowed in the AC/comments. The live production migration was not run.
+
+### Information-barrier attestation and files opened
+
+- **Pass 1**: Opened only `specs/stories/code-review/issue-1152-eqc1-diff.txt`, in chunks after the first full read was output-truncated. I did not list or open repository source, the story, or the Dev Agent Record before writing the complete Pass 1 text to this file.
+- **Pass 2**: After Pass 1 was frozen, opened/searched production and test context including `public/js/data/equipment-derivation.js`, `public/js/suite/roll.js`, `public/js/suite/roll-v2.js`, `public/js/editor/sheet.js`, `public/js/admin/equipment-catalogue-admin.js`, `public/css/admin-layout.css`, `server/schemas/character.schema.js`, `server/schemas/equipment_catalogue.schema.js`, `server/routes/characters.js`, `server/routes/equipment-catalogue.js`, `server/db.js`, `server/config.js`, `server/tests/helpers/db-setup.js`, `server/tests/helpers/setup-env.js`, and repository-wide grep results under `public/`, `server/`, `tests/`, and `specs/` excluding the issue-1152 story. An attempted open of the nonexistent `server/data/equipment-catalogue.js` failed. I did not open the issue-1152 story until Pass 2 was frozen.
+- **Pass 3a**: Opened only story lines 1-53 (Story, Background, taxonomy, and AC 1-7) before freezing Pass 3a. I did not read Tasks, Dev Notes, or the Dev Agent Record during 3a.
+- **Pass 3b**: After 3a was frozen, opened the Dev Agent Record (lines 105-end), then the intervening Tasks/Dev Notes/Project Structure/References (lines 54-104) to read the author's account in full. Also opened the named tests and source lines surfaced by test failures, plus Git metadata/diffs for the specified commits. I did not read or modify any sibling repository.
+
+### Commands run and real results
+
+- Read the supplied diff with `Get-Content` and located/chunked its file sections with `Select-String`/array slicing. The first full output was truncated; the subsequent three chunks covered all 938 lines.
+- Ran repository searches with `rg` for old bucket comparisons, all `.bucket` readers, `container_id`, CSS bucket classes, discriminator fields, and new-taxonomy test coverage. One first regex command failed with a PowerShell parser error; one broad grep timed out after 22 seconds but returned useful partial results; narrower production-source searches completed. No production old-bucket comparison was found. All five new CSS classes exist and no old class remains. `container_id` has no production read site.
+- Directly executed `planBucketMigration` on `null`, `undefined`, `{}`, null/empty/number/array buckets. It did not throw; `['weapon']` was coerced to the mapping-table key and migrated.
+- Directly executed the three-item armour trace. Result: `{ penalty: 3, count: 1 }`; only the armour-shaped item contributed, while weapon-shaped and neither-shaped items contributed nothing. Static tracing of both roller predicates showed only the weapon-shaped item enters weapon reference and none of the three enters skill chips. Sheet predicates make the neither-shaped item reach `Other Combat Gear`.
+- Checked `git status`, HEAD, and `git diff --name-status ddf059f8 c7e6771b`. HEAD is `c7e6771b19367f0a6face5e675c7a3e2ac3737b4`. The EQC-1 source/tooling diff has 2 new files and 14 modified files; including the deliberately excluded story, the commit has 17 paths. The workspace already contained numerous unrelated tracked/untracked user changes, which were not touched.
+- Checked Mongo availability. A `mongod` process existed. The first `equipment.test.js` run used the repository's remote URI and failed with `EACCES 159.143.141.178:27017` (1 failed file, 14 skipped tests). Retrying with `MONGODB_URI=mongodb://127.0.0.1:27017` failed TLS negotiation with `ECONNRESET` because `server/db.js` hard-codes `tls: true` (1 failed file, 14 skipped tests).
+- Temporarily changed only `server/db.js` from `tls: true` to `tls: false` to use the local non-TLS daemon. With `MONGODB_URI=mongodb://127.0.0.1:27017`, the nine requested suites were run individually and produced the exact current gate totals below (all integration slices ran; no skips):
+
+  - `tests/equipment.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/equipment-client-fixes.test.js`: 1 file passed; **6/6 tests passed**.
+  - `tests/issue-868-ecm-1-equipment-catalogue-api.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-871-876-ecm-4-9-bundle.test.js`: 1 file passed; **19/19 tests passed**.
+  - `tests/issue-872-ecm-5-editor-cache.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/issue-896-availability-filter.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-879-defence-penalty-wirein.test.js`: 1 file passed; **36/36 tests passed**.
+  - `tests/issue-1152-eqc1-bucket-migration.test.js`: 1 file passed; **11/11 tests passed**.
+  - `tests/issue-873-ecm-6-admin-sidebar.test.js`: 1 file passed; **14/14 tests passed**.
+  - Aggregate: **9/9 files passed; 170/170 tests passed; 0 skipped**.
+
+- Ran the ten named historical-failure files together on current HEAD. Result: **10 failed files; 5 failed tests; 63 passed (68 tests)**. The failing tests were exactly: `epic.708.3-cycle-phase-controls` — “exports setGamePhase function”, “uses data-phase attribute on phase buttons”, “highlights active phase with gold2 colour”; `n7-n9-allocator-readers` — “all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly)”; `oath-a-pledge-helpers` — “meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form”. Seven other named files failed during suite loading. Searching the three files containing the five failed tests found none of `equipment|bucket|armour|weapon|container`.
+- Attempted full `npm test` with the local TLS workaround. It **did not complete**: the command timed out after 302.6 seconds, many integration suites reported `ECONNRESET` and skipped, and Vitest emitted no final aggregate. Therefore this review could not reproduce the claimed **2463 total / 2458 pass / 5 fail** full-suite numbers.
+- Independently reproduced the pre-existing-failure claim without stashing the dirty workspace: `git archive ddf059f8` into an isolated workspace temp directory, junctioned its `server/node_modules`, applied the same temporary local-TLS change inside that disposable export, and ran the same ten files. Result was identical: **10 failed files; 5 failed tests; 63 passed (68 tests), same five test names and same seven suite-load failures**. A first attempt to archive under `D:\tmp` failed with permission denied; the workspace-local isolated export succeeded.
+- Queried the local `tm_suite.equipment_catalogue` read-only. It contained zero documents, so production prevalence of missing discriminator fields could not be assessed. No live migration was run.
+- Directly compared legacy/new discriminator behavior: old armour `{ armour_value: null, defence_penalty: 2 }` yielded old penalty 2 versus new penalty 0; old weapon `{ weapon_type: null, damage_mod: 2 }` yielded old weapon-reference eligibility `true` versus new `false`.
+
+### Could not run or verify
+
+- Could not query the real production `tm_suite` catalogue; the configured remote Mongo endpoint is blocked by this environment's network policy. The local database was empty.
+- Could not obtain a valid full-suite aggregate because the five-minute run timed out after local Mongo connection resets under parallel load. Consequently the historical 2463/2458 total remains unverified here.
+- Did not run the live migration against production, per the story's explicit deploy-time scope and this review's no-write constraint.
+- Could not use `git checkout -- server/db.js` to restore the temporary edit because `.git` is read-only in this sandbox (`index.lock: Permission denied`). I restored the exact committed line with `apply_patch` instead; `git diff -- server/db.js` and path-scoped `git status --short` are empty afterward.
+
+### Workspace integrity
+
+- The temporary base archive/export and its `node_modules` junction were deleted after exact-path checks; existence checks returned false for both archive and directory.
+- No source change remains from this review. The requested findings file is the only created review artifact. Final path-scoped status showed only `?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md`; `server/db.js` had no diff. The pre-existing unrelated dirty-worktree entries were preserved.

2026-08-13T03:06:11.742616Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5
codex
Review complete: [issue-1152-eqc1-codex-findings.md](D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-findings.md).

Verdict: **needs patches; do not run the production migration as-is.**

Key blocker: legacy weapon/armour records may lack the discriminator fields required after migration. Such records migrate successfully but silently lose armour penalties or weapon-reference visibility.

Validation highlights:

- Equipment gates: **9/9 files, 170/170 tests passed, 0 skipped**.
- Historical failures reproduced on HEAD and `ddf059f8`: **10 failed files, 5 failed tests, 63 passed**, identical failure names.
- Full-suite 2463/2458 claim could not be reproduced because the run timed out amid local Mongo connection resets.
- Temporary TLS edit was restored; no source diff remains.
- Only the requested findings document was created.
diff --git a/.review-tmp-eqc1152-ddf059f8/server/db.js b/.review-tmp-eqc1152-ddf059f8/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..d8c791e8dab0c1835650c185a8d5376a55d9e763
--- a/.review-tmp-eqc1152-ddf059f8/server/db.js
+++ b/.review-tmp-eqc1152-ddf059f8/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: false,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/server/db.js b/server/db.js
index 70848adc11debc9f167906b329b116cdcdeb6982..986a0398ed166dea25c9142eb59b1dbc4824687e
--- a/server/db.js
+++ b/server/db.js
@@ -28,7 +28,7 @@
   const uri = config.MONGODB_URI.replace(/[&?]ssl=[^&]*/g, '');
   client = new MongoClient(uri, {
     serverSelectionTimeoutMS: 5000,
-    tls: true,
+    tls: true,
   });
   await client.connect();
   db = client.db(dbName);
diff --git a/specs/stories/code-review/issue-1152-eqc1-codex-findings.md b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
new file mode 100644
index 0000000000000000000000000000000000000000..4b692c2f41127bf086610419ccf2e8a821338d8a
--- /dev/null
+++ b/specs/stories/code-review/issue-1152-eqc1-codex-findings.md
@@ -0,0 +1,179 @@
+# Adversarial review findings — issue #1152 / EQC-1
+
+Earlier-pass text is frozen in place. Later-pass evidence is recorded as a new tagged finding and does not rewrite an earlier conclusion.
+
+## High
+
+### Pass 1
+
+- None found.
+
+### Pass 2
+
+- None found.
+
+### Pass 3a
+
+- None found.
+
+### Pass 3b
+
+#### [Pass 3b] Migration silently breaks legacy rows that lack the new discriminator field
+
+- **Severity**: High
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:82`, `public/js/data/equipment-derivation.js:68`, `public/js/suite/roll.js:237`, and `public/js/suite/roll-v2.js:313`
+- **Triggering input or sequence**: A schema-valid legacy armour document has `bucket: 'armour'`, `armour_value: null`, and `defence_penalty: 2`, or a schema-valid legacy weapon has `bucket: 'weapon'`, `weapon_type: null`, and another weapon stat such as `damage_mod: 2`. Bucket-specific stats were optional/nullable under the old schema and admin form. The migration changes only the bucket, does not backfill or audit the required discriminator, and reports both documents as successfully touched.
+- **Observable consequence**: The armour item contributed a defence penalty under the old bucket-only predicate but contributes zero after migration because `armour_value == null`; the weapon appeared in the old weapon-reference panel but disappears after migration because `weapon_type == null`. The sheet can still classify these via `defence_penalty`/`damage_mod`, so UI and combat mechanics disagree silently. Any character holding such a live row gets changed combat behavior even though the migration reports success, directly violating AC #5.
+- **Confidence**: High for the behavior: direct execution produced `{ oldPenalty: 2, newPenalty: 0 }` and `{ oldWeaponRef: true, newWeaponRef: false }`. Production prevalence is unknown because the local `tm_suite` catalogue is empty and production was intentionally not queried.
+
+## Medium
+
+### Pass 1
+
+#### [Pass 1] The advertised single-level/reference containment contract is not enforced
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:318` (new commentary) and `server/schemas/character.schema.js:346` (new field)
+- **Triggering input or sequence**: A character equipment entry is submitted with a syntactically valid 24-lowercase-hex `container_id` that equals its own `catalogue_id`, points to a missing entry, points to a non-container catalogue item, or forms a container chain. The schema checks only the string shape. The diff adds no corresponding cross-item validation while its comment says the write route is responsible and says containment is single-level.
+- **Observable consequence**: Invalid or recursive containment topology is accepted into character data despite the code comment's contract. This is currently described as display-inert, but the diff does not implement the promised reference validation or the claimed single-level restriction, leaving later containment consumers to interpret corrupt/ambiguous data.
+- **Confidence**: High. This pass is limited to the diff; Pass 2 must determine whether an unchanged pre-existing write path happens to supply the missing validation.
+
+#### [Pass 1] Apply mode can leave a partially migrated live catalogue without reporting completed/missed IDs
+
+- **Severity**: Medium
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:102`
+- **Triggering input or sequence**: Run the script with `--apply`; updates for documents 1 through N-1 succeed, then `updateOne` for document N rejects (for example, a transient Mongo connection failure). `totals` is incremented before each write, but summary logging occurs only after the entire loop.
+- **Observable consequence**: Documents already written remain on the new taxonomy while document N and all later documents remain on the old taxonomy. The top-level handler prints the thrown error and exits non-zero, but the accumulated mapping summary and the identities/count of missed documents are never emitted. Until an operator safely reruns the idempotent script, new-only consumers can silently omit or misclassify the remaining old-bucket records.
+- **Confidence**: High for the partial-write and missing-progress behavior; Medium for operational impact because a prompt rerun should recover.
+
+### Pass 2
+
+#### [Pass 2] The full-character PUT route persists invalid containment references and nesting
+
+- **Severity**: Medium
+- **File:line**: `server/routes/characters.js:455` and `server/routes/characters.js:462`; schema contract at `server/schemas/character.schema.js:319`
+- **Triggering input or sequence**: An ST sends `PUT /api/characters/:id` with a schema-valid `equipment[]` containing a 24-hex `container_id` that is dangling, equals the item's own `catalogue_id`, names a non-container catalogue item, or assigns a `container_id` to an entry that is itself a container. `validateCharacterPartial` checks shape only; the hydration loop converts only `catalogue_id`, performs no catalogue/container/topology lookup, and then `$set`s the array.
+- **Observable consequence**: The route the schema comment explicitly makes responsible for reference checking accepts and stores all of the forbidden cases, including multi-level containment. No current production file reads `container_id`, so this does not crash today's sheet; instead it allows invalid topology to accumulate before the first containment reader/UI exists.
+- **Confidence**: High. A repository-wide search found `container_id` only in the two schema comments/definitions, and the PUT implementation was inspected in full around its equipment hydration path.
+
+### Pass 3a
+
+#### [Pass 3a] `container_id` cannot identify a container instance when catalogue items repeat
+
+- **Severity**: Medium
+- **File:line**: `server/schemas/character.schema.js:339` and `server/schemas/character.schema.js:346`; duplicate-permitting append at `server/routes/characters.js:883`
+- **Triggering input or sequence**: A character owns two instances of the same container catalogue entry (for example, two identical backpacks). Both equipment rows have the same `catalogue_id`; equipment rows have no independent instance `_id`. Setting another item's `container_id` to that 24-hex catalogue ID therefore matches both rows.
+- **Observable consequence**: The data model cannot express which actual container instance holds the item, so AC #2's literal “referencing another item” contract becomes ambiguous as soon as duplicate containers exist. The distinction matters if the two instances have different state/notes or one is removed. No assignment/display UI exists in this story, but this schema is meant to be the foundation for that later work.
+- **Confidence**: High that the ambiguity exists and duplicates are not rejected; Medium that product semantics require per-instance distinction rather than treating identical catalogue holdings as one logical container.
+
+### Pass 3b
+
+#### [Pass 3b] AC #6 says the full suite passes, while the same story records ten failed files
+
+- **Severity**: Medium
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:49` and `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Evaluate the literal acceptance criterion “`npm test` (vitest) passes in full” against the Dev Agent Record and the independently repeated ten-file run. Both current HEAD and an isolated `ddf059f8` export produce 10 failed files and 5 failed tests (63 passed, 68 tests in that subset), even though the failures are genuinely pre-existing and unrelated to EQC-1.
+- **Observable consequence**: AC #6 is marked complete despite its literal pass condition being false. The pre-existing provenance is well supported, so this is not evidence that EQC-1 caused those failures; it is an acceptance-record overstatement that can mislead a ship decision into treating a qualified regression result as a full green gate.
+- **Confidence**: High.
+
+## Low
+
+### Pass 1
+
+#### [Pass 1] The dry-run integration assertion is satisfied by unrelated catalogue rows
+
+- **Severity**: Low
+- **File:line**: `server/tests/issue-1152-eqc1-bucket-migration.test.js:91`
+- **Triggering input or sequence**: The test inserts `Dry Run Knife`, then asserts only `totals.touched >= 1` over an unfiltered scan of the entire shared test collection. If that inserted document were not planned for migration but any unrelated old-taxonomy row existed, the assertion would still pass; the following assertion merely confirms that dry-run did not write the inserted row.
+- **Observable consequence**: This integration test can report the seeded dry-run case as covered without proving that the seeded row contributed to the plan. The pure mapping test separately reduces immediate regression risk, but the integration assertion itself is not isolated or pinned to its fixture.
+- **Confidence**: High.
+
+### Pass 2
+
+#### [Pass 2] Array-valued legacy buckets are silently coerced into mapping-table keys
+
+- **Severity**: Low
+- **File:line**: `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:72` and `server/scripts/migrate-eqc1-bucket-taxonomy.mjs:76`
+- **Triggering input or sequence**: `planBucketMigration({ bucket: ['weapon'] })` (or another one-element array containing an old value) reaches `BUCKET_MAP[from]`. JavaScript coerces the array property key to `'weapon'`, so the planner returns `touched: true` and `toBucket: 'combat_gear'` instead of flagging the non-string bucket as malformed/unrecognised.
+- **Observable consequence**: A malformed live document is silently normalised and disappears from the human-review list, contrary to the planner's “flag rather than guess” error-path policy. The resulting bucket happens to be the plausible mapping for this exact one-element array, which limits immediate mechanical harm but loses the audit signal.
+- **Confidence**: High; executed directly against the committed planner. `null`, `undefined`, `{}`, null/empty/number buckets, and an empty array all returned non-throwing untouched plans, while `['weapon']` reproduced the coercion.
+
+#### [Pass 2] Several equipment gate fixtures still encode the retired taxonomy
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment.test.js:51`, `server/tests/issue-868-ecm-1-equipment-catalogue-api.test.js:54`, and `server/tests/issue-896-availability-filter.test.js:43`
+- **Triggering input or sequence**: Run or maintain the equipment suites after the taxonomy migration. These tests still seed or synthesize `weapon`, `armour`, `equipment`, and `asset` values in cases not updated by the diff; many direct-insert fixtures bypass the new catalogue schema and can therefore continue passing.
+- **Observable consequence**: Green results in those cases do not consistently represent correctly migrated catalogue data, and future assertions can preserve old assumptions without exercising the five-value taxonomy. This is primarily a coverage/maintenance defect; the production-source grep did not find a missed old-value comparison.
+- **Confidence**: High that the stale fixtures exist; Medium on their gate impact pending the Pass 3b individual suite runs.
+
+### Pass 3a
+
+#### [Pass 3a] AC #5's roll-chip parity is asserted structurally, not demonstrated behaviorally
+
+- **Severity**: Low
+- **File:line**: `server/tests/equipment-client-fixes.test.js:64` and `server/tests/equipment-client-fixes.test.js:74`
+- **Triggering input or sequence**: Regress either roller while leaving text matching `bucket === 'skill_gear'` / `bucket === 'combat_gear' && entry.weapon_type != null` and an `active` state check within the regex window—for example, break the catalogue lookup result consumed later, render the wrong item, or alter chip toggle behavior. The changed tests read source text and match proximity; they do not feed equivalent old/new catalogue-and-character fixtures through both roll consumers and compare observable output.
+- **Observable consequence**: AC #5's “same for weapon/skill-gear chips” no-regression claim lacks an executable behavioral parity proof. Armour math has literal expected-output behavior tests on the new shape, but no test pins old-shape-derived output against equivalent new-shape output, and the roller coverage can stay green through some behavioral regressions.
+- **Confidence**: High about the test shape and absence of a new-taxonomy behavioral roller test in the repository-wide test search; Medium that AC #5 strictly requires a parity test rather than correct implementation alone.
+
+### Pass 3b
+
+#### [Pass 3b] The claimed 2463/2458 full-suite total could not be reproduced in this environment
+
+- **Severity**: Low
+- **File:line**: `specs/stories/feature.1152.eqc1-bucket-container-schema.story.md:117`
+- **Triggering input or sequence**: Run `npm test` against the available local Mongo daemon. The repository normally points at a network Mongo URI (blocked here), and its client hard-codes TLS while the local daemon is non-TLS. After a temporary TLS-only workaround, the full parallel run repeatedly lost local DB connections (`ECONNRESET`), skipped many integration suites, and timed out at five minutes without a final count.
+- **Observable consequence**: The exact “2463 total, 2458 pass” statement remains unverified-as-stated by this review and should not be presented as a fresh current-environment gate result. This does not contradict the record: all nine equipment suites independently passed 170/170, and the claimed ten-file failure signature reproduced exactly on both current HEAD and base commit.
+- **Confidence**: High about the verification gap and targeted results; no conclusion either way about whether the historical 2463/2458 numbers were accurate in the author's environment.
+
+## Validation notes
+
+### Ship assessment
+
+**Needs patches; do not ship or run the production migration as-is.** The direct readers and ordinary migrated fixtures behave correctly, and the nine equipment gates are green, but the migration does not preflight legacy weapon/armour rows for the discriminator fields on which the new mechanics depend. I would not trust it against real character data today until a production dry-run reports/audits every old weapon lacking `weapon_type` and every old armour lacking `armour_value` (with an explicit remediation policy), and containment's stated reference/single-level contract is either enforced or narrowed in the AC/comments. The live production migration was not run.
+
+### Information-barrier attestation and files opened
+
+- **Pass 1**: Opened only `specs/stories/code-review/issue-1152-eqc1-diff.txt`, in chunks after the first full read was output-truncated. I did not list or open repository source, the story, or the Dev Agent Record before writing the complete Pass 1 text to this file.
+- **Pass 2**: After Pass 1 was frozen, opened/searched production and test context including `public/js/data/equipment-derivation.js`, `public/js/suite/roll.js`, `public/js/suite/roll-v2.js`, `public/js/editor/sheet.js`, `public/js/admin/equipment-catalogue-admin.js`, `public/css/admin-layout.css`, `server/schemas/character.schema.js`, `server/schemas/equipment_catalogue.schema.js`, `server/routes/characters.js`, `server/routes/equipment-catalogue.js`, `server/db.js`, `server/config.js`, `server/tests/helpers/db-setup.js`, `server/tests/helpers/setup-env.js`, and repository-wide grep results under `public/`, `server/`, `tests/`, and `specs/` excluding the issue-1152 story. An attempted open of the nonexistent `server/data/equipment-catalogue.js` failed. I did not open the issue-1152 story until Pass 2 was frozen.
+- **Pass 3a**: Opened only story lines 1-53 (Story, Background, taxonomy, and AC 1-7) before freezing Pass 3a. I did not read Tasks, Dev Notes, or the Dev Agent Record during 3a.
+- **Pass 3b**: After 3a was frozen, opened the Dev Agent Record (lines 105-end), then the intervening Tasks/Dev Notes/Project Structure/References (lines 54-104) to read the author's account in full. Also opened the named tests and source lines surfaced by test failures, plus Git metadata/diffs for the specified commits. I did not read or modify any sibling repository.
+
+### Commands run and real results
+
+- Read the supplied diff with `Get-Content` and located/chunked its file sections with `Select-String`/array slicing. The first full output was truncated; the subsequent three chunks covered all 938 lines.
+- Ran repository searches with `rg` for old bucket comparisons, all `.bucket` readers, `container_id`, CSS bucket classes, discriminator fields, and new-taxonomy test coverage. One first regex command failed with a PowerShell parser error; one broad grep timed out after 22 seconds but returned useful partial results; narrower production-source searches completed. No production old-bucket comparison was found. All five new CSS classes exist and no old class remains. `container_id` has no production read site.
+- Directly executed `planBucketMigration` on `null`, `undefined`, `{}`, null/empty/number/array buckets. It did not throw; `['weapon']` was coerced to the mapping-table key and migrated.
+- Directly executed the three-item armour trace. Result: `{ penalty: 3, count: 1 }`; only the armour-shaped item contributed, while weapon-shaped and neither-shaped items contributed nothing. Static tracing of both roller predicates showed only the weapon-shaped item enters weapon reference and none of the three enters skill chips. Sheet predicates make the neither-shaped item reach `Other Combat Gear`.
+- Checked `git status`, HEAD, and `git diff --name-status ddf059f8 c7e6771b`. HEAD is `c7e6771b19367f0a6face5e675c7a3e2ac3737b4`. The EQC-1 source/tooling diff has 2 new files and 14 modified files; including the deliberately excluded story, the commit has 17 paths. The workspace already contained numerous unrelated tracked/untracked user changes, which were not touched.
+- Checked Mongo availability. A `mongod` process existed. The first `equipment.test.js` run used the repository's remote URI and failed with `EACCES 159.143.141.178:27017` (1 failed file, 14 skipped tests). Retrying with `MONGODB_URI=mongodb://127.0.0.1:27017` failed TLS negotiation with `ECONNRESET` because `server/db.js` hard-codes `tls: true` (1 failed file, 14 skipped tests).
+- Temporarily changed only `server/db.js` from `tls: true` to `tls: false` to use the local non-TLS daemon. With `MONGODB_URI=mongodb://127.0.0.1:27017`, the nine requested suites were run individually and produced the exact current gate totals below (all integration slices ran; no skips):
+
+  - `tests/equipment.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/equipment-client-fixes.test.js`: 1 file passed; **6/6 tests passed**.
+  - `tests/issue-868-ecm-1-equipment-catalogue-api.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-871-876-ecm-4-9-bundle.test.js`: 1 file passed; **19/19 tests passed**.
+  - `tests/issue-872-ecm-5-editor-cache.test.js`: 1 file passed; **14/14 tests passed**.
+  - `tests/issue-896-availability-filter.test.js`: 1 file passed; **28/28 tests passed**.
+  - `tests/issue-879-defence-penalty-wirein.test.js`: 1 file passed; **36/36 tests passed**.
+  - `tests/issue-1152-eqc1-bucket-migration.test.js`: 1 file passed; **11/11 tests passed**.
+  - `tests/issue-873-ecm-6-admin-sidebar.test.js`: 1 file passed; **14/14 tests passed**.
+  - Aggregate: **9/9 files passed; 170/170 tests passed; 0 skipped**.
+
+- Ran the ten named historical-failure files together on current HEAD. Result: **10 failed files; 5 failed tests; 63 passed (68 tests)**. The failing tests were exactly: `epic.708.3-cycle-phase-controls` — “exports setGamePhase function”, “uses data-phase attribute on phase buttons”, “highlights active phase with gold2 colour”; `n7-n9-allocator-readers` — “all three dropdown builders consume meritPrereqOK (not _meetsPrereq directly)”; `oath-a-pledge-helpers` — “meritRating and meritEffectiveRating are byte-identical to their pre-OATH-A form”. Seven other named files failed during suite loading. Searching the three files containing the five failed tests found none of `equipment|bucket|armour|weapon|container`.
+- Attempted full `npm test` with the local TLS workaround. It **did not complete**: the command timed out after 302.6 seconds, many integration suites reported `ECONNRESET` and skipped, and Vitest emitted no final aggregate. Therefore this review could not reproduce the claimed **2463 total / 2458 pass / 5 fail** full-suite numbers.
+- Independently reproduced the pre-existing-failure claim without stashing the dirty workspace: `git archive ddf059f8` into an isolated workspace temp directory, junctioned its `server/node_modules`, applied the same temporary local-TLS change inside that disposable export, and ran the same ten files. Result was identical: **10 failed files; 5 failed tests; 63 passed (68 tests), same five test names and same seven suite-load failures**. A first attempt to archive under `D:\tmp` failed with permission denied; the workspace-local isolated export succeeded.
+- Queried the local `tm_suite.equipment_catalogue` read-only. It contained zero documents, so production prevalence of missing discriminator fields could not be assessed. No live migration was run.
+- Directly compared legacy/new discriminator behavior: old armour `{ armour_value: null, defence_penalty: 2 }` yielded old penalty 2 versus new penalty 0; old weapon `{ weapon_type: null, damage_mod: 2 }` yielded old weapon-reference eligibility `true` versus new `false`.
+
+### Could not run or verify
+
+- Could not query the real production `tm_suite` catalogue; the configured remote Mongo endpoint is blocked by this environment's network policy. The local database was empty.
+- Could not obtain a valid full-suite aggregate because the five-minute run timed out after local Mongo connection resets under parallel load. Consequently the historical 2463/2458 total remains unverified here.
+- Did not run the live migration against production, per the story's explicit deploy-time scope and this review's no-write constraint.
+- Could not use `git checkout -- server/db.js` to restore the temporary edit because `.git` is read-only in this sandbox (`index.lock: Permission denied`). I restored the exact committed line with `apply_patch` instead; `git diff -- server/db.js` and path-scoped `git status --short` are empty afterward.
+
+### Workspace integrity
+
+- The temporary base archive/export and its `node_modules` junction were deleted after exact-path checks; existence checks returned false for both archive and directory.
+- No source change remains from this review. The requested findings file is the only created review artifact. Final path-scoped status showed only `?? specs/stories/code-review/issue-1152-eqc1-codex-findings.md`; `server/db.js` had no diff. The pre-existing unrelated dirty-worktree entries were preserved.

tokens used
463,508
Review complete: [issue-1152-eqc1-codex-findings.md](D:\Terra Mortis\TM Suite\specs\stories\code-review\issue-1152-eqc1-codex-findings.md).

Verdict: **needs patches; do not run the production migration as-is.**

Key blocker: legacy weapon/armour records may lack the discriminator fields required after migration. Such records migrate successfully but silently lose armour penalties or weapon-reference visibility.

Validation highlights:

- Equipment gates: **9/9 files, 170/170 tests passed, 0 skipped**.
- Historical failures reproduced on HEAD and `ddf059f8`: **10 failed files, 5 failed tests, 63 passed**, identical failure names.
- Full-suite 2463/2458 claim could not be reproduced because the run timed out amid local Mongo connection resets.
- Temporary TLS edit was restored; no source diff remains.
- Only the requested findings document was created.
