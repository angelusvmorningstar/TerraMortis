# generated: 2026-04-15
# last_updated: 2026-04-15
# project: TM Suite — DT Story Tab
# project_key: NOKEY
# tracking_system: file-system
# story_location: specs/stories

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - B1 must ship before any other B story (tab shell + migration script)
# - B2 and B3 can run in parallel after B1 ships
# - B4–B7 can run in parallel with each other after B1 ships
# - A1 and A2 can run in parallel with any B story
# - A1 output (cross-action derivation functions) feeds B3 — implement A1 before or alongside B3
# - A2 touches downtime-views.js only (3 one-line changes); safe to ship at any time
# - Migration script (server/migrate-dt-story.js, part of B1) must run against production before B1 deploys
# - B1 dev note: getUser() imports from ../auth/discord.js — NOT admin.js (confirmed during SM session)
# - B6 adds resolveTerrId to downtime-story.js; A1 depends on it — implement B6 before or alongside A1

# STORY DEPENDENCY MAP:
# =====================
# B1 (shell) → B2, B3, B4, B5, B6, B7 (all depend on B1)
# B3 + A1 → implement together (A1 functions live in downtime-story.js alongside buildActionContext)
# B6 + A1 → implement together or B6 first (resolveTerrId shared)
# B4 + B5 → B5 can reuse CSS classes introduced by B4 (touchstone list/entry/attached/detached)
# A2 → standalone, no dependencies

generated: 2026-04-15
last_updated: 2026-04-15 (all stories ready-for-dev; epic backlog complete)
project: TM Suite — DT Story Tab
project_key: NOKEY
tracking_system: file-system
story_location: specs/stories

development_status:
  # === EPIC 1: DT Story Tab ===
  epic-1: in-progress

  # B1 — tab shell, migration script, section scaffold, save pattern, completion helpers
  # MUST SHIP FIRST — all other B stories depend on it
  1-1-dt-story-tab-shell: done

  # B2–B3 — prompt generators; can run in parallel after B1
  # Implement A1 before or alongside B3 (cross-action derivation functions)
  1-2-prompt-generator-projects: review
  1-3-prompt-generator-merit-actions: ready-for-dev

  # B4–B7 — section implementations; depend on B1; can run in parallel with each other
  # B5 reuses CSS classes from B4 (implement B4 first or in same commit)
  # B6 adds resolveTerrId — implement before or alongside A1
  1-4-letter-from-home-section: ready-for-dev
  1-5-touchstone-vignette-section: ready-for-dev
  1-6-territory-report-section: ready-for-dev
  1-7-cacophony-savvy-section: ready-for-dev

  # A1–A2 — action data + validation; can run in parallel with B stories
  # A1: pure derivation functions in downtime-story.js; no schema changes; no downtime-views.js changes
  # A2: 3 one-line changes to downtime-views.js only; standalone
  1-8-action-data-completeness: ready-for-dev
  1-9-validation-button-consistency: ready-for-dev

  epic-1-retrospective: optional
