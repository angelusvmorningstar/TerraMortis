[build]
  publish = "public"

# Short cache for JS/CSS so deploys take effect quickly
[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=300"

[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=300"

# Proxy API requests to the Render-hosted Express server.
# Update the 'to' URL when the Render service is deployed.
[[redirects]]
  from = "/api/*"
  to = "https://tm-game-api.onrender.com/api/:splat"
  status = 200
  force = true

# Maintenance mode was active here 2026-08-21 during the tm_suite->tm_game rebrand's Render
# cutover -- removed once the cutover was verified end-to-end. See ../rebrand-game-story-admin.md.

# Block dev-login in production — only meaningful locally
[[redirects]]
  from = "/dev-login"
  to = "/dev-login.html"
  status = 404

# Named entry points — serve the correct HTML for each app
[[redirects]]
  from = "/admin"
  to = "/admin.html"
  status = 200

# SPA fallback — serve index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
