*.zip
*.docx
*-backup.html
VtR 2e - Core Book.pdf
Terra Mortis Treatment.pdf
.claude/settings.local.json
private/*
!private/.gitkeep
.env
node_modules/
test-results/

# IDE
.vscode/

# macOS
.DS_Store

# Generated/exported files
Character Data Export.csv
Character Template.csv
TM Characters.pdf
Terra Mortis Character Master*.xlsx

# Temp backups
data/chars_v2_backup.json
data/backup/

# Character data — source of truth is MongoDB, not git
data/chars_v2.json
data/chars_v3.json
public/data/chars_v3.json
data/chars_dev.json
data/chars_test.json
archive/tm_characters.json

# Google Forms exports & misc data files
Copy of Terra Mortis*.pdf
Terra Mortis - *.pdf
Terra Mortis Downtime*.pdf
*.csv
!data/*.csv
Downtime Mammon.xlsx

# ST operational files — not code
st-working/

# Atlas exports — source of truth is MongoDB, not git
data copy/

# Scratch working directory — ad-hoc inputs/outputs, never tracked
scratch/

# DT prototype — static data snapshot, never merges to dev/main
public/dt-proto-data/
server/scripts/build-rubric-digests.js

# Map-build & ad-hoc script scratch — underscore-prefixed working artifacts
# (renders, kmz extracts, per-edge builds). Tracked _*.json under server/scripts
# stay tracked; gitignore never untracks. Local-only, never committed.
server/scripts/_*

# Generated data exports — regenerated on demand, source of truth is MongoDB
data/exports/

# One-off DB cleanup script backups — local-only audit trail
server/scripts/_backups/
server/scripts/archive/_backups/

# resweep dump (generated; may carry player ordeal answers) - never commit
server/scripts/_resweep-q1q19-dump.json

# standalone chronicle map (generated, may carry player PII) - never commit
tm-map.html

# ST Cockpit — its OWN repo (TerraMortis-cockpit), nested here as a subfolder for convenience.
# Not part of this repo; never track it from the suite.
/cockpit/
