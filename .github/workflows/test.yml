name: API Tests

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: server

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check secret presence
        run: |
          if [ -z "$MONGODB_URI" ]; then
            echo "ERROR: MONGODB_URI secret is empty or not set"
            exit 1
          else
            echo "MONGODB_URI is set (length: ${#MONGODB_URI})"
          fi
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}

      - name: Run tests
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
          MONGODB_DB: tm_suite_test
          DISCORD_CLIENT_ID: placeholder
          DISCORD_CLIENT_SECRET: placeholder
          SESSION_SECRET: placeholder-ci-secret
        run: npm test
